# Input Semantic Checklist

- Is the FEAT user-visible and suitable for prototype generation?
- Does the FEAT define enough journey intent to build a static HTML prototype?
- Are happy path and failure / retry / skip expectations visible in the FEAT?
- Are we still at experience-layer scope, not implementation-layer scope?
