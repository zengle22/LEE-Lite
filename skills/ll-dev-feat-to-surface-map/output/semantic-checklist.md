# Output Semantic Checklist

- Does the selected FEAT have a clear `surface_map_ref`?
- Are impacted surfaces explicit and owned?
- Does every `create` entry carry a reason that justifies a new long-lived boundary?
- If the FEAT is non-design-impacting, is the bypass rationale explicit?
- Does the downstream handoff point to the next design consumers without inventing new authority?
