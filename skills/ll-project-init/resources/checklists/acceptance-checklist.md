# Acceptance Checklist

- The target repository contains the managed root files defined by this skill.
- The durable top-level directories exist.
- Runtime shell directories exist only as shells and are not treated as durable outputs.
- The output package includes every required artifact in `output/contract.yaml`.
- The package report explains created, updated, and skipped paths clearly enough for handoff.
