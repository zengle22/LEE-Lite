# Input Contract: ll-patch-aware-context

input_contract:
  skill: ll-patch-aware-context
  parameters:
    - name: feat_ref
      type: string
      required: true
      description: "FEAT reference identifier (e.g. FEAT-SRC-001-001)"
      example: "FEAT-SRC-001-001"
    - name: workspace_root
      type: string
      required: false
      description: "Workspace root directory path (defaults to current directory)"
    - name: output_dir
      type: string
      required: true
      description: "Directory where patch-awareness.yaml will be written"
