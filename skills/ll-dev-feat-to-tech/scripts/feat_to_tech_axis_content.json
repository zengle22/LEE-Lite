{
  "projection_generation": {
    "architecture_topics": [
      "Boundary to Machine SSOT: 输入只读取冻结后的 SSOT authoritative fields，不改写 SSOT 本体。",
      "Boundary to reviewer: 本 FEAT 只生成 Human Review Projection，不负责 gate decision issuance 或 formal publication。",
      "Dedicated renderer placement is required so template rendering、derived-only marker 与 traceability stay in one implementation surface."
    ],
    "responsibility_splits": [
      "Projection renderer owns template rendering and derived-only markers.",
      "SSOT owner owns authoritative source updates and field stability.",
      "Reviewer consumes the projection but does not treat it as an inheritable source."
    ],
    "api_surfaces": [],
    "api_cli_commands": [
      "`ll gate submit-handoff --request <gate_submit_handoff.request.json> --response-out <gate_submit_handoff.response.json>` via `cli/commands/gate/command.py`",
      "`ll gate show-pending --request <gate_show_pending.request.json> --response-out <gate_show_pending.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [],
    "implementation_architecture": [
      "Machine SSOT remains the only authority; renderer reads frozen SSOT fields and produces a Human Review Projection as a derived-only artifact.",
      "Projection template、derived-only markers and traceability metadata are produced in one rendering surface so reviewer-facing output stays stable across runs.",
      "No handoff runtime, gate-decision engine, formal publication, or governed IO platform is introduced in this FEAT."
    ],
    "implementation_modules": [
      "Projection template resolver：负责选择稳定模板块并绑定 SSOT trace refs。",
      "Projection renderer：负责把 Product Summary / Roles / Main Flow / Deliverables 等块渲染为 reviewer-facing view。",
      "Projection marker guard：负责注入 derived-only / non-authoritative / non-inheritable markers。"
    ],
    "state_model": [
      "`ssot_ready` -> `projection_requested` -> `projection_rendered` -> `review_visible`",
      "`projection_rendered` must always retain `derived_only_marked` and `traceable_to_ssot` invariants."
    ],
    "architecture_diagram": "```text\n[Machine SSOT] -> [Template Resolver] -> [Projection Renderer] -> [Human Review Projection]\n                                      |\n                                      +--> [Derived-only / Trace Markers]\n```",
    "tech_runtime_view": "```text\n[review_projection/template.py]\n              |\n              v\n[review_projection/renderer.py] --> [review_projection/markers.py]\n```",
    "flow_diagram": "```text\nMachine SSOT     -> Template Resolver   : load frozen authoritative fields\nTemplate Resolver-> Projection Renderer : render stable review template\nProjection Renderer -> Marker Guard     : attach derived-only markers\nMarker Guard     -> Reviewer            : publish Human Review Projection\n```",
    "implementation_strategy": [
      "先冻结 Projection 模板块和 traceability 字段，再实现渲染器本体。",
      "把 derived-only / non-authoritative / non-inheritable markers 作为硬约束统一注入，避免视图误被当成新真相源。",
      "最后用同类 SSOT 输入重复渲染，验证模板稳定性和 reviewer 可读性。"
    ],
    "implementation_unit_mapping": [
      "`review_projection/template.py` (`new`): 定义 Projection 固定模板块与字段绑定。",
      "`review_projection/renderer.py` (`new`): 从 Machine SSOT 渲染 Human Review Projection。",
      "`review_projection/markers.py` (`new`): 注入 derived-only / non-authoritative / non-inheritable markers 与 trace refs。"
    ],
    "main_sequence": [
      "1. resolve freeze-ready Machine SSOT",
      "2. resolve Projection template version",
      "3. render Product Summary / Roles / Main Flow / Deliverables blocks",
      "4. attach derived-only markers and source trace refs",
      "5. publish Human Review Projection to gate reviewer"
    ],
    "exception_compensation": [
      "SSOT 可读但模板缺失：拒绝自由发挥 narrative，返回 `template_missing` 并保留 review request。",
      "Projection rendered 但 markers 缺失：阻止发布给 reviewer，要求补齐 derived-only markers。",
      "trace refs persist fail：允许暂存 Projection，但标记 `traceability_pending`，不得进入正式 reviewer gate。"
    ],
    "integration_points": [
      "调用方：gate review surface 在进入审核前调用 Projection renderer，输入只能是 Machine SSOT ref。",
      "挂接点：Projection 在 gate 审核阶段生成，不进入下游 TECH/TASK/TESTSET 继承入口。",
      "旧系统兼容：缺少 Projection 时 reviewer 可回看 SSOT，但不得把自由 narrative 当成正式 Projection。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef render_projection(request: ProjectionRenderRequest) -> ProjectionRenderResult:\n    ssot = load_machine_ssot(request.ssot_ref)\n    template = load_projection_template(request.template_version)\n    blocks = render_review_blocks(ssot, template)\n    markers = attach_projection_markers(ssot.ref, template.version)\n    return ProjectionRenderResult(blocks=blocks, markers=markers)\n```",
      "failure_path": "```python\ndef render_projection_or_fail(request: ProjectionRenderRequest) -> ProjectionRenderResult:\n    template = load_projection_template(request.template_version)\n    if template is None:\n        raise ProjectionRenderError('template_missing')\n    return render_projection(request)\n```"
    }
  },
  "authoritative_snapshot": {
    "architecture_topics": [
      "Boundary to Projection narrative: 只提取 completed state、authoritative output、frozen boundary、open technical decisions，不扩张 narrative。",
      "Boundary to SSOT authority: Snapshot 只能解释 SSOT 已有约束，不能新增 authoritative semantics。",
      "Dedicated snapshot extractor placement is required so authoritative fields remain short, stable, and traceable."
    ],
    "responsibility_splits": [
      "Snapshot extractor owns authoritative field selection and short-form rendering.",
      "SSOT owner owns the underlying completed state / output / boundary truth.",
      "Reviewer uses Snapshot for hard-constraint checks without redefining authority."
    ],
    "api_surfaces": [],
    "api_cli_commands": [
      "`ll gate submit-handoff --request <gate_submit_handoff.request.json> --response-out <gate_submit_handoff.response.json>` via `cli/commands/gate/command.py`",
      "`ll gate show-pending --request <gate_show_pending.request.json> --response-out <gate_show_pending.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [],
    "implementation_architecture": [
      "Snapshot extractor reads authoritative SSOT fields and emits a short Authoritative Snapshot inside the Projection.",
      "Snapshot rendering stays separate from narrative rendering so hard constraints remain compact, explicit, and traceable.",
      "The extractor never adds new authority; it only reorders and shortens fields already frozen by SSOT."
    ],
    "implementation_modules": [
      "Authoritative field selector：负责选择 completed state、authoritative output、frozen boundary、open technical decisions。",
      "Snapshot formatter：负责把 authoritative fields 压缩成 reviewer 可快速核对的短摘要。",
      "Traceability binder：负责把 Snapshot 字段回链到对应 SSOT refs。"
    ],
    "state_model": [
      "`projection_rendered` -> `snapshot_extracted` -> `snapshot_attached` -> `constraint_check_visible`",
      "`snapshot_extracted` only succeeds when authoritative SSOT fields remain resolvable."
    ],
    "architecture_diagram": "```text\n[Machine SSOT] -> [Authoritative Field Selector] -> [Snapshot Formatter] -> [Authoritative Snapshot]\n                                                                        |\n                                                                        +--> [Projection]\n```",
    "tech_runtime_view": "```text\n[review_projection/snapshot.py]\n              |\n              v\n[review_projection/field_selector.py] --> [review_projection/traceability.py]\n```",
    "flow_diagram": "```text\nMachine SSOT       -> Field Selector      : select authoritative fields\nField Selector     -> Snapshot Formatter  : compress into short authoritative view\nSnapshot Formatter -> Projection          : attach Snapshot block\nProjection         -> Reviewer            : expose hard constraints\n```",
    "implementation_strategy": [
      "先冻结 Snapshot 必含字段集合，再实现 authoritative field selector。",
      "把 Snapshot 与 narrative rendering 解耦，避免 hard constraints 被 narrative 噪音淹没。",
      "最后验证 Snapshot 只引用 SSOT 已有字段，不引入新的权威定义。"
    ],
    "implementation_unit_mapping": [
      "`review_projection/field_selector.py` (`new`): 选择 completed state、authoritative output、frozen boundary、open technical decisions。",
      "`review_projection/snapshot.py` (`new`): 生成 Authoritative Snapshot 短摘要。",
      "`review_projection/traceability.py` (`new`): 绑定 Snapshot 字段到对应 SSOT refs。"
    ],
    "main_sequence": [
      "1. resolve authoritative SSOT fields",
      "2. extract completed state / authoritative output / frozen boundary / open technical decisions",
      "3. compress fields into Authoritative Snapshot",
      "4. attach Snapshot to Projection with trace refs"
    ],
    "exception_compensation": [
      "authoritative fields 缺失：返回 `authoritative_field_missing`，Projection 必须显示约束缺失提示而不是捏造内容。",
      "Snapshot build 成功但 trace bind fail：标记 `snapshot_trace_pending`，不得把该 Snapshot 视为可审核完成态。"
    ],
    "integration_points": [
      "调用方：Projection renderer 在生成 reviewer view 时同步调用 Snapshot extractor。",
      "挂接点：Snapshot 固定出现在 Projection 的 authoritative check 区块，不单独成为下游 authority object。",
      "旧系统兼容：若 Snapshot 暂不可得，必须显式标记字段缺失，不允许 silent omission。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef build_authoritative_snapshot(ssot_ref: str) -> AuthoritativeSnapshot:\n    ssot = load_machine_ssot(ssot_ref)\n    fields = select_authoritative_fields(ssot)\n    return format_authoritative_snapshot(fields)\n```",
      "failure_path": "```python\ndef build_authoritative_snapshot_or_flag(ssot_ref: str) -> AuthoritativeSnapshot:\n    fields = select_authoritative_fields(load_machine_ssot(ssot_ref))\n    if not fields:\n        raise SnapshotError('authoritative_field_missing')\n    return format_authoritative_snapshot(fields)\n```"
    }
  },
  "review_focus_risk": {
    "architecture_topics": [
      "Boundary to human narrative: 本 FEAT 生成 Review Focus / Risks / Ambiguities，帮助 reviewer 聚焦判断，不替代 SSOT 或 Snapshot。",
      "Boundary to adjacent runtime/governance: 不定义 handoff orchestration、formal publication、governed IO、queue 或 registry。",
      "Dedicated analysis placement is required so risk extraction、ambiguity highlighting and review prompts stay deterministic."
    ],
    "responsibility_splits": [
      "Risk analyzer owns focus prompts, ambiguity detection, and reviewer-facing issue highlighting.",
      "Projection renderer owns placement of Review Focus / Risks blocks in the review view.",
      "Reviewer decides based on highlighted focus areas, but SSOT remains the only authoritative source."
    ],
    "api_surfaces": [],
    "api_cli_commands": [
      "`ll gate submit-handoff --request <gate_submit_handoff.request.json> --response-out <gate_submit_handoff.response.json>` via `cli/commands/gate/command.py`",
      "`ll gate show-pending --request <gate_show_pending.request.json> --response-out <gate_show_pending.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [],
    "implementation_architecture": [
      "Review Focus / Risks generation analyzes SSOT content and existing projection blocks to surface what the human reviewer should inspect next.",
      "Risk prompts and ambiguity hints are derived artifacts for reviewer attention management, not new runtime or governance objects.",
      "The analyzer must remain deterministic enough that the same SSOT yields the same review emphasis under the same template version."
    ],
    "implementation_modules": [
      "Review focus extractor：负责从 SSOT / Projection 中提取 reviewer 应优先关注的产品判断点。",
      "Risk and ambiguity analyzer：负责识别术语歧义、边界遗漏、异常流缺失等风险提示。",
      "Review prompt composer：负责把 focus/risk 输出成稳定的 reviewer-facing blocks。"
    ],
    "state_model": [
      "`projection_rendered` -> `focus_extracted` -> `risk_flags_attached` -> `review_guidance_visible`",
      "`risk_flags_attached` must remain derived-only and traceable to source SSOT content."
    ],
    "architecture_diagram": "```text\n[Machine SSOT / Projection] -> [Focus Extractor] -> [Risk Analyzer] -> [Review Focus / Risks Block]\n```",
    "tech_runtime_view": "```text\n[review_projection/focus.py]\n              |\n              v\n[review_projection/risk_analyzer.py] --> [review_projection/prompt_blocks.py]\n```",
    "flow_diagram": "```text\nMachine SSOT / Projection -> Focus Extractor : gather review anchors\nFocus Extractor           -> Risk Analyzer   : detect ambiguity / omission / risk\nRisk Analyzer             -> Reviewer        : expose Review Focus / Risks / Ambiguities\n```",
    "implementation_strategy": [
      "先冻结 Review Focus / Risks / Ambiguities 的输出模板和优先级规则。",
      "把风险与歧义提取限制在 SSOT 已有内容和边界推断，不引入新的执行语义。",
      "最后用多组同类 SSOT 样本验证 focus/risk 输出稳定且可复现。"
    ],
    "implementation_unit_mapping": [
      "`review_projection/focus.py` (`new`): 生成 Review Focus 提示项。",
      "`review_projection/risk_analyzer.py` (`new`): 提取风险、遗漏和歧义点。",
      "`review_projection/prompt_blocks.py` (`new`): 把 focus/risk 输出编排进 Projection 模板。"
    ],
    "main_sequence": [
      "1. collect SSOT and Projection context relevant to review",
      "2. extract review focus items",
      "3. detect risk / ambiguity / omission signals",
      "4. render Review Focus / Risks / Ambiguities blocks into Projection"
    ],
    "exception_compensation": [
      "focus extraction context 不足：返回 `insufficient_context`，保留空模板位并提示 reviewer 直接查看 SSOT。",
      "risk signal 无法回链到 SSOT：丢弃该 signal，不允许把不可追溯提示写入 Projection。"
    ],
    "integration_points": [
      "调用方：review projection flow 在 Projection 渲染后调用 focus/risk analyzer。",
      "挂接点：Review Focus / Risks / Ambiguities 块只服务 reviewer 决策，不输出给下游继承。",
      "旧系统兼容：analysis 结果不可用时退化为空块和缺失提示，不得退化到 runtime governance 语义。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef build_review_focus(ssot_ref: str, projection_ref: str) -> ReviewFocusResult:\n    context = load_review_context(ssot_ref, projection_ref)\n    focus_items = extract_review_focus(context)\n    risk_items = detect_risk_signals(context)\n    return ReviewFocusResult(focus_items=focus_items, risk_items=risk_items)\n```",
      "failure_path": "```python\ndef build_review_focus_or_empty(ssot_ref: str, projection_ref: str) -> ReviewFocusResult:\n    context = load_review_context(ssot_ref, projection_ref)\n    if not context:\n        return ReviewFocusResult.empty(reason='insufficient_context')\n    return build_review_focus(ssot_ref, projection_ref)\n```"
    }
  },
  "feedback_writeback": {
    "architecture_topics": [
      "Boundary to reviewer comments: Projection comments 只是修订输入，不是新的真相源。",
      "Boundary to SSOT authority: 所有修订必须回写 Machine SSOT，再重新生成 Projection，不允许直接 patch Projection。",
      "Dedicated writeback placement is required so comment mapping、SSOT revision request 与 regeneration stay traceable."
    ],
    "responsibility_splits": [
      "Reviewer owns comments and revision intent only.",
      "Writeback mapper owns mapping comments back to SSOT fields and creating revision requests.",
      "SSOT owner owns authoritative updates and projection regeneration."
    ],
    "api_surfaces": [],
    "api_cli_commands": [
      "`ll gate submit-handoff --request <gate_submit_handoff.request.json> --response-out <gate_submit_handoff.response.json>` via `cli/commands/gate/command.py`",
      "`ll gate show-pending --request <gate_show_pending.request.json> --response-out <gate_show_pending.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [],
    "implementation_architecture": [
      "Reviewer comments map back to SSOT-owned fields through a writeback mapper; Projection is regenerated only after SSOT changes land.",
      "Writeback flow freezes traceability between comment, revision request, updated SSOT, and regenerated Projection.",
      "Direct Projection patching is forbidden; the writeback path protects SSOT as the only authoritative source."
    ],
    "implementation_modules": [
      "Comment mapper：负责把 reviewer comment 映射回 SSOT field / section / boundary。",
      "Revision request builder：负责生成可追踪的 SSOT revision request，并保留 comment provenance。",
      "Projection regeneration trigger：负责在 SSOT 更新后触发 Projection 重新生成。"
    ],
    "state_model": [
      "`review_comment_captured` -> `writeback_mapped` -> `ssot_revision_requested` -> `ssot_updated` -> `projection_regenerated`",
      "`writeback_mapped(fail)` -> `comment_mapping_pending`, and Projection must not be edited in place."
    ],
    "architecture_diagram": "```text\n[Reviewer Comment] -> [Comment Mapper] -> [SSOT Revision Request] -> [Machine SSOT Update] -> [Projection Regeneration]\n```",
    "tech_runtime_view": "```text\n[review_projection/writeback.py]\n              |\n              v\n[review_projection/revision_request.py] --> [review_projection/regeneration.py]\n```",
    "flow_diagram": "```text\nReviewer          -> Comment Mapper        : submit revision intent\nComment Mapper    -> Revision Request      : map comment to SSOT-owned fields\nRevision Request  -> Machine SSOT          : apply authoritative update\nMachine SSOT      -> Projection Regeneration : render updated Projection\n```",
    "implementation_strategy": [
      "先冻结 comment -> SSOT field 的映射规则，再实现 revision request 生成。",
      "确保所有 Projection comment 都通过 SSOT writeback 闭环，不允许直接 patch Projection。",
      "最后验证 SSOT 更新后 Projection 必须重生成，并保留 comment provenance。"
    ],
    "implementation_unit_mapping": [
      "`review_projection/writeback.py` (`new`): 将 reviewer comments 映射回 SSOT-owned fields。",
      "`review_projection/revision_request.py` (`new`): 生成可追踪的 SSOT revision request。",
      "`review_projection/regeneration.py` (`new`): 在 SSOT 更新后触发 Projection 重生成。"
    ],
    "main_sequence": [
      "1. capture reviewer comment against Projection",
      "2. map comment to SSOT-owned field or boundary",
      "3. create SSOT revision request and preserve provenance",
      "4. update Machine SSOT",
      "5. regenerate Projection from updated SSOT"
    ],
    "exception_compensation": [
      "comment mapping fail：保留 comment，标记 `comment_mapping_pending`，禁止直接编辑 Projection。",
      "SSOT 更新成功但 regeneration fail：保留 revision request completed 状态，标记 `projection_regeneration_pending` 并阻止旧 Projection 继续作为当前审核视图。"
    ],
    "integration_points": [
      "调用方：reviewer 在 Projection 上提出 comment，writeback mapper 将其回写到 SSOT revision request。",
      "挂接点：Projection regeneration 发生在 SSOT authoritative update 之后，而不是 comment capture 当下。",
      "旧系统兼容：历史 Projection 批注只能作为 comment provenance，不能直接视为 authoritative patch。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef writeback_projection_comment(comment: ProjectionComment) -> ProjectionRegenerationRequest:\n    mapped = map_comment_to_ssot_fields(comment)\n    revision = create_revision_request(mapped)\n    updated_ssot = apply_ssot_revision(revision)\n    return request_projection_regeneration(revision.ref, updated_ssot.ref)\n```",
      "failure_path": "```python\ndef writeback_projection_comment_or_hold(comment: ProjectionComment) -> ProjectionRegenerationRequest:\n    mapped = map_comment_to_ssot_fields(comment)\n    if not mapped:\n        raise WritebackError('mapping_failed')\n    return writeback_projection_comment(comment)\n```"
    }
  },
  "runner_ready_job": {
    "architecture_topics": [
      "Boundary to gate decision: 本 FEAT 消费 approve decision 并物化 ready execution job，不重写 decision vocabulary。",
      "Boundary to runner entry/control: 本 FEAT 只负责 ready job emission，不承担 runner 的用户入口、控制面或运行 ownership。",
      "Dedicated dispatch placement is required so approve-to-job lineage、ready queue write 和 next skill target stay authoritative."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "ready execution job materialization contract",
      "approve-to-job lineage contract"
    ],
    "api_cli_commands": [
      "`ll gate dispatch --request <gate_dispatch.request.json> --response-out <gate_dispatch.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll gate dispatch",
        "surface": "`ll gate dispatch --request <gate_dispatch.request.json> --response-out <gate_dispatch.response.json>` via `cli/commands/gate/command.py`",
        "request_schema": [
          "`decision_ref: string`",
          "`dispatch_target: string`",
          "`next_skill_ref: string`",
          "`authoritative_input_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`ready_job_ref`, `ready_queue_path`, `approve_to_job_lineage_ref`, `next_skill_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`ready_job_ref` is the authoritative job emitted for runner consumption after approve.",
          "`approve_to_job_lineage_ref` binds the gate approve decision to the emitted ready execution job."
        ],
        "enum_domain": [
          "`dispatch_target ∈ {ready_execution_queue}`"
        ],
        "invariants": [
          "one approve decision emits at most one authoritative ready execution job",
          "non-approve decisions must not allocate a ready queue record"
        ],
        "canonical_refs": [
          "`ready_job_ref`",
          "`approve_to_job_lineage_ref`",
          "`ready_queue_path`"
        ],
        "errors": [
          "`decision_not_dispatchable`",
          "`next_skill_missing`",
          "`job_materialization_failed`"
        ],
        "idempotency": "`decision_ref + next_skill_ref + authoritative_input_ref`",
        "preconditions": [
          "`decision_ref` already resolves to an approve decision",
          "`authoritative_input_ref` is readable by the runner pipeline"
        ],
        "caller_context": "Operator calls ll gate dispatch via CLI with valid request envelope.",
        "idempotency_key_strategy": "`decision_ref + next_skill_ref + authoritative_input_ref`",
        "post_conditions": [
          "ready_job_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`decision_ref` already resolves to an approve decision",
        "side_effects": [
          "Emits dispatch trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll gate dispatch result state.",
        "event_outputs": [
          "ll.gate.dispatch.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Approve dispatch resolver：负责从 approve decision 解析 next skill target、authoritative input 和 dispatch target。",
      "Ready job materializer：负责生成 ready execution job 与 approve-to-job lineage。",
      "Ready queue writer：负责把 ready job 写入 artifacts/jobs/ready 并发布 queue record。"
    ],
    "state_model": [
      "`decision_approved` -> `dispatch_resolved` -> `ready_job_materialized` -> `ready_queue_published`",
      "`decision_not_approved` must never transition into `ready_queue_published`."
    ],
    "architecture_diagram": "```text\n[Gate Decision] -> [Approve Dispatch Resolver] -> [Ready Job Materializer] -> [artifacts/jobs/ready]\n                                                           |\n                                                           +--> [Approve-to-Job Lineage]\n```",
    "tech_runtime_view": "```text\n[cli/commands/gate/command.py]\n              |\n              v\n[cli/lib/ready_job_dispatch.py] --> [cli/lib/job_queue.py]\n              |\n              +--> [cli/lib/protocol.py]\n```",
    "flow_diagram": "```text\nGate Decision        -> Dispatch Resolver : resolve approve target\nDispatch Resolver    -> Job Materializer  : build ready execution job\nJob Materializer     -> Job Queue         : write artifacts/jobs/ready\nJob Queue            -> Lineage Store     : persist approve-to-job lineage\n```",
    "implementation_strategy": [
      "先冻结 approve -> ready job 的 authoritative object 和 lineage 字段，再实现 queue write。",
      "把 non-approve path 明确挡在 ready queue 之外，避免 ready job 语义被 formal publication 替代。",
      "最后用真实 approve decision 验证 ready execution job 和 approve-to-job lineage。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `ReadyExecutionJob`、`ApproveToJobLineage` 结构。",
      "`cli/lib/ready_job_dispatch.py` (`new`): 解析 approve decision 并物化 ready execution job。",
      "`cli/lib/job_queue.py` (`new`): 写入 artifacts/jobs/ready 并返回 queue receipt。",
      "`cli/commands/gate/command.py` (`extend`): 在 dispatch 路径发出 ready execution job，而不是停在 formal publication trigger。"
    ],
    "main_sequence": [
      "1. resolve approve decision and next-skill target",
      "2. materialize ready execution job and approve-to-job lineage",
      "3. write job into artifacts/jobs/ready",
      "4. publish ready queue receipt"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "runner_operator_entry": {
    "architecture_topics": [
      "Boundary to ready-job emission: 本 FEAT 不生成 ready job，只提供 operator 可见的 runner 启动/恢复入口。",
      "Boundary to control surface: 本 FEAT 只冻结 runner skill entry 与 run context bootstrap，不定义 job claim/run/complete/fail verbs。",
      "Dedicated runner entry placement is required so Claude/Codex CLI entry, run context bootstrap, and invocation receipt stay in one authoritative surface."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "runner skill entry contract",
      "runner start/resume contract"
    ],
    "api_cli_commands": [
      "`ll loop run-execution --request <loop_run_execution.request.json> --response-out <loop_run_execution.response.json>` via `cli/commands/loop/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll loop run-execution",
        "surface": "`ll loop run-execution --request <loop_run_execution.request.json> --response-out <loop_run_execution.response.json>` via `cli/commands/loop/command.py`",
        "request_schema": [
          "`runner_scope_ref: string`",
          "`entry_mode: enum<start|resume>`",
          "`queue_ref: string?`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`runner_run_ref`, `runner_context_ref`, `entry_receipt_ref`, `entry_mode`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`runner_run_ref` identifies one authoritative execution-loop run owned by the runner skill entry.",
          "`runner_context_ref` binds subsequent control and observability commands to the same governed run context."
        ],
        "enum_domain": [
          "`entry_mode ∈ {start, resume}`"
        ],
        "invariants": [
          "one run-execution command must create or resume exactly one runner context",
          "entry must not directly invoke downstream skills outside the runner lifecycle"
        ],
        "canonical_refs": [
          "`runner_run_ref`",
          "`runner_context_ref`",
          "`entry_receipt_ref`"
        ],
        "errors": [
          "`runner_scope_missing`",
          "`runner_context_conflict`",
          "`resume_target_not_found`"
        ],
        "idempotency": "`runner_scope_ref + entry_mode`",
        "preconditions": [
          "`runner_scope_ref` resolves to a governed execution-runner scope"
        ],
        "caller_context": "Operator calls ll loop run-execution via CLI with valid request envelope.",
        "idempotency_key_strategy": "`runner_scope_ref + entry_mode`",
        "post_conditions": [
          "runner_run_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`runner_scope_ref` resolves to a governed execution-runner scope",
        "side_effects": [
          "Emits run-execution trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll loop run-execution result state.",
        "event_outputs": [
          "ll.loop.run_execution.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Runner skill entry adapter：负责提供 Claude/Codex CLI 可见的 Execution Loop Job Runner 入口。",
      "Runner context bootstrapper：负责初始化或恢复 authoritative runner context。",
      "Entry receipt publisher：负责记录 runner invocation receipt 与 run ref。"
    ],
    "state_model": [
      "`runner_entry_requested` -> `runner_context_initialized` -> `runner_entry_published`",
      "`runner_entry_requested(resume)` -> `runner_context_restored` -> `runner_entry_published`"
    ],
    "architecture_diagram": "```text\n[Claude/Codex CLI] -> [Runner Skill Entry] -> [Runner Context Bootstrapper] -> [Runner Run Context]\n```",
    "tech_runtime_view": "```text\n[cli/commands/loop/command.py]\n              |\n              v\n[cli/lib/runner_entry.py] --> [cli/lib/execution_runner.py]\n              |\n              +--> [cli/lib/protocol.py]\n```",
    "flow_diagram": "```text\nOperator             -> Runner Skill Entry  : request start or resume\nRunner Skill Entry   -> Context Bootstrapper: initialize governed run context\nContext Bootstrapper -> Entry Receipt       : publish runner invocation receipt\n```",
    "implementation_strategy": [
      "先冻结独立 runner skill 入口和 start/resume 语义，再实现 run context bootstrap。",
      "把 operator-facing entry 和后台 queue consumption 解耦，避免入口逻辑吞掉运行时边界。",
      "最后验证 Claude/Codex CLI 能通过单一 skill 入口启动或恢复 runner。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `ExecutionRunnerStartRequest`、`ExecutionRunnerRunRef`、`RunnerEntryReceipt` 结构。",
      "`cli/lib/runner_entry.py` (`new`): 提供 runner skill start/resume 的入口适配层。",
      "`cli/lib/execution_runner.py` (`new`): 管理 runner context bootstrap 与恢复逻辑。",
      "`cli/commands/loop/command.py` (`new`): 暴露 `run-execution` / `resume-execution` 入口。"
    ],
    "main_sequence": [
      "1. accept start/resume request from Claude/Codex CLI",
      "2. bootstrap or restore runner context",
      "3. publish runner invocation receipt",
      "4. hand off to queue consumption lifecycle"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "runner_control_surface": {
    "architecture_topics": [
      "Boundary to operator entry: 本 FEAT 依赖 runner skill entry，但不重新定义 start/resume 入口本身。",
      "Boundary to dispatch/outcome: 本 FEAT 冻结 runner 的控制面和 state transition，不直接拥有 next-skill invocation 或 final outcome semantics。",
      "Dedicated runner control placement is required so lifecycle commands, ownership guards, and control evidence share one authoritative carrier."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "runner CLI control contract",
      "runner lifecycle command contract"
    ],
    "api_cli_commands": [
      "`ll job claim --request <job_claim.request.json> --response-out <job_claim.response.json>` via `cli/commands/job/command.py`",
      "`ll job run --request <job_run.request.json> --response-out <job_run.response.json>` via `cli/commands/job/command.py`",
      "`ll job complete --request <job_complete.request.json> --response-out <job_complete.response.json>` via `cli/commands/job/command.py`",
      "`ll job fail --request <job_fail.request.json> --response-out <job_fail.response.json>` via `cli/commands/job/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll job claim",
        "surface": "`ll job claim --request <job_claim.request.json> --response-out <job_claim.response.json>` via `cli/commands/job/command.py`",
        "request_schema": [
          "`runner_context_ref: string`",
          "`ready_job_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`claimed_job_ref`, `ownership_ref`, `claim_receipt_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`ownership_ref` records the single-owner runtime claim for this job."
        ],
        "enum_domain": [],
        "invariants": [
          "a ready job can only be claimed by one active runner context"
        ],
        "canonical_refs": [
          "`claimed_job_ref`",
          "`ownership_ref`",
          "`claim_receipt_ref`"
        ],
        "errors": [
          "`job_not_ready`",
          "`ownership_conflict`"
        ],
        "idempotency": "`runner_context_ref + ready_job_ref`",
        "preconditions": [
          "`runner_context_ref` is active",
          "`ready_job_ref` resolves inside artifacts/jobs/ready"
        ],
        "caller_context": "Operator calls ll job claim via CLI with valid request envelope.",
        "idempotency_key_strategy": "`runner_context_ref + ready_job_ref`",
        "post_conditions": [
          "claimed_job_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`runner_context_ref` is active",
        "side_effects": [
          "Emits claim trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll job claim result state.",
        "event_outputs": [
          "ll.job.claim.executed"
        ]
      },
      {
        "command": "ll job run / complete / fail",
        "surface": "`ll job run|complete|fail --request <job_*.request.json> --response-out <job_*.response.json>` via `cli/commands/job/command.py`",
        "request_schema": [
          "`runner_context_ref: string`",
          "`claimed_job_ref: string`",
          "`execution_attempt_ref?: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`runner_state_ref`, `control_action_ref`, `execution_outcome_ref?`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`control_action_ref` records the authoritative control-plane action taken against one runner job."
        ],
        "enum_domain": [
          "`command ∈ {run, complete, fail}`"
        ],
        "invariants": [
          "control actions must preserve existing run context and job ownership"
        ],
        "canonical_refs": [
          "`control_action_ref`",
          "`runner_state_ref`",
          "`execution_outcome_ref`"
        ],
        "errors": [
          "`runner_context_missing`",
          "`claimed_job_missing`",
          "`invalid_transition`"
        ],
        "idempotency": "`runner_context_ref + claimed_job_ref + command`",
        "preconditions": [
          "`claimed_job_ref` is already owned by `runner_context_ref`"
        ],
        "caller_context": "Operator calls ll job run / complete / fail via CLI with valid request envelope.",
        "idempotency_key_strategy": "`runner_context_ref + claimed_job_ref + command`",
        "post_conditions": [
          "runner_state_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`claimed_job_ref` is already owned by `runner_context_ref`",
        "side_effects": [
          "Emits fail trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll job run / complete / fail result state.",
        "event_outputs": [
          "ll.job.run...complete...fail.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Runner command router：负责解析 start/claim/run/complete/fail/resume 等 control verbs。",
      "Ownership guard：负责校验 runner context 与 claimed job ownership。",
      "Control evidence writer：负责记录 control action、state update 与 operator-facing receipt。"
    ],
    "state_model": [
      "`control_requested` -> `ownership_validated` -> `control_action_recorded` -> `runner_state_updated`",
      "`ownership_validated(fail)` -> `control_rejected`"
    ],
    "architecture_diagram": "```text\n[CLI Control Command] -> [Runner Command Router] -> [Ownership Guard] -> [Control Evidence Writer]\n```",
    "tech_runtime_view": "```text\n[cli/commands/job/command.py]\n              |\n              v\n[cli/lib/runner_control.py] --> [cli/lib/execution_runner.py]\n              |\n              +--> [cli/lib/job_state.py]\n```",
    "flow_diagram": "```text\nOperator         -> Runner Command Router : issue control verb\nCommand Router   -> Ownership Guard       : validate run context and job ownership\nOwnership Guard  -> Control Evidence      : record control action and state transition\n```",
    "implementation_strategy": [
      "先冻结 runner CLI verbs 与状态边界，再实现 ownership guard 和 control evidence。",
      "把 control plane 与 dispatch/outcome plane 分开，避免 CLI 命令直接越权改写业务结果。",
      "最后验证 claim/run/complete/fail 都能留下 authoritative control records。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `RunnerControlAction`、`RunnerStateRecord`、`JobOwnershipRef` 结构。",
      "`cli/lib/runner_control.py` (`new`): 解析并执行 runner lifecycle CLI verbs。",
      "`cli/lib/job_state.py` (`new`): 管理 claimed/running/completed/failed 状态与 ownership guard。",
      "`cli/commands/job/command.py` (`new`): 暴露 `claim` / `run` / `complete` / `fail` 命令。"
    ],
    "main_sequence": [
      "1. accept runner lifecycle command",
      "2. validate runner context and ownership",
      "3. apply control-plane state transition",
      "4. publish control action record"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "runner_intake": {
    "architecture_topics": [
      "Boundary to ready-job emission: intake 只消费 authoritative ready jobs，不重写 approve-to-job materialization。",
      "Boundary to operator/control surfaces: intake 负责 queue claim 和 running ownership，不承担 CLI entry 或 broad control-plane 设计。",
      "Dedicated queue-intake placement is required so ready queue scan, single-owner claim, and running ownership are authoritative and replay-safe."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "ready queue claim contract",
      "running ownership contract"
    ],
    "api_cli_commands": [
      "`ll job claim --request <job_claim.request.json> --response-out <job_claim.response.json>` via `cli/commands/job/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll job claim",
        "surface": "`ll job claim --request <job_claim.request.json> --response-out <job_claim.response.json>` via `cli/commands/job/command.py`",
        "request_schema": [
          "`runner_context_ref: string`",
          "`ready_job_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`claimed_job_ref`, `ownership_ref`, `running_state_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`running_state_ref` marks the job as claimed and owned by a single runner."
        ],
        "enum_domain": [],
        "invariants": [
          "claim success implies a single running owner"
        ],
        "canonical_refs": [
          "`claimed_job_ref`",
          "`ownership_ref`",
          "`running_state_ref`"
        ],
        "errors": [
          "`job_not_ready`",
          "`already_claimed`"
        ],
        "idempotency": "`runner_context_ref + ready_job_ref`",
        "preconditions": [
          "`ready_job_ref` exists in ready queue"
        ],
        "caller_context": "Operator calls ll job claim via CLI with valid request envelope.",
        "idempotency_key_strategy": "`runner_context_ref + ready_job_ref`",
        "post_conditions": [
          "claimed_job_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`ready_job_ref` exists in ready queue",
        "side_effects": [
          "Emits claim trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll job claim result state.",
        "event_outputs": [
          "ll.job.claim.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Ready queue scanner：负责发现可消费的 ready execution job。",
      "Single-owner claimer：负责 claim ready job 并生成 running ownership record。",
      "Running state publisher：负责把 claimed job 暴露给后续 dispatch flow。"
    ],
    "state_model": [
      "`ready_job_visible` -> `job_claimed` -> `running_ownership_recorded`",
      "`job_claimed(conflict)` -> `claim_rejected`"
    ],
    "architecture_diagram": "```text\n[artifacts/jobs/ready] -> [Ready Queue Scanner] -> [Single-owner Claimer] -> [Running Ownership Record]\n```",
    "tech_runtime_view": "```text\n[cli/commands/job/command.py]\n              |\n              v\n[cli/lib/job_queue.py] --> [cli/lib/job_state.py] --> [Claimed Job]\n              |\n              +--> [cli/lib/protocol.py]\n```",
    "flow_diagram": "```text\nJob Queue        -> Queue Scanner         : find ready execution jobs\nQueue Scanner    -> Single-owner Claimer  : attempt claim\nClaimer          -> Job State             : publish running ownership record\n```",
    "implementation_strategy": [
      "先实现 ready queue scan 和 single-owner claim，再补 running ownership publication。",
      "把 claim 冲突和 replay guard 放在同一条队列消费路径上，避免多 runner 并发漂移。",
      "最后用真实 ready queue 样例验证 claim 只会成功一次。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `JobClaimRequest`、`ClaimedExecutionJob`、`RunningOwnershipRecord` 结构。",
      "`cli/lib/job_queue.py` (`new`): 扫描 ready queue 并 claim ready execution job。",
      "`cli/lib/job_state.py` (`new`): 写入 single-owner running ownership。",
      "`cli/commands/job/command.py` (`new`): 暴露 `claim` 命令并返回 claimed job receipt。"
    ],
    "main_sequence": [
      "1. scan ready queue for visible jobs",
      "2. claim one ready execution job",
      "3. record running ownership",
      "4. publish claimed job to dispatch stage"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "runner_dispatch": {
    "architecture_topics": [
      "Boundary to intake: dispatch 只在 claimed job 进入 running ownership 后启动，不重新定义 claim semantics。",
      "Boundary to feedback: 本 FEAT 负责 next-skill invocation 与 execution attempt record，不直接决定 done/failed/retry outcome。",
      "Dedicated dispatch placement is required so target skill resolution, authoritative input binding, and invocation lineage stay authoritative."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "next-skill invocation contract",
      "execution attempt contract"
    ],
    "api_cli_commands": [
      "`ll job run --request <job_run.request.json> --response-out <job_run.response.json>` via `cli/commands/job/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll job run",
        "surface": "`ll job run --request <job_run.request.json> --response-out <job_run.response.json>` via `cli/commands/job/command.py`",
        "request_schema": [
          "`claimed_job_ref: string`",
          "`target_skill_ref: string`",
          "`authoritative_input_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`invocation_ref`, `execution_attempt_ref`, `dispatch_lineage_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`invocation_ref` is the authoritative downstream skill invocation record emitted by the runner."
        ],
        "enum_domain": [],
        "invariants": [
          "dispatch must use the declared target skill and authoritative input from the claimed job"
        ],
        "canonical_refs": [
          "`invocation_ref`",
          "`execution_attempt_ref`",
          "`dispatch_lineage_ref`"
        ],
        "errors": [
          "`target_skill_missing`",
          "`authoritative_input_missing`",
          "`dispatch_failed`"
        ],
        "idempotency": "`claimed_job_ref + target_skill_ref + authoritative_input_ref`",
        "preconditions": [
          "`claimed_job_ref` is already in running ownership state"
        ],
        "caller_context": "Operator calls ll job run via CLI with valid request envelope.",
        "idempotency_key_strategy": "`claimed_job_ref + target_skill_ref + authoritative_input_ref`",
        "post_conditions": [
          "invocation_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`claimed_job_ref` is already in running ownership state",
        "side_effects": [
          "Emits run trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll job run result state.",
        "event_outputs": [
          "ll.job.run.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Target skill resolver：负责从 claimed job 解析 next skill ref 与 authoritative input。",
      "Invocation adapter：负责调用目标 governed skill 并生成 invocation record。",
      "Execution attempt writer：负责记录 dispatch lineage 与 execution attempt。"
    ],
    "state_model": [
      "`claimed_job_ready` -> `target_skill_resolved` -> `invocation_recorded` -> `execution_attempt_open`",
      "`target_skill_resolved(fail)` -> `dispatch_failed_pending_feedback`"
    ],
    "architecture_diagram": "```text\n[Claimed Job] -> [Target Skill Resolver] -> [Invocation Adapter] -> [Governed Skill Invocation]\n                                                  |\n                                                  +--> [Execution Attempt Record]\n```",
    "tech_runtime_view": "```text\n[cli/commands/job/command.py]\n              |\n              v\n[cli/lib/execution_runner.py] --> [cli/lib/skill_invoker.py] --> [Invocation Record]\n              |\n              +--> [cli/lib/protocol.py]\n```",
    "flow_diagram": "```text\nClaimed Job      -> Target Skill Resolver : resolve skill + authoritative input\nResolver         -> Skill Invoker         : invoke downstream governed skill\nSkill Invoker    -> Attempt Writer        : publish invocation and execution attempt\n```",
    "implementation_strategy": [
      "先冻结 claimed job 到 target skill / authoritative input 的解析规则，再实现 skill invocation adapter。",
      "保持 dispatch 只做 invocation 与 attempt record，不越界承担最终 outcome ownership。",
      "最后验证 claimed job 能稳定派发到声明的 governed skill。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `NextSkillInvocation`、`ExecutionAttemptRecord` 结构。",
      "`cli/lib/skill_invoker.py` (`new`): 解析 target skill 并执行 governed invocation。",
      "`cli/lib/execution_runner.py` (`new`): 把 claimed job 绑定到 invocation 与 execution attempt。",
      "`cli/commands/job/command.py` (`new`): 暴露 `run` 命令并返回 invocation receipt。"
    ],
    "main_sequence": [
      "1. resolve target skill and authoritative input",
      "2. invoke downstream governed skill",
      "3. record invocation and execution attempt",
      "4. expose dispatch lineage to feedback stage"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "runner_feedback": {
    "architecture_topics": [
      "Boundary to dispatch: feedback 只消费 execution attempt 结果，不重写 next-skill invocation 本身。",
      "Boundary to control/observability: 本 FEAT 冻结 outcome 与 retry-reentry 语义，监控面只读取这些结果而不重新定义它们。",
      "Dedicated feedback placement is required so execution outcome, retry-reentry directive, and failure evidence stay authoritative."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "execution outcome contract",
      "retry-reentry contract"
    ],
    "api_cli_commands": [
      "`ll job complete --request <job_complete.request.json> --response-out <job_complete.response.json>` via `cli/commands/job/command.py`",
      "`ll job fail --request <job_fail.request.json> --response-out <job_fail.response.json>` via `cli/commands/job/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll job complete / fail",
        "surface": "`ll job complete|fail --request <job_complete.request.json|job_fail.request.json> --response-out <...response.json>` via `cli/commands/job/command.py`",
        "request_schema": [
          "`execution_attempt_ref: string`",
          "`outcome: enum<done|failed|retry_reentry>`",
          "`failure_evidence_ref?: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`execution_outcome_ref`, `retry_reentry_ref?`, `failure_evidence_ref?`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`execution_outcome_ref` freezes the authoritative post-dispatch result for one execution attempt."
        ],
        "enum_domain": [
          "`outcome ∈ {done, failed, retry_reentry}`"
        ],
        "invariants": [
          "every finished execution attempt must emit exactly one authoritative outcome"
        ],
        "canonical_refs": [
          "`execution_outcome_ref`",
          "`retry_reentry_ref`",
          "`failure_evidence_ref`"
        ],
        "errors": [
          "`attempt_missing`",
          "`invalid_outcome_transition`"
        ],
        "idempotency": "`execution_attempt_ref + outcome`",
        "preconditions": [
          "`execution_attempt_ref` exists and belongs to a runner-owned dispatch"
        ],
        "caller_context": "Operator calls ll job complete / fail via CLI with valid request envelope.",
        "idempotency_key_strategy": "`execution_attempt_ref + outcome`",
        "post_conditions": [
          "execution_outcome_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`execution_attempt_ref` exists and belongs to a runner-owned dispatch",
        "side_effects": [
          "Emits fail trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll job complete / fail result state.",
        "event_outputs": [
          "ll.job.complete...fail.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Outcome collector：负责收集 downstream skill result 与 runner state。",
      "Outcome writer：负责写出 done/failed/retry-reentry 的 authoritative outcome。",
      "Failure evidence binder：负责把失败证据与 execution attempt/outcome 绑定。"
    ],
    "state_model": [
      "`execution_attempt_open` -> `outcome_recorded` -> `done|failed|retry_reentry`",
      "`failed` must retain `failure_evidence_bound` before it becomes externally visible."
    ],
    "architecture_diagram": "```text\n[Execution Attempt] -> [Outcome Collector] -> [Outcome Writer] -> [Execution Outcome]\n                                               |\n                                               +--> [Failure Evidence Binder]\n```",
    "tech_runtime_view": "```text\n[cli/commands/job/command.py]\n              |\n              v\n[cli/lib/job_outcome.py] --> [cli/lib/execution_runner.py]\n              |\n              +--> [cli/lib/protocol.py]\n```",
    "flow_diagram": "```text\nExecution Attempt -> Outcome Collector    : collect downstream result\nOutcome Collector -> Outcome Writer       : emit done / failed / retry-reentry\nOutcome Writer    -> Evidence Binder      : attach failure evidence when needed\n```",
    "implementation_strategy": [
      "先冻结 done/failed/retry-reentry outcome schema，再实现 outcome writer 和 evidence binder。",
      "把 failure evidence 与 execution attempt 强绑定，避免失败状态只留日志不留 authoritative outcome。",
      "最后验证 success/fail/retry 三条反馈路径都能回写主链。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `ExecutionOutcomeRecord`、`RetryReentryDirective`、`FailureEvidenceRef` 结构。",
      "`cli/lib/job_outcome.py` (`new`): 写出 done/failed/retry-reentry authoritative outcome。",
      "`cli/lib/execution_runner.py` (`new`): 将 execution attempt 结果绑定回 runner lifecycle。",
      "`cli/commands/job/command.py` (`new`): 暴露 `complete` / `fail` 命令并返回 outcome receipt。"
    ],
    "main_sequence": [
      "1. collect downstream execution result",
      "2. emit authoritative execution outcome",
      "3. bind failure evidence or retry-reentry directive",
      "4. expose post-dispatch result to orchestration and monitoring"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "runner_observability": {
    "architecture_topics": [
      "Boundary to runner control: 监控面只读取 ready/running/failed/waiting-human 状态，不直接执行控制动作。",
      "Boundary to queue/runtime records: 监控面必须聚合 authoritative ready queue、running ownership、dispatch 和 outcome records，而不是扫目录猜测状态。",
      "Dedicated observability placement is required so backlog、running、failed、deadletter、waiting-human views share one authoritative query surface."
    ],
    "responsibility_splits": [],
    "api_surfaces": [
      "runner observability snapshot contract",
      "runner status query contract"
    ],
    "api_cli_commands": [
      "`ll loop show-status --request <loop_show_status.request.json> --response-out <loop_show_status.response.json>` via `cli/commands/loop/command.py`",
      "`ll loop show-backlog --request <loop_show_backlog.request.json> --response-out <loop_show_backlog.response.json>` via `cli/commands/loop/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll loop show-status / show-backlog",
        "surface": "`ll loop show-status|show-backlog --request <loop_show_*.request.json> --response-out <...response.json>` via `cli/commands/loop/command.py`",
        "request_schema": [
          "`runner_scope_ref: string`",
          "`status_filter?: enum<ready|running|failed|deadletter|waiting_human>`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`observability_snapshot_ref`, `ready_backlog`, `running_items`, `failed_items`, `waiting_human_items`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`observability_snapshot_ref` points to one authoritative snapshot of runner state across queue, running ownership, and outcomes."
        ],
        "enum_domain": [
          "`status_filter ∈ {ready, running, failed, deadletter, waiting_human}`"
        ],
        "invariants": [
          "observability must read authoritative runner records instead of directory guessing"
        ],
        "canonical_refs": [
          "`observability_snapshot_ref`"
        ],
        "errors": [
          "`runner_scope_missing`",
          "`status_projection_failed`"
        ],
        "idempotency": "`runner_scope_ref + status_filter`",
        "preconditions": [
          "`runner_scope_ref` resolves to an existing execution runner scope"
        ],
        "caller_context": "Operator calls ll loop show-status / show-backlog via CLI with valid request envelope.",
        "idempotency_key_strategy": "`runner_scope_ref + status_filter`",
        "post_conditions": [
          "observability_snapshot_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`runner_scope_ref` resolves to an existing execution runner scope",
        "side_effects": [
          "Emits show-backlog trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll loop show-status / show-backlog result state.",
        "event_outputs": [
          "ll.loop.show_status...show_backlog.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Runner status projector：负责聚合 ready backlog、running、failed、deadletters 和 waiting-human 状态。",
      "Snapshot query service：负责按 runner scope 生成 authoritative observability snapshot。",
      "Operator monitor presenter：负责输出 CLI 可读的 runner status/backlog view。"
    ],
    "state_model": [
      "`runner_state_requested` -> `snapshot_projected` -> `status_view_published`",
      "`snapshot_projected(fail)` -> `observability_unavailable`"
    ],
    "architecture_diagram": "```text\n[Ready Queue / Running / Outcome Records] -> [Runner Status Projector] -> [Observability Snapshot] -> [Operator View]\n```",
    "tech_runtime_view": "```text\n[cli/commands/loop/command.py]\n              |\n              v\n[cli/lib/runner_monitor.py] --> [cli/lib/job_queue.py] --> [Observability Snapshot]\n              |\n              +--> [cli/lib/job_outcome.py]\n```",
    "flow_diagram": "```text\nQueue / Runtime Records -> Status Projector : aggregate ready/running/failed states\nStatus Projector        -> Snapshot View   : publish runner observability snapshot\nSnapshot View           -> Operator        : expose backlog and health view\n```",
    "implementation_strategy": [
      "先冻结 ready/running/failed/deadletter/waiting-human 状态词表，再实现 status projector。",
      "监控面只消费 authoritative runner records，不允许从目录结构推断状态。",
      "最后验证 operator 能通过统一视图观察 backlog、运行态和异常态。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `RunnerObservabilitySnapshot`、`RunnerStatusItem` 结构。",
      "`cli/lib/runner_monitor.py` (`new`): 聚合 ready/running/failed/waiting-human 状态。",
      "`cli/lib/job_queue.py` (`extend`): 提供 backlog/status 查询接口。",
      "`cli/commands/loop/command.py` (`new`): 暴露 `show-status` / `show-backlog` 监控命令。"
    ],
    "main_sequence": [
      "1. collect ready/running/failed/deadletter/waiting-human states",
      "2. project authoritative observability snapshot",
      "3. render operator-facing backlog / status view",
      "4. expose lineage refs for diagnostics and recovery"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "collaboration": {
    "architecture_topics": [
      "Boundary to gate decision / publication: 本 FEAT 负责 authoritative handoff submission、gate-pending visibility 与 decision-driven runtime re-entry routing，不负责 decision vocabulary、decision issuance 与 formal publication trigger semantics。",
      "Boundary to admission/layering: 本 FEAT 可以提交 candidate / proposal / evidence，但 formal admission、formal refs 与 downstream read eligibility 由对象分层 FEAT 决定。",
      "Dedicated runtime placement is required so submission receipt、pending visibility 和 re-entry routing 由同一 authoritative carrier 负责，而不是散落在 producer skill 或 gate worker 中。"
    ],
    "responsibility_splits": [
      "Execution loop owns candidate/proposal/evidence preparation and authoritative submission initiation.",
      "Gate loop and human review own decision semantics; this FEAT only consumes returned decision objects as structured inputs for visibility and routing.",
      "Runtime owns gate-pending visibility and revise/retry re-entry routing after decision return, but does not own formal publication or final approval semantics."
    ],
    "api_surfaces": [
      "handoff submission contract",
      "gate pending visibility contract"
    ],
    "api_cli_commands": [
      "`ll gate submit-handoff --request <gate_submit_handoff.request.json> --response-out <gate_submit_handoff.response.json>` via `cli/commands/gate/command.py`",
      "`ll gate show-pending --request <gate_show_pending.request.json> --response-out <gate_show_pending.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll gate submit-handoff",
        "surface": "`ll gate submit-handoff --request <gate_submit_handoff.request.json> --response-out <gate_submit_handoff.response.json>` via `cli/commands/gate/command.py`",
        "request_schema": [
          "`producer_ref: string`",
          "`proposal_ref: string`",
          "`payload_ref: string`",
          "`pending_state: enum<gate_pending|human_review_pending|reentry_pending|retry_pending>`",
          "`trace_context_ref: string?`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`handoff_ref`, `queue_slot`, `gate_pending_ref`, `canonical_payload_path`, `pending_state`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`",
          "error fields=`code`, `message`, `retryable`, `idempotent_replay`"
        ],
        "field_semantics": [
          "`pending_state` freezes only pending-visibility states; it must not encode final gate decisions.",
          "`gate_pending_ref` is the canonical ref of the pending-intake record created by this submission.",
          "`queue_slot` identifies the current gate queue slot responsible for the handoff.",
          "`canonical_payload_path` must point to the normalized runtime-visible payload location used for traceability."
        ],
        "enum_domain": [
          "`pending_state ∈ {gate_pending, human_review_pending, reentry_pending, retry_pending}`"
        ],
        "invariants": [
          "one accepted submission creates exactly one authoritative `handoff_ref`",
          "the same idempotency key must not allocate a second queue slot",
          "`gate_pending_ref` and `trace_ref` must be returned on every successful submission"
        ],
        "canonical_refs": [
          "`handoff_ref`",
          "`gate_pending_ref`",
          "`trace_ref`",
          "`canonical_payload_path`"
        ],
        "errors": [
          "`missing_payload`",
          "`invalid_state`",
          "`duplicate_submission`"
        ],
        "idempotency": "`producer_ref + proposal_ref + payload_digest`",
        "preconditions": [
          "`payload_ref` is already readable from the governed runtime carrier",
          "`proposal_ref` resolves to the same submission intent as the payload"
        ],
        "caller_context": "Operator calls ll gate submit-handoff via CLI with valid request envelope.",
        "idempotency_key_strategy": "`producer_ref + proposal_ref + payload_digest`",
        "post_conditions": [
          "handoff_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`payload_ref` is already readable from the governed runtime carrier",
        "side_effects": [
          "Emits submit-handoff trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll gate submit-handoff result state.",
        "event_outputs": [
          "ll.gate.submit_handoff.executed"
        ]
      },
      {
        "command": "ll gate show-pending",
        "surface": "`ll gate show-pending --request <gate_show_pending.request.json> --response-out <gate_show_pending.response.json>` via `cli/commands/gate/command.py`",
        "request_schema": [
          "`handoff_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`handoff_ref`, `pending_state`, `assigned_gate_queue`, `gate_pending_ref`, `trace_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`",
          "error fields=`code`, `message`, `retryable`"
        ],
        "field_semantics": [
          "`assigned_gate_queue` is the queue currently responsible for the pending handoff, not a downstream execution queue.",
          "`pending_state` must stay inside the frozen pending-state enum and must not leak approve/reject semantics."
        ],
        "enum_domain": [
          "`pending_state ∈ {gate_pending, human_review_pending, reentry_pending, retry_pending}`"
        ],
        "invariants": [
          "the command returns the current pending-intake view for exactly one `handoff_ref`",
          "successful lookups must echo the same `handoff_ref` and `gate_pending_ref` pair created during submission"
        ],
        "canonical_refs": [
          "`handoff_ref`",
          "`gate_pending_ref`",
          "`trace_ref`"
        ],
        "errors": [
          "`handoff_missing`",
          "`pending_state_unavailable`"
        ],
        "idempotency": "`handoff_ref`",
        "preconditions": [
          "`handoff_ref` has been submitted into the mainline runtime"
        ],
        "caller_context": "Operator calls ll gate show-pending via CLI with valid request envelope.",
        "idempotency_key_strategy": "`handoff_ref`",
        "post_conditions": [
          "handoff_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`handoff_ref` has been submitted into the mainline runtime",
        "side_effects": [
          "Emits show-pending trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll gate show-pending result state.",
        "event_outputs": [
          "ll.gate.show_pending.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Execution loop、gate loop、human review 通过文件化 handoff runtime 协作；authoritative handoff submission、pending visibility、decision-return intake 都以结构化对象驱动。",
      "Runtime 在收到 gate decision object 后，只负责可见性回写与 revise/retry re-entry routing；decision vocabulary 仍由 gate decision FEAT authoritative freeze。",
      "Formal publication、approve/handoff 的最终发布语义不在本 FEAT 内实现，本 FEAT 只保留对相邻 publication FEAT 的 authoritative boundary handoff。"
    ],
    "implementation_modules": [
      "Handoff runtime adapter：负责把受治理对象写入/读取主链 runtime，并维持 traceability。",
      "Decision boundary adapter：负责把上游 FEAT 约束映射成 runtime 可执行边界，不把实现责任散落到业务 skill。",
      "Submission coordinator：定义 candidate/proposal/evidence 进入 authoritative handoff 的入口、receipt 与 pending visibility。",
      "Decision return adapter：消费 gate decision object，并把 revise/retry 映射成 runtime re-entry directive，而不重写 decision semantics。"
    ],
    "state_model": [
      "`handoff_prepared` -> `handoff_submitted` -> `gate_pending_visible` -> `decision_returned`",
      "`decision_returned(revise|retry)` -> `runtime_reentry_directive_written` -> `handoff_prepared`",
      "`decision_returned(approve|handoff|reject)` -> `boundary_handoff_recorded`，由 formalization / downstream runtime 消费后续推进"
    ],
    "architecture_diagram": "```text\n[Execution Loop] --> [Handoff Runtime] --> [Gate Loop] --> [Human Review]\n       ^                    |                     |\n       |                    +--> pending visibility --> [Producer / Upstream]\n       |                                          |\n       +---- revise/retry routing <---------------+\n                            decision object -----> [Handoff Runtime]\n```",
    "tech_runtime_view": "```text\n[cli/commands/gate/command.py]\n              |\n              v\n[cli/lib/mainline_runtime.py] --> [Gate Pending Receipt / Visibility]\n              |\n              +--> [cli/lib/protocol.py]\n              |\n              +--> [cli/lib/reentry.py] (revise/retry routing only)\n```",
    "flow_diagram": "```text\nExecution Loop -> Runtime      : submit candidate / proposal / evidence\nRuntime        -> Gate Loop    : persist authoritative handoff and publish pending visibility\nGate Loop      -> Human Review : escalate when required\nHuman Review   -> Gate Loop    : return decision object\nGate Loop      -> Runtime      : return decision object\nRuntime        -> Execution Loop: write revise/retry re-entry directive when applicable\nRuntime        -> Downstream   : expose boundary handoff record for approve/handoff/reject outcomes\n```",
    "implementation_strategy": [
      "先冻结 authoritative handoff、pending visibility 和 decision return intake，再接入 human review escalation。",
      "把 revise / retry 收敛为 runtime-owned re-entry routing，不允许 business skill 或 gate worker 私下拼接回流路径。",
      "最后用至少一条真实 submit -> pending -> decision-return -> re-entry pilot 验证协作闭环成立，同时证明 gate decision issuance / formal publication 仍在本 FEAT 外。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `HandoffEnvelope`、`PendingVisibilityRecord`、`DecisionReturnEnvelope`、`ReentryDirective` 结构。",
      "`cli/lib/mainline_runtime.py` (`new`): 管理 authoritative submission、pending visibility、decision-return intake 与 boundary handoff record。",
      "`cli/lib/reentry.py` (`new`): 只处理 revise / retry 的 runtime routing、directive 写回与 replay guard，不拥有 decision semantics。",
      "`cli/commands/gate/command.py` (`extend`): 接入 submit-handoff / show-pending 路径，并把 returned decision 交给 `cli/lib/mainline_runtime.py` 消费。",
      "`cli/commands/audit/command.py` (`extend`): 作为 human review escalation 的旁路消费方，回写 structured review context 而非 formalization result。"
    ],
    "main_sequence": [
      "1. normalize candidate/proposal/evidence submission and producer state",
      "2. persist authoritative handoff object and emit gate-pending visibility",
      "3. route proposal into gate loop and escalate to human review when required",
      "4. consume structured decision object when it returns to runtime",
      "5. if decision in {revise, retry}, write re-entry directive and replay guard",
      "6. if decision in {approve, handoff, reject}, persist boundary handoff record without materializing formal output here"
    ],
    "exception_compensation": [
      "authoritative handoff 已提交但 pending visibility build fail：不得重复创建 handoff；保留 handoff object，标记 `visibility_pending` 并要求补写 receipt。",
      "decision return consumed 但 re-entry directive write fail：返回 `reentry_pending`，要求修复写入后重放，不允许业务 skill 绕回。",
      "boundary handoff record persist fail：不得偷跑 formalization；保持 decision visible but `downstream_handoff_pending`，等待 runtime repair。"
    ],
    "integration_points": [
      "调用方：producer skill 通过 `cli/commands/gate/command.py` / `cli/lib/mainline_runtime.py` 写入 authoritative handoff；gate loop 和 human review 在返回 decision object 时仍沿现有 mainline loop 挂接。",
      "挂接点：file-handoff 发生在 producer 提交之后；external gate 作为现有 loop 的独立阶段消费 proposal 并返回 structured decision object。",
      "旧系统兼容：旧 skill 若未接入统一 re-entry routing，只能以 compat mode 观察 pending visibility，不允许自定义 revise/retry 回流规则。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef advance_mainline(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    next_state = apply_runtime_transition(slot, decision)\n    record_transition_evidence(slot.handoff_ref, next_state)\n    return next_state\n```",
      "failure_path": "```python\ndef advance_mainline_with_reentry(handoff: HandoffEnvelope) -> RuntimeTransition:\n    slot = persist_handoff(handoff)\n    decision = request_gate_decision(slot.handoff_ref)\n    if decision.kind in {'revise', 'retry'}:\n        return write_reentry_command(slot.handoff_ref, decision)\n    return advance_mainline(handoff)\n```"
    }
  },
  "formalization": {
    "architecture_topics": [
      "Boundary to collaboration runtime: formalization FEAT 消费 authoritative handoff 与 proposal，不重新定义 submission receipt 或 pending visibility。",
      "Boundary to downstream publication/admission: 本 FEAT 负责 gate brief、pending human decision、authoritative decision object 与 dispatch trigger，不负责 formal publish / consumer admission policy 本身。",
      "Dedicated gate placement is required so brief、pending、decision、dispatch 使用同一 authoritative path，而不是散落在 gate worker 或 business skill 中。"
    ],
    "responsibility_splits": [
      "External gate owns gate brief build and approve / revise / retry / handoff / reject decision semantics, and emits the only authoritative decision object.",
      "Dispatch router owns execution / delegate / publication-trigger routing after decision issuance, but does not own formal publish itself or downstream admission policy.",
      "Business skills do not bypass gate by issuing decision objects or writing formal publication triggers directly."
    ],
    "api_surfaces": [
      "gate decision contract",
      "formal publication contract"
    ],
    "api_cli_commands": [
      "`ll gate evaluate --request <gate_evaluate.request.json> --response-out <gate_evaluate.response.json>` via `cli/commands/gate/command.py`",
      "`ll gate dispatch --request <gate_dispatch.request.json> --response-out <gate_dispatch.response.json>` via `cli/commands/gate/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll gate evaluate",
        "surface": "`ll gate evaluate --request <gate_evaluate.request.json> --response-out <gate_evaluate.response.json>` via `cli/commands/gate/command.py`",
        "request_schema": [
          "`handoff_ref: string`",
          "`proposal_ref: string`",
          "`evidence_refs: string[]`",
          "`brief_round: integer`",
          "`priority_hint: enum<P0|P1|P2>?`",
          "`merge_group_hint: string?`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`brief_record_ref`, `pending_human_decision_ref`, `decision_ref`, `decision`, `decision_target`, `decision_basis_refs`, `dispatch_target`, `trace_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`brief_record_ref` is the authoritative gate-brief record built from the handoff submission and evidence set.",
          "`pending_human_decision_ref` identifies the uniquely claimed pending-human record consumed by the decision round.",
          "`decision_ref` is the only authoritative gate result for downstream execution, delegation, or publication-trigger consumers.",
          "`decision_basis_refs` must point back to the exact brief/evidence set used for this decision round."
        ],
        "enum_domain": [
          "`priority_hint ∈ {P0, P1, P2}`",
          "`decision ∈ {approve, revise, retry, handoff, reject}`"
        ],
        "invariants": [
          "one brief round yields at most one authoritative `brief_record_ref` and one active `pending_human_decision_ref`",
          "one decision round yields at most one authoritative `decision_ref`",
          "`decision_target` and `decision_basis_refs` must be present before the decision is accepted"
        ],
        "canonical_refs": [
          "`brief_record_ref`",
          "`pending_human_decision_ref`",
          "`decision_ref`",
          "`handoff_ref`",
          "`trace_ref`"
        ],
        "errors": [
          "`handoff_missing`",
          "`invalid_state`",
          "`decision_conflict`",
          "`missing_basis_refs`"
        ],
        "idempotency": "`pending_human_decision_ref + decision_round`",
        "preconditions": [
          "`handoff_ref` is already in gate-pending state",
          "the pending human decision is active and uniquely claimed"
        ],
        "caller_context": "Operator calls ll gate evaluate via CLI with valid request envelope.",
        "idempotency_key_strategy": "`pending_human_decision_ref + decision_round`",
        "post_conditions": [
          "brief_record_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`handoff_ref` is already in gate-pending state",
        "side_effects": [
          "Emits evaluate trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll gate evaluate result state.",
        "event_outputs": [
          "ll.gate.evaluate.executed"
        ]
      },
      {
        "command": "ll gate dispatch",
        "surface": "`ll gate dispatch --request <gate_dispatch.request.json> --response-out <gate_dispatch.response.json>` via `cli/commands/gate/command.py`",
        "request_schema": [
          "`decision_ref: string`",
          "`dispatch_target: enum<execution_return|delegated_handler|formal_publication_trigger|reject_terminal>`",
          "`decision_target: string`",
          "`decision_basis_refs: string[]`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`dispatch_receipt_ref`, `dispatch_status`, `materialized_job_ref`, `materialized_handoff_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`dispatch_target` is derived from the authoritative decision object and is not an independently chosen runtime state.",
          "`dispatch_receipt_ref` records the exact downstream handoff or execution return initiated by this command."
        ],
        "enum_domain": [
          "`dispatch_target ∈ {execution_return, delegated_handler, formal_publication_trigger, reject_terminal}`"
        ],
        "invariants": [
          "dispatch must not succeed without an authoritative decision object",
          "`formal_publication_trigger` may only be selected for `approve` or `handoff` decisions"
        ],
        "canonical_refs": [
          "`decision_ref`",
          "`dispatch_receipt_ref`",
          "`materialized_job_ref`",
          "`materialized_handoff_ref`"
        ],
        "errors": [
          "`decision_not_dispatchable`",
          "`dispatch_failed`",
          "`unknown_dispatch_target`"
        ],
        "idempotency": "`decision_ref + dispatch_target`",
        "preconditions": [
          "`decision_ref` resolves to one authoritative decision object",
          "`decision_target` and `decision_basis_refs` are already present on the decision object"
        ],
        "caller_context": "Operator calls ll gate dispatch via CLI with valid request envelope.",
        "idempotency_key_strategy": "`decision_ref + dispatch_target`",
        "post_conditions": [
          "dispatch_receipt_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`decision_ref` resolves to one authoritative decision object",
        "side_effects": [
          "Emits dispatch trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll gate dispatch result state.",
        "event_outputs": [
          "ll.gate.dispatch.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Business skill 只产出 candidate package / proposal / evidence，由 handoff runtime 承接进入 external gate。",
      "External gate 先把 handoff 压缩成 `gate-brief-record` 与 `gate-pending-human-decision`，再由 reviewer 给出 approve / revise / retry / handoff / reject 决策。",
      "本 FEAT 只负责 decision issuance、trace persistence 与 downstream dispatch trigger；formal publication 由相邻 FEAT 消费 decision object 后继续完成。"
    ],
    "implementation_modules": [
      "Handoff runtime adapter：负责把受治理对象写入/读取主链 runtime，并维持 traceability。",
      "Decision boundary adapter：负责把上游 FEAT 约束映射成 runtime 可执行边界，不把实现责任散落到业务 skill。",
      "Gate brief builder：负责把 handoff submission 压缩成 `gate-brief-record` 与 `gate-pending-human-decision`。",
      "Gate decision processor：解析 approve / revise / retry / handoff / reject，并产出带 `decision_target` 与 `decision_basis_refs` 的唯一 authoritative decision object。",
      "Dispatch router：负责把 decision object 回交给 execution、delegated handler 或 formal publication trigger，而不是直接执行 formal publish。"
    ],
    "state_model": [
      "`candidate_prepared` -> `submitted_to_gate` -> `brief_prepared` -> `pending_human_decision` -> `decision_issued` -> `execution_returned|delegated|publication_triggered|rejected`",
      "`decision_issued(revise)` -> `returned_for_revision` -> `candidate_prepared`",
      "`decision_issued(retry)` -> `retry_pending` -> `submitted_to_gate`"
    ],
    "architecture_diagram": "```text\n[Business Skill]\n      |\n      v\n[Candidate Package] --> [Handoff Runtime] --> [External Gate] --> [Gate Brief Record]\n                                                              |\n                                                              +--> [Pending Human Decision] --> [Decision Object]\n                                                                                                  |\n                                                                                                  +--> revise/retry/reject --> [Execution / Runtime Return]\n                                                                                                  |\n                                                                                                  +--> approve/handoff --> [Dispatch Trigger]\n```",
    "tech_runtime_view": "```text\n[cli/commands/gate/command.py]\n              |\n              +--> [cli/lib/protocol.py]\n              |\n              +--> [cli/lib/registry_store.py]\n              |\n              +--> [Gate Brief Record / Pending Human Decision / Gate Decision]\n```",
    "flow_diagram": "```text\nBusiness Skill -> Runtime         : submit candidate + proposal\nRuntime        -> External Gate   : enqueue handoff for decision\nExternal Gate  -> Runtime         : build gate brief + pending human decision\nReviewer       -> External Gate   : approve / revise / retry / handoff / reject\nExternal Gate  -> Runtime         : persist decision object with target/basis\nRuntime        -> Execution Loop  : return structured decision on revise/retry/reject\nRuntime        -> Delegate/Publish Trigger : dispatch handoff or approve trigger\n```",
    "implementation_strategy": [
      "先固化 candidate -> brief -> pending human -> decision 的对象链与 decision vocabulary。",
      "实现 revise / retry / handoff 回流时，必须先打通 structured decision 回写，并确保 `decision_target` 与 `decision_basis_refs` 完整。",
      "最后把 approve decision 只作为 dispatch trigger 暴露给相邻 formal publication FEAT，不在本 FEAT 内直接 publish。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `GateBriefRecord`、`GatePendingHumanDecision`、`GateDecision` 结构。",
      "`cli/lib/registry_store.py` (`extend`): 写入 brief/decision trace、`decision_target`、`decision_basis_refs` 与 dispatch receipt。",
      "`cli/commands/gate/command.py` (`extend`): 接入 `evaluate` / `dispatch` 语义，生成 brief record、decision object 与回交流水。"
    ],
    "main_sequence": [
      "1. normalize handoff and proposal refs",
      "2. validate gate-pending state and build `gate-brief-record`",
      "3. persist `gate-pending-human-decision` and human-facing projection",
      "4. capture human decision action and persist authoritative decision object",
      "5. validate `decision_target` and `decision_basis_refs`",
      "6. dispatch structured result to execution, delegated handler, or formal publication trigger"
    ],
    "exception_compensation": [
      "brief persisted 但 pending human decision 未建立：保留 `brief_record_ref`，阻止 decision issuance，并记录 `pending_build_failed`。",
      "decision capture 缺少 `decision_target` 或 `decision_basis_refs`：拒绝落 decision object，保留 active pending human decision，要求补充依据后重试。",
      "dispatch fail：保留 authoritative decision object，不伪造下游 publish 完成态，并记录 `dispatch_pending` 供后续 repair。"
    ],
    "integration_points": [
      "调用方：现有 governed skill 通过 handoff runtime 提交 candidate package，由 `cli/commands/gate/command.py` 负责 evaluate / dispatch。",
      "挂接点：file-handoff 发生在 candidate package 写入 runtime 之后；本 FEAT 只把 approve 决策交接为 formal publication trigger，不直接 materialize formal object。",
      "旧系统兼容：business skill 保持只产出 candidate/proposal/evidence，不新增直接 formal write 路径。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef evaluate_gate_decision(handoff_ref: str) -> GateDecision:\n    envelope = load_handoff_envelope(handoff_ref)\n    brief = build_gate_brief_record(envelope)\n    pending = persist_pending_human_decision(brief)\n    action = capture_human_action(pending)\n    decision = persist_gate_decision(\n        pending_ref=pending.ref,\n        human_action=action.kind,\n        decision_target=action.target,\n        decision_basis_refs=action.basis_refs,\n    )\n    dispatch_decision(decision)\n    return decision\n```",
      "failure_path": "```python\ndef evaluate_gate_decision_with_repair(handoff_ref: str) -> RepairOutcome:\n    envelope = load_handoff_envelope(handoff_ref)\n    brief = build_gate_brief_record(envelope)\n    pending = persist_pending_human_decision(brief)\n    action = capture_human_action(pending)\n    if not action.target or not action.basis_refs:\n        mark_pending_repair_required(pending.ref)\n        return request_basis_completion(pending.ref)\n    decision = persist_gate_decision(\n        pending_ref=pending.ref,\n        human_action=action.kind,\n        decision_target=action.target,\n        decision_basis_refs=action.basis_refs,\n    )\n    return dispatch_with_repair(decision)\n```"
    }
  },
  "layering": {
    "architecture_topics": [
      "Boundary to collaboration/formalization: 本 FEAT 消费已有 candidate/formal objects，但不定义 handoff submission 或 materialization dispatch。",
      "Boundary to IO governance: 本 FEAT 冻结 authoritative refs 与 admission policy，不重写 path / mode / overwrite 规则。",
      "Dedicated lineage/admission placement is required so formal refs、layer policy 与 downstream eligibility stay authoritative."
    ],
    "responsibility_splits": [
      "Lineage resolver owns candidate/formal/downstream object identity mapping and authoritative refs.",
      "Admission checker owns consumer allow/deny decisions based on formal refs and layer eligibility.",
      "Business skills and consumers do not infer eligibility from paths, filenames, or adjacency."
    ],
    "api_surfaces": [
      "formal ref resolution contract",
      "admission validation contract"
    ],
    "api_cli_commands": [
      "`ll registry resolve-formal-ref --request <registry_resolve_formal_ref.request.json> --response-out <registry_resolve_formal_ref.response.json>` via `cli/commands/registry/command.py`",
      "`ll registry validate-admission --request <registry_validate_admission.request.json> --response-out <registry_validate_admission.response.json>` via `cli/commands/registry/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll registry resolve-formal-ref",
        "surface": "`ll registry resolve-formal-ref --request <registry_resolve_formal_ref.request.json> --response-out <registry_resolve_formal_ref.response.json>` via `cli/commands/registry/command.py`",
        "request_schema": [
          "`requested_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`authoritative_ref`, `layer`, `lineage_ref`, `upstream_refs`, `downstream_refs`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`authoritative_ref` must point to the formal-layer object when one exists.",
          "`layer` is the authoritative object-layer classification consumed by admission logic."
        ],
        "enum_domain": [
          "`layer ∈ {candidate, formal, downstream}`"
        ],
        "invariants": [
          "formal-layer resolution must not fall back to path guessing"
        ],
        "canonical_refs": [
          "`authoritative_ref`",
          "`lineage_ref`"
        ],
        "errors": [
          "`unknown_ref`",
          "`ambiguous_lineage`"
        ],
        "idempotency": "`requested_ref`",
        "preconditions": [
          "`requested_ref` exists in registry or lineage storage"
        ],
        "caller_context": "Operator calls ll registry resolve-formal-ref via CLI with valid request envelope.",
        "idempotency_key_strategy": "`requested_ref`",
        "post_conditions": [
          "authoritative_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`requested_ref` exists in registry or lineage storage",
        "side_effects": [
          "Emits resolve-formal-ref trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll registry resolve-formal-ref result state.",
        "event_outputs": [
          "ll.registry.resolve_formal_ref.executed"
        ]
      },
      {
        "command": "ll registry validate-admission",
        "surface": "`ll registry validate-admission --request <registry_validate_admission.request.json> --response-out <registry_validate_admission.response.json>` via `cli/commands/registry/command.py`",
        "request_schema": [
          "`consumer_ref: string`",
          "`requested_ref: string`",
          "`lineage_ref: string?`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`allow`, `resolved_formal_ref`, `reason_code`, `evidence_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`allow=false` is still a successful admission verdict, not a transport error.",
          "`resolved_formal_ref` must be present whenever `allow=true`."
        ],
        "enum_domain": [
          "`allow ∈ {true, false}`"
        ],
        "invariants": [
          "consumers may only read formal-layer objects after an allow verdict"
        ],
        "canonical_refs": [
          "`resolved_formal_ref`",
          "`evidence_ref`"
        ],
        "errors": [
          "`formal_ref_missing`",
          "`lineage_missing`",
          "`layer_violation`"
        ],
        "idempotency": "`consumer_ref + requested_ref`",
        "preconditions": [
          "`requested_ref` resolves through lineage or registry"
        ],
        "caller_context": "Operator calls ll registry validate-admission via CLI with valid request envelope.",
        "idempotency_key_strategy": "`consumer_ref + requested_ref`",
        "post_conditions": [
          "allow are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`requested_ref` resolves through lineage or registry",
        "side_effects": [
          "Emits validate-admission trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll registry validate-admission result state.",
        "event_outputs": [
          "ll.registry.validate_admission.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Business skill、gate、consumer 分别围绕 candidate layer、formal layer、downstream consumption layer 协作，不共享隐式旁路对象。",
      "Admission checker 只根据 formal refs 与 lineage 放行 downstream 读取，不根据路径邻近关系或目录猜测放行。",
      "Layering policy 与 IO/path policy 分离：本 FEAT 只定义对象资格和引用方向，底层落盘仍消费外部治理基础。"
    ],
    "implementation_modules": [
      "Handoff runtime adapter：负责把受治理对象写入/读取主链 runtime，并维持 traceability。",
      "Decision boundary adapter：负责把上游 FEAT 约束映射成 runtime 可执行边界，不把实现责任散落到业务 skill。",
      "Object lineage resolver：为 candidate/formal/downstream object 建立 lineage 与 authoritative refs。",
      "Admission checker：在 consumer 读取前验证 formal refs、lineage 与 layer eligibility。"
    ],
    "state_model": [
      "`candidate_only`：仅允许 gate/runtime 消费，不允许 downstream 直接读取。",
      "`formal_authorized`：已具备 formal refs 与 lineage，可成为正式输入。",
      "`consumer_admitted`：consumer 通过 admission checker 后可读取 formal layer。"
    ],
    "architecture_diagram": "```text\n[Business Skill] --> [Candidate Layer] --> [Gate / Runtime] --> [Formal Layer] --> [Admission Checker] --> [Consumer Read]\n                                |\n                                +--> candidate-only objects\n                                      x blocked from direct consumer read\n```",
    "tech_runtime_view": "```text\n[cli/commands/registry/command.py]\n              |\n              v\n[cli/lib/lineage.py] --> [cli/lib/admission.py] --> [Admission Verdict]\n              |\n              +--> [cli/lib/protocol.py]\n```",
    "flow_diagram": "```text\nBusiness Skill   -> Gate / Runtime    : write candidate package\nGate / Runtime   -> Lineage Resolver  : resolve formal refs and lineage\nLineage Resolver -> Gate / Runtime    : return formal object metadata\nConsumer         -> Admission Checker : request read\nAdmission Checker-> Consumer          : allow only with formal refs\n```",
    "implementation_strategy": [
      "先定义 candidate/formal/downstream 三层对象与 authoritative refs，再补 admission checker。",
      "把 consumer read path 全部切到 formal refs 校验后，再清理路径猜测或旁路读取。",
      "最后用至少一条真实 consumer consumption 验证 layer boundary 是否成立。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `CandidateRef`、`FormalRef`、`AdmissionRequest`、`AdmissionVerdict` 结构。",
      "`cli/lib/lineage.py` (`new`): 维护 candidate/formal/downstream object 的 lineage 与 authoritative refs。",
      "`cli/lib/admission.py` (`new`): 基于 formal refs、lineage 与 layer eligibility 做准入判断。",
      "`cli/lib/registry_store.py` (`extend`): 提供 lineage 查询和 formal ref 解析能力。",
      "`cli/commands/registry/command.py` (`extend`): 暴露 resolve-formal-ref / validate-admission 操作，依赖 `cli/lib/lineage.py` 和 `cli/lib/admission.py`。"
    ],
    "main_sequence": [
      "1. normalize requested ref and consumer identity",
      "2. resolve lineage and authoritative formal ref",
      "3. verify requested layer and consumer eligibility",
      "4. emit admission verdict and resolved formal ref",
      "5. record read evidence for audit / gate traceability"
    ],
    "exception_compensation": [
      "lineage resolve fail：直接 deny admission，不允许退化为路径猜测。",
      "formal ref 存在但 layer mismatch：返回 `layer_violation`，consumer 不得继续读 candidate layer。",
      "admission evidence persist fail：允许 verdict 返回，但把 read 标记为 `audit_pending`，后续 gate 需感知缺失 evidence。"
    ],
    "integration_points": [
      "调用方：downstream consumer 在正式读取前调用 admission checker；registry 负责提供 formal refs 与 lineage。",
      "挂接点：file-handoff 完成后先 resolve formal refs，再决定 consumer admission。",
      "旧系统兼容：现有路径猜测读取必须逐步迁移到 formal-ref based access，兼容模式只允许只读告警，不允许默认放行。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef validate_admission(request: AdmissionRequest) -> AdmissionVerdict:\n    lineage = resolve_lineage(request.requested_ref)\n    formal_ref = require_formal_ref(lineage)\n    check_consumer_policy(request.consumer_ref, formal_ref)\n    record_read_evidence(request.consumer_ref, formal_ref)\n    return AdmissionVerdict.allow(formal_ref=formal_ref, lineage_ref=lineage.lineage_ref)\n```",
      "failure_path": "```python\ndef validate_admission_or_deny(request: AdmissionRequest) -> AdmissionVerdict:\n    lineage = resolve_lineage(request.requested_ref)\n    if lineage is None:\n        return AdmissionVerdict.deny(reason_code='lineage_missing')\n    if lineage.layer != 'formal':\n        return AdmissionVerdict.deny(reason_code='layer_violation')\n    return validate_admission(request)\n```"
    }
  },
  "io_governance": {
    "architecture_topics": [
      "Boundary to object layering: 本 FEAT 冻结受治理 IO/path 边界，但不决定对象层级与 admission policy。",
      "Boundary to gate decision / publication: 本 FEAT 约束 write/read carrier 与 receipt/registry 行为，不定义 approve/reject 等 decision semantics。",
      "Dedicated gateway placement is required so policy、IO execution、registry bind 与 receipt publication use one governed carrier."
    ],
    "responsibility_splits": [
      "Path policy owns allow/deny and mode decisions before any governed read/write executes.",
      "Gateway owns write/read orchestration, registry prerequisite checks, receipt generation, and managed ref publication.",
      "Callers do not bypass Gateway with direct filesystem writes once the operation is declared governed."
    ],
    "api_surfaces": [
      "governed write contract",
      "governed read contract"
    ],
    "api_cli_commands": [
      "`ll artifact commit --request <artifact_commit.request.json> --response-out <artifact_commit.response.json>` via `cli/commands/artifact/command.py`",
      "`ll artifact read --request <artifact_read.request.json> --response-out <artifact_read.response.json>` via `cli/commands/artifact/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll artifact commit",
        "surface": "`ll artifact commit --request <artifact_commit.request.json> --response-out <artifact_commit.response.json>` via `cli/commands/artifact/command.py`",
        "request_schema": [
          "`logical_path: string`",
          "`path_class: string`",
          "`mode: enum<read|write>`",
          "`payload_ref: string`",
          "`overwrite: bool`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`managed_ref`, `registry_record_ref`, `write_receipt_ref`, `mode_decision`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`mode_decision` is the normalized governed mode actually applied after policy evaluation.",
          "`managed_ref` is the canonical downstream-consumable artifact reference."
        ],
        "enum_domain": [
          "`mode ∈ {read, write}`"
        ],
        "invariants": [
          "a successful governed write must return both `managed_ref` and `write_receipt_ref`"
        ],
        "canonical_refs": [
          "`managed_ref`",
          "`registry_record_ref`",
          "`write_receipt_ref`"
        ],
        "errors": [
          "`policy_deny`",
          "`registry_prerequisite_failed`",
          "`write_failed`"
        ],
        "idempotency": "`logical_path + payload_digest + mode`",
        "preconditions": [
          "request is normalized and payload is readable"
        ],
        "caller_context": "Operator calls ll artifact commit via CLI with valid request envelope.",
        "idempotency_key_strategy": "`logical_path + payload_digest + mode`",
        "post_conditions": [
          "managed_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "request is normalized and payload is readable",
        "side_effects": [
          "Emits commit trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll artifact commit result state.",
        "event_outputs": [
          "ll.artifact.commit.executed"
        ]
      },
      {
        "command": "ll artifact read",
        "surface": "`ll artifact read --request <artifact_read.request.json> --response-out <artifact_read.response.json>` via `cli/commands/artifact/command.py`",
        "request_schema": [
          "`managed_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`payload_ref`, `resolved_path`, `registry_record_ref`, `read_receipt_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`resolved_path` is the policy-approved physical path behind the managed ref."
        ],
        "enum_domain": [],
        "invariants": [
          "managed reads do not bypass registry-record resolution"
        ],
        "canonical_refs": [
          "`managed_ref`",
          "`registry_record_ref`",
          "`read_receipt_ref`"
        ],
        "errors": [
          "`managed_ref_missing`",
          "`registry_record_missing`",
          "`read_forbidden`"
        ],
        "idempotency": "`managed_ref`",
        "preconditions": [
          "`managed_ref` is already registered"
        ],
        "caller_context": "Operator calls ll artifact read via CLI with valid request envelope.",
        "idempotency_key_strategy": "`managed_ref`",
        "post_conditions": [
          "payload_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "`managed_ref` is already registered",
        "side_effects": [
          "Emits read trace event"
        ],
        "ui_surface_impact": "CLI output reflects ll artifact read result state.",
        "event_outputs": [
          "ll.artifact.read.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Governed skill、handoff runtime、formal publication 相关写入都通过 ADR-005 提供的 Gateway / Path Policy / Registry 接入受治理 IO。",
      "Mainline handoff 与 formal writes 共享同一套 path / mode 约束，不允许局部目录策略绕过受治理写入。",
      "全局文件治理、仓库级目录重构与非 governed skill 自由写入不进入本实现范围。"
    ],
    "implementation_modules": [
      "Handoff runtime adapter：负责把受治理对象写入/读取主链 runtime，并维持 traceability。",
      "Decision boundary adapter：负责把上游 FEAT 约束映射成 runtime 可执行边界，不把实现责任散落到业务 skill。",
      "Gateway integration adapter：把 handoff/materialization 写入重定向到 ADR-005 Gateway。",
      "Path governance guard：在写入前校验 path / mode / overwrite 规则，并拒绝自由写入 fallback。"
    ],
    "state_model": [
      "`write_requested` -> `path_validated` -> `gateway_committed` -> `registry_recorded` -> `consumable_ref_published`",
      "`path_validated(fail)` -> `write_rejected`，不得 silent fallback 到自由写入。"
    ],
    "architecture_diagram": "```text\n[Governed Skill / Runtime]\n          |\n          v\n[Gateway Integration Adapter] --> [Path Policy] --> [Artifact IO Gateway] --> [Artifact Registry] --> [Managed Artifact Ref] --> [Gate / Consumer]\n```",
    "tech_runtime_view": "```text\n[cli/commands/artifact/command.py]\n              |\n              v\n[cli/lib/managed_gateway.py] --> [cli/lib/policy.py]\n              |\n              +--> [cli/lib/fs.py] --> [cli/lib/registry_store.py]\n```",
    "flow_diagram": "```text\nRuntime / Skill     -> Gateway Adapter : request governed write\nGateway Adapter     -> Path Policy     : validate path / mode\nPath Policy         -> Gateway Adapter : allow or reject\nGateway Adapter     -> IO Gateway      : commit artifact when allowed\nIO Gateway          -> Registry        : register managed ref\nRegistry            -> Runtime / Skill : publish consumable ref\nGateway Adapter     -> Runtime / Skill : return governed rejection when blocked\n```",
    "implementation_strategy": [
      "先接通 runtime 到 ADR-005 Gateway / Path Policy / Registry 的调用路径，再禁止自由写入 fallback。",
      "把 mainline handoff 与 formal publication 相关写入都切到同一条受治理 IO 链路，避免双轨写盘。",
      "最后用真实 handoff write + formal write 两条样例验证 path / mode / registry 行为。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/policy.py` (`extend`): 定义 path / mode / overwrite 的 preflight verdict 规则。",
      "`cli/lib/fs.py` (`extend`): 实现 governed read/write 的底层文件访问与 receipt 落盘。",
      "`cli/lib/managed_gateway.py` (`new`): 编排 preflight、gateway commit、registry bind、receipt build。",
      "`cli/lib/registry_store.py` (`extend`): 记录 managed artifact ref、registry prerequisite 和 publish 状态。",
      "`cli/commands/artifact/command.py` (`extend`): 暴露 governed artifact commit / read 入口，依赖 `cli/lib/managed_gateway.py`。"
    ],
    "main_sequence": [
      "1. normalize request",
      "2. preflight policy check",
      "3. registry prerequisite check",
      "4. execute governed handler",
      "5. build receipt and managed ref",
      "6. persist staging / evidence / registry record",
      "7. return result"
    ],
    "exception_compensation": [
      "policy pass 但 registry prerequisite fail：拒绝写入，返回 `registry_prerequisite_failed`，不得绕过 registry 直接落盘。",
      "write success 但 receipt build fail：保留 staged artifact，标记 `receipt_pending`，禁止发布 managed ref 给 consumer。",
      "staging retention fail：允许主写入成功，但必须追加 degraded evidence，并要求后续 cleanup job 补偿。"
    ],
    "integration_points": [
      "调用方：runtime、formal publication 相关写入、governed skill 的正式写入都通过 `cli/commands/artifact/command.py` 进入 Gateway。",
      "挂接点：file-handoff 写入发生在 policy preflight 之后、registry bind 之前；external gate 读取 formal refs 时只消费 managed artifact ref。",
      "旧系统兼容：compat mode 仅允许受控 read fallback；正式 write 不允许 bypass Gateway。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef governed_write(request: GatewayWriteRequest) -> GatewayWriteResult:\n    normalized = normalize_write_request(request)\n    verdict = preflight_policy_check(normalized)\n    require(verdict.allow, verdict.reason_code)\n    ensure_registry_prerequisite(normalized)\n    artifact_ref = commit_via_gateway(normalized, verdict.resolved_path)\n    receipt = build_write_receipt(artifact_ref, verdict)\n    persist_gateway_evidence(receipt)\n    return GatewayWriteResult(artifact_ref=artifact_ref, receipt_ref=receipt.receipt_ref)\n```",
      "failure_path": "```python\ndef governed_write_with_compensation(request: GatewayWriteRequest) -> GatewayWriteResult:\n    normalized = normalize_write_request(request)\n    verdict = preflight_policy_check(normalized)\n    if not verdict.allow:\n        return GatewayWriteResult.reject(reason_code=verdict.reason_code)\n    artifact_ref = commit_via_gateway(normalized, verdict.resolved_path)\n    try:\n        receipt = build_write_receipt(artifact_ref, verdict)\n    except ReceiptBuildError:\n        mark_receipt_pending(artifact_ref)\n        return GatewayWriteResult.partial_success(artifact_ref=artifact_ref)\n    return GatewayWriteResult(artifact_ref=artifact_ref, receipt_ref=receipt.receipt_ref)\n```"
    }
  },
  "first_ai_advice": {
    "architecture_topics": [
      "Boundary to minimal onboarding: 本 FEAT 只消费已完成的最小建档结果与风险门槛输入，不回写最小建档流程本身。",
      "Boundary to homepage shell: 本 FEAT 只定义首页首轮建议 panel 与生成链，不扩张首页其他信息架构。",
      "Boundary to expanded profile / device data: 缺失扩展画像或设备数据只能走保守建议与补充提示，不能阻塞首轮建议释放。"
    ],
    "responsibility_splits": [
      "First advice panel owns homepage presentation of training_advice_level, first_week_action, needs_more_info_prompt, and device_connect_prompt.",
      "First advice service owns minimal-input advice generation and advice_visible release.",
      "Risk gate evaluator owns running_level / recent_injury_status checks before the advice payload becomes user-visible."
    ],
    "api_surfaces": [
      "first advice generation contract",
      "first advice risk gate contract"
    ],
    "api_cli_commands": [
      "`POST /v1/home/first-advice/generate` via first-advice service surface",
      "`POST /v1/home/first-advice/risk-gate` via first-advice service surface"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/home/first-advice/generate",
        "surface": "`POST /v1/home/first-advice/generate` via first-advice service surface",
        "request_schema": [
          "`user_ref: string`",
          "`profile_ref: string`",
          "`running_level: string`",
          "`recent_injury_status: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`training_advice_level`, `first_week_action`, `needs_more_info_prompt`, `device_connect_prompt`, `advice_visible`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`training_advice_level` is the minimum actionable advice grade visible on first release.",
          "`needs_more_info_prompt` is the conservative follow-up when advice quality is constrained by missing inputs."
        ],
        "enum_domain": [
          "`running_level` must use the frozen onboarding domain.",
          "`recent_injury_status` must stay machine-readable for risk gating."
        ],
        "invariants": [
          "first advice must remain available without expanded profile or device data when risk gate passes",
          "advice_visible can only become true after risk gate evaluation"
        ],
        "canonical_refs": [
          "`profile_ref`",
          "`training_advice_level`"
        ],
        "errors": [
          "`risk_gate_blocked`",
          "`minimal_profile_missing`"
        ],
        "idempotency": "`user_ref + profile_ref`",
        "preconditions": [
          "minimal profile already completed"
        ],
        "caller_context": "Operator calls POST /v1/home/first-advice/generate via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + profile_ref`",
        "post_conditions": [
          "training_advice_level are authoritative and consistent"
        ],
        "system_dependency_pre_state": "minimal profile already completed",
        "side_effects": [
          "Emits /v1/home/first-advice/generate trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/home/first-advice/generate result state.",
        "event_outputs": [
          "post..v1.home.first_advice.generate.executed"
        ]
      },
      {
        "command": "POST /v1/home/first-advice/risk-gate",
        "surface": "`POST /v1/home/first-advice/risk-gate` via first-advice service surface",
        "request_schema": [
          "`running_level: string`",
          "`recent_injury_status: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`risk_gate_passed`, `advice_mode`, `blocking_reason?`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`advice_mode` distinguishes normal first-advice release from conservative prompt fallback."
        ],
        "enum_domain": [
          "`advice_mode ∈ {normal_release, conservative_prompt}`"
        ],
        "invariants": [
          "risk gate must run before advice payload becomes user-visible"
        ],
        "canonical_refs": [
          "`risk_gate_passed`",
          "`advice_mode`"
        ],
        "errors": [
          "`risk_input_missing`",
          "`risk_domain_invalid`"
        ],
        "idempotency": "`running_level + recent_injury_status`",
        "preconditions": [
          "risk inputs are present"
        ],
        "caller_context": "Operator calls POST /v1/home/first-advice/risk-gate via CLI with valid request envelope.",
        "idempotency_key_strategy": "`running_level + recent_injury_status`",
        "post_conditions": [
          "risk_gate_passed are authoritative and consistent"
        ],
        "system_dependency_pre_state": "risk inputs are present",
        "side_effects": [
          "Emits /v1/home/first-advice/risk-gate trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/home/first-advice/risk-gate result state.",
        "event_outputs": [
          "post..v1.home.first_advice.risk_gate.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "首页在 `profile_minimal_done` 后立即允许渲染 first advice panel，并通过 risk gate evaluator 决定 advice_visible 与 fallback mode。",
      "First advice service 在最小输入上生成最低可执行输出：training_advice_level、first_week_action、needs_more_info_prompt、device_connect_prompt。",
      "缺失扩展画像或设备数据时不阻塞首轮建议，只影响 advice mode 与补充提示。"
    ],
    "implementation_modules": [
      "First advice panel renderer：负责首页首轮建议展示与 advice_visible 切换。",
      "First advice service：负责生成 training_advice_level / first_week_action 等最低输出。",
      "Risk gate evaluator：负责 running_level / recent_injury_status 风险门槛判断。",
      "Advice prompt builder：负责 normal_release 与 conservative_prompt 模式下的提示拼装。"
    ],
    "state_model": [
      "`profile_minimal_done` -> `advice_inputs_ready` -> `advice_generated` -> `advice_visible`",
      "`advice_inputs_ready(fail)` -> `risk_gate_blocked` -> `completion_prompt_visible`",
      "`advice_visible` must not require expanded profile completion or device connection first."
    ],
    "architecture_diagram": "```text\n[Minimal Profile Complete] -> [Risk Gate Evaluator] -> [First Advice Service] -> [First Advice Panel]\n                                                           |\n                                                           +--> [Conservative Prompt / Device Connect Prompt]\n```",
    "tech_runtime_view": "```text\n[home/first_advice_panel]\n            |\n            v\n[advice/first_advice_service] --> [advice/risk_gate_evaluator]\n                                 |\n                                 +--> [advice/first_advice_prompt_builder]\n```",
    "flow_diagram": "```text\nHomepage -> Risk Gate Evaluator   : evaluate running_level / recent_injury_status\nRisk Gate -> First Advice Service : release first advice when safe\nFirst Advice Service -> Panel     : publish training_advice_level / first_week_action\nFirst Advice Service -> Prompt    : publish needs_more_info_prompt / device_connect_prompt\n```",
    "implementation_strategy": [
      "先冻结最低 advice 输出字段与 risk gate 条件，再实现 generation carrier。",
      "把首轮建议释放和 conservative prompt fallback 放进同一条 advice generation 链，避免旁路分叉。",
      "最后验证最小输入可出建议、风险门槛可拦截、扩展画像/设备数据缺失不阻塞三条主干。"
    ],
    "implementation_unit_mapping": [
      "`home/first_advice_panel` (`new`): 首页展示首轮建议和补充提示。",
      "`advice/first_advice_service` (`new`): 生成 training_advice_level、first_week_action 与 advice_visible。",
      "`advice/risk_gate_evaluator` (`new`): 判断 running_level / recent_injury_status 风险门槛。",
      "`advice/first_advice_prompt_builder` (`new`): 生成 needs_more_info_prompt 和 device_connect_prompt。"
    ],
    "main_sequence": [
      "1. resolve minimal-profile completion and first-advice input readiness",
      "2. evaluate running_level and recent_injury_status through GenerateFirstAdvice / EvaluateFirstAdviceRiskGate",
      "3. build training_advice_level and first_week_action when risk gate passes",
      "4. publish needs_more_info_prompt and device_connect_prompt when conservative fallback is needed",
      "5. mark advice_visible on the homepage first advice panel"
    ],
    "exception_compensation": [
      "risk gate input missing：返回 conservative prompt mode，不得伪造 normal_release advice。",
      "advice generation fail：保留 homepage 可用，显示 retryable degraded message 与补充信息入口。",
      "panel render fail：不回退 minimal onboarding completion，仅记录 advice_pending visibility state。"
    ],
    "integration_points": [
      "调用方：homepage shell 在 profile_minimal_done 后调用本 FEAT 的 first advice generation surface。",
      "挂接点：risk gate evaluator 必须在 advice_visible 前执行，不能由页面层直接跳过。",
      "旧系统兼容：device connect prompt 可以增强首轮建议，但不能成为 release 前置。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef generate_first_advice(request: GenerateFirstAdvice) -> FirstAdviceResult:\n    gate = evaluate_first_advice_risk_gate(request.running_level, request.recent_injury_status)\n    require(gate.risk_gate_passed, gate.blocking_reason)\n    payload = build_first_advice_payload(request.profile_ref, gate.advice_mode)\n    return FirstAdviceResult(training_advice_level=payload.training_advice_level, first_week_action=payload.first_week_action, needs_more_info_prompt=payload.needs_more_info_prompt, device_connect_prompt=payload.device_connect_prompt, advice_visible=True)\n```",
      "failure_path": "```python\ndef generate_first_advice_with_fallback(request: GenerateFirstAdvice) -> FirstAdviceResult:\n    gate = evaluate_first_advice_risk_gate(request.running_level, request.recent_injury_status)\n    if not gate.risk_gate_passed:\n        return FirstAdviceResult.conservative_prompt(reason=gate.blocking_reason)\n    return generate_first_advice(request)\n```"
    }
  },
  "extended_profile_completion": {
    "architecture_topics": [
      "Boundary to minimal onboarding: 本 FEAT 只在首页后继续扩展画像，不把扩展字段重新前置成首日阻塞条件。",
      "Boundary to homepage shell: 本 FEAT 只定义 task card 渲染、增量保存与完成度更新，不改写首页主链进入条件。",
      "Boundary to first advice: 扩展画像补全可以增强建议质量，但保存失败不得撤销首页或首轮建议可用性。"
    ],
    "responsibility_splits": [
      "Task card renderer owns homepage task card presentation and retry entry.",
      "Extended profile patch service owns incremental save and patch validation.",
      "Profile completion updater owns completion percent and next_task_cards projection."
    ],
    "api_surfaces": [
      "extended profile patch contract",
      "profile completion tasks contract"
    ],
    "api_cli_commands": [
      "`PATCH /v1/profile/extended-patch` via extended-profile service surface",
      "`GET /v1/profile/completion-tasks` via extended-profile service surface"
    ],
    "api_command_specs": [
      {
        "command": "PATCH /v1/profile/extended-patch",
        "surface": "`PATCH /v1/profile/extended-patch` via extended-profile service surface",
        "request_schema": [
          "`user_ref: string`",
          "`task_card_ref: string`",
          "`patch_fields: object`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`saved_patch_ref`, `profile_completion_percent`, `next_task_cards`",
          "error envelope=`{ ok: false, error, field_errors? }`"
        ],
        "field_semantics": [
          "`patch_fields` applies only to extended profile ownership fields and must remain incrementally savable.",
          "`profile_completion_percent` is a progress projection, not the homepage-entry gate."
        ],
        "enum_domain": [],
        "invariants": [
          "patch save failure must not revoke homepage usability",
          "each successful save must update next_task_cards deterministically"
        ],
        "canonical_refs": [
          "`saved_patch_ref`",
          "`profile_completion_percent`"
        ],
        "errors": [
          "`patch_invalid`",
          "`save_failed`"
        ],
        "idempotency": "`user_ref + patch_digest`",
        "preconditions": [
          "homepage already entered"
        ],
        "caller_context": "Operator calls PATCH /v1/profile/extended-patch via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + patch_digest`",
        "post_conditions": [
          "saved_patch_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "homepage already entered",
        "side_effects": [
          "Emits /v1/profile/extended-patch trace event"
        ],
        "ui_surface_impact": "CLI output reflects PATCH /v1/profile/extended-patch result state.",
        "event_outputs": [
          "patch..v1.profile.extended_patch.executed"
        ]
      },
      {
        "command": "GET /v1/profile/completion-tasks",
        "surface": "`GET /v1/profile/completion-tasks` via extended-profile service surface",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`task_cards`, `completion_percent`, `in_progress_task_ref?`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`task_cards` is the homepage-facing backlog for progressive completion."
        ],
        "enum_domain": [],
        "invariants": [
          "completion tasks must remain available even when a previous patch save failed"
        ],
        "canonical_refs": [
          "`task_cards`",
          "`completion_percent`"
        ],
        "errors": [
          "`profile_state_missing`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "extended profile completion already enabled for homepage"
        ],
        "caller_context": "Operator calls GET /v1/profile/completion-tasks via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "task_cards are authoritative and consistent"
        ],
        "system_dependency_pre_state": "extended profile completion already enabled for homepage",
        "side_effects": [
          "Emits /v1/profile/completion-tasks trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/profile/completion-tasks result state.",
        "event_outputs": [
          "get..v1.profile.completion_tasks.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "首页通过 task card renderer 暴露扩展画像补全过程，用户可分步填写、分步保存。",
      "Extended profile patch service 只处理 patch_fields 与 incremental save，不把部分失败升级成首页不可用。",
      "Profile completion updater 每次保存后更新 completion percent 和 next_task_cards。"
    ],
    "implementation_modules": [
      "Homepage task card renderer：负责渲染扩展画像任务卡和 retry entry。",
      "Extended profile form units：负责分步字段编组与 patch_fields 校验。",
      "Extended profile patch service：负责 SaveExtendedProfilePatch 的增量保存。",
      "Profile completion updater：负责 completion percent 与 next_task_cards 更新。"
    ],
    "state_model": [
      "`homepage_entered` -> `profile_extension_in_progress` -> `profile_extension_partially_done` -> `profile_extension_done`",
      "`profile_extension_in_progress(fail)` -> `patch_save_failed_retryable` -> `profile_extension_in_progress`",
      "`homepage_entered` must remain usable regardless of patch save outcome."
    ],
    "architecture_diagram": "```text\n[Homepage] -> [Task Card Renderer] -> [Extended Profile Form Units] -> [Extended Profile Patch Service]\n                                                                         |\n                                                                         +--> [Profile Completion Updater]\n```",
    "tech_runtime_view": "```text\n[home/profile_task_card_renderer]\n                 |\n                 v\n[profile/profile_extension_form_units] --> [profile/profile_extension_patch_service]\n                                          |\n                                          +--> [profile/profile_completion_updater]\n```",
    "flow_diagram": "```text\nHomepage -> Task Card Renderer        : show completion tasks\nTask Card -> Patch Service            : save extended profile patch\nPatch Service -> Completion Updater   : update completion percent and next_task_cards\nCompletion Updater -> Homepage        : refresh visible task cards without revoking homepage access\n```",
    "implementation_strategy": [
      "先冻结 task card 结构、patch 粒度和 completion percent 口径，再实现增量保存。",
      "把 patch save failure 设计成 retryable side path，而不是 homepage blocking failure。",
      "最后验证 task card 渐进补全、分步保存、失败后可重试三条主干。"
    ],
    "implementation_unit_mapping": [
      "`home/profile_task_card_renderer` (`new`): 首页任务卡与补全入口渲染。",
      "`profile/profile_extension_form_units` (`new`): 扩展画像 patch 字段编组与校验。",
      "`profile/profile_extension_patch_service` (`new`): 执行 SaveExtendedProfilePatch 增量保存。",
      "`profile/profile_completion_updater` (`new`): 计算 completion percent 和 next_task_cards。"
    ],
    "main_sequence": [
      "1. render homepage task cards via GetProfileCompletionTasks",
      "2. open one task card and collect patch_fields",
      "3. save patch through SaveExtendedProfilePatch",
      "4. recompute profile_completion_percent and next_task_cards",
      "5. refresh homepage task cards while keeping homepage usable"
    ],
    "exception_compensation": [
      "patch validation fail：停留在当前 task card，并返回 field-level errors。",
      "patch save fail：保留 homepage 可用和 retry entry，不回退到 blocking onboarding。",
      "completion updater fail：允许 patch 已保存，但把 completion projection 标记为 refresh_pending。"
    ],
    "integration_points": [
      "调用方：homepage shell 在用户进入首页后调用任务卡和增量保存 surface。",
      "挂接点：completion updater 只更新扩展画像完成度，不改写首页进入条件。",
      "旧系统兼容：已存在的完整画像页面可保留，但其保存结果需要回写到同一 incremental patch service。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef save_extended_profile_patch(request: SaveExtendedProfilePatch) -> ExtendedProfilePatchResult:\n    patch = validate_extended_profile_patch(request.patch_fields)\n    saved_patch_ref = persist_extended_profile_patch(request.user_ref, patch)\n    completion = recompute_profile_completion(request.user_ref)\n    return ExtendedProfilePatchResult(saved_patch_ref=saved_patch_ref, profile_completion_percent=completion.percent, next_task_cards=completion.next_task_cards)\n```",
      "failure_path": "```python\ndef save_extended_profile_patch_or_retry(request: SaveExtendedProfilePatch) -> ExtendedProfilePatchResult:\n    patch = validate_extended_profile_patch(request.patch_fields)\n    if patch.errors:\n        return ExtendedProfilePatchResult.reject(field_errors=patch.errors)\n    try:\n        return save_extended_profile_patch(request)\n    except ExtendedProfileSaveError:\n        return ExtendedProfilePatchResult.retryable_failure(reason='patch_save_failed')\n```"
    }
  },
  "device_deferred_entry": {
    "architecture_topics": [
      "Boundary to homepage availability: 本 FEAT 只消费用户已进入首页和首轮建议已可见的前置，不得反向把设备连接变成前置门槛。",
      "Boundary to device vendor auth/sync: 这里只冻结 deferred entry、callback handling 和 non-blocking result，不在本 FEAT 内展开厂商接入实现细节。",
      "Device connection result may enhance experience surfaces, but must not rewrite minimal onboarding completion or homepage admission facts."
    ],
    "responsibility_splits": [
      "Homepage deferred device entry owns entry visibility、skip affordance and retry entry.",
      "Device connection flow owns auth initiation、callback completion and connection result persistence.",
      "Experience enhancement integrator owns post-connection enrichments without revoking homepage entry or first-advice availability."
    ],
    "api_surfaces": [
      "deferred device connection start contract",
      "deferred device connection finalize contract"
    ],
    "api_cli_commands": [
      "`POST /v1/device/deferred-connection/start` via deferred device connection service",
      "`POST /v1/device/deferred-connection/finalize` via deferred device connection service"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/device/deferred-connection/start",
        "surface": "`POST /v1/device/deferred-connection/start` via deferred device connection service",
        "request_schema": [
          "`user_ref: string`",
          "`homepage_session_ref: string`",
          "`device_vendor: string`",
          "`entry_source: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`connection_attempt_ref`, `redirect_uri`, `homepage_access_preserved`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`homepage_access_preserved` must remain true for all start outcomes in this FEAT.",
          "`entry_source` distinguishes deferred homepage entry from any future settings re-entry."
        ],
        "enum_domain": [
          "`entry_source ∈ {homepage_banner, homepage_card, followup_prompt}`"
        ],
        "invariants": [
          "device connection start must not revoke homepage entry",
          "device connection start must remain skippable"
        ],
        "canonical_refs": [
          "`connection_attempt_ref`"
        ],
        "errors": [
          "`unsupported_device_vendor`",
          "`connection_start_unavailable`"
        ],
        "idempotency": "`user_ref + device_vendor + homepage_session_ref`",
        "preconditions": [
          "user has already entered homepage"
        ],
        "caller_context": "Operator calls POST /v1/device/deferred-connection/start via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + device_vendor + homepage_session_ref`",
        "post_conditions": [
          "connection_attempt_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user has already entered homepage",
        "side_effects": [
          "Emits /v1/device/deferred-connection/start trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/device/deferred-connection/start result state.",
        "event_outputs": [
          "post..v1.device.deferred_connection.start.executed"
        ]
      },
      {
        "command": "POST /v1/device/deferred-connection/finalize",
        "surface": "`POST /v1/device/deferred-connection/finalize` via deferred device connection service",
        "request_schema": [
          "`user_ref: string`",
          "`connection_attempt_ref: string`",
          "`callback_payload_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`connection_status`, `experience_enhancement_ready`, `homepage_access_preserved`, `first_advice_access_preserved`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`connection_status` distinguishes connected, skipped and failed_nonblocking outcomes.",
          "`experience_enhancement_ready` only reflects optional enhancement availability."
        ],
        "enum_domain": [
          "`connection_status ∈ {device_connected, device_skipped, device_failed_nonblocking}`"
        ],
        "invariants": [
          "finalize failure must not revoke homepage entry",
          "connected device data may enhance experience but must not overwrite minimal onboarding canonical profile facts"
        ],
        "canonical_refs": [
          "`connection_status`",
          "`experience_enhancement_ready`"
        ],
        "errors": [
          "`device_auth_failed`",
          "`device_sync_failed`"
        ],
        "idempotency": "`connection_attempt_ref + callback_payload_ref`",
        "preconditions": [
          "connection attempt has been started from deferred homepage entry"
        ],
        "caller_context": "Operator calls POST /v1/device/deferred-connection/finalize via CLI with valid request envelope.",
        "idempotency_key_strategy": "`connection_attempt_ref + callback_payload_ref`",
        "post_conditions": [
          "connection_status are authoritative and consistent"
        ],
        "system_dependency_pre_state": "connection attempt has been started from deferred homepage entry",
        "side_effects": [
          "Emits /v1/device/deferred-connection/finalize trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/device/deferred-connection/finalize result state.",
        "event_outputs": [
          "post..v1.device.deferred_connection.finalize.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "首页在主链已放行后暴露 deferred device entry，允许立即跳过，不把设备连接重新拉回 blocking onboarding。",
      "设备连接 flow 只负责 auth start、callback finalize 和 connection result persistence；失败语义必须是 non-blocking。",
      "连接成功后的数据只进入体验增强分支，不得反写最小建档完成态或覆盖 canonical 身体信息事实。"
    ],
    "implementation_modules": [
      "Homepage deferred device entry surface：渲染入口、跳过动作和重试入口。",
      "Deferred device connection coordinator：负责发起 auth、接 callback、写连接状态。",
      "Connection result handler：负责把 connected / skipped / failed_nonblocking 投影到首页增强入口。",
      "Experience enhancement guard：确保增强数据只影响加分体验，不回写 onboarding/mainline completion facts。"
    ],
    "state_model": [
      "`homepage_entered` -> `device_connection_offered` -> `device_connected`",
      "`device_connection_offered` -> `device_skipped`",
      "`device_connection_offered` -> `device_failed_nonblocking`",
      "`device_connected` may enable enhancement surfaces, but homepage and first-advice access remain preserved in all three terminal outcomes."
    ],
    "architecture_diagram": "```text\n[Homepage Entered] -> [Deferred Device Entry] -> [Connection Coordinator] -> [Connection Result Store]\n                                  |                       |\n                                  +--> [Skip Path]        +--> [Experience Enhancement Guard]\n```",
    "tech_runtime_view": "```text\n[homepage/deferred_device_entry]\n              |\n              v\n[device_connection/start] --> [device_connection/finalize] --> [device_connection/result_handler]\n                                                               |\n                                                               +--> [experience/enhancement_guard]\n```",
    "flow_diagram": "```text\nUser -> Homepage Deferred Entry : choose connect or skip\nEntry -> Connection Coordinator : start auth\nCoordinator -> Device Vendor     : redirect / callback\nCoordinator -> Result Handler    : persist connected / skipped / failed_nonblocking\nResult Handler -> Homepage       : keep mainline usable\nResult Handler -> Enhancement    : unlock optional enriched experience only\n```",
    "implementation_strategy": [
      "先冻结 deferred / non-blocking 语义和终态枚举，再接 start/finalize 两个接口面。",
      "把 homepage_access_preserved 与 first_advice_access_preserved 视为强不变量，避免设备异常回撤主链可用性。",
      "最后验证 skip、auth fail、sync fail 三条异常分支都不会影响首页主链。"
    ],
    "implementation_unit_mapping": [
      "`homepage/deferred_device_entry` (`new`): 首页后置设备连接入口与 skip/retry 交互。",
      "`device_connection/start` (`new`): 启动 deferred auth flow 并生成 connection_attempt_ref。",
      "`device_connection/finalize` (`new`): 接 callback、持久化连接终态和 enhancement readiness。",
      "`device_connection/result_handler` (`new`): 统一投影 connected / skipped / failed_nonblocking 结果。",
      "`experience/enhancement_guard` (`new`): 隔离设备增强分支，不让其回写 onboarding canonical facts。"
    ],
    "main_sequence": [
      "1. enter homepage after minimal onboarding mainline completion",
      "2. expose deferred device connection entry with an explicit skip path",
      "3. start device auth flow when the user chooses connect",
      "4. finalize callback and persist `device_connected`, `device_skipped`, or `device_failed_nonblocking`",
      "5. keep homepage and first-advice access preserved for all deferred outcomes",
      "6. unlock optional enhancement surfaces only when device connection succeeds"
    ],
    "exception_compensation": [
      "device auth start fail：保留首页可用并提供 retry entry，不回退主链。",
      "device auth callback/finalize fail：记录 `device_failed_nonblocking`，首页和首轮建议继续可用。",
      "enhancement projection fail：允许连接状态已成功，但把增强体验标记为 refresh_pending，不覆盖 canonical onboarding/profile facts。"
    ],
    "integration_points": [
      "调用方：homepage shell 在主链放行后调用 deferred device entry。",
      "挂接点：连接状态写入后只能影响 optional enhancement entry，不得改写首页放行或首轮建议 readiness。",
      "旧系统兼容：已有设备连接设置页可保留，但它的结果必须汇入同一 deferred connection result model。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef finalize_deferred_device_connection(request: FinalizeDeviceConnection) -> DeferredDeviceConnectionResult:\n    callback = validate_device_callback(request.callback_payload_ref)\n    connection = persist_device_connection_result(request.user_ref, callback)\n    return DeferredDeviceConnectionResult(connection_status='device_connected', experience_enhancement_ready=True, homepage_access_preserved=True, first_advice_access_preserved=True)\n```",
      "failure_path": "```python\ndef finalize_deferred_device_connection_or_keep_homepage(request: FinalizeDeviceConnection) -> DeferredDeviceConnectionResult:\n    try:\n        return finalize_deferred_device_connection(request)\n    except DeviceConnectionError:\n        return DeferredDeviceConnectionResult(connection_status='device_failed_nonblocking', experience_enhancement_ready=False, homepage_access_preserved=True, first_advice_access_preserved=True)\n```"
    }
  },
  "state_profile_boundary": {
    "architecture_topics": [
      "Boundary to page flow: 本 FEAT 冻结业务状态与存储边界，不重新定义页面交互细节。",
      "Boundary to downstream recommendation/runtime: 下游只能读取 canonical boundary 暴露的统一视图，不得自行拼装冲突字段。",
      "Primary state、capability flags 和 canonical profile ownership must be explicit so completion and eligibility judgments never depend on derived aliases."
    ],
    "responsibility_splits": [
      "Onboarding state store owns primary_state and capability_flags transitions.",
      "Physical profile store owns canonical body facts such as birthdate、height and weight.",
      "Canonical state reader owns unified reads across stores and fail-closed conflict handling."
    ],
    "api_surfaces": [
      "primary state write contract",
      "canonical physical profile write contract",
      "unified onboarding state read contract"
    ],
    "api_cli_commands": [
      "`POST /v1/onboarding/primary-state` via onboarding state boundary service",
      "`POST /v1/profile/physical-canonical` via canonical profile boundary service",
      "`GET /v1/onboarding/unified-state` via canonical state reader"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/onboarding/primary-state",
        "surface": "`POST /v1/onboarding/primary-state` via onboarding state boundary service",
        "request_schema": [
          "`user_ref: string`",
          "`primary_state: string`",
          "`capability_flags: object`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`primary_state`, `capability_flags_ref`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`primary_state` is the authoritative onboarding/business progression state.",
          "`capability_flags` capture availability facts and must not replace primary_state."
        ],
        "enum_domain": [
          "`primary_state ∈ {registered, minimal_profile_in_progress, profile_minimal_done, homepage_entered}`"
        ],
        "invariants": [
          "write must preserve known legal state transitions",
          "capability flag mutation must not invent completion states"
        ],
        "canonical_refs": [
          "`primary_state`",
          "`capability_flags_ref`"
        ],
        "errors": [
          "`illegal_primary_state_transition`",
          "`invalid_capability_flag_payload`"
        ],
        "idempotency": "`user_ref + primary_state + capability_flags_digest`",
        "preconditions": [
          "user exists in onboarding state boundary"
        ],
        "caller_context": "Operator calls POST /v1/onboarding/primary-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + primary_state + capability_flags_digest`",
        "post_conditions": [
          "primary_state are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user exists in onboarding state boundary",
        "side_effects": [
          "Emits /v1/onboarding/primary-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/onboarding/primary-state result state.",
        "event_outputs": [
          "post..v1.onboarding.primary_state.executed"
        ]
      },
      {
        "command": "POST /v1/profile/physical-canonical",
        "surface": "`POST /v1/profile/physical-canonical` via canonical profile boundary service",
        "request_schema": [
          "`user_ref: string`",
          "`birthdate?: string`",
          "`height?: number`",
          "`weight?: number`",
          "`running_level?: string`",
          "`recent_injury_status?: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`physical_profile_ref`, `canonical_fields_written`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`birthdate` remains the canonical age-related field; derived age must not become the truth source.",
          "Body-field conflicts must resolve in favor of `user_physical_profile` as the canonical store."
        ],
        "enum_domain": [
          "`canonical_fields_written` is a frozen list of canonical body fields written in this operation"
        ],
        "invariants": [
          "conflicting writes across non-canonical stores must fail closed",
          "canonical profile writes must not mutate primary_state directly"
        ],
        "canonical_refs": [
          "`physical_profile_ref`",
          "`canonical_fields_written`"
        ],
        "errors": [
          "`cross_boundary_conflict`",
          "`canonical_profile_validation_failed`"
        ],
        "idempotency": "`user_ref + canonical_profile_digest`",
        "preconditions": [
          "user identity is resolved"
        ],
        "caller_context": "Operator calls POST /v1/profile/physical-canonical via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + canonical_profile_digest`",
        "post_conditions": [
          "physical_profile_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user identity is resolved",
        "side_effects": [
          "Emits /v1/profile/physical-canonical trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/profile/physical-canonical result state.",
        "event_outputs": [
          "post..v1.profile.physical_canonical.executed"
        ]
      },
      {
        "command": "GET /v1/onboarding/unified-state",
        "surface": "`GET /v1/onboarding/unified-state` via canonical state reader",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`primary_state`, `capability_flags`, `canonical_profile_boundary`, `conflict_blocked`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`canonical_profile_boundary` identifies the stores that may author onboarding-related body facts.",
          "`conflict_blocked` indicates unified reads are fail-closed until conflicting writes are resolved."
        ],
        "enum_domain": [
          "`canonical_profile_boundary ∈ {user_physical_profile, runner_profiles_readonly_projection}`"
        ],
        "invariants": [
          "eligibility and completion checks must read canonical fields instead of derived aliases",
          "reader must fail closed when cross-boundary conflicts remain unresolved"
        ],
        "canonical_refs": [
          "`primary_state`",
          "`canonical_profile_boundary`",
          "`conflict_blocked`"
        ],
        "errors": [
          "`unified_state_missing`",
          "`conflict_resolution_required`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "user exists in canonical boundary services"
        ],
        "caller_context": "Operator calls GET /v1/onboarding/unified-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "primary_state are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user exists in canonical boundary services",
        "side_effects": [
          "Emits /v1/onboarding/unified-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/onboarding/unified-state result state.",
        "event_outputs": [
          "get..v1.onboarding.unified_state.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "把 `primary_state` 和 `capability_flags` 从页面局部态中剥离出来，作为 onboarding state boundary 的显式业务状态承载。",
      "把 `birthdate / height / weight / running_level / recent_injury_status` 等 canonical 身体字段的所有权固定到 `user_physical_profile`，其余存储只能做 projection。",
      "统一读取层必须通过 canonical reader 暴露 completion/eligibility 所需字段，并在跨边界冲突时 fail closed。"
    ],
    "implementation_modules": [
      "Onboarding state boundary service：读写 `primary_state` 与 `capability_flags`。",
      "Canonical physical profile store adapter：统一写入和校验 canonical 身体字段。",
      "Unified onboarding state reader：基于 canonical 字段拼装完成态和能力开关视图。",
      "Cross-boundary conflict validator：检测 users / user_physical_profile / runner_profiles 之间的冲突写入。"
    ],
    "state_model": [
      "`primary_state` captures business progression state and is authoritative for onboarding completion.",
      "`capability_flags` capture feature availability facts and must not replace `primary_state`.",
      "`canonical_profile_boundary` identifies `user_physical_profile` as the sole authority for body facts while `runner_profiles` remains a read model or downstream projection.",
      "`conflict_blocked=true` means unified reads and downstream eligibility checks fail closed until conflicts are resolved."
    ],
    "architecture_diagram": "```text\n[Onboarding State Service] ----> [Unified Onboarding State Reader]\n          |                                 ^\n          v                                 |\n[Primary State + Capability Flags]   [Canonical Physical Profile Store]\n                                              ^\n                                              |\n                                  [Cross-Boundary Conflict Validator]\n```",
    "tech_runtime_view": "```text\n[state_boundary/write_primary_state]\n                |\n                +--> [state_boundary/write_capability_flags]\n                |\n                +--> [profile_boundary/write_physical_profile_canonical]\n                |\n                +--> [state_boundary/read_unified_onboarding_state]\n```",
    "flow_diagram": "```text\nService -> WritePrimaryState             : persist primary_state + flags\nService -> WritePhysicalProfileCanonical : persist canonical body fields\nReader  -> Conflict Validator            : check cross-boundary conflicts\nReader  -> Unified State Result          : return canonical completion/eligibility view or fail closed\n```",
    "implementation_strategy": [
      "先冻结 state/store ownership 规则，再定义读写 contract，最后补统一 reader。",
      "把 completion、eligibility 和 downstream gating 全部绑定到 canonical fields + primary_state，禁止依赖 derived age 或旁路 store。",
      "最后验证冲突写入、非法状态迁移和 unified read fail-closed 三条异常主干。"
    ],
    "implementation_unit_mapping": [
      "`state_boundary/write_primary_state` (`new`): 持久化 `primary_state` 和合法状态迁移。",
      "`state_boundary/write_capability_flags` (`new`): 持久化 capability flags，不越权创造完成态。",
      "`profile_boundary/write_physical_profile_canonical` (`new`): 向 `user_physical_profile` 写入 canonical 身体字段。",
      "`state_boundary/read_unified_onboarding_state` (`new`): 输出统一 completion/eligibility 视图。",
      "`state_boundary/conflict_validator` (`new`): 检测并阻断跨边界冲突写入。"
    ],
    "main_sequence": [
      "1. write legal `primary_state` transitions into onboarding state boundary",
      "2. write canonical body fields into `user_physical_profile`",
      "3. reject cross-boundary conflicts instead of silently merging conflicting stores",
      "4. read unified onboarding state through canonical reader",
      "5. derive completion and eligibility from `primary_state`, `capability_flags`, and canonical fields only"
    ],
    "exception_compensation": [
      "illegal primary state transition：拒绝写入并保留上一个 authoritative state。",
      "cross-boundary canonical profile conflict：fail closed，记录 audit/conflict evidence，禁止 unified reader 放行下游判断。",
      "unified reader detects unresolved conflict：返回 `conflict_blocked=true`，要求先修复边界冲突。"
    ],
    "integration_points": [
      "调用方：onboarding completion、homepage guard、first-advice gate 都必须通过 unified reader 读取状态。",
      "挂接点：旧的 `runner_profiles` 读取面可以保留，但只能作为 projection，不得在身体字段冲突时胜出。",
      "旧系统兼容：`age` 可作为临时派生展示值保留，但 completion 和 eligibility 判断必须基于 canonical `birthdate`。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef read_unified_onboarding_state(user_ref: str) -> UnifiedOnboardingState:\n    state = load_primary_state(user_ref)\n    flags = load_capability_flags(user_ref)\n    profile = load_canonical_physical_profile(user_ref)\n    ensure_no_cross_boundary_conflict(user_ref, profile)\n    return UnifiedOnboardingState(primary_state=state, capability_flags=flags, canonical_profile_boundary='user_physical_profile', conflict_blocked=False)\n```",
      "failure_path": "```python\ndef write_physical_profile_canonical_or_block(request: WritePhysicalProfileCanonical) -> WriteResult:\n    conflicts = detect_cross_boundary_conflicts(request.user_ref, request)\n    if conflicts:\n        raise CrossBoundaryConflict('conflict_blocked')\n    return persist_canonical_physical_profile(request)\n```"
    }
  },
  "minimal_onboarding": {
    "architecture_topics": [
      "Boundary to registration/authentication: 本 FEAT 只消费“已登录/已注册”前置条件，不重写登录注册实现。",
      "Boundary to homepage shell: 本 FEAT 只负责最小建档完成后的首页放行条件，不扩张首页内容或推荐逻辑。",
      "Boundary to device connection: 设备连接入口必须后置，不能重新变回首进主链阻塞条件。"
    ],
    "responsibility_splits": [
      "Minimal profile page owns one-page input collection and inline validation feedback.",
      "Profile completion service owns canonical field normalization, persistence, and `profile_minimal_done` state transition.",
      "Homepage entry guard owns post-submit entry allowance; device connection remains a deferred follow-up entry."
    ],
    "api_surfaces": [
      "minimal profile submission contract",
      "minimal profile state contract"
    ],
    "api_cli_commands": [
      "`POST /v1/onboarding/minimal-profile` via onboarding service surface",
      "`GET /v1/onboarding/minimal-profile-state` via onboarding service surface"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/onboarding/minimal-profile",
        "surface": "`POST /v1/onboarding/minimal-profile` via onboarding service surface",
        "request_schema": [
          "`gender: string`",
          "`birthdate: string`",
          "`height: number`",
          "`weight: number`",
          "`running_level: string`",
          "`recent_injury_status: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`profile_ref`, `profile_minimal_done`, `homepage_entry_allowed`, `device_connection_deferred`",
          "error envelope=`{ ok: false, error, field_errors? }`"
        ],
        "field_semantics": [
          "`birthdate` is the canonical age-related field for downstream persistence and derivation.",
          "`profile_minimal_done` is the authoritative completion marker that gates homepage entry."
        ],
        "enum_domain": [
          "`running_level` must use a frozen onboarding domain rather than free-text labels.",
          "`recent_injury_status` must remain machine-readable for downstream recommendations."
        ],
        "invariants": [
          "homepage entry can only be allowed after all required minimal profile fields validate",
          "device connection must remain deferred even when submission succeeds"
        ],
        "canonical_refs": [
          "`profile_ref`",
          "`profile_minimal_done`"
        ],
        "errors": [
          "`missing_required_field`",
          "`invalid_birthdate`",
          "`invalid_running_level`",
          "`invalid_recent_injury_status`"
        ],
        "idempotency": "`user_ref + payload_digest`",
        "preconditions": [
          "user has completed login/registration and is routed into the minimal profile page"
        ],
        "caller_context": "Operator calls POST /v1/onboarding/minimal-profile via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + payload_digest`",
        "post_conditions": [
          "profile_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user has completed login/registration and is routed into the minimal profile page",
        "side_effects": [
          "Emits /v1/onboarding/minimal-profile trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/onboarding/minimal-profile result state.",
        "event_outputs": [
          "post..v1.onboarding.minimal_profile.executed"
        ]
      },
      {
        "command": "GET /v1/onboarding/minimal-profile-state",
        "surface": "`GET /v1/onboarding/minimal-profile-state` via onboarding service surface",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, result }`",
          "result fields=`onboarding_state`, `missing_fields`, `homepage_entry_allowed`, `device_connection_deferred`",
          "error envelope=`{ ok: false, error }`"
        ],
        "field_semantics": [
          "`onboarding_state` distinguishes in-progress minimal profile from completed minimal profile.",
          "`missing_fields` is a UI-facing recovery hint, not a replacement for validation."
        ],
        "enum_domain": [
          "`onboarding_state ∈ {minimal_profile_in_progress, profile_minimal_done}`"
        ],
        "invariants": [
          "completed users must not be forced back into blocking device connection before homepage entry"
        ],
        "canonical_refs": [
          "`onboarding_state`",
          "`missing_fields`"
        ],
        "errors": [
          "`profile_state_missing`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "user already exists in onboarding state store"
        ],
        "caller_context": "Operator calls GET /v1/onboarding/minimal-profile-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "onboarding_state are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user already exists in onboarding state store",
        "side_effects": [
          "Emits /v1/onboarding/minimal-profile-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/onboarding/minimal-profile-state result state.",
        "event_outputs": [
          "get..v1.onboarding.minimal_profile_state.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "登录/注册成功后，路由守卫先判断 `profile_minimal_done`，未完成时进入单页最小建档页，完成后直接进入首页。",
      "最小建档提交链只负责收集和校验首日必要字段、写入 canonical profile 数据，并显式写下 `profile_minimal_done` 完成标记。",
      "设备连接入口与主链解耦，只能在首页放行后以 deferred entry 方式继续出现。"
    ],
    "implementation_modules": [
      "Minimal profile form schema：定义 `gender / birthdate / height / weight / running_level / recent_injury_status` 的页面输入与校验规则。",
      "Minimal profile submission handler：负责 canonical normalization、字段校验、持久化与错误返回。",
      "Onboarding completion state service：负责写入 `profile_minimal_done`、暴露当前最小建档状态。",
      "Homepage entry guard：负责登录后与提交后的首页放行判断，并隔离设备连接后置入口。"
    ],
    "state_model": [
      "`registered` -> `minimal_profile_in_progress` -> `profile_minimal_done` -> `homepage_entered`",
      "`minimal_profile_in_progress(fail)` -> `field_error_visible` -> `minimal_profile_in_progress`",
      "`profile_minimal_done` must preserve `device_connection_deferred=true` until the post-home entry path is explicitly opened."
    ],
    "architecture_diagram": "```text\n[Auth Complete] -> [Homepage Entry Guard] -> [Minimal Profile Page] -> [Profile Completion Service] -> [Profile State Store]\n                                                |\n                                                +--> [Homepage]\n                                                +--> [Deferred Device Connection Entry]\n```",
    "tech_runtime_view": "```text\n[ui/minimal_profile_page]\n              |\n              v\n[onboarding/minimal_profile_schema] --> [onboarding/minimal_profile_submit]\n                                       |\n                                       +--> [onboarding/profile_state_service] --> [routing/homepage_entry_guard]\n                                       +--> [device_connection/deferred_entry]\n```",
    "flow_diagram": "```text\nUser -> Minimal Profile Page         : fill required fields\nPage -> Submission Handler           : submit minimal profile\nSubmission Handler -> Profile Store  : persist canonical fields + completion marker\nProfile Store -> Homepage Entry Guard: evaluate homepage entry eligibility\nHomepage Entry Guard -> Homepage     : allow enter home immediately\nHomepage Entry Guard -> Device Entry : keep deferred follow-up only\n```",
    "implementation_strategy": [
      "先冻结最小建档字段、完成态与设备连接后置边界，再落页面与提交服务。",
      "把 `profile_minimal_done` 视为单一 authoritative 完成标记，避免页面局部态与业务完成态分裂。",
      "最后验证登录后路由、提交成功放行首页、提交失败留在表单、设备连接不阻塞这四条主干语义。"
    ],
    "implementation_unit_mapping": [
      "`ui/minimal_profile_page` (`new`): 单页收集最小建档字段并渲染字段级错误。",
      "`onboarding/minimal_profile_schema` (`new`): 冻结 required fields、枚举域与 canonical normalization 规则。",
      "`onboarding/minimal_profile_submit` (`new`): 执行校验、持久化、完成态写入与 structured error 返回。",
      "`onboarding/profile_state_service` (`new`): 读写 `minimal_profile_in_progress` / `profile_minimal_done` 状态。",
      "`routing/homepage_entry_guard` (`extend`): 登录后与提交后统一判断首页放行与后置设备连接入口。"
    ],
    "main_sequence": [
      "1. complete login/registration and resolve current onboarding state",
      "2. route incomplete users into the single-page minimal profile flow",
      "3. validate `gender`, `birthdate`, `height`, `weight`, `running_level`, and `recent_injury_status`",
      "4. persist canonical minimal profile fields and mark `profile_minimal_done`",
      "5. allow immediate homepage entry",
      "6. expose device connection only as a deferred follow-up entry"
    ],
    "exception_compensation": [
      "required field missing or invalid：停留在最小建档页并返回 field-level errors，不得提前放行首页。",
      "profile write succeeds but completion marker write fails：标记 `completion_state_pending`，禁止首页放行并保留可重试提交。",
      "homepage guard reads inconsistent state：fail closed 到最小建档页，禁止偷偷按旧状态放行首页。"
    ],
    "integration_points": [
      "调用方：登录/注册完成后的路由守卫进入本 FEAT，输入必须是已认证 user context。",
      "挂接点：提交成功后必须经由统一 homepage entry guard，而不是页面层直接跳转绕过状态判断。",
      "旧系统兼容：设备连接入口可保留，但只能从首页后置入口进入，不能重新插回首进阻塞链路。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef submit_minimal_profile(request: SubmitMinimalProfile) -> MinimalProfileResult:\n    normalized = normalize_minimal_profile(request)\n    validate_required_fields(normalized)\n    profile_ref = persist_minimal_profile(normalized)\n    mark_profile_minimal_done(request.user_ref, profile_ref)\n    return MinimalProfileResult(profile_ref=profile_ref, profile_minimal_done=True, homepage_entry_allowed=True, device_connection_deferred=True)\n```",
      "failure_path": "```python\ndef submit_minimal_profile_or_stay(request: SubmitMinimalProfile) -> MinimalProfileResult:\n    normalized = normalize_minimal_profile(request)\n    errors = validate_minimal_profile(normalized)\n    if errors:\n        return MinimalProfileResult.reject(field_errors=errors, homepage_entry_allowed=False)\n    return submit_minimal_profile(request)\n```"
    }
  },
  "first_ai_advice_release": {
    "architecture_topics": [
      "Boundary to minimal onboarding: 本 FEAT 只消费 minimal profile completion 和 safety gate，不要求扩展画像或设备连接先完成。",
      "Boundary to profile enrichment: first advice release 只产出首轮建议与提示，不承担后续补全任务卡编排。",
      "Dedicated advice release placement is required so first-release payload、safety gate 和 follow-up prompt remain authoritative."
    ],
    "responsibility_splits": [
      "Advice release engine owns首轮建议生成与释放状态写入。",
      "Safety gate owns running_level 与 recent_injury_status 的风险门槛判断。",
      "Onboarding/homepage surfaces own only visibility and navigation, not advice wording."
    ],
    "api_surfaces": [
      "first advice release contract",
      "first advice readiness snapshot contract"
    ],
    "api_cli_commands": [
      "`POST /v1/advice/first-release` via advice release service",
      "`GET /v1/advice/first-release-state` via advice release service"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/advice/first-release",
        "surface": "`POST /v1/advice/first-release` via advice release service",
        "request_schema": [
          "`user_ref: string`",
          "`profile_ref: string`",
          "`minimal_profile_snapshot_ref: string`",
          "`release_context_ref?: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`advice_release_ref`, `training_advice_level`, `first_week_action`, `needs_more_info_prompt`, `device_connect_prompt`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`training_advice_level` is a coarse, release-safe level for the first week.",
          "`needs_more_info_prompt` is a follow-up hint, not a blocker for advice release.",
          "`device_connect_prompt` remains a deferred follow-up and must not block first advice."
        ],
        "enum_domain": [
          "`training_advice_level ∈ {gentle, standard, cautious}`"
        ],
        "invariants": [
          "first advice release must not require extended profile or device data",
          "release payload must always preserve a machine-readable first-week action"
        ],
        "canonical_refs": [
          "`advice_release_ref`",
          "`training_advice_level`",
          "`first_week_action`"
        ],
        "errors": [
          "`profile_not_minimal_done`",
          "`risk_gate_failed`",
          "`advice_generation_failed`"
        ],
        "idempotency": "`profile_ref + minimal_profile_snapshot_ref`",
        "preconditions": [
          "minimal profile has reached completed state",
          "safety gate resolved from running_level and recent_injury_status"
        ],
        "caller_context": "Operator calls POST /v1/advice/first-release via CLI with valid request envelope.",
        "idempotency_key_strategy": "`profile_ref + minimal_profile_snapshot_ref`",
        "post_conditions": [
          "advice_release_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "minimal profile has reached completed state",
        "side_effects": [
          "Emits /v1/advice/first-release trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/advice/first-release result state.",
        "event_outputs": [
          "post..v1.advice.first_release.executed"
        ]
      },
      {
        "command": "GET /v1/advice/first-release-state",
        "surface": "`GET /v1/advice/first-release-state` via advice release service",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`advice_state`, `ready_for_release`, `blocking_missing_fields`, `next_prompt_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`blocking_missing_fields` is a release hint for the next follow-up, not a recommendation to block advice.",
          "`next_prompt_ref` should point to the lowest-friction next action."
        ],
        "enum_domain": [
          "`advice_state ∈ {advice_pending, advice_ready, advice_released}`"
        ],
        "invariants": [
          "state query must never rewrite release state",
          "device connection must never appear as a prerequisite for advice readiness"
        ],
        "canonical_refs": [
          "`advice_state`",
          "`ready_for_release`",
          "`next_prompt_ref`"
        ],
        "errors": [
          "`advice_state_missing`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "user onboarding state is already known"
        ],
        "caller_context": "Operator calls GET /v1/advice/first-release-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "advice_state are authoritative and consistent"
        ],
        "system_dependency_pre_state": "user onboarding state is already known",
        "side_effects": [
          "Emits /v1/advice/first-release-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/advice/first-release-state result state.",
        "event_outputs": [
          "get..v1.advice.first_release_state.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Minimal profile completion produces a release-ready snapshot that can be consumed without waiting for extended profile or device connection.",
      "Safety gate evaluates the canonical minimal fields and emits a first advice release verdict plus follow-up prompts.",
      "Homepage and device flows can surface the released advice, but they must not own advice text generation."
    ],
    "implementation_modules": [
      "Advice release engine：负责生成并落盘首轮 AI 建议。",
      "Safety gate：负责基于 minimal profile 进行风险门槛判断。",
      "Follow-up prompt composer：负责组装 needs_more_info_prompt 与 device_connect_prompt。"
    ],
    "state_model": [
      "`profile_minimal_done` -> `advice_ready` -> `advice_released` -> `advice_visible`",
      "`risk_gate_failed` -> `needs_more_info_visible` -> `advice_ready`"
    ],
    "architecture_diagram": "```text\n[Minimal Profile Done] -> [Safety Gate] -> [Advice Release Engine] -> [First Advice Visible]\n                                         |\n                                         +--> [Follow-up Prompt Composer]\n```",
    "tech_runtime_view": "```text\n[advice/release_gate.py]\n              |\n              v\n[advice/first_release_engine.py] --> [advice/first_release_store.py]\n              |\n              +--> [advice/follow_up_prompt.py]\n```",
    "flow_diagram": "```text\nMinimal Profile -> Safety Gate          : verify release-safe inputs\nSafety Gate     -> Release Engine       : authorize first advice\nRelease Engine   -> Advice Store        : persist release payload\nAdvice Store     -> Homepage / Profile  : expose first advice and follow-up prompts\n```",
    "implementation_strategy": [
      "先冻结 minimal profile snapshot 和 safety gate，再实现首轮建议释放。",
      "把 device_connect_prompt 作为后续提示而不是 release 阻塞项。",
      "最后验证 advice release 可以在 minimal profile 完成后独立成立。"
    ],
    "implementation_unit_mapping": [
      "`advice/release_gate.py` (`new`): 评估 minimal profile 和安全门槛。",
      "`advice/first_release_engine.py` (`new`): 生成首轮 AI 建议 payload。",
      "`advice/first_release_store.py` (`new`): 持久化 advice release state 与结果。",
      "`advice/follow_up_prompt.py` (`new`): 组装后续补充提示，不阻塞首轮建议。"
    ],
    "main_sequence": [
      "1. load minimal profile snapshot",
      "2. evaluate safety gate for first advice release",
      "3. generate training_advice_level and first_week_action",
      "4. persist advice release state",
      "5. surface follow-up prompts without blocking release"
    ],
    "exception_compensation": [
      "minimal profile 未完成：返回 `profile_not_minimal_done` 并保留 release pending。",
      "safety gate 失败：返回可解释的 `needs_more_info_prompt`，但不得把 device connection 变成 blocker。",
      "advice generation 失败：保留 release gate verdict，标记 `advice_generation_pending`，允许重试而不回退首页流程。"
    ],
    "integration_points": [
      "调用方：最小建档完成后由 advice release service 拉起首轮建议，不需要等扩展画像。",
      "挂接点：首页和 onboarding 完成态都可以展示 release 结果，但不重新解释风险门槛。",
      "旧系统兼容：缺少扩展画像时，系统必须退化为首轮建议 + follow-up prompt，而不是阻断。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef release_first_advice(request: FirstAdviceReleaseRequest) -> FirstAdviceReleaseResult:\n    snapshot = load_minimal_profile_snapshot(request.minimal_profile_snapshot_ref)\n    verdict = evaluate_first_advice_gate(snapshot)\n    advice = generate_first_advice(snapshot, verdict)\n    persist_first_advice(request.user_ref, advice, verdict)\n    return FirstAdviceReleaseResult(advice_release_ref=advice.ref, training_advice_level=advice.level, first_week_action=advice.first_week_action, needs_more_info_prompt=advice.needs_more_info_prompt, device_connect_prompt=advice.device_connect_prompt)\n```",
      "failure_path": "```python\ndef release_first_advice_or_hold(request: FirstAdviceReleaseRequest) -> FirstAdviceReleaseResult:\n    snapshot = load_minimal_profile_snapshot(request.minimal_profile_snapshot_ref)\n    verdict = evaluate_first_advice_gate(snapshot)\n    if not verdict.allow:\n        return FirstAdviceReleaseResult.hold(reason='risk_gate_failed', needs_more_info_prompt=verdict.prompt)\n    return release_first_advice(request)\n```"
    }
  },
  "extended_profile_progressive_completion": {
    "architecture_topics": [
      "Boundary to minimal onboarding: 本 FEAT 只承接主页后的扩展画像渐进补全，不回写最小建档的完成判定。",
      "Boundary to first advice release: 扩展补全可优化后续建议，但不得成为首轮建议的前置阻塞。",
      "Dedicated task-card flow is required so each patch save remains independently persistent and independently recoverable."
    ],
    "responsibility_splits": [
      "Task card orchestrator owns card ordering, save intent, and next-step recommendation.",
      "Patch persistence service owns incremental field writes and patch-level idempotency.",
      "Progress tracker owns completion percent and derived task visibility, not canonical profile authority."
    ],
    "api_surfaces": [
      "`POST /v1/profile/extended-patch`",
      "`GET /v1/profile/extended-tasks`"
    ],
    "api_cli_commands": [
      "`POST /v1/profile/extended-patch` via profile progressive completion service",
      "`GET /v1/profile/extended-tasks` via profile progressive completion service"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/profile/extended-patch",
        "surface": "`POST /v1/profile/extended-patch` via profile progressive completion service",
        "request_schema": [
          "`user_ref: string`",
          "`task_card_ref: string`",
          "`patch_fields: object`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`saved_patch_ref`, `profile_completion_percent`, `next_task_cards`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`patch_fields` is a partial profile update and must not replace the full profile document.",
          "`profile_completion_percent` is derived from persisted patch coverage, not from UI state."
        ],
        "enum_domain": [],
        "invariants": [
          "each patch save must be independently replayable",
          "completion percent must never exceed the canonical field coverage actually persisted"
        ],
        "canonical_refs": [
          "`saved_patch_ref`",
          "`profile_completion_percent`",
          "`next_task_cards`"
        ],
        "errors": [
          "`patch_invalid`",
          "`save_failed`",
          "`task_card_stale`"
        ],
        "idempotency": "`user_ref + task_card_ref + patch_digest`",
        "preconditions": [
          "homepage is already entered",
          "task card metadata has been resolved"
        ],
        "caller_context": "Operator calls POST /v1/profile/extended-patch via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + task_card_ref + patch_digest`",
        "post_conditions": [
          "saved_patch_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "homepage is already entered",
        "side_effects": [
          "Emits /v1/profile/extended-patch trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/profile/extended-patch result state.",
        "event_outputs": [
          "post..v1.profile.extended_patch.executed"
        ]
      },
      {
        "command": "GET /v1/profile/extended-tasks",
        "surface": "`GET /v1/profile/extended-tasks` via profile progressive completion service",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`task_cards`, `completion_percent`, `in_progress_task_ref?`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`task_cards` are homepage-visible extension prompts, not required onboarding blockers.",
          "`in_progress_task_ref` only identifies the current patch branch."
        ],
        "enum_domain": [],
        "invariants": [
          "task card list must be stable for the same profile snapshot",
          "querying tasks must not mutate the underlying profile"
        ],
        "canonical_refs": [
          "`task_cards`",
          "`completion_percent`",
          "`in_progress_task_ref`"
        ],
        "errors": [
          "`profile_state_missing`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "extended profile flow is available from homepage"
        ],
        "caller_context": "Operator calls GET /v1/profile/extended-tasks via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "task_cards are authoritative and consistent"
        ],
        "system_dependency_pre_state": "extended profile flow is available from homepage",
        "side_effects": [
          "Emits /v1/profile/extended-tasks trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/profile/extended-tasks result state.",
        "event_outputs": [
          "get..v1.profile.extended_tasks.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Homepage task cards present progressive completion work without changing the minimal onboarding gate.",
      "Each patch save is persisted as a standalone increment so a partial completion can resume from the last saved card.",
      "Progress snapshots are derived from canonical field coverage and are never treated as the source of truth."
    ],
    "implementation_modules": [
      "Extended profile task card catalog：负责提供主页上的扩展补全任务卡。",
      "Patch validation service：负责校验局部 patch 的字段与格式。",
      "Progress tracker：负责计算 completion percent 并产出下一批 task cards。"
    ],
    "state_model": [
      "`homepage_entered` -> `extended_profile_available` -> `extended_profile_editing` -> `patch_saved` -> `progress_advanced` -> `profile_complete`",
      "`patch_invalid` -> `extended_profile_editing`"
    ],
    "architecture_diagram": "```text\n[Homepage Entered] -> [Task Card Catalog] -> [Patch Validation Service] -> [Patch Store] -> [Progress Tracker]\n                                                            |\n                                                            +--> [Next Task Cards]\n```",
    "tech_runtime_view": "```text\n[profile/extended_patch_service.py]\n              |\n              v\n[profile/task_card_catalog.py] --> [profile/progress_tracker.py]\n              |\n              +--> [profile/completion_snapshot.py]\n```",
    "flow_diagram": "```text\nHomepage Task Card -> Patch Validation : validate incremental fields\nPatch Validation   -> Patch Store      : persist one patch branch\nPatch Store        -> Progress Tracker : recalculate completion\nProgress Tracker   -> Homepage         : surface next task cards\n```",
    "implementation_strategy": [
      "先冻结 task-card 目录和 patch schema，再实现局部保存。",
      "把 completion percent 视作派生值，避免 UI 进度反写成权威状态。",
      "最后验证单卡保存、连续保存和中断恢复都能保持独立 idempotency。"
    ],
    "implementation_unit_mapping": [
      "`profile/task_card_catalog.py` (`new`): 提供扩展画像任务卡与排序。",
      "`profile/extended_patch_service.py` (`new`): 校验并保存局部 patch。",
      "`profile/progress_tracker.py` (`new`): 计算 completion percent 与下一批任务卡。",
      "`profile/completion_snapshot.py` (`new`): 生成可展示的进度快照。"
    ],
    "main_sequence": [
      "1. load homepage task-card catalog",
      "2. open selected patch branch",
      "3. validate patch_fields and task_card_ref",
      "4. persist patch increment and completion snapshot",
      "5. recalculate next task cards and return progress"
    ],
    "exception_compensation": [
      "patch_invalid：返回字段级错误，不影响已保存的其他扩展字段。",
      "save_failed：保留 task card 当前位置并允许重试同一 patch branch。",
      "task card version stale：返回 `task_card_stale` 并重新拉取任务目录。"
    ],
    "integration_points": [
      "调用方：主页 task card 区域拉起扩展补全 patch。",
      "挂接点：首轮建议只读取已完成的 canonical minimal/profile snapshot，不等待扩展补全结束。",
      "旧系统兼容：未完成的扩展画像只影响 task cards，不得阻断首页进入或 advice release。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef save_extended_profile_patch(request: ExtendedProfilePatchRequest) -> ExtendedProfilePatchResult:\n    catalog = load_task_card_catalog(request.user_ref)\n    validate_patch(request.patch_fields, request.task_card_ref, catalog)\n    saved_patch = persist_patch_increment(request.user_ref, request.task_card_ref, request.patch_fields)\n    progress = recalculate_completion(request.user_ref)\n    return ExtendedProfilePatchResult(saved_patch_ref=saved_patch.ref, profile_completion_percent=progress.percent, next_task_cards=progress.next_cards)\n```",
      "failure_path": "```python\ndef save_extended_profile_patch_or_retry(request: ExtendedProfilePatchRequest) -> ExtendedProfilePatchResult:\n    if not is_valid_task_card(request.task_card_ref):\n        return ExtendedProfilePatchResult.fail(reason='task_card_stale')\n    return save_extended_profile_patch(request)\n```"
    }
  },
  "device_connect_deferred_entry": {
    "architecture_topics": [
      "Boundary to homepage entry: 设备连接只能作为首页后的增强入口，不得反向阻塞主链进入。",
      "Boundary to advice release and profile completion: 设备连接可补充上下文，但不得成为建议释放或扩展画像的前置条件。",
      "Deferred connection session must stay optional so unsupported providers degrade to skip/fail-nonblocking instead of blocking the user."
    ],
    "responsibility_splits": [
      "Deferred entry coordinator owns when to offer device connection and how to surface it after homepage entry.",
      "Provider adapter owns auth, sync, and device-specific result normalization.",
      "Connection session store owns session lifecycle and nonblocking result persistence."
    ],
    "api_surfaces": [
      "`POST /v1/device/connect/deferred`",
      "`GET /v1/device/connect/deferred-state`"
    ],
    "api_cli_commands": [
      "`POST /v1/device/connect/deferred` via device deferred connection service",
      "`GET /v1/device/connect/deferred-state` via device deferred connection service"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/device/connect/deferred",
        "surface": "`POST /v1/device/connect/deferred` via device deferred connection service",
        "request_schema": [
          "`user_ref: string`",
          "`provider: string`",
          "`entry_context: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`connection_session_ref`, `device_connection_offered`, `nonblocking`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`device_connection_offered` signals an optional post-entry enhancement, not a required gate.",
          "`nonblocking` must always be true for supported responses."
        ],
        "enum_domain": [],
        "invariants": [
          "homepage entry must stay open even if device setup fails",
          "offering device connection must not mutate primary onboarding state"
        ],
        "canonical_refs": [
          "`connection_session_ref`",
          "`device_connection_offered`",
          "`nonblocking`"
        ],
        "errors": [
          "`provider_unsupported`",
          "`session_build_failed`"
        ],
        "idempotency": "`user_ref + provider + entry_context`",
        "preconditions": [
          "homepage is already entered"
        ],
        "caller_context": "Operator calls POST /v1/device/connect/deferred via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + provider + entry_context`",
        "post_conditions": [
          "connection_session_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "homepage is already entered",
        "side_effects": [
          "Emits /v1/device/connect/deferred trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/device/connect/deferred result state.",
        "event_outputs": [
          "post..v1.device.connect.deferred.executed"
        ]
      },
      {
        "command": "GET /v1/device/connect/deferred-state",
        "surface": "`GET /v1/device/connect/deferred-state` via device deferred connection service",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`device_connection_state`, `enhancement_status`, `last_provider_ref?`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`enhancement_status` only reflects post-entry enhancement state and never blocks onboarding completion."
        ],
        "enum_domain": [
          "`device_connection_state ∈ {not_offered, offered, connected, skipped, failed_nonblocking}`"
        ],
        "invariants": [
          "querying deferred state must not open a new connection session"
        ],
        "canonical_refs": [
          "`device_connection_state`",
          "`enhancement_status`"
        ],
        "errors": [
          "`state_missing`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "onboarding state is already known"
        ],
        "caller_context": "Operator calls GET /v1/device/connect/deferred-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "device_connection_state are authoritative and consistent"
        ],
        "system_dependency_pre_state": "onboarding state is already known",
        "side_effects": [
          "Emits /v1/device/connect/deferred-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/device/connect/deferred-state result state.",
        "event_outputs": [
          "get..v1.device.connect.deferred_state.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "Deferred device connection is initialized only after homepage entry so unsupported or slow providers remain nonblocking.",
      "A provider adapter normalizes device-specific auth/sync results into a single enhancement result contract.",
      "The session store records optional sessions separately from core onboarding state to keep the main chain isolated."
    ],
    "implementation_modules": [
      "Deferred device connection service：负责首页后的连接发起与收口。",
      "Provider adapter：负责第三方设备供应商的 auth 和 sync 适配。",
      "Connection session store：负责会话状态和非阻塞结果持久化。"
    ],
    "state_model": [
      "`homepage_entered` -> `device_connection_offered` -> `connection_session_opened` -> `device_connected`",
      "`device_skipped` / `device_failed_nonblocking` remain terminal enhancement states without blocking the main flow"
    ],
    "architecture_diagram": "```text\n[Homepage Entered] -> [Deferred Connection Offer] -> [Provider Adapter] -> [Connection Session Store]\n                                                   |\n                                                   +--> [Nonblocking Result]\n```",
    "tech_runtime_view": "```text\n[device/deferred_connection_service.py]\n              |\n              v\n[device/provider_adapter.py] --> [device/connection_session_store.py]\n              |\n              +--> [device/connection_result_mapper.py]\n```",
    "flow_diagram": "```text\nHomepage Entry -> Offer Device Connection : surface optional enhancement\nOffer Device  -> Provider Adapter         : open deferred session\nProvider      -> Session Store            : persist device result\nSession Store -> Homepage                 : expose enhancement status without blocking\n```",
    "implementation_strategy": [
      "先冻结 deferred session contract，再实现 provider 适配。",
      "把连接结果归类为 enhancement 状态，不允许把 device 成败回写为主链 gate。",
      "最后验证 unsupported provider、auth fail、sync fail 都是非阻塞退化。"
    ],
    "implementation_unit_mapping": [
      "`device/deferred_connection_service.py` (`new`): 拉起首页后的延迟设备连接。",
      "`device/provider_adapter.py` (`new`): 适配设备供应商的 auth/sync 行为。",
      "`device/connection_session_store.py` (`new`): 保存 deferred session 和 enhancement state。",
      "`device/connection_result_mapper.py` (`new`): 将 provider result 映射成统一状态。"
    ],
    "main_sequence": [
      "1. detect homepage entry completion",
      "2. offer optional device connection",
      "3. open deferred connection session",
      "4. complete provider auth/sync or skip/fail nonblocking",
      "5. persist enhancement status and return to homepage"
    ],
    "exception_compensation": [
      "provider unsupported：立即降级为 `device_skipped`，不影响首页进入。",
      "auth failed or sync failed：记录 `device_failed_nonblocking` 并允许后续重新发起。",
      "session expired：关闭旧会话并允许重新打开新会话，不触发主链回滚。"
    ],
    "integration_points": [
      "调用方：首页进入后可展示设备连接卡片，但不在首轮建档/首轮建议之前出现。",
      "挂接点：设备连接结果可供首页和扩展画像读取，但只能作为 enhancement 反馈。",
      "旧系统兼容：没有设备能力时系统必须直接退化为 skipped，不可阻塞主链。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef start_deferred_device_connection(request: DeferredDeviceConnectionRequest) -> DeferredDeviceConnectionResult:\n    session = open_device_connection_session(request.user_ref, request.provider, request.entry_context)\n    provider_result = connect_provider(session)\n    return finalize_device_connection(session.ref, provider_result)\n```",
      "failure_path": "```python\ndef start_deferred_device_connection_or_skip(request: DeferredDeviceConnectionRequest) -> DeferredDeviceConnectionResult:\n    if not provider_supported(request.provider):\n        return DeferredDeviceConnectionResult.skip(reason='provider_unsupported')\n    return start_deferred_device_connection(request)\n```"
    }
  },
  "state_and_profile_boundary_alignment": {
    "architecture_topics": [
      "Boundary to onboarding state: primary_state 与 capability_flags 是统一状态视图，但不能替代各自 owner 的写入责任。",
      "Boundary to physical profile authority: user_physical_profile is the canonical source for physical profile conflicts and must override stale mirror data.",
      "Dedicated boundary alignment service is required so reads can unify state without collapsing storage ownership."
    ],
    "responsibility_splits": [
      "Boundary alignment service owns unified read synthesis and conflict resolution order.",
      "Primary state store owns onboarding progression and capability flags for state gating.",
      "Physical profile store owns canonical physical attributes and conflict resolution truth."
    ],
    "api_surfaces": [
      "`POST /v1/onboarding/primary-state`",
      "`GET /v1/onboarding/unified-state`"
    ],
    "api_cli_commands": [
      "`POST /v1/onboarding/primary-state` via onboarding state boundary service",
      "`GET /v1/onboarding/unified-state` via onboarding state boundary service"
    ],
    "api_command_specs": [
      {
        "command": "POST /v1/onboarding/primary-state",
        "surface": "`POST /v1/onboarding/primary-state` via onboarding state boundary service",
        "request_schema": [
          "`user_ref: string`",
          "`primary_state: string`",
          "`capability_flags: object`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`primary_state_ref`, `capability_flags_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`primary_state` is the canonical onboarding progression state.",
          "`capability_flags` are derived from state and profile boundaries, not arbitrary UI flags."
        ],
        "enum_domain": [],
        "invariants": [
          "state writes must remain separate from physical profile writes",
          "the same request must not rewrite profile authority"
        ],
        "canonical_refs": [
          "`primary_state_ref`",
          "`capability_flags_ref`"
        ],
        "errors": [
          "`state_transition_invalid`",
          "`conflict_blocked`"
        ],
        "idempotency": "`user_ref + primary_state`",
        "preconditions": [
          "state boundary service initialized"
        ],
        "caller_context": "Operator calls POST /v1/onboarding/primary-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref + primary_state`",
        "post_conditions": [
          "primary_state_ref are authoritative and consistent"
        ],
        "system_dependency_pre_state": "state boundary service initialized",
        "side_effects": [
          "Emits /v1/onboarding/primary-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects POST /v1/onboarding/primary-state result state.",
        "event_outputs": [
          "post..v1.onboarding.primary_state.executed"
        ]
      },
      {
        "command": "GET /v1/onboarding/unified-state",
        "surface": "`GET /v1/onboarding/unified-state` via onboarding state boundary service",
        "request_schema": [
          "`user_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`primary_state`, `capability_flags`, `canonical_profile_boundary`, `resolved_profile_refs`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`canonical_profile_boundary` exposes which profile source currently wins conflict resolution."
        ],
        "enum_domain": [],
        "invariants": [
          "unified read must prefer canonical physical profile over stale mirrors",
          "querying unified state must not mutate either store"
        ],
        "canonical_refs": [
          "`primary_state`",
          "`capability_flags`",
          "`canonical_profile_boundary`"
        ],
        "errors": [
          "`state_missing`"
        ],
        "idempotency": "`user_ref`",
        "preconditions": [
          "at least one state or profile record exists"
        ],
        "caller_context": "Operator calls GET /v1/onboarding/unified-state via CLI with valid request envelope.",
        "idempotency_key_strategy": "`user_ref`",
        "post_conditions": [
          "primary_state are authoritative and consistent"
        ],
        "system_dependency_pre_state": "at least one state or profile record exists",
        "side_effects": [
          "Emits /v1/onboarding/unified-state trace event"
        ],
        "ui_surface_impact": "CLI output reflects GET /v1/onboarding/unified-state result state.",
        "event_outputs": [
          "get..v1.onboarding.unified_state.executed"
        ]
      }
    ],
    "implementation_architecture": [
      "The alignment service merges primary state and physical profile boundaries into one unified read path while preserving separate ownership on writes.",
      "Conflict resolution always prefers user_physical_profile when profile data disagree with mirrored or stale copies.",
      "Unified state is a read model only; it does not become a new authoritative write source."
    ],
    "implementation_modules": [
      "Boundary alignment service：负责合并 primary_state 与 capability_flags 的统一读取。",
      "Canonical profile resolver：负责处理 user_physical_profile 的冲突优先级。",
      "Unified state view：负责输出可供主页、建议和设备流共享的统一状态。"
    ],
    "state_model": [
      "`primary_state_recorded` -> `capability_flags_resolved` -> `canonical_profile_boundary_resolved` -> `unified_onboarding_state_ready`",
      "`conflict_detected` -> `boundary_resolution_required` -> `canonical_profile_preferred`"
    ],
    "architecture_diagram": "```text\n[Primary State Store] --> [Boundary Alignment Service] <-- [Canonical Physical Profile Store]\n                                       |\n                                       v\n                              [Unified Onboarding State]\n```",
    "tech_runtime_view": "```text\n[state/boundary_service.py]\n              |\n              v\n[state/unified_state_view.py] --> [profile/canonical_profile_resolver.py]\n              |\n              +--> [state/capability_flag_store.py]\n```",
    "flow_diagram": "```text\nState Write      -> Boundary Service      : persist primary state separately\nProfile Write    -> Canonical Resolver    : keep physical profile authoritative\nBoundary Service -> Unified State View    : synthesize read-only combined state\nUnified State    -> Consumers            : serve homepage, advice, and device flows\n```",
    "implementation_strategy": [
      "先冻结 primary_state 与 physical_profile 的写边界，再实现统一读模型。",
      "冲突解析必须优先 physical profile authority，避免镜像表写入污染主链。",
      "最后验证 advice、extended profile、device flows 都只消费统一视图，不直接争抢写权。"
    ],
    "implementation_unit_mapping": [
      "`state/boundary_service.py` (`new`): 合并 primary_state 与 capability_flags。",
      "`state/unified_state_view.py` (`new`): 输出统一 onboarding 状态读模型。",
      "`profile/canonical_profile_resolver.py` (`new`): 处理 physical profile canonical 冲突优先级。",
      "`state/capability_flag_store.py` (`new`): 保存 capability flags 派生结果。"
    ],
    "main_sequence": [
      "1. resolve authoritative owner for state and profile writes",
      "2. write or update primary_state separately",
      "3. write canonical physical profile separately",
      "4. resolve capability flags and conflict precedence",
      "5. serve unified onboarding state to downstream consumers"
    ],
    "exception_compensation": [
      "conflicting state/profile writes：阻止写入并返回 `conflict_blocked`，要求重新拉取统一视图。",
      "canonical profile missing：返回 `state_missing` 或 `canonical_profile_missing`，不得伪造边界。",
      "stale mirror read：降级到 canonical physical profile 并标记 `resolved_profile_refs`。"
    ],
    "integration_points": [
      "调用方：minimal onboarding、extended profile、first advice release 和 device deferred entry 都应读取 unified state。",
      "挂接点：写入仍按各自 owner 分离，统一状态只作为消费侧视图。 ",
      "旧系统兼容：若某个旧表存在镜像数据，读取必须显式标记 canonical winner，不得模糊化。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef write_primary_state(request: PrimaryStateWriteRequest) -> PrimaryStateWriteResult:\n    primary_state_ref = persist_primary_state(request.user_ref, request.primary_state)\n    capability_flags_ref = recalculate_capability_flags(request.user_ref, request.primary_state)\n    return PrimaryStateWriteResult(primary_state_ref=primary_state_ref, capability_flags_ref=capability_flags_ref)\n```",
      "failure_path": "```python\ndef read_unified_onboarding_state_or_resolve(request: UnifiedStateRequest) -> UnifiedStateResult:\n    unified = load_unified_onboarding_state(request.user_ref)\n    if unified.conflict_detected:\n        return UnifiedStateResult.fail(reason='conflict_blocked')\n    return unified\n```"
    }
  },
  "adoption_e2e": {
    "architecture_topics": [
      "Boundary to foundation FEATs: 本 FEAT 只定义 onboarding/pilot/cutover 挂接边界，不重写 collaboration、gate decision/publication、IO foundation internals。",
      "Boundary to audit/gate consumption: 本 FEAT 组织 pilot evidence 与 cutover routing，不新建平行 decision 体系。",
      "Dedicated rollout placement is required so wave state、compat mode 与 fallback remain authoritative across skill adoption."
    ],
    "responsibility_splits": [
      "Rollout controller owns onboarding wave、compat mode 与 cutover/fallback routing.",
      "Pilot verifier owns end-to-end evidence completeness checks across producer / consumer / audit / gate.",
      "Foundation FEATs keep ownership of their technical semantics; onboarding does not rewrite them."
    ],
    "api_surfaces": [
      "skill onboarding contract",
      "pilot evidence submission contract"
    ],
    "api_cli_commands": [
      "`ll rollout onboard-skill --request <rollout_onboard_skill.request.json> --response-out <rollout_onboard_skill.response.json>` via `cli/commands/rollout/command.py`",
      "`ll audit submit-pilot-evidence --request <audit_submit_pilot_evidence.request.json> --response-out <audit_submit_pilot_evidence.response.json>` via `cli/commands/audit/command.py`"
    ],
    "api_command_specs": [
      {
        "command": "ll rollout onboard-skill",
        "surface": "`ll rollout onboard-skill --request <rollout_onboard_skill.request.json> --response-out <rollout_onboard_skill.response.json>` via `cli/commands/rollout/command.py`",
        "request_schema": [
          "`skill_ref: string`",
          "`wave_id: string`",
          "`scope: string`",
          "`compat_mode: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`status`, `runtime_binding_ref`, `cutover_guard_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`compat_mode` freezes the transition mode used before full cutover."
        ],
        "enum_domain": [],
        "invariants": [
          "onboarding must keep a cutover guard ref for every accepted wave"
        ],
        "canonical_refs": [
          "`runtime_binding_ref`",
          "`cutover_guard_ref`"
        ],
        "errors": [
          "`unknown_skill`",
          "`scope_invalid`",
          "`foundation_missing`"
        ],
        "idempotency": "`skill_ref + wave_id`",
        "preconditions": [
          "foundation features are freeze-ready"
        ],
        "caller_context": "Rollout controller calls this after verifying foundation readiness and wave scope.",
        "idempotency_key_strategy": "Composite key: skill_ref + wave_id",
        "post_conditions": [
          "runtime_binding_ref is authoritative",
          "cutover_guard_ref is persisted"
        ],
        "system_dependency_pre_state": "Foundation features must be freeze-ready and runtime must be accessible",
        "side_effects": [
          "Creates onboarding registry entry",
          "Emits skill onboarded trace event"
        ],
        "ui_surface_impact": "Onboarding matrix UI updates to show new skill in wave",
        "event_outputs": [
          "rollout.skill_onboarded",
          "rollout.wave_updated"
        ]
      },
      {
        "command": "ll audit submit-pilot-evidence",
        "surface": "`ll audit submit-pilot-evidence --request <audit_submit_pilot_evidence.request.json> --response-out <audit_submit_pilot_evidence.response.json>` via `cli/commands/audit/command.py`",
        "request_schema": [
          "`pilot_chain_ref: string`",
          "`producer_ref: string`",
          "`consumer_ref: string`",
          "`audit_ref: string`",
          "`gate_ref: string`"
        ],
        "response_schema": [
          "success envelope=`{ ok: true, command_ref, trace_ref, result }`",
          "result fields=`evidence_status`, `cutover_recommendation`, `evidence_ref`",
          "error envelope=`{ ok: false, command_ref, trace_ref, error }`"
        ],
        "field_semantics": [
          "`cutover_recommendation` is a rollout recommendation, not a foundation design rewrite."
        ],
        "enum_domain": [],
        "invariants": [
          "pilot evidence must trace one complete producer -> consumer -> audit -> gate path"
        ],
        "canonical_refs": [
          "`pilot_chain_ref`",
          "`evidence_ref`"
        ],
        "errors": [
          "`missing_chain_step`",
          "`audit_not_traceable`"
        ],
        "idempotency": "`pilot_chain_ref`",
        "preconditions": [
          "pilot chain has executed at least once"
        ],
        "caller_context": "Pilot orchestrator calls this after producer -> consumer -> audit -> gate chain completes.",
        "idempotency_key_strategy": "Composite key: pilot_chain_ref",
        "post_conditions": [
          "evidence_ref is authoritative",
          "cutover_recommendation is emitted"
        ],
        "system_dependency_pre_state": "Pilot chain must have executed at least once with all steps traceable",
        "side_effects": [
          "Creates pilot evidence record",
          "Emits evidence submitted trace event"
        ],
        "ui_surface_impact": "Pilot evidence dashboard updates with latest submission",
        "event_outputs": [
          "audit.pilot_evidence_submitted",
          "audit.cutover_recommendation_ready"
        ]
      }
    ],
    "implementation_architecture": [
      "Foundation 能力先稳定，再把 governed skill 按 onboarding matrix 与 migration wave 接入主链。",
      "Pilot chain 必须覆盖 producer -> gate -> formal object -> consumer -> audit 的真实闭环，而不是组件内局部验证。",
      "Cutover controller 只负责切换与回退边界，不重写 foundation FEAT 或 ADR-005 的实现模块。"
    ],
    "implementation_modules": [
      "Handoff runtime adapter：负责把受治理对象写入/读取主链 runtime，并维持 traceability。",
      "Decision boundary adapter：负责把上游 FEAT 约束映射成 runtime 可执行边界，不把实现责任散落到业务 skill。",
      "Onboarding registry：记录 governed skill 接入矩阵、scope 与 migration wave。",
      "Pilot orchestration verifier：收集 producer -> consumer -> audit -> gate 的真实闭环证据并支撑 cutover/fallback。"
    ],
    "state_model": [
      "`skill_registered` -> `pilot_enabled` -> `cutover_guarded` -> `e2e_verified` -> `wave_accepted`",
      "`cutover_guarded(fail)` -> `fallback_triggered` -> `pilot_enabled`"
    ],
    "architecture_diagram": "```text\n[Producer Skill] --> [Mainline Runtime] --> [Gate / Formalization] --> [Consumer Skill] --> [Audit Evidence] --> [Cutover Decision]\n                                                                                                              |\n                                                                                                              +--> fallback --> [Producer Skill]\n```",
    "tech_runtime_view": "```text\n[cli/commands/rollout/command.py]\n              |\n              v\n[cli/lib/rollout_state.py] --> [cli/lib/pilot_chain.py] --> [Pilot / Cutover Evidence]\n              |\n              +--> [cli/commands/audit/command.py]\n```",
    "flow_diagram": "```text\nProducer -> Runtime : emit governed object\nRuntime  -> Gate    : route through mainline gate\nGate     -> Consumer: publish formal refs\nConsumer -> Audit   : produce audit evidence\nAudit    -> Gate    : confirm pilot readiness or fallback\n```",
    "implementation_strategy": [
      "先冻结 onboarding matrix、pilot chain 和 cutover guard，再按 wave 接入 governed skill。",
      "先跑最小真实 producer -> consumer -> audit -> gate pilot，稳定后再扩大接入波次。",
      "每个 wave 都必须保留 fallback 条件与 rollback evidence，不能一次性全量切换。"
    ],
    "implementation_unit_mapping": [
      "`cli/lib/protocol.py` (`extend`): 定义 `OnboardingMatrix`、`CutoverDirective`、`PilotEvidenceRef` 结构。",
      "`cli/lib/rollout_state.py` (`new`): 保存 onboarding wave、cutover state 和 fallback marker。",
      "`cli/lib/pilot_chain.py` (`new`): 校验 producer -> consumer -> audit -> gate 的真实闭环证据。",
      "`cli/commands/rollout/command.py` (`extend`): 提供 onboarding wave、cutover、fallback 操作，依赖 `cli/lib/rollout_state.py`。",
      "`cli/commands/audit/command.py` (`extend`): 消费 pilot evidence 并把 findings 回交给 cutover decision。"
    ],
    "main_sequence": [
      "1. resolve onboarding directive and targeted wave",
      "2. verify foundation readiness and compat mode",
      "3. bind selected skill to mainline runtime / gate hooks",
      "4. run pilot chain and capture producer -> consumer -> audit -> gate evidence",
      "5. evaluate cutover guard and emit fallback recommendation when needed",
      "6. persist wave status and rollout evidence"
    ],
    "exception_compensation": [
      "pilot chain 中任一步 evidence 缺失：cutover 直接 fail closed，维持 compat mode。",
      "cutover success 但 audit handoff fail：允许 partial success，但 wave 状态标记 `audit_pending`，禁止扩大 rollout。",
      "fallback trigger fail：保留 current wave 冻结，要求人工介入，不允许自动继续下一波次。"
    ],
    "integration_points": [
      "调用方：现有 governed skill 的 onboarding/cutover 由 `cli/commands/rollout/command.py` 发起，audit findings 由 `cli/commands/audit/command.py` 消费。",
      "挂接点：compat mode 在 skill 接入 wave 前打开；file-handoff 和 gate/repair 路径必须进入 pilot evidence 链。",
      "旧系统兼容：先接入选定 pilot skill，再按 wave 扩大；未在 onboarding matrix 内的旧 skill 保持现状不切换。"
    ],
    "minimal_code_skeleton": {
      "happy_path": "```python\ndef run_pilot_wave(directive: OnboardingDirective) -> PilotWaveResult:\n    binding = bind_skill_to_mainline(directive.skill_ref, directive.compat_mode)\n    evidence = execute_pilot_chain(binding, directive.scope)\n    verdict = evaluate_cutover_guard(evidence)\n    persist_wave_state(directive.wave_id, verdict, evidence)\n    return PilotWaveResult(binding_ref=binding.binding_ref, cutover_verdict=verdict)\n```",
      "failure_path": "```python\ndef run_pilot_wave_with_fallback(directive: OnboardingDirective) -> PilotWaveResult:\n    binding = bind_skill_to_mainline(directive.skill_ref, directive.compat_mode)\n    evidence = execute_pilot_chain(binding, directive.scope)\n    if not evidence.is_complete:\n        keep_compat_mode(binding.binding_ref)\n        return PilotWaveResult.fail(reason='pilot_evidence_incomplete')\n    return run_pilot_wave(directive)\n```"
    }
  }
}
