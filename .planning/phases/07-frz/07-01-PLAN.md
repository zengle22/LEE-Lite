---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/lib/frz_schema.py
  - cli/lib/test_frz_schema.py
autonomous: true
requirements:
  - FRZ-01
  - FRZ-02
must_haves:
  truths:
    - "FRZPackage dataclass exists with all 5 MSC dimension fields"
    - "MSCValidator rejects packages missing any MSC dimension"
    - "MSCValidator accepts packages with all 5 dimensions populated"
    - "CLI entry point validates a FRZ YAML file from the command line"
  artifacts:
    - path: "cli/lib/frz_schema.py"
      provides: "FRZPackage dataclass, MSCValidator, parser, CLI entry point"
      exports:
        - "FRZPackage"
        - "MSCValidator"
        - "FRZStatus"
        - "ProductBoundary"
        - "CoreJourney"
        - "DomainEntity"
        - "StateMachine"
        - "AcceptanceContract"
        - "FRZSchemaError"
    - path: "cli/lib/test_frz_schema.py"
      provides: "Unit tests for FRZ schema and MSC validation"
      contains: "test_frz_package_structure"
      contains: "test_msc_validator"
  key_links:
    - from: "cli/lib/frz_schema.py"
      to: "cli/lib/errors.py"
      via: "import CommandError"
      pattern: "from cli\\.lib\\.errors import CommandError"
    - from: "cli/lib/frz_schema.py"
      to: "cli/lib/qa_schemas.py"
      via: "follows same pattern (dataclass + validation)"
      pattern: "@dataclass\\(frozen=True\\)"
---

<objective>
Create `cli/lib/frz_schema.py` with FRZPackage dataclass hierarchy, MSC 5-dimension validator, YAML parser, and CLI entry point — plus comprehensive unit tests.

Purpose: Deliver FRZ-01 (FRZ package structure with MSC 5-dim fields) and FRZ-02 (MSC validator that checks minimum semantic completeness). This is the foundational data model for the entire v2.0 semantic governance upgrade.

Output: `cli/lib/frz_schema.py` (~200-250 lines), `cli/lib/test_frz_schema.py` (~150-200 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frz/07-RESEARCH.md
@.planning/phases/07-frz/07-VALIDATION.md

# Pattern source — follow exact same structure
@cli/lib/qa_schemas.py
@cli/lib/patch_schema.py
@cli/lib/errors.py
@ssot/adr/ADR-050-SSOT语义治理总纲.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cli/lib/frz_schema.py with FRZPackage dataclass hierarchy, MSCValidator, YAML parser, and CLI entry point</name>
  <files>cli/lib/frz_schema.py</files>
  <read_first>
    - cli/lib/qa_schemas.py (pattern: frozen dataclasses, _require helper, validation functions, CLI main)
    - cli/lib/patch_schema.py (pattern: Enum validation, validate_file entry point)
    - cli/lib/errors.py (pattern: CommandError, ensure function)
    - ssot/adr/ADR-050-SSOT语义治理总纲.md (MSC 5 dimensions: product_boundary, core_journeys, domain_model, state_machine, acceptance_contract)
  </read_first>
  <action>
Create `cli/lib/frz_schema.py` following the exact same structure as `cli/lib/qa_schemas.py` and `cli/lib/patch_schema.py`.

1. Imports section:
   - `from __future__ import annotations`
   - `from dataclasses import dataclass, field`
   - `from enum import Enum`
   - `from pathlib import Path`
   - `from typing import Any`
   - `import yaml`

2. Enum definitions:
```python
class FRZStatus(str, Enum):
    draft = "draft"
    freeze_ready = "freeze_ready"
    frozen = "frozen"
    blocked = "blocked"
    revised = "revised"
    superseded = "superseded"
```

3. MSC dimension constants:
```python
MSC_DIMENSIONS = [
    "product_boundary",
    "core_journeys",
    "domain_model",
    "state_machine",
    "acceptance_contract",
]
```

4. Sub-entity frozen dataclasses (all use `@dataclass(frozen=True)`):
   - `ProductBoundary`: `in_scope: list[str]`, `out_of_scope: list[str]`
   - `CoreJourney`: `id: str`, `name: str`, `steps: list[str]`
   - `DomainEntity`: `id: str`, `name: str`, `contract: dict[str, Any]`
   - `StateMachine`: `id: str`, `name: str`, `states: list[str]`, `transitions: list[dict[str, Any]]`
   - `AcceptanceContract`: `expected_outcomes: list[str]`, `acceptance_impact: list[str]`
   - `KnownUnknown`: `id: str`, `topic: str`, `status: str = "open"`, `owner: str | None = None`, `expires_in: str = "2 cycles"`
   - `FRZEvidence`: `source_refs: list[str]`, `raw_path: str | None = None`

5. FRZPackage frozen dataclass with all fields:
   - `artifact_type: str = "frz_package"`
   - `frz_id: str | None = None` (FRZ-xxx format)
   - `version: str = "1.0"`
   - `status: FRZStatus = FRZStatus.draft`  ← **[REVIEW-FIX]** Use FRZStatus enum type, not bare str (Winston)
   - `created_at: str | None = None` (ISO 8601)
   - `frozen_at: str | None = None`
   - `product_boundary: ProductBoundary | None = None`
   - `core_journeys: list[CoreJourney] = field(default_factory=list)`
   - `domain_model: list[DomainEntity] = field(default_factory=list)`
   - `state_machine: list[StateMachine] = field(default_factory=list)`
   - `acceptance_contract: AcceptanceContract | None = None`
   - `constraints: list[str] = field(default_factory=list)`
   - `derived_allowed: list[str] = field(default_factory=list)`
   - `known_unknowns: list[KnownUnknown] = field(default_factory=list)`
   - `enums: list[dict[str, Any]] = field(default_factory=list)`
   - `evidence: FRZEvidence | None = None`

5b. **[REVIEW-FIX] FRZ ID format validation** (Winston, Amelia):
```python
FRZ_ID_PATTERN = re.compile(r"^FRZ-\d{3,}$")
```
Add to `_parse_frz_dict`: if `data.get("frz_id")` is not None, validate against `FRZ_ID_PATTERN`. If invalid, raise `FRZSchemaError(f"Invalid FRZ ID format: {frz_id}")`.

5c. **[REVIEW-FIX] Sub-entity ID format validation** (Winston):
In `_parse_frz_dict`, after constructing sub-entity dicts, validate:
- `CoreJourney.id` matches `^JRN-\d{3,}$`
- `DomainEntity.id` matches `^ENT-\d{3,}$`
- `StateMachine.id` matches `^SM-\d{3,}$`
- `KnownUnknown.id` matches `^UNK-\d{3,}$`
If any sub-entity ID is invalid, raise `FRZSchemaError`.

5d. **[REVIEW-FIX] Sub-entity dict-to-dataclass recursive conversion** (Amelia):
`_parse_frz_dict` MUST recursively convert nested dicts to their respective frozen dataclass instances:
- `product_boundary` dict → `ProductBoundary(**data)`
- Each item in `core_journeys` list → `CoreJourney(**item)`
- Each item in `domain_model` list → `DomainEntity(**item)`
- Each item in `state_machine` list → `StateMachine(**item)`
- `acceptance_contract` dict → `AcceptanceContract(**data)`
- Each item in `known_unknowns` list → `KnownUnknown(**item)`
- `evidence` dict → `FRZEvidence(**data)`
Do NOT leave nested data as raw dicts — the FRZPackage must have fully typed sub-entities.

5e. **[REVIEW-FIX] YAML top-level key** (Amelia):
The top-level key in FRZ YAML files is `frz_package`. `_detect_schema_type()` in `qa_schemas.py` pattern should match this key. If YAML root is a dict with key `"frz_package"`, auto-detect as FRZ type.

6. FRZSchemaError class: `class FRZSchemaError(ValueError):` — follows `QaSchemaError` and `PatchSchemaError` pattern.

7. Helper function: `def _require(data: dict, key: str, label: str) -> None:` — identical to qa_schemas.py version.

8. MSCValidator class with two static methods:
   - `validate(pkg: FRZPackage) -> dict[str, Any]`: Check all 5 MSC dimensions. Must enforce minimum content rules (not just key presence):
     - `product_boundary`: must have at least 1 item in `in_scope` OR `out_of_scope`
     - `core_journeys`: must have >= 1 journey with >= 2 steps
     - `domain_model`: must have >= 1 entity with non-empty `contract` dict
     - `state_machine`: must have >= 1 state machine with >= 2 states
     - `acceptance_contract`: must have >= 1 `expected_outcome`
     - Returns dict with keys: `frz_id`, `msc_valid` (bool), `present` (list), `missing` (list), `status` ("frozen" or "blocked")
   - `validate_file(path: str | Path) -> dict[str, Any]`: Load YAML via `yaml.safe_load()`, parse to FRZPackage via `_parse_frz_dict()`, call `validate()`.

9. Parser function: `def _parse_frz_dict(data: dict) -> FRZPackage:` — converts raw YAML dict to FRZPackage instance. Handle each sub-entity: product_boundary, core_journeys list, domain_model list, state_machine list, acceptance_contract, known_unknowns list, evidence. Use `.get()` with defaults for optional fields.

10. CLI entry point `def main() -> None:` — follows `qa_schemas.py main()` pattern:
    - Accept `--type frz <file.yaml>` or auto-detect
    - Call `validate_file()` for each file
    - Print "OK: {path}" or "FAIL: {path} — {error}"
    - Exit with appropriate code

11. `if __name__ == "__main__": main()` block.

DO NOT:
- Use Pydantic for this file (research recommends it but existing pattern is dataclass + manual validation — stay consistent with qa_schemas.py)
- Use `print()` for error messages (use logging or raise exceptions)
- Use `yaml.load()` (must use `yaml.safe_load()`)
- Make FRZPackage mutable (must use `frozen=True`)
  </action>
  <verify>
    <automated>python -c "from cli.lib.frz_schema import FRZPackage, MSCValidator, FRZStatus, FRZSchemaError; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `cli/lib/frz_schema.py` exists with at least 150 lines
    - Contains `@dataclass(frozen=True)` on FRZPackage
    - Contains class `FRZStatus(str, Enum)` with values: draft, freeze_ready, frozen, blocked, revised, superseded
    - Contains class `MSCValidator` with static methods `validate` and `validate_file`
    - Contains function `_parse_frz_dict` that converts dict to FRZPackage
    - Contains class `FRZSchemaError(ValueError)`
    - Contains `def main()` CLI entry point with `if __name__ == "__main__"` block
    - Uses `yaml.safe_load` (grep for "safe_load", must NOT find "yaml.load(" without "safe")
    - Imports `CommandError` from `cli.lib.errors`
    - All sub-entity dataclasses are frozen: ProductBoundary, CoreJourney, DomainEntity, StateMachine, AcceptanceContract, KnownUnknown, FRZEvidence
  </acceptance_criteria>
  <done>
    FRZPackage dataclass with all 5 MSC dimension sub-types exists, MSCValidator class with validate/validate_file methods implemented, YAML parser converts dict to FRZPackage, CLI entry point accepts file paths and validates them, all dataclasses are frozen/immutable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cli/lib/test_frz_schema.py with unit tests for FRZPackage construction, MSC validation pass/fail, and file validation</name>
  <files>cli/lib/test_frz_schema.py</files>
  <read_first>
    - cli/lib/frz_schema.py (file just created — verify exports and function signatures)
    - cli/lib/qa_schemas.py (pattern: test structure, fixtures)
  </read_first>
  <action>
Create `cli/lib/test_frz_schema.py` with pytest unit tests. Follow the existing test patterns in the project (see `cli/lib/test_exec_ui_flow.py` for style).

Test file structure:

1. Imports: `pytest`, `tempfile`, `yaml`, and all exports from `cli.lib.frz_schema`.

2. Test `test_frz_package_structure`:
   - Create FRZPackage with minimal valid data (all 5 MSC dimensions populated)
   - Assert `artifact_type == "frz_package"`
   - Assert `status == "draft"`
   - Assert `version == "1.0"`
   - Assert `frz_id` is None by default
   - Verify dataclass is frozen by attempting to set a field and catching `FrozenInstanceError`

3. Test `test_msc_validator_valid_package`:
   - Create FRZPackage with all 5 MSC dimensions populated per minimum content rules:
     - `product_boundary` with at least 1 in_scope item
     - `core_journeys` with 1 journey with 2 steps
     - `domain_model` with 1 entity with non-empty contract
     - `state_machine` with 1 machine with 2 states
     - `acceptance_contract` with 1 expected_outcome
   - Call `MSCValidator.validate(pkg)`
   - Assert `result["msc_valid"] == True`
   - Assert `result["status"] == "frozen"`
   - Assert `result["missing"] == []`
   - Assert `len(result["present"]) == 5`

4. Test `test_msc_validator_missing_all_dims`:
   - Create FRZPackage with default (empty) values
   - Call `MSCValidator.validate(pkg)`
   - Assert `result["msc_valid"] == False`
   - Assert `result["status"] == "blocked"`
   - Assert `len(result["missing"]) == 5`
   - Assert each of the 5 dimension names appears in missing list

5. Test `test_msc_validator_partial_package`:
   - Create FRZPackage with only product_boundary populated (in_scope has 1 item)
   - Call `MSCValidator.validate(pkg)`
   - Assert `result["msc_valid"] == False`
   - Assert `len(result["missing"]) == 4`
   - Assert "product_boundary" in `result["present"]`

6. Test `test_msc_validator_empty_boundary_rejected`:
   - Create FRZPackage with `product_boundary=ProductBoundary(in_scope=[], out_of_scope=[])`
   - Call `MSCValidator.validate(pkg)`
   - Assert "product_boundary" in `result["missing"]` (minimum content rule: must have at least 1 item)

7. Test `test_msc_validator_single_step_journey_rejected`:
   - Create FRZPackage with 1 core_journey that has only 1 step
   - Assert "core_journeys" in `result["missing"]` (minimum content rule: >= 2 steps)

8. Test `test_msc_validator_empty_contract_entity_rejected`:
   - Create FRZPackage with 1 domain entity with empty contract dict `{}`
   - Assert "domain_model" in `result["missing"]` (minimum content rule: non-empty contract)

9. Test `test_msc_validator_single_state_machine_rejected`:
   - Create FRZPackage with 1 state machine with only 1 state
   - Assert "state_machine" in `result["missing"]` (minimum content rule: >= 2 states)

10. Test `test_msc_validator_empty_acceptance_contract_rejected`:
    - Create FRZPackage with `AcceptanceContract(expected_outcomes=[], acceptance_impact=[])`
    - Assert "acceptance_contract" in `result["missing"]`

11. Test `test_validate_file_not_found`:
    - Call `MSCValidator.validate_file("/nonexistent/path.yaml")`
    - Assert raises `FileNotFoundError`

12. Test `test_validate_file_valid_yaml`:
    - Use `tempfile.NamedTemporaryFile` to write a valid FRZ YAML
    - Call `MSCValidator.validate_file(tmpfile.name)`
    - Assert `result["msc_valid"] == True`

13. Test `test_frz_schema_error`:
    - Assert `FRZSchemaError` is subclass of `ValueError`

14. Test `test_parse_frz_dict_minimal`:
    - Call `_parse_frz_dict({"frz_id": "FRZ-001"})`
    - Assert returned FRZPackage has `frz_id == "FRZ-001"`
    - Assert all lists are empty by default, all optionals are None

15. Test `test_parse_frz_dict_full`:
    - Call `_parse_frz_dict()` with a complete FRZ YAML structure (all fields)
    - Assert all sub-entities are correctly parsed
    - Assert `len(core_journeys) == 1`, `len(domain_model) == 1`, etc.

16. Test `test_frz_status_enum_values`:
    - Assert `FRZStatus.frozen.value == "frozen"`
    - Assert `FRZStatus.draft.value == "draft"`
    - Assert `FRZStatus.blocked.value == "blocked"`

17. **[REVIEW-FIX] Test `test_validate_file_malformed_yaml`** (Murat):
    - Write invalid YAML like `"{{{{"` to tempfile
    - Call `MSCValidator.validate_file()`, assert raises `yaml.YAMLError` or `FRZSchemaError`

18. **[REVIEW-FIX] Test `test_validate_file_empty_yaml`** (Murat):
    - Write empty file or YAML with only comments to tempfile
    - Call `MSCValidator.validate_file()`, assert raises `FRZSchemaError` (safe_load returns None)

19. **[REVIEW-FIX] Test `test_parse_frz_dict_invalid_frz_id_format`** (Winston, Amelia):
    - Call `_parse_frz_dict({"frz_id": "invalid-id"})` and `_parse_frz_dict({"frz_id": "frz-001"})`
    - Assert both raise `FRZSchemaError`
    - Call `_parse_frz_dict({"frz_id": "FRZ-001"})` — succeeds

20. **[REVIEW-FIX] Test `test_parse_frz_dict_invalid_subentity_id`** (Winston):
    - Call `_parse_frz_dict` with `core_journeys=[{"id": "bad-id", "name": "Test", "steps": ["a", "b"]}]`
    - Assert raises `FRZSchemaError`

21. **[REVIEW-FIX] Test `test_parse_frz_dict_unknown_keys_ignored`** (Murat):
    - Call `_parse_frz_dict({"frz_id": "FRZ-001", "unknown_field": 42})`
    - Assert succeeds, `unknown_field` is silently ignored

22. **[REVIEW-FIX] Test `test_frz_package_status_is_enum`** (Winston):
    - Create FRZPackage, assert `pkg.status` is `FRZStatus.draft` (not bare string "draft")
    - Parse dict with `status: "frozen"`, assert returns FRZPackage with `status == FRZStatus.frozen`
  </action>
  <verify>
    <automated>pytest cli/lib/test_frz_schema.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `cli/lib/test_frz_schema.py` exists with at least 12 test functions
    - All tests pass: `pytest cli/lib/test_frz_schema.py -v` exits 0
    - Contains test function `test_frz_package_structure`
    - Contains test function `test_msc_validator_valid_package`
    - Contains test function `test_msc_validator_missing_all_dims`
    - Contains test function `test_validate_file_not_found`
    - Contains test function `test_frz_schema_error`
    - Contains test function `test_parse_frz_dict_full`
    - Test file imports `from cli.lib.frz_schema import FRZPackage, MSCValidator, FRZSchemaError, FRZStatus`
    - Test file imports `pytest`
  </acceptance_criteria>
  <done>
    Unit tests cover: FRZPackage construction with all fields, frozen dataclass enforcement, MSC validator pass case with all 5 dims, MSC validator fail case with missing dims, MSC validator partial package, minimum content rules for each dimension, file not found error, YAML file validation, FRZSchemaError type, _parse_frz_dict minimal and full parsing, FRZStatus enum values.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML input → FRZPackage | Untrusted YAML crosses here; must use safe_load only |
| FRZPackage → MSCValidator | Data model validated; no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | YAML deserialization in `_parse_frz_dict` | mitigate | Use `yaml.safe_load()` only — no `yaml.load()` — already project standard per `qa_schemas.py` line 314 |
| T-07-02 | Tampering | FRZ ID format validation | mitigate | `_parse_frz_dict` must validate `frz_id` matches `^FRZ-\d{3,}$` regex before accepting |
| T-07-03 | Spoofing | FRZ package with forged `status: frozen` | accept | MSC validator independently verifies completeness; status alone doesn't gate — downstream phases call validate(), not trust status field |
</threat_model>

<verification>
- All 12+ test functions in test_frz_schema.py pass via `pytest cli/lib/test_frz_schema.py -v`
- `from cli.lib.frz_schema import FRZPackage, MSCValidator` succeeds without errors
- `python -m cli.lib.frz_schema --help` or file execution shows CLI usage
  </verification>

<success_criteria>
- FRZ-01 delivered: `cli/lib/frz_schema.py` defines FRZPackage dataclass with all 5 MSC dimension fields (product_boundary, core_journeys, domain_model, state_machine, acceptance_contract)
- FRZ-02 delivered: MSCValidator.validate() returns `msc_valid=False` when any dimension is missing or empty, `msc_valid=True` only when all 5 have minimum content
- All unit tests pass with 0 failures
- File follows existing pattern from qa_schemas.py (frozen dataclasses, _require helper, CLI main)
  </success_criteria>

<output>
After completion, create `.planning/phases/07-frz/07-01-SUMMARY.md`
</output>
