---
phase: 07
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - skills/ll-frz-manage/SKILL.md
  - skills/ll-frz-manage/ll.contract.yaml
  - skills/ll-frz-manage/ll.lifecycle.yaml
  - skills/ll-frz-manage/input/contract.yaml
  - skills/ll-frz-manage/input/semantic-checklist.md
  - skills/ll-frz-manage/output/contract.yaml
  - skills/ll-frz-manage/output/semantic-checklist.md
  - skills/ll-frz-manage/agents/executor.md
  - skills/ll-frz-manage/agents/supervisor.md
  - skills/ll-frz-manage/scripts/frz_manage_runtime.py
  - skills/ll-frz-manage/scripts/test_frz_manage_runtime.py
  - skills/ll-frz-manage/scripts/validate_input.sh
  - skills/ll-frz-manage/scripts/validate_output.sh
autonomous: true
requirements:
  - FRZ-04
  - FRZ-05
  - FRZ-06
must_haves:
  truths:
    - "User can run ll frz-manage validate on a doc directory and get MSC report"
    - "User can run ll frz-manage freeze with --id to register an FRZ package"
    - "User can run ll frz-manage list to see all registered FRZ packages"
    - "Validate rejects packages with incomplete MSC dimensions"
    - "Freeze rejects packages that haven't passed MSC validation"
  artifacts:
    - path: "skills/ll-frz-manage/scripts/frz_manage_runtime.py"
      provides: "Python CLI runtime with validate, freeze, list subcommands"
      exports:
        - "validate_frz"
        - "freeze_frz"
        - "list_frz"
    - path: "skills/ll-frz-manage/SKILL.md"
      provides: "Skill description with execution protocol"
      contains: "Execution Protocol"
    - path: "skills/ll-frz-manage/scripts/test_frz_manage_runtime.py"
      provides: "Integration tests for runtime commands"
      contains: "test_validate, test_freeze, test_list"
  key_links:
    - from: "skills/ll-frz-manage/scripts/frz_manage_runtime.py"
      to: "cli/lib/frz_schema.py"
      via: "import MSCValidator, FRZPackage"
      pattern: "from cli\\.lib\\.frz_schema import"
    - from: "skills/ll-frz-manage/scripts/frz_manage_runtime.py"
      to: "cli/lib/frz_registry.py"
      via: "import register_frz, list_frz, get_frz"
      pattern: "from cli\\.lib\\.frz_registry import"
    - from: "skills/ll-frz-manage/scripts/frz_manage_runtime.py"
      to: "cli/lib/errors.py"
      via: "import CommandError"
      pattern: "from cli\\.lib\\.errors import"
---

<objective>
Create the `skills/ll-frz-manage` skill with complete directory structure, Python CLI runtime (validate, freeze, list commands), shell validation scripts, agent instructions, and integration tests.

Purpose: Deliver FRZ-04 (CLI validate command), FRZ-05 (CLI freeze command), and FRZ-06 (CLI list command). This is the user-facing interface for the entire FRZ infrastructure — it wraps the library functions from Plans 07-01 and 07-02 into a cohesive CLI skill.

Output: Complete `skills/ll-frz-manage/` directory (~10 files), integration test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frz/07-RESEARCH.md
@.planning/phases/07-frz/07-VALIDATION.md

# Skill template — follow exact same structure
@skills/ll-patch-capture/SKILL.md
@skills/ll-patch-capture/ll.contract.yaml
@skills/ll-patch-capture/ll.lifecycle.yaml
@skills/ll-patch-capture/input/contract.yaml
@skills/ll-patch-capture/output/contract.yaml
@skills/ll-patch-capture/input/semantic-checklist.md
@skills/ll-patch-capture/output/semantic-checklist.md

# Library dependencies (created by Plans 07-01, 07-02)
@cli/lib/frz_schema.py
@cli/lib/frz_registry.py
@cli/lib/anchor_registry.py
@cli/lib/errors.py
@cli/lib/fs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/ll-frz-manage skill structure (SKILL.md, contracts, lifecycle, agents, shell scripts)</name>
  <files>skills/ll-frz-manage/SKILL.md, skills/ll-frz-manage/ll.contract.yaml, skills/ll-frz-manage/ll.lifecycle.yaml, skills/ll-frz-manage/input/contract.yaml, skills/ll-frz-manage/input/semantic-checklist.md, skills/ll-frz-manage/output/contract.yaml, skills/ll-frz-manage/output/semantic-checklist.md, skills/ll-frz-manage/agents/executor.md, skills/ll-frz-manage/agents/supervisor.md, skills/ll-frz-manage/scripts/validate_input.sh, skills/ll-frz-manage/scripts/validate_output.sh</files>
  <read_first>
    - skills/ll-patch-capture/SKILL.md (exact template for skill structure, execution protocol, non-negotiable rules)
    - skills/ll-patch-capture/ll.contract.yaml (skill metadata format)
    - skills/ll-patch-capture/ll.lifecycle.yaml (lifecycle states format)
    - skills/ll-patch-capture/input/contract.yaml (input contract format)
    - skills/ll-patch-capture/output/contract.yaml (output contract format)
    - skills/ll-patch-capture/input/semantic-checklist.md (semantic checklist format)
    - skills/ll-patch-capture/output/semantic-checklist.md (semantic checklist format)
    - cli/lib/frz_schema.py (MSC dimensions, FRZStatus enum)
    - cli/lib/frz_registry.py (register_frz, list_frz, get_frz functions)
  </read_first>
  <action>
Create the complete skill directory structure. Follow the ll-patch-capture template exactly.

**skills/ll-frz-manage/ll.contract.yaml:**
```yaml
skill: ll-frz-manage
version: "1.0"
adr: ADR-050
category: freeze
chain: frz-management
phase: governance
```

**skills/ll-frz-manage/ll.lifecycle.yaml:**
```yaml
skill: ll-frz-manage
version: "1.0"
state: draft
lifecycle_states:
  - draft
  - validated
  - frozen
  - superseded
  - archived
```

**skills/ll-frz-manage/SKILL.md:**
- Title: "LL FRZ Manage"
- Description: "ADR-050 governed skill for FRZ (Freeze Package) management: validate, freeze, list, and extract operations."
- Canonical Authority section referencing ADR-050
- Required Read Order (same pattern as ll-patch-capture)
- Execution Protocol with 4 modes:
  1. validate: `ll frz-manage validate --input <doc-dir>` — reads docs, produces FRZ YAML, runs MSC validation, outputs report
  2. freeze: `ll frz-manage freeze --input <doc-dir> --id FRZ-xxx` — validates MSC, registers FRZ to registry, status=frozen
  3. list: `ll frz-manage list [--status frozen|blocked]` — queries registry, displays results
  4. extract: `ll frz-manage extract --frz <frz-id> --output <dir>` — (Phase 8, stub for now)
- Non-Negotiable Rules:
  - Do not bypass scripts/frz_manage_runtime.py
  - Do not freeze without passing MSC validation first
  - Do not accept duplicate FRZ IDs
  - All AI pre-filled fields must be marked as human-reviewed
  - FRZ IDs must match FRZ-xxx format
- Workflow Boundary: input = doc directory or FRZ ID, output = FRZ package / MSC report / registry listing

**skills/ll-frz-manage/input/contract.yaml:**
```yaml
# Input Contract: ll-frz-manage
## Required Input for validate/freeze:
- doc_dir: string (path) — directory containing PRD/UX/Arch source documents
- frz_id: string (optional, required for freeze) — FRZ-xxx format

## Required Input for list:
- status: string (optional) — filter by "frozen" or "blocked"

## Input Validation:
- [ ] doc_dir exists and is a directory
- [ ] frz_id matches pattern FRZ-\d{3,} when provided
- [ ] status (if provided) is one of: frozen, blocked, draft
```

**skills/ll-frz-manage/output/contract.yaml:**
```yaml
# Output Contract: ll-frz-manage
## Required Output for validate:
- FRZ YAML file at artifacts/frz-input/<FRZ-ID>/frz-package.json
- MSC validation report printed to stdout

## Required Output for freeze:
- FRZ registered in ssot/registry/frz-registry.yaml
- Confirmation: "FRZ-xxx registered, status: frozen"

## Required Output for list:
- Table of registered FRZ packages with id, status, created_at
```

**skills/ll-frz-manage/input/semantic-checklist.md:**
- Checklist for validating input documents before FRZ creation:
  - [ ] Product boundary defined (in_scope and out_of_scope items identified)
  - [ ] Core journeys documented (at least 1 journey with 2+ steps)
  - [ ] Domain model entities identified (at least 1 entity with contract)
  - [ ] State machines specified (at least 1 with 2+ states)
  - [ ] Acceptance criteria documented (at least 1 expected outcome)

**skills/ll-frz-manage/output/semantic-checklist.md:**
- Checklist for validating FRZ package output:
  - [ ] FRZ YAML passes MSC validation (all 5 dimensions present with minimum content)
  - [ ] FRZ ID format is valid (FRZ-xxx)
  - [ ] Registry updated with correct status
  - [ ] No duplicate FRZ IDs in registry

**skills/ll-frz-manage/agents/executor.md:**
- Executor agent instructions for running frz_manage_runtime.py
- Steps: accept input, determine mode (validate/freeze/list), invoke appropriate runtime function, format output

**skills/ll-frz-manage/agents/supervisor.md:**
- Supervisor agent instructions for validating runtime output
- Steps: verify MSC report completeness, check registry consistency, validate output format

**skills/ll-frz-manage/scripts/validate_input.sh:**
```bash
#!/usr/bin/env bash
# Validate input for ll-frz-manage
# Usage: validate_input.sh <doc_dir>
set -euo pipefail
doc_dir="${1:?doc_dir required}"
if [ ! -d "$doc_dir" ]; then
  echo "ERROR: doc_dir '$doc_dir' is not a directory" >&2
  exit 2
fi
echo "OK: input directory validated"
exit 0
```

**skills/ll-frz-manage/scripts/validate_output.sh:**
```bash
#!/usr/bin/env bash
# Validate output for ll-frz-manage
# Usage: validate_output.sh <frz_yaml_path>
set -euo pipefail
frz_path="${1:?frz_yaml_path required}"
if [ ! -f "$frz_path" ]; then
  echo "ERROR: FRZ YAML file not found: $frz_path" >&2
  exit 2
fi
# **[REVIEW-FIX] Use --file to avoid shell injection** (Winston)
python -c "
import sys
from cli.lib.frz_schema import MSCValidator
report = MSCValidator.validate_file(sys.argv[1])
print(f'MSC valid: {report[\"msc_valid\"]}')
if not report['msc_valid']:
    print(f'Missing dimensions: {report[\"missing\"]}')
    sys.exit(1)
" "$frz_path"
echo "OK: output validated"
exit 0
```
  </action>
  <verify>
    <automated>test -f skills/ll-frz-manage/SKILL.md && test -f skills/ll-frz-manage/ll.contract.yaml && test -f skills/ll-frz-manage/ll.lifecycle.yaml && test -f skills/ll-frz-manage/scripts/validate_input.sh && test -f skills/ll-frz-manage/scripts/validate_output.sh && echo "all skill files exist"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `skills/ll-frz-manage/` exists
    - File `skills/ll-frz-manage/SKILL.md` exists, contains "ll-frz-manage" and "ADR-050" and "Execution Protocol"
    - File `skills/ll-frz-manage/ll.contract.yaml` exists, contains `skill: ll-frz-manage` and `adr: ADR-050`
    - File `skills/ll-frz-manage/ll.lifecycle.yaml` exists, contains `lifecycle_states:` with at least draft, validated, frozen
    - File `skills/ll-frz-manage/input/contract.yaml` exists, contains `doc_dir:` and `frz_id:` input fields
    - File `skills/ll-frz-manage/output/contract.yaml` exists, contains output descriptions for validate, freeze, list
    - File `skills/ll-frz-manage/input/semantic-checklist.md` exists, contains at least 5 checklist items (one per MSC dimension)
    - File `skills/ll-frz-manage/output/semantic-checklist.md` exists, contains at least 4 checklist items
    - File `skills/ll-frz-manage/agents/executor.md` exists
    - File `skills/ll-frz-manage/agents/supervisor.md` exists
    - File `skills/ll-frz-manage/scripts/validate_input.sh` exists and is executable (contains `#!/usr/bin/env bash`)
    - File `skills/ll-frz-manage/scripts/validate_output.sh` exists and is executable (contains `#!/usr/bin/env bash`)
    - SKILL.md contains Non-Negotiable Rules section with "Do not bypass scripts/frz_manage_runtime.py"
  </acceptance_criteria>
  <done>
    Complete ll-frz-manage skill structure created with 11 files following ll-patch-capture template: SKILL.md with execution protocol for validate/freeze/list/extract modes, ll.contract.yaml with ADR-050 reference, ll.lifecycle.yaml with governance states, input/output contracts, semantic checklists, agent instructions, shell validation scripts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create skills/ll-frz-manage/scripts/frz_manage_runtime.py with validate, freeze, list subcommands</name>
  <files>skills/ll-frz-manage/scripts/frz_manage_runtime.py</files>
  <read_first>
    - cli/lib/frz_schema.py (MSCValidator.validate, MSCValidator.validate_file, _parse_frz_dict, FRZPackage)
    - cli/lib/frz_registry.py (register_frz, list_frz, get_frz, update_frz_status)
    - cli/lib/errors.py (CommandError, ensure)
    - cli/lib/fs.py (ensure_parent, read_text, write_text, to_canonical_path)
  </read_first>
  <action>
Create `skills/ll-frz-manage/scripts/frz_manage_runtime.py` as the Python CLI runtime implementation.

1. Imports: `argparse`, `sys`, `pathlib.Path`, `typing`, `yaml`, `datetime`, `re`, `cli.lib.frz_schema` (MSCValidator, FRZPackage, FRZSchemaError, FRZStatus), `cli.lib.frz_registry` (register_frz, list_frz as _list_frz_registry, get_frz), `cli.lib.anchor_registry` (AnchorRegistry), `cli.lib.errors` (CommandError).

   **[REVIEW-FIX] Import alias** (Amelia): `from cli.lib.frz_registry import list_frz as _list_frz_registry` to avoid naming conflict with the `list_frz` command function.

2. Helper functions:
   - `def _load_frz_from_dir(doc_dir: Path) -> dict[str, Any]`:
     - **[REVIEW-FIX] Explicit file priority** (Amelia): First check for `frz-package.yaml` in doc_dir. If found, load it. Otherwise, glob for `*.yaml` files, sort by name, load the first one.
     - Load YAML with `yaml.safe_load()`
     - If safe_load returns None (empty file), raise `CommandError("INVALID_REQUEST", f"FRZ YAML file is empty: {path}")`
     - If no YAML found, raise `CommandError("INVALID_REQUEST", f"No FRZ YAML found in {doc_dir}")`
     - Return parsed dict
   - `def _find_workspace_root(start: Path = None) -> Path`:
     - **[REVIEW-FIX] Reuse fs.py** (Winston, Amelia): First check if `cli.lib.fs` has `resolve_workspace_root()`. If yes, import and use it. If not, implement: walk up from `start` (or `Path.cwd()`) looking for `.planning` directory or `ssot` directory. Stop at filesystem root (`/` or `C:\`). If not found, raise `CommandError("INVALID_REQUEST", "Workspace root not found — no .planning or ssot directory in parent chain")`
   - `def _format_frz_list(frz_records: list[dict]) -> str`:
     - Format as a table with columns: FRZ_ID, STATUS, CREATED_AT, MSC_VALID
     - Sort records by `created_at` descending (newest first) **[REVIEW-FIX: define sort order]** (Winston)
     - Use aligned columns (simple string formatting, no rich dependency)

2b. **[REVIEW-FIX] Freeze saves YAML not JSON** (Winston, Amelia):
    In `freeze_frz`, save the FRZ package as YAML (not JSON) to maintain consistency:
    - Save to `artifacts/frz-input/{frz_id}/freeze.yaml` (not `frz-package.json`)
    - Use `yaml.dump(frz_data, f, default_flow_style=False, allow_unicode=True, sort_keys=False)`
    - Also copy input source documents to `artifacts/frz-input/{frz_id}/input/` for evidence trail **[REVIEW-FIX: source snapshot]** (Mary)
    - Use `shutil.copytree(doc_dir, artifact_dir / "input", dirs_exist_ok=True)`

3. Command functions:
   - `def validate_frz(args: argparse.Namespace) -> int`:
     - doc_dir = Path(args.input)
     - Load FRZ YAML from doc_dir
     - Parse to FRZPackage via `_parse_frz_dict()`
     - Run MSCValidator.validate(pkg)
     - Print MSC report: "MSC Validation Report for {frz_id}", list present/missing dimensions
     - If msc_valid: print "STATUS: PASS — all 5 MSC dimensions satisfied", return 0
     - If not msc_valid: print "STATUS: FAIL — missing dimensions: {missing}", return 1

   - `def freeze_frz(args: argparse.Namespace) -> int`:
     - doc_dir = Path(args.input)
     - frz_id = args.id (required)
     - Validate frz_id format: must match `^FRZ-\d{3,}$`
     - Load FRZ YAML from doc_dir
     - Parse to FRZPackage
     - Run MSCValidator.validate(pkg)
     - If NOT msc_valid: print "ERROR: Cannot freeze — MSC validation failed", print missing dims, return 1
     - workspace_root = _find_workspace_root()
     - **[REVIEW-FIX] Create artifact directory**: `artifact_dir = workspace_root / "artifacts" / "frz-input" / frz_id`
     - **[REVIEW-FIX] Copy source documents for evidence trail**: `shutil.copytree(doc_dir, artifact_dir / "input", dirs_exist_ok=True)`
     - **[REVIEW-FIX] Save as YAML not JSON**: `freeze_yaml = artifact_dir / "freeze.yaml"`. Write FRZ data as YAML using `yaml.dump()`.
     - Check if frz_id already exists via get_frz() — if exists, raise CommandError
     - Call register_frz() with msc_report, package_ref=str(freeze_yaml)
     - Print "FRZ-{xxx} registered, status: frozen"
     - Return 0

   - `def list_frz(args: argparse.Namespace) -> int`:
     - workspace_root = _find_workspace_root()
     - Call list_frz(workspace_root, status=args.status if hasattr(args, 'status') else None)
     - If no records: print "No FRZ packages registered"
     - Else: print formatted table with FRZ_ID, STATUS, CREATED_AT, MSC_VALID columns
     - Return 0

4. CLI argument parser `build_parser()`:
   - `frz-manage` subparser with subcommands: validate, freeze, list
   - validate: `--input` (required, doc directory path)
   - freeze: `--input` (required), `--id` (required, FRZ-xxx format), optional `--type revise`, `--reason`, `--previous-frz`
   - list: `--status` (optional, filter)

5. `main(argv=None) -> int`:
   - Parse args, dispatch to appropriate command function
   - Handle CommandError gracefully: print error message, return exit code

6. `if __name__ == "__main__": sys.exit(main())`

DO NOT:
- Use `rich` library for output formatting (not verified as installed — use plain string formatting)
- Implement extract mode in detail (Phase 8) — stub with "not implemented yet, use in Phase 8" message
- Hardcode workspace root paths — use _find_workspace_root()
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'skills/ll-frz-manage/scripts'); from frz_manage_runtime import build_parser; p = build_parser(); print('parser OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `skills/ll-frz-manage/scripts/frz_manage_runtime.py` exists
    - Contains functions: validate_frz, freeze_frz, list_frz, build_parser, main
    - Contains `def build_parser()` returning argparse.ArgumentParser
    - build_parser defines subcommands: validate, freeze, list
    - validate_frz calls MSCValidator.validate and prints MSC report
    - freeze_frz validates MSC before registering, rejects if msc_valid=False
    - list_frz calls list_frz from frz_registry and formats output as table
    - Uses yaml.safe_load (not yaml.load)
    - Handles CommandError with try/except in main()
    - `if __name__ == "__main__"` block present with sys.exit(main())
  </acceptance_criteria>
  <done>
    frz_manage_runtime.py implements validate (load YAML, parse, MSC validate, print report), freeze (validate first, save to artifacts, register, print confirmation), list (query registry, display table) subcommands with proper argument parsing and error handling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create skills/ll-frz-manage/scripts/test_frz_manage_runtime.py with integration tests</name>
  <files>skills/ll-frz-manage/scripts/test_frz_manage_runtime.py</files>
  <read_first>
    - skills/ll-frz-manage/scripts/frz_manage_runtime.py (file just created — verify function signatures)
    - cli/lib/frz_schema.py (FRZPackage, MSCValidator)
    - cli/lib/frz_registry.py (register_frz, list_frz)
  </read_first>
  <action>
Create `skills/ll-frz-manage/scripts/test_frz_manage_runtime.py` with pytest integration tests.

Tests use temporary directories for workspace isolation. Each test creates a temp workspace with ssot/registry/ directory.

1. `test_validate_valid_frz_package`:
   - Create temp workspace with valid FRZ YAML (all 5 MSC dims)
   - Create argparse.Namespace(input=temp_dir)
   - Call validate_frz(args)
   - Assert returns 0
   - Assert stdout contains "PASS" or "msc_valid"

2. `test_validate_missing_dimension`:
   - Create temp workspace with incomplete FRZ YAML (missing core_journeys)
   - Call validate_frz(args)
   - Assert returns 1
   - Assert stdout contains "FAIL" and "missing"

3. `test_validate_file_not_found`:
   - Call validate_frz with nonexistent input directory
   - Assert returns non-zero exit code
   - Assert raises CommandError or prints error

4. `test_freeze_success`:
   - Create temp workspace with ssot/registry/ directory and empty frz-registry.yaml
   - Create valid FRZ YAML in input dir
   - Create argparse.Namespace(input=temp_input_dir, id="FRZ-001")
   - Call freeze_frz(args)
   - Assert returns 0
   - Assert stdout contains "FRZ-001 registered" and "frozen"
   - Assert registry file contains "FRZ-001"

5. `test_freeze_rejects_invalid_msc`:
   - Create temp workspace with incomplete FRZ YAML
   - Call freeze_frz with id="FRZ-002"
   - Assert returns 1
   - Assert stdout contains "Cannot freeze" or "MSC validation failed"

6. `test_freeze_rejects_duplicate_id`:
   - Freeze FRZ-001 successfully
   - Attempt to freeze FRZ-001 again (same ID, different dir)
   - Assert raises CommandError or returns non-zero
   - Assert error message contains "already registered"

7. `test_freeze_invalid_frz_id`:
   - Call freeze_frz with id="INVALID-ID" or "frz-001" (lowercase)
   - Assert raises CommandError

8. `test_list_empty_registry`:
   - Create temp workspace with empty frz-registry.yaml
   - Call list_frz(args)
   - Assert stdout contains "No FRZ packages registered"

9. `test_list_with_registered_frz`:
   - Freeze FRZ-001 first
   - Call list_frz(args)
   - Assert stdout contains "FRZ-001" and "frozen"

10. `test_list_filter_by_status`:
    - Freeze FRZ-001 (status=frozen)
    - Call list_frz with status="blocked"
    - Assert returns 0, does not show FRZ-001 in output

11. `test_build_parser_has_all_subcommands`:
    - Call build_parser()
    - Parse "frz-manage validate --input /tmp" — no error
    - Parse "frz-manage freeze --input /tmp --id FRZ-001" — no error
    - Parse "frz-manage list" — no error

12. `test_main_dispatch_validate`:
    - Call main(["frz-manage", "validate", "--input", "/nonexistent"])
    - Assert returns non-zero (directory not found)

13. `test_freeze_with_revision_type`:
    - Freeze FRZ-001 first
    - Create revised FRZ YAML in different dir
    - Call freeze with --type revise --reason "scope change" --previous-frz FRZ-001 --id FRZ-002
    - Assert returns 0
    - Assert registry contains revision_type="revise" and previous_frz_ref="FRZ-001"

14. **[REVIEW-FIX] Test `test_validate_input_is_file_not_dir`** (Murat):
    - Call validate_frz with --input pointing to a file (not directory)
    - Assert returns non-zero, error message mentions "not a directory"

15. **[REVIEW-FIX] Test `test_validate_no_yaml_files_in_dir`** (Murat):
    - Create empty directory (no .yaml/.yml files)
    - Call validate_frz, assert returns non-zero, error mentions "No FRZ YAML found"

16. **[REVIEW-FIX] Test `test_find_workspace_root_not_found`** (Murat):
    - Create temp dir with no .planning or ssot in any parent
    - Call freeze_frz, assert raises CommandError about workspace root

17. **[REVIEW-FIX] Test `test_freeze_creates_source_snapshot`** (Mary):
    - Freeze FRZ-001 with input dir containing test files
    - Assert `artifacts/frz-input/FRZ-001/input/` contains copies of source files
    - Assert `artifacts/frz-input/FRZ-001/freeze.yaml` exists (not .json)

18. **[REVIEW-FIX] Test `test_extract_mode_returns_not_implemented`** (Murat):
    - Call main(["frz-manage", "extract", "--frz", "FRZ-001", "--output", "/tmp"])
    - Assert returns non-zero or prints "not implemented" message

19. **[REVIEW-FIX] Test `test_main_dispatch_with_no_args`** (Murat):
    - Call main([]) or main(["frz-manage"]) with no subcommand
    - Assert returns non-zero, prints usage help

20. **[REVIEW-FIX] Test `test_validate_malicious_yaml_rejected`** (Murat):
    - Create YAML with `!!python/object` tag in input dir
    - Call validate_frz, assert yaml.safe_load rejects it or raises clean error
  </action>
  <verify>
    <automated>pytest skills/ll-frz-manage/scripts/test_frz_manage_runtime.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `skills/ll-frz-manage/scripts/test_frz_manage_runtime.py` exists with at least 20 test functions
    - All tests pass: `pytest skills/ll-frz-manage/scripts/test_frz_manage_runtime.py -v` exits 0
    - Contains test functions: test_validate_valid_frz_package, test_validate_missing_dimension, test_freeze_success, test_freeze_rejects_invalid_msc, test_freeze_rejects_duplicate_id, test_list_empty_registry, test_list_with_registered_frz, test_build_parser_has_all_subcommands, test_validate_input_is_file_not_dir, test_validate_no_yaml_files_in_dir, test_freeze_creates_source_snapshot, test_extract_mode_returns_not_implemented
    - Tests use tempfile/tempdir for workspace isolation
    - Tests verify both return codes and stdout content
    - Test file imports pytest and sys
  </acceptance_criteria>
  <done>
    Integration tests cover: validate pass/fail cases, freeze success and rejection cases (invalid MSC, duplicate ID, invalid ID format), list empty and populated, status filtering, parser subcommand parsing, main dispatch, revision-type freeze. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input directory → YAML load | Untrusted YAML files from user-provided directories |
| FRZ ID parameter → registry write | User-provided ID must be validated before registration |
| Workspace root discovery → filesystem traversal | Path traversal risk if workspace root is incorrectly resolved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | YAML deserialization in _load_frz_from_dir | mitigate | Use yaml.safe_load() only — no yaml.load() — already project standard per qa_schemas.py |
| T-07-09 | Spoofing | FRZ ID format validation in freeze_frz | mitigate | Regex `^FRZ-\d{3,}$` enforced; uniqueness check via get_frz() before registration |
| T-07-10 | Tampering | Workspace root path resolution | mitigate | _find_workspace_root() searches for ssot/.planning anchors only; does not follow symlinks |
| T-07-11 | Denial of Service | Registry file lock contention (single YAML file) | accept | v2.0 single-user CLI; document as known limitation per RESEARCH.md Pitfall 2 |
| T-07-12 | Elevation of Privilege | Bypassing MSC validation to freeze directly | mitigate | freeze_frz explicitly calls MSCValidator.validate() before register_frz — cannot skip |
</threat_model>

<verification>
- All integration tests in test_frz_manage_runtime.py pass (13+ tests)
- Skill directory has complete structure: SKILL.md, ll.contract.yaml, ll.lifecycle.yaml, input/, output/, agents/, scripts/
- validate_input.sh and validate_output.sh are executable
- frz_manage_runtime.py can be run standalone: `python skills/ll-frz-manage/scripts/frz_manage_runtime.py frz-manage --help`
  </verification>

<success_criteria>
- FRZ-04 delivered: `ll frz-manage validate --input <doc-dir>` loads FRZ YAML, runs MSC validation, prints structured report
- FRZ-05 delivered: `ll frz-manage freeze --input <doc-dir> --id FRZ-xxx` validates MSC first, saves package, registers to registry
- FRZ-06 delivered: `ll frz-manage list [--status frozen|blocked]` queries registry and displays formatted table
- Complete skill structure follows ll-patch-capture template (11+ files)
- All integration tests pass with 0 failures
- No dependency on rich library (uses plain string formatting)
  </success_criteria>

<output>
After completion, create `.planning/phases/07-frz/07-03-SUMMARY.md`
</output>
