---
phase: 03-settlement-exec
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - cli/commands/skill/command.py
  - cli/lib/qa_skill_runtime.py
  - cli/lib/qa_schemas.py
  - skills/ll-test-exec-cli/SKILL.md
  - skills/ll-test-exec-cli/ll.lifecycle.yaml
  - skills/ll-test-exec-web-e2e/SKILL.md
  - skills/ll-test-exec-web-e2e/ll.lifecycle.yaml
autonomous: true
requirements: [REQ-03]
user_setup: []

must_haves:
  truths:
    - "CLI _QA_SKILL_MAP includes settlement, gate-evaluate, render-testset-view actions"
    - "qa_skill_runtime.py mappings include all 3 new skill actions"
    - "Deprecated skills have visible deprecation notices in SKILL.md and ll.lifecycle.yaml"
    - "qa_schemas.py has gate validator for release_gate_input.yaml output"
  artifacts:
    - path: "cli/commands/skill/command.py"
      provides: "CLI handler with 3 new entries in _QA_SKILL_MAP"
      contains: "settlement"
    - path: "cli/lib/qa_skill_runtime.py"
      provides: "Runtime mappings for 3 new actions across all 4 mapping dicts"
      exports: ["action_to_skill", "input_keys", "output_keys", "_action_to_schema_type"]
    - path: "cli/lib/qa_schemas.py"
      provides: "Gate output validator for release_gate_input.yaml"
      contains: "validate_gate"
    - path: "skills/ll-test-exec-cli/SKILL.md"
      provides: "Deprecated skill with visible notice"
      contains: "DEPRECATED"
    - path: "skills/ll-test-exec-web-e2e/SKILL.md"
      provides: "Deprecated skill with visible notice"
      contains: "DEPRECATED"
  key_links:
    - from: "cli/commands/skill/command.py"
      to: "cli/lib/qa_skill_runtime.py"
      via: "run_skill() call for new actions"
      pattern: "run_skill.*action=ctx.action"
    - from: "cli/lib/qa_skill_runtime.py"
      to: "cli/lib/qa_schemas.py"
      via: "_action_to_schema_type mapping"
      pattern: "_action_to_schema_type"

---

<objective>
Register 3 new skill actions in CLI, extend qa_skill_runtime.py mappings, add gate output validator to qa_schemas.py, and mark 2 legacy skills as deprecated.

Purpose: The 3 new skills created in Wave 1 (Plans 01-03) need CLI registration so they can be invoked via `python -m cli skill <action>`. The shared runtime (qa_skill_runtime.py) must map the new actions to their skill directories, input keys, output keys, and schema types. Legacy ADR-035 skills must be visibly deprecated without modifying their execution files.
Output: 4 modified files (command.py, qa_skill_runtime.py, qa_schemas.py) and 2 deprecation-noticed files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-settlement-exec/01-CONTEXT.md
@.planning/phases/03-settlement-exec/01-RESEARCH.md

@cli/commands/skill/command.py
@cli/lib/qa_skill_runtime.py
@cli/lib/qa_schemas.py
@skills/ll-test-exec-cli/SKILL.md
@skills/ll-test-exec-cli/ll.lifecycle.yaml
@skills/ll-test-exec-web-e2e/SKILL.md
@skills/ll-test-exec-web-e2e/ll.lifecycle.yaml

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From cli/commands/skill/command.py -- existing _QA_SKILL_MAP (lines 102-109):
_QA_SKILL_MAP has 6 entries. Add 3 new entries after e2e-spec-gen:
  "settlement" -> ("ll-qa-settlement", "qa_settlement")
  "gate-evaluate" -> ("ll-qa-gate-evaluate", "qa_gate_evaluate")
  "render-testset-view" -> ("render-testset-view", "qa_render_testset")
Also update the ensure() check on line 19 to include the 3 new actions.

From cli/lib/qa_skill_runtime.py -- 4 mapping dicts to extend:
_find_skill_dir action_to_skill: add "settlement" -> "ll-qa-settlement", "gate-evaluate" -> "ll-qa-gate-evaluate", "render-testset-view" -> "render-testset-view"
_resolve_input_path input_keys: add "settlement" -> "manifest_path", "gate-evaluate" -> "api_manifest_path", "render-testset-view" -> "api_plan_path"
_resolve_output_path output_keys: add "settlement" -> "settlement-report", "gate-evaluate" -> "release_gate_input", "render-testset-view" -> "testset-view"
_action_to_schema_type: add "settlement" -> "settlement", "gate-evaluate" -> "gate", "render-testset-view" -> None

From cli/lib/qa_schemas.py -- _VALIDATORS dict:
Add gate validator with key "gate" mapping to ("gate_evaluation", validate_gate).
Create validate_gate() function that checks: top-level key gate_evaluation, required fields evaluated_at, feature_id, final_decision (enum: pass/fail/conditional_pass via GateResult), api_chain object (total, passed, failed, blocked, uncovered as int; pass_rate as number; evidence_status as EvidenceStatus enum), e2e_chain (same plus exception_journeys_executed as int), anti_laziness_checks (7 booleans: manifest_frozen, cut_records_valid, pending_waivers_counted, evidence_consistent, min_exception_coverage, no_evidence_not_executed, evidence_hash_binding), evidence_hash (64-char lowercase hex via regex ^[a-f0-9]{64}$), decision_reason (non-empty string).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register new actions in CLI handler and extend qa_skill_runtime.py mappings</name>
  <files>cli/commands/skill/command.py
cli/lib/qa_skill_runtime.py</files>
  <read_first>cli/commands/skill/command.py
cli/lib/qa_skill_runtime.py</read_first>
  <action>Modify 2 files:

**cli/commands/skill/command.py** -- Two changes:

1. Update the ensure() call on line 19. The current action set contains 13 actions. Add 3 new actions: "settlement", "gate-evaluate", "render-testset-view". The new set should contain all 16 actions including these 3 new ones.

2. In _QA_SKILL_MAP (line 102-109), add 3 entries after the existing 6:
   "settlement": ("ll-qa-settlement", "qa_settlement"),
   "gate-evaluate": ("ll-qa-gate-evaluate", "qa_gate_evaluate"),
   "render-testset-view": ("render-testset-view", "qa_render_testset"),

**cli/lib/qa_skill_runtime.py** -- Extend all 4 mapping dicts:

1. In _find_skill_dir() action_to_skill dict (line 79-86), add:
   "settlement": "ll-qa-settlement",
   "gate-evaluate": "ll-qa-gate-evaluate",
   "render-testset-view": "render-testset-view",

2. In _resolve_input_path() input_keys dict (line 93-100), add:
   "settlement": "manifest_path",
   "gate-evaluate": "api_manifest_path",
   "render-testset-view": "api_plan_path",

3. In _resolve_output_path() output_keys dict (line 115-122), add:
   "settlement": "settlement-report",
   "gate-evaluate": "release_gate_input",
   "render-testset-view": "testset-view",

4. In _action_to_schema_type() (line 183-192), add:
   "settlement": "settlement",
   "gate-evaluate": "gate",
   "render-testset-view": None,

Use the Read tool to get exact current content, then Edit to make precise insertions. Do NOT rewrite entire files.</action>
  <verify>
    <automated>python -c "
import sys
sys.path.insert(0, 'E:/ai/LEE-Lite-skill-first')
from cli.commands.skill.command import _QA_SKILL_MAP
assert 'settlement' in _QA_SKILL_MAP
assert 'gate-evaluate' in _QA_SKILL_MAP
assert 'render-testset-view' in _QA_SKILL_MAP
from cli.lib.qa_skill_runtime import _action_to_schema_type
assert _action_to_schema_type('settlement') == 'settlement'
assert _action_to_schema_type('gate-evaluate') == 'gate'
assert _action_to_schema_type('render-testset-view') is None
print('All CLI registrations verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - cli/commands/skill/command.py _QA_SKILL_MAP contains 9 entries (6 existing + 3 new)
    - cli/commands/skill/command.py _QA_SKILL_MAP contains key "settlement" with tuple value ("ll-qa-settlement", "qa_settlement")
    - cli/commands/skill/command.py _QA_SKILL_MAP contains key "gate-evaluate" with tuple value ("ll-qa-gate-evaluate", "qa_gate_evaluate")
    - cli/commands/skill/command.py _QA_SKILL_MAP contains key "render-testset-view" with tuple value ("render-testset-view", "qa_render_testset")
    - cli/commands/skill/command.py ensure() action set contains "settlement", "gate-evaluate", "render-testset-view"
    - cli/lib/qa_skill_runtime.py _find_skill_dir action_to_skill dict contains all 3 new mappings
    - cli/lib/qa_skill_runtime.py _resolve_input_path input_keys dict contains all 3 new mappings
    - cli/lib/qa_skill_runtime.py _resolve_output_path output_keys dict contains all 3 new mappings
    - cli/lib/qa_skill_runtime.py _action_to_schema_type contains "settlement": "settlement", "gate-evaluate": "gate", "render-testset-view": None
    - Python import test passes without errors
  </acceptance_criteria>
  <done>CLI registered with all 3 new actions, runtime mappings extended across all 4 dicts, Python import verification passes</done>
</task>

<task type="auto">
  <name>Task 2: Add validate_gate() to qa_schemas.py for release_gate_input.yaml validation</name>
  <files>cli/lib/qa_schemas.py</files>
  <read_first>cli/lib/qa_schemas.py</read_first>
  <action>Add a gate output validator to qa_schemas.py. Two changes:

1. Add validate_gate() function -- Insert between validate_settlement() (line 630) and the _VALIDATORS dict (line 637). The function takes data dict, uses _require() and _enum_check() patterns from existing validators. Checks: evaluated_at (required), feature_id (required), final_decision (required, enum GateResult), api_chain (required object with total/passed/failed/blocked/uncovered as int, pass_rate as number, evidence_status as EvidenceStatus enum), e2e_chain (required object with same fields plus exception_journeys_executed as int), anti_laziness_checks (required object with 7 booleans: manifest_frozen, cut_records_valid, pending_waivers_counted, evidence_consistent, min_exception_coverage, no_evidence_not_executed, evidence_hash_binding), evidence_hash (required, regex ^[a-f0-9]{64}$), decision_reason (required non-empty string). Returns data dict.

2. Add to _VALIDATORS dict (line 637-642): add "gate": ("gate_evaluation", validate_gate) as the 5th entry.

Use the Read tool to get exact current content, then Edit to make precise insertions. Do NOT rewrite entire file.</action>
  <verify>
    <automated>python -c "
import sys
sys.path.insert(0, 'E:/ai/LEE-Lite-skill-first')
from cli.lib.qa_schemas import validate_gate, _VALIDATORS
assert 'gate' in _VALIDATORS
assert _VALIDATORS['gate'][0] == 'gate_evaluation'
sample = {
    'evaluated_at': '2026-04-14T00:00:00Z',
    'feature_id': 'test-001',
    'final_decision': 'pass',
    'api_chain': {'total': 10, 'passed': 9, 'failed': 1, 'blocked': 0, 'uncovered': 0, 'pass_rate': 0.9, 'evidence_status': 'complete'},
    'e2e_chain': {'total': 5, 'passed': 5, 'failed': 0, 'blocked': 0, 'uncovered': 0, 'pass_rate': 1.0, 'exception_journeys_executed': 2, 'evidence_status': 'complete'},
    'anti_laziness_checks': {'manifest_frozen': True, 'cut_records_valid': True, 'pending_waivers_counted': True, 'evidence_consistent': True, 'min_exception_coverage': True, 'no_evidence_not_executed': True, 'evidence_hash_binding': True},
    'evidence_hash': 'a' * 64,
    'decision_reason': 'All checks passed'
}
result = validate_gate(sample)
assert result is not None
print('validate_gate verified with sample data')
"</automated>
  </verify>
  <acceptance_criteria>
    - cli/lib/qa_schemas.py contains `def validate_gate(data: dict)` function
    - cli/lib/qa_schemas.py validate_gate checks final_decision enum using GateResult
    - cli/lib/qa_schemas.py validate_gate checks all api_chain fields (total, passed, failed, blocked, uncovered, pass_rate, evidence_status)
    - cli/lib/qa_schemas.py validate_gate checks all e2e_chain fields including exception_journeys_executed
    - cli/lib/qa_schemas.py validate_gate checks all 7 anti_laziness_checks boolean fields
    - cli/lib/qa_schemas.py validate_gate checks evidence_hash is 64-char hex pattern
    - cli/lib/qa_schemas.py validate_gate checks decision_reason is non-empty string
    - cli/lib/qa_schemas.py _VALIDATORS dict contains "gate" key with tuple value ("gate_evaluation", validate_gate)
    - Python import and sample validation test passes
  </acceptance_criteria>
  <done>validate_gate() function added to qa_schemas.py, _VALIDATORS extended with gate type, Python validation test passes</done>
</task>

<task type="auto">
  <name>Task 3: Add deprecation notices to legacy skills (ll-test-exec-cli, ll-test-exec-web-e2e)</name>
  <files>skills/ll-test-exec-cli/SKILL.md
skills/ll-test-exec-cli/ll.lifecycle.yaml
skills/ll-test-exec-web-e2e/SKILL.md
skills/ll-test-exec-web-e2e/ll.lifecycle.yaml</files>
  <read_first>skills/ll-test-exec-cli/SKILL.md
skills/ll-test-exec-cli/ll.lifecycle.yaml
skills/ll-test-exec-web-e2e/SKILL.md
skills/ll-test-exec-web-e2e/ll.lifecycle.yaml</read_first>
  <action>Add deprecation notices to 4 files. DO NOT modify any other files in these legacy skill directories.

**skills/ll-test-exec-cli/SKILL.md** -- Modify the YAML frontmatter at the top of the file:
- Change description to: "DEPRECATED -- ADR-035 TESTSET-driven execution skill. Superseded by spec-driven execution (ADR-047). Do not use for new features."
- Add frontmatter fields: deprecated: true, deprecated_since: "2026-04-14", superseded_by: "spec-driven execution via ll-qa-api-spec-gen + ll-qa-e2e-spec-gen"
- After the closing frontmatter marker, add a paragraph: > **DEPRECATED**: This skill is superseded by ADR-047 spec-driven execution. New features should use ll-qa-api-spec-gen and ll-qa-e2e-spec-gen which compile tests directly from coverage manifests, not from TESTSET files. This skill is retained for backward compatibility only and will be removed in a future release.
- Keep ALL existing content after the frontmatter intact.

**skills/ll-test-exec-cli/ll.lifecycle.yaml** -- Add these fields at the TOP of the file:
- deprecated: true
- deprecated_since: "2026-04-14"
- superseded_by: "spec-driven execution (ADR-047)"
- Change state to: deprecated
- Keep all existing states, transitions, limits intact.

**skills/ll-test-exec-web-e2e/SKILL.md** -- Same pattern as ll-test-exec-cli:
- Change description to: "DEPRECATED -- ADR-035 TESTSET-driven web E2E execution skill. Superseded by spec-driven execution (ADR-047). Do not use for new features."
- Add frontmatter fields: deprecated: true, deprecated_since: "2026-04-14", superseded_by: "spec-driven execution via ll-qa-e2e-spec-gen"
- After frontmatter, add: > **DEPRECATED**: This skill is superseded by ADR-047 spec-driven execution. New features should use ll-qa-e2e-spec-gen which compiles E2E journeys directly from coverage manifests. This skill is retained for backward compatibility only and will be removed in a future release.

**skills/ll-test-exec-web-e2e/ll.lifecycle.yaml** -- Same pattern:
- Add deprecated: true, deprecated_since: "2026-04-14", superseded_by: "spec-driven execution (ADR-047)" at top
- Change state to: deprecated

IMPORTANT: Only modify SKILL.md frontmatter + add deprecation paragraph, and ll.lifecycle.yaml top fields. Do NOT modify any scripts, agents, evidence, or other files in these directories.</action>
  <verify>
    <automated>grep -q "DEPRECATED" E:/ai/LEE-Lite-skill-first/skills/ll-test-exec-cli/SKILL.md && grep -q "deprecated: true" E:/ai/LEE-Lite-skill-first/skills/ll-test-exec-cli/ll.lifecycle.yaml && grep -q "DEPRECATED" E:/ai/LEE-Lite-skill-first/skills/ll-test-exec-web-e2e/SKILL.md && grep -q "deprecated: true" E:/ai/LEE-Lite-skill-first/skills/ll-test-exec-web-e2e/ll.lifecycle.yaml && echo "All deprecation notices present"</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-test-exec-cli/SKILL.md contains "deprecated: true" in frontmatter
    - skills/ll-test-exec-cli/SKILL.md contains "DEPRECATED" in description field
    - skills/ll-test-exec-cli/SKILL.md contains deprecation notice paragraph with DEPRECATED text
    - skills/ll-test-exec-cli/SKILL.md contains superseded_by reference to spec-driven execution
    - skills/ll-test-exec-cli/ll.lifecycle.yaml contains "deprecated: true" at top level
    - skills/ll-test-exec-cli/ll.lifecycle.yaml contains "state: deprecated"
    - skills/ll-test-exec-web-e2e/SKILL.md contains "deprecated: true" in frontmatter
    - skills/ll-test-exec-web-e2e/SKILL.md contains "DEPRECATED" in description field
    - skills/ll-test-exec-web-e2e/SKILL.md contains deprecation notice paragraph with DEPRECATED text
    - skills/ll-test-exec-web-e2e/SKILL.md contains superseded_by reference to spec-driven execution
    - skills/ll-test-exec-web-e2e/ll.lifecycle.yaml contains "deprecated: true" at top level
    - skills/ll-test-exec-web-e2e/ll.lifecycle.yaml contains "state: deprecated"
    - No files modified in these skill directories EXCEPT SKILL.md and ll.lifecycle.yaml
  </acceptance_criteria>
  <done>Both legacy skills have visible deprecation notices in SKILL.md and ll.lifecycle.yaml, no other files modified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI action string to handler routing | New actions must be in both the ensure() allowlist and _QA_SKILL_MAP to avoid rejection or routing failure |
| qa_skill_runtime.py mappings to skill resolution | Incorrect mappings cause runtime to look up wrong skill directories or use wrong schema types |
| qa_schemas.py gate validator to validate_output.sh | Gate output validation must match the exact structure expected by the gate evaluate skill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Elevation of Privilege | CLI action routing in command.py | mitigate | New actions added to both ensure() allowlist AND _QA_SKILL_MAP; Python import test verifies routing |
| T-03-15 | Tampering | qa_skill_runtime.py mapping dicts | mitigate | All 4 dicts extended consistently; automated test imports and verifies each mapping returns correct value |
| T-03-16 | Tampering | qa_schemas.py gate validator | mitigate | validate_gate() uses same _require, _enum_check patterns as existing validators; evidence_hash regex enforces SHA-256 format |
| T-03-17 | Repudiation | Deprecation of legacy skills | mitigate | deprecation notices include deprecated_since date and superseded_by reference; ll.lifecycle.yaml state changed to deprecated |
</threat_model>

<verification>
Overall phase checks:
- Python import test verifies _QA_SKILL_MAP has 9 entries with correct values
- Python import test verifies qa_skill_runtime.py mappings return correct values for all 3 new actions
- Python import test verifies qa_schemas.py validate_gate() works with sample data
- grep verifies deprecation notices in both legacy skill SKILL.md and ll.lifecycle.yaml files
- Only SKILL.md and ll.lifecycle.yaml modified in legacy skill directories
</verification>

<success_criteria>
- CLI handler accepts all 3 new skill actions (settlement, gate-evaluate, render-testset-view)
- qa_skill_runtime.py maps all 3 actions across all 4 mapping dicts (action_to_skill, input_keys, output_keys, _action_to_schema_type)
- qa_schemas.py has validate_gate() function that validates release_gate_input.yaml structure
- qa_schemas.py _VALIDATORS dict includes "gate" type
- Both legacy skills (ll-test-exec-cli, ll-test-exec-web-e2e) have visible DEPRECATED notices
- No files modified in legacy skill directories except SKILL.md and ll.lifecycle.yaml
</success_criteria>

<output>
After completion, create `.planning/phases/03-settlement-exec/03-04-SUMMARY.md`
</output>
