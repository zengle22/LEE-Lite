---
phase: 03-settlement-exec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/ll-qa-settlement/scripts/run.sh
  - skills/ll-qa-settlement/scripts/validate_input.sh
  - skills/ll-qa-settlement/scripts/validate_output.sh
  - skills/ll-qa-settlement/evidence/settlement.schema.json
  - skills/ll-qa-settlement/ll.lifecycle.yaml
autonomous: true
requirements: [REQ-03]
user_setup: []

must_haves:
  truths:
    - "Settlement reports can be generated from executed manifests and pass schema validation"
    - "Input manifests are validated before settlement generation begins"
    - "Settlement output statistics are self-consistent (executed = passed + failed + blocked)"
    - "Settlement skill can be invoked via CLI protocol with chain-specific arguments"
    - "Gap lists and waiver lists accurately reflect manifest item states"
  artifacts:
    - path: "skills/ll-qa-settlement/scripts/run.sh"
      provides: "Skill entry point — parses args, validates, invokes CLI, validates output"
      min_lines: 40
    - path: "skills/ll-qa-settlement/scripts/validate_input.sh"
      provides: "Input validation — checks manifest file existence and schema"
      exports: ["bash script"]
    - path: "skills/ll-qa-settlement/scripts/validate_output.sh"
      provides: "Output validation — checks settlement report conforms to schema"
      exports: ["bash script"]
    - path: "skills/ll-qa-settlement/ll.lifecycle.yaml"
      provides: "Lifecycle state machine for skill"
      contains: "skill: ll-qa-settlement"
    - path: "skills/ll-qa-settlement/evidence/settlement.schema.json"
      provides: "Evidence schema for settlement artifacts"
      contains: "settlement"
  key_links:
    - from: "skills/ll-qa-settlement/scripts/run.sh"
      to: "cli/commands/skill/command.py"
      via: "python -m cli skill settlement"
      pattern: "python -m cli skill settlement"
    - from: "skills/ll-qa-settlement/scripts/validate_output.sh"
      to: "cli/lib/qa_schemas.py"
      via: "python -m cli.lib.qa_schemas --type settlement"
      pattern: "qa_schemas.*--type settlement"

---

<objective>
Add Prompt-first runtime infrastructure to ll-qa-settlement skill (scripts/run.sh, validate_input.sh, validate_output.sh, evidence schema, ll.lifecycle.yaml).

Purpose: The ll-qa-settlement skill already has SKILL.md, ll.contract.yaml, agents/executor.md, agents/supervisor.md, and input/output contracts. It is missing the 6 standard infrastructure files that make it executable via the CLI protocol established in Phase 2.
Output: 5 new files in the ll-qa-settlement skill directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-settlement-exec/01-CONTEXT.md
@.planning/phases/03-settlement-exec/01-RESEARCH.md

@skills/ll-qa-settlement/SKILL.md
@skills/ll-qa-settlement/agents/executor.md
@skills/ll-qa-settlement/ll.contract.yaml
@skills/ll-qa-feat-to-apiplan/scripts/run.sh
@skills/ll-qa-api-manifest-init/scripts/validate_input.sh
@skills/ll-qa-api-spec-gen/scripts/validate_output.sh
@skills/ll-qa-api-manifest-init/ll.lifecycle.yaml
@cli/lib/qa_schemas.py
@ssot/schemas/qa/settlement.yaml

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From cli/lib/qa_schemas.py — SettlementReport dataclass:
```python
@dataclass(frozen=True)
class SettlementReport:
    chain: str  # enum[api, e2e]
    summary: Summary  # total_capability_count, total_coverage_item_count, mapped_count, generated_count, executed_count, passed_count, failed_count, blocked_count, uncovered_count
    by_capability: list[ByCapability]
    by_feature_ref: list[ByFeatureRef]
    evidence_completeness: EvidenceCompleteness | None
    gap_list: list[GapEntry]
    waiver_list: list[WaiverEntry]
    verdict: Verdict | None
    gate_evaluation: GateEvaluation | None
```

From cli/lib/qa_schemas.py — validate_settlement() expects top-level key `settlement_report` in YAML, then checks chain enum and summary fields.

From skills/ll-qa-feat-to-apiplan/scripts/run.sh — Reference pattern for run.sh:
- Parse args with while/case loop
- Validate input with bash validate_input.sh
- Invoke `python -m cli skill <action>` with JSON request via process substitution
- Validate output with bash validate_output.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ll-qa-settlement scripts (run.sh, validate_input.sh, validate_output.sh)</name>
  <files>skills/ll-qa-settlement/scripts/run.sh
skills/ll-qa-settlement/scripts/validate_input.sh
skills/ll-qa-settlement/scripts/validate_output.sh</files>
  <read_first>skills/ll-qa-settlement/SKILL.md
skills/ll-qa-settlement/agents/executor.md
skills/ll-qa-feat-to-apiplan/scripts/run.sh
skills/ll-qa-api-manifest-init/scripts/validate_input.sh
skills/ll-qa-api-spec-gen/scripts/validate_output.sh
cli/lib/qa_schemas.py</read_first>
  <action>Create 3 shell scripts following the Phase 2 pattern exactly:

1. **scripts/run.sh** — Entry point for ll-qa-settlement:
   - `#!/usr/bin/env bash` with `set -euo pipefail`
   - Parse arguments: `--manifest-path`, `--chain` (values: api or e2e, default: api), `--output-dir`, `--workspace` (default: $PWD)
   - Require `--manifest-path`, exit 1 with error message if missing
   - Call `bash "${SCRIPT_DIR}/validate_input.sh" "${MANIFEST_PATH}"` before running skill
   - Invoke CLI: `python -m cli skill settlement` with JSON request payload containing `manifest_path`, `chain`, `output_dir` (default: `${WORKSPACE}/ssot/tests/.artifacts/settlement`)
   - After CLI returns, validate output file: `${OUTPUT_DIR}/${CHAIN}-settlement-report.yaml` using `bash "${SCRIPT_DIR}/validate_output.sh"`
   - Follow the exact argument parsing, process substitution, and structure from `skills/ll-qa-feat-to-apiplan/scripts/run.sh`

2. **scripts/validate_input.sh** — Input validation:
   - Accept manifest path as $1
   - Check file exists, exit 1 with "FAIL: file not found" if not
   - Validate against manifest schema: `python -m cli.lib.qa_schemas --type manifest "${MANIFEST_PATH}"`
   - On failure: exit 1 with "FAIL: input does not conform to manifest schema"
   - On success: echo "OK: input validated"
   - Follow exact pattern from `skills/ll-qa-api-manifest-init/scripts/validate_input.sh`

3. **scripts/validate_output.sh** — Output validation:
   - Accept output path as $1
   - Check file exists, exit 1 if not
   - Validate against settlement schema: `python -m cli.lib.qa_schemas --type settlement "${OUTPUT_PATH}"`
   - On failure: exit 1 with "FAIL: invalid settlement output"
   - On success: echo "OK: output validated"
   - Follow exact pattern from `skills/ll-qa-api-spec-gen/scripts/validate_output.sh`

Make all 3 scripts executable with chmod +x.</action>
  <verify>
    <automated>bash -n E:/ai/LEE-Lite-skill-first/skills/ll-qa-settlement/scripts/run.sh && bash -n E:/ai/LEE-Lite-skill-first/skills/ll-qa-settlement/scripts/validate_input.sh && bash -n E:/ai/LEE-Lite-skill-first/skills/ll-qa-settlement/scripts/validate_output.sh && echo "All scripts pass syntax check"</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-qa-settlement/scripts/run.sh exists and contains `set -euo pipefail`
    - skills/ll-qa-settlement/scripts/run.sh contains `--manifest-path` argument parsing
    - skills/ll-qa-settlement/scripts/run.sh contains `--chain` argument parsing with default value `api`
    - skills/ll-qa-settlement/scripts/run.sh contains `python -m cli skill settlement`
    - skills/ll-qa-settlement/scripts/run.sh contains `validate_input.sh` call before CLI invocation
    - skills/ll-qa-settlement/scripts/run.sh contains `validate_output.sh` call after CLI invocation
    - skills/ll-qa-settlement/scripts/validate_input.sh exists and contains `python -m cli.lib.qa_schemas --type manifest`
    - skills/ll-qa-settlement/scripts/validate_output.sh exists and contains `python -m cli.lib.qa_schemas --type settlement`
    - All 3 scripts pass `bash -n` syntax check
    - All 3 scripts are executable (chmod +x)
  </acceptance_criteria>
  <done>3 scripts created with correct bash syntax, following Phase 2 patterns, executable</done>
</task>

<task type="auto">
  <name>Task 2: Create ll-qa-settlement ll.lifecycle.yaml and evidence schema</name>
  <files>skills/ll-qa-settlement/ll.lifecycle.yaml
skills/ll-qa-settlement/evidence/settlement.schema.json</files>
  <read_first>skills/ll-qa-api-manifest-init/ll.lifecycle.yaml
skills/ll-qa-api-manifest-init/evidence/execution-evidence.schema.json
skills/ll-qa-settlement/ll.contract.yaml
cli/lib/qa_schemas.py</read_first>
  <action>Create 2 files:

1. **skills/ll-qa-settlement/ll.lifecycle.yaml** — Follow the exact structure from `skills/ll-qa-api-manifest-init/ll.lifecycle.yaml`:
   ```yaml
   skill: ll-qa-settlement
   version: "1.0"
   state: draft
   lifecycle_states:
     - draft
     - validated
     - executed
     - frozen
   ```

2. **skills/ll-qa-settlement/evidence/settlement.schema.json** — JSON Schema describing settlement evidence artifacts:
   - Create `evidence/` directory if not exists
   - Define schema with `$schema`, `title: "SettlementEvidence"`, `type: object`
   - Required properties: `chain` (enum: ["api", "e2e"]), `manifest_path` (string), `settlement_report_path` (string), `generated_at` (string, format: date-time)
   - Optional properties: `evidence_hash` (string, pattern: "^[a-f0-9]{64}$" for SHA-256 hex)
   - Reference the settlement schema fields: summary statistics, gap_list entries, waiver_list entries

Make sure the evidence directory is created before writing the schema file.</action>
  <verify>
    <automated>python -c "import json; json.load(open('E:/ai/LEE-Lite-skill-first/skills/ll-qa-settlement/evidence/settlement.schema.json')); print('Valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-qa-settlement/ll.lifecycle.yaml exists and contains `skill: ll-qa-settlement`
    - skills/ll-qa-settlement/ll.lifecycle.yaml contains `lifecycle_states:` with at least `draft`, `validated`, `executed`, `frozen`
    - skills/ll-qa-settlement/evidence/settlement.schema.json exists and is valid JSON
    - skills/ll-qa-settlement/evidence/settlement.schema.json contains `"chain"` in required or properties
    - skills/ll-qa-settlement/evidence/ directory exists
  </acceptance_criteria>
  <done>ll.lifecycle.yaml and evidence schema created, JSON validates, lifecycle states match Phase 2 pattern</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest file → validate_input.sh | External YAML file crosses trust boundary; must be validated before processing |
| settlement report → validate_output.sh | LLM-generated output must conform to settlement schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | validate_input.sh manifest parsing | mitigate | Use `python -m cli.lib.qa_schemas --type manifest` which uses yaml.safe_load() only, never yaml.load() with unsafe Loader |
| T-03-02 | Tampering | validate_output.sh settlement parsing | mitigate | Use `python -m cli.lib.qa_schemas --type settlement` which uses yaml.safe_load() and validates all enum fields |
| T-03-03 | Elevation of Privilege | run.sh argument parsing | mitigate | Validate --manifest-path is a file that exists before passing to any processing; use set -euo pipefail to fail fast on undefined variables |
| T-03-04 | Repudiation | Settlement report generation | accept | Settlement reports are written to filesystem with timestamps; audit trail in YAML gap_list and waiver_list includes approver and approved_at fields |
</threat_model>

<verification>
Overall phase checks:
- bash -n on all 3 scripts passes
- JSON schema validates
- ll.lifecycle.yaml has correct structure
- Scripts follow Phase 2 pattern (diff against reference shows same structure)
</verification>

<success_criteria>
- ll-qa-settlement skill has all 6 new infrastructure files created
- scripts/run.sh can parse arguments and construct valid CLI invocation
- scripts/validate_input.sh rejects non-existent manifest files
- scripts/validate_output.sh calls qa_schemas.py with --type settlement
- ll.lifecycle.yaml matches Phase 2 pattern
- evidence/settlement.schema.json is valid JSON with correct properties
</success_criteria>

<output>
After completion, create `.planning/phases/03-settlement-exec/03-01-SUMMARY.md`
</output>
