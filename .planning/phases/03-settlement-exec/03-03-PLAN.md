---
phase: 03-settlement-exec
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/render-testset-view/SKILL.md
  - skills/render-testset-view/ll.contract.yaml
  - skills/render-testset-view/ll.lifecycle.yaml
  - skills/render-testset-view/agents/executor.md
  - skills/render-testset-view/agents/supervisor.md
  - skills/render-testset-view/input/contract.yaml
  - skills/render-testset-view/input/semantic-checklist.md
  - skills/render-testset-view/output/contract.yaml
  - skills/render-testset-view/output/semantic-checklist.md
  - skills/render-testset-view/evidence/testset-view.schema.json
  - skills/render-testset-view/scripts/run.sh
  - skills/render-testset-view/scripts/validate_input.sh
  - skills/render-testset-view/scripts/validate_output.sh
autonomous: true
requirements: [REQ-03]
user_setup: []

must_haves:
  truths:
    - "Old testset consumers can read render-testset-view output as backward-compatible coverage data"
    - "render-testset-view correctly aggregates plan/manifest/spec/settlement into legacy testset format"
    - "render-testset-view validates input against all 4 schema types before rendering"
    - "render-testset-view output validates against the legacy testset schema"
    - "render-testset-view is a read-only aggregation skill that does not modify input artifacts or execute tests"
  artifacts:
    - path: "skills/render-testset-view/SKILL.md"
      provides: "Skill definition — backward compatibility, aggregates plan/manifest/spec/settlement into testset view"
      min_lines: 30
    - path: "skills/render-testset-view/scripts/run.sh"
      provides: "Skill entry point — parses args for plan/manifest/spec/settlement inputs, validates, invokes CLI, validates output"
      min_lines: 40
    - path: "skills/render-testset-view/scripts/validate_input.sh"
      provides: "Input validation — checks all input files exist and conform to schemas"
      exports: ["bash script"]
    - path: "skills/render-testset-view/scripts/validate_output.sh"
      provides: "Output validation — checks testset-compatible output has required structure"
      exports: ["bash script"]
    - path: "skills/render-testset-view/ll.lifecycle.yaml"
      provides: "Lifecycle state machine for skill"
      contains: "skill: render-testset-view"
  key_links:
    - from: "skills/render-testset-view/scripts/run.sh"
      to: "cli/commands/skill/command.py"
      via: "python -m cli skill render-testset-view"
      pattern: "python -m cli skill render-testset-view"
    - from: "skills/render-testset-view/scripts/validate_input.sh"
      to: "cli/lib/qa_schemas.py"
      via: "python -m cli.lib.qa_schemas --type plan/manifest/spec/settlement"
      pattern: "qa_schemas.*--type"

---

<objective>
Create render-testset-view as a new skill directory from scratch with full Prompt-first runtime infrastructure.

Purpose: This skill provides backward compatibility by aggregating plan/manifest/spec/settlement artifacts from the new dual-chain system and rendering them into the old testset-compatible view format. It must follow the exact same 6-file pattern as Phase 2 skills. This is a read-only aggregation skill — it does NOT execute tests.
Output: Complete skill directory with 13 new files (SKILL.md, ll.contract.yaml, ll.lifecycle.yaml, 2 agents, 2 input files, 2 output files, evidence schema, 3 scripts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-settlement-exec/01-CONTEXT.md
@.planning/phases/03-settlement-exec/01-RESEARCH.md

@skills/ll-qa-settlement/SKILL.md
@skills/ll-qa-gate-evaluate/SKILL.md
@skills/ll-qa-feat-to-apiplan/SKILL.md
@skills/ll-qa-feat-to-apiplan/scripts/run.sh
@skills/ll-qa-api-manifest-init/scripts/validate_input.sh
@skills/ll-qa-api-spec-gen/scripts/validate_output.sh
@skills/ll-qa-api-manifest-init/ll.lifecycle.yaml
@skills/ll-qa-api-manifest-init/ll.contract.yaml
@skills/ll-qa-api-manifest-init/input/contract.yaml
@skills/ll-qa-api-manifest-init/output/contract.yaml
@skills/ll-qa-api-manifest-init/input/semantic-checklist.md
@skills/ll-qa-api-manifest-init/output/semantic-checklist.md
@skills/ll-qa-api-manifest-init/agents/executor.md
@skills/ll-qa-api-manifest-init/agents/supervisor.md
@.local/adr018-skill-trial/real-001/artifacts/active/formal/testset/testset-real-001-u01-d125bb50f9.json
@cli/lib/qa_schemas.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Old testset format (from .local example):
```json
{
  "assigned_id": "TESTSET-REAL-001-U01-D125BB50F9",
  "test_set_ref": "TESTSET-REAL-001-U01-D125BB50F9",
  "target_skill": "skill.qa.test_exec_cli",
  "title": "Qualification fixture u01"
}
```

The old testset format has: assigned_id, test_set_ref, target_skill, title. The render skill must aggregate new dual-chain artifacts (plan, manifest, spec, settlement) and produce output conforming to this legacy format for backward compatibility.

Phase 2 skill pattern for SKILL.md structure:
- name/description frontmatter
- "## Canonical Authority" section with ADR reference
- "## Required Read Order" section
- "## Execution Protocol" section
- "## Workflow Boundary" section
- "## Non-Negotiable Rules" section

Phase 2 skill pattern for ll.contract.yaml: version, states, transitions, limits structure.
Phase 2 skill pattern for ll.lifecycle.yaml: skill name, version, state, lifecycle_states.
Phase 2 skill pattern for agents/executor.md: Role + Instructions sections.
Phase 2 skill pattern for agents/supervisor.md: Validation Checklist section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create render-testset-view skill directory with definition files (SKILL.md, contracts, agents, lifecycle)</name>
  <files>skills/render-testset-view/SKILL.md
skills/render-testset-view/ll.contract.yaml
skills/render-testset-view/ll.lifecycle.yaml
skills/render-testset-view/agents/executor.md
skills/render-testset-view/agents/supervisor.md
skills/render-testset-view/input/contract.yaml
skills/render-testset-view/input/semantic-checklist.md
skills/render-testset-view/output/contract.yaml
skills/render-testset-view/output/semantic-checklist.md
skills/render-testset-view/evidence/testset-view.schema.json</files>
  <read_first>skills/ll-qa-settlement/SKILL.md
skills/ll-qa-gate-evaluate/SKILL.md
skills/ll-qa-feat-to-apiplan/SKILL.md
skills/ll-qa-feat-to-apiplan/ll.contract.yaml
skills/ll-qa-feat-to-apiplan/ll.lifecycle.yaml
skills/ll-qa-api-manifest-init/agents/executor.md
skills/ll-qa-api-manifest-init/agents/supervisor.md
skills/ll-qa-api-manifest-init/input/contract.yaml
skills/ll-qa-api-manifest-init/input/semantic-checklist.md
skills/ll-qa-api-manifest-init/output/contract.yaml
skills/ll-qa-api-manifest-init/output/semantic-checklist.md
.local/adr018-skill-trial/real-001/artifacts/active/formal/testset/testset-real-001-u01-d125bb50f9.json</read_first>
  <action>Create the render-testset-view skill directory with 10 files. Create subdirectories: agents/, input/, output/, evidence/.

1. **SKILL.md** — Skill definition following Phase 2 pattern:
   - Frontmatter: `name: render-testset-view`, `description: Backward compatibility skill — aggregates plan/manifest/spec/settlement artifacts from dual-chain system and renders old testset-compatible view.`
   - Canonical Authority: ADR-047 section 3.7 and 11.1, upstream skills (ll-qa-feat-to-apiplan, ll-qa-api-manifest-init, ll-qa-api-spec-gen, ll-qa-settlement)
   - Required Read Order: ll.contract.yaml, input/contract.yaml, output/contract.yaml, agents/executor.md, agents/supervisor.md, input/semantic-checklist.md, output/semantic-checklist.md
   - Execution Protocol: (1) Read input artifacts — api-test-plan, api-coverage-manifest, api-test-spec, api-settlement-report (and E2E equivalents). (2) Aggregate coverage data from all layers. (3) Map to old testset format: generate assigned_id, test_set_ref, title, functional_areas from manifest items, coverage_matrix from plan+spec. (4) Write output as JSON at specified output path, matching exact legacy testset schema.
   - Workflow Boundary: Input = plan + manifest + spec + settlement (API and/or E2E), Output = testset-compatible JSON view, Out of scope = test execution, manifest modification
   - Non-Negotiable Rules: Do not execute tests, do not modify input artifacts, output must be readable by old testset consumers, aggregate all available chains if both provided

2. **ll.contract.yaml** — Follow Phase 2 pattern (copy structure from skills/ll-qa-feat-to-apiplan/ll.contract.yaml, adapt states/transitions for read-only aggregation skill). Include version 0.1.0, states: drafted → structurally_validated → semantically_reviewed → accepted → frozen, with revision loops.

3. **ll.lifecycle.yaml**:
   ```yaml
   skill: render-testset-view
   version: "1.0"
   state: draft
   lifecycle_states:
     - draft
     - validated
     - executed
     - frozen
   ```

4. **agents/executor.md**:
   ```
   # Executor Agent: render-testset-view
   ## Role
   Aggregate plan/manifest/spec/settlement artifacts and render backward-compatible testset view
   ## Instructions
   1. Read all provided input artifacts (api-test-plan, api-coverage-manifest, api-test-spec, api-settlement-report, and E2E equivalents if provided)
   2. Extract coverage items from manifest, map to capabilities from plan
   3. Aggregate pass/fail/blocked/uncovered statistics from settlement
   4. Generate old testset format output: assigned_id (derived from feature_id), test_set_ref, title, functional_areas (from api_objects), coverage_matrix (from manifest items with lifecycle_status), logic_dimensions (from plan priorities)
   5. Write output JSON to specified output path
   ```

5. **agents/supervisor.md**:
   ```
   # Supervisor Agent: render-testset-view
   ## Validation Checklist
   1. All provided input artifacts were read
   2. Output has assigned_id, test_set_ref, title fields
   3. Coverage data is consistent with input manifest counts
   4. Statistics match input settlement report numbers
   5. Output is valid JSON matching legacy testset schema
   ```

6. **input/contract.yaml** — Define input contract: list of optional input file paths (api_plan_path, api_manifest_path, api_spec_path, api_settlement_path, e2e_plan_path, e2e_manifest_path, e2e_spec_path, e2e_settlement_path), at least one chain must be provided.

7. **input/semantic-checklist.md** — "Input files must exist on disk", "Plan files must conform to api_test_plan schema", "Manifest files must conform to api_coverage_manifest schema", "Spec files must conform to api_test_spec schema", "Settlement files must conform to settlement_report schema", "At least one chain (api or e2e) must have all 4 artifacts".

8. **output/contract.yaml** — Define output contract: output file path, format (json), top-level structure with assigned_id, test_set_ref, title, functional_areas array, coverage_matrix array.

9. **output/semantic-checklist.md** — "Output file must exist", "Output has assigned_id string field", "Output has test_set_ref string field", "Output has title string field", "functional_areas is a non-empty array", "coverage_matrix entries have coverage_id and lifecycle_status fields".

10. **evidence/testset-view.schema.json** — JSON Schema for the output:
    - $schema, title: "TestsetView", type: object
    - Required: assigned_id (string), test_set_ref (string), title (string), functional_areas (array of objects), coverage_matrix (array of objects with coverage_id, capability, lifecycle_status, passed, failed)
    - Optional: logic_dimensions (object), generation_metadata (object with generated_at, source_artifacts)</action>
  <verify>
    <automated>python -c "import json; json.load(open('E:/ai/LEE-Lite-skill-first/skills/render-testset-view/evidence/testset-view.schema.json')); print('Valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - skills/render-testset-view/ directory exists
    - skills/render-testset-view/SKILL.md exists and contains `name: render-testset-view`
    - skills/render-testset-view/SKILL.md contains "Execution Protocol" with 5 numbered steps
    - skills/render-testset-view/SKILL.md contains "Non-Negotiable Rules" section
    - skills/render-testset-view/ll.contract.yaml exists and contains `states:` list
    - skills/render-testset-view/ll.lifecycle.yaml exists and contains `skill: render-testset-view`
    - skills/render-testset-view/agents/executor.md exists and contains "Role" and "Instructions" sections
    - skills/render-testset-view/agents/supervisor.md exists and contains "Validation Checklist"
    - skills/render-testset-view/input/contract.yaml exists and defines at least api_plan_path and api_manifest_path
    - skills/render-testset-view/input/semantic-checklist.md exists and contains at least 4 checklist items
    - skills/render-testset-view/output/contract.yaml exists and defines output structure
    - skills/render-testset-view/output/semantic-checklist.md exists and contains at least 3 checklist items
    - skills/render-testset-view/evidence/testset-view.schema.json exists and is valid JSON
    - skills/render-testset-view/evidence/testset-view.schema.json contains "assigned_id" and "test_set_ref" as required properties
  </acceptance_criteria>
  <done>All 10 definition files created with proper structure, following Phase 2 patterns, JSON schema validates</done>
</task>

<task type="auto">
  <name>Task 2: Create render-testset-view scripts (run.sh, validate_input.sh, validate_output.sh)</name>
  <files>skills/render-testset-view/scripts/run.sh
skills/render-testset-view/scripts/validate_input.sh
skills/render-testset-view/scripts/validate_output.sh</files>
  <read_first>skills/ll-qa-feat-to-apiplan/scripts/run.sh
skills/ll-qa-api-manifest-init/scripts/validate_input.sh
skills/ll-qa-api-spec-gen/scripts/validate_output.sh
cli/lib/qa_schemas.py</read_first>
  <action>Create 3 shell scripts following Phase 2 pattern:

1. **scripts/run.sh** — Entry point for render-testset-view:
   - `#!/usr/bin/env bash` with `set -euo pipefail`
   - Parse arguments: `--api-plan`, `--api-manifest`, `--api-spec`, `--api-settlement`, `--e2e-plan`, `--e2e-manifest`, `--e2e-spec`, `--e2e-settlement`, `--output`, `--workspace` (default: $PWD)
   - At least one chain's 4 artifacts must be provided (api or e2e), exit 1 if neither chain is complete
   - Call `bash "${SCRIPT_DIR}/validate_input.sh"` with all provided input paths
   - Invoke CLI: `python -m cli skill render-testset-view` with JSON request payload containing all provided input paths and output path (default: `${WORKSPACE}/ssot/tests/.artifacts/testset-view.json`)
   - After CLI returns, validate output: `bash "${SCRIPT_DIR}/validate_output.sh" "${OUTPUT_FILE}"`
   - Follow exact pattern from `skills/ll-qa-feat-to-apiplan/scripts/run.sh`

2. **scripts/validate_input.sh** — Input validation:
   - Accept variable number of arguments (all provided input file paths)
   - For each argument: check file exists, exit 1 with "FAIL: file not found: <path>" if not
   - For files matching plan pattern: `python -m cli.lib.qa_schemas --type plan`
   - For files matching manifest pattern: `python -m cli.lib.qa_schemas --type manifest`
   - For files matching spec pattern: `python -m cli.lib.qa_schemas --type spec`
   - For files matching settlement pattern: `python -m cli.lib.qa_schemas --type settlement`
   - Determine schema type by argument position or by checking file content for top-level keys
   - On all success: echo "OK: all inputs validated"
   - Follow exact pattern from `skills/ll-qa-api-manifest-init/scripts/validate_input.sh`

3. **scripts/validate_output.sh** — Output validation:
   - Accept output path as $1
   - Check file exists, exit 1 if not
   - Use Python inline to verify: file is valid JSON, top-level keys `assigned_id` (string), `test_set_ref` (string), `title` (string) exist, `functional_areas` is a non-empty array, `coverage_matrix` is an array with entries having `coverage_id` field
   - On failure: exit 1 with "FAIL: invalid testset view output: <reason>"
   - On success: echo "OK: output validated"
   - Follow exact pattern from `skills/ll-qa-api-spec-gen/scripts/validate_output.sh`

Create scripts/ directory if not exists. Make all 3 scripts executable with chmod +x.</action>
  <verify>
    <automated>bash -n E:/ai/LEE-Lite-skill-first/skills/render-testset-view/scripts/run.sh && bash -n E:/ai/LEE-Lite-skill-first/skills/render-testset-view/scripts/validate_input.sh && bash -n E:/ai/LEE-Lite-skill-first/skills/render-testset-view/scripts/validate_output.sh && echo "All scripts pass syntax check"</automated>
  </verify>
  <acceptance_criteria>
    - skills/render-testset-view/scripts/run.sh exists and contains `set -euo pipefail`
    - skills/render-testset-view/scripts/run.sh contains argument parsers for `--api-plan`, `--api-manifest`, `--api-spec`, `--api-settlement`
    - skills/render-testset-view/scripts/run.sh contains `python -m cli skill render-testset-view`
    - skills/render-testset-view/scripts/run.sh checks that at least one complete chain (4 artifacts) is provided
    - skills/render-testset-view/scripts/run.sh contains `validate_input.sh` call before CLI invocation
    - skills/render-testset-view/scripts/run.sh contains `validate_output.sh` call after CLI invocation
    - skills/render-testset-view/scripts/validate_input.sh exists and validates files using `python -m cli.lib.qa_schemas --type`
    - skills/render-testset-view/scripts/validate_input.sh handles all 4 schema types (plan, manifest, spec, settlement)
    - skills/render-testset-view/scripts/validate_output.sh exists and checks for `assigned_id`, `test_set_ref`, `title` top-level keys
    - skills/render-testset-view/scripts/validate_output.sh checks `functional_areas` is non-empty array
    - All 3 scripts pass `bash -n` syntax check
    - All 3 scripts are executable (chmod +x)
  </acceptance_criteria>
  <done>3 scripts created with correct bash syntax, following Phase 2 patterns, executable, validating all input types and testset output structure</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Input YAML files → validate_input.sh | External YAML files from multiple skills cross trust boundary |
| Rendered testset output → validate_output.sh | LLM-generated output must conform to legacy testset format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | validate_input.sh plan/manifest/spec/settlement parsing | mitigate | Use `python -m cli.lib.qa_schemas --type <type>` for each input, all use yaml.safe_load() |
| T-03-11 | Tampering | validate_output.sh testset output parsing | mitigate | Inline Python uses yaml.safe_load() to parse output; validates required top-level keys |
| T-03-12 | Elevation of Privilege | run.sh argument parsing with 8 optional inputs | mitigate | Validate each provided path is an existing file before processing; use set -euo pipefail; require at least one complete chain |
| T-03-13 | Repudiation | Testset view generation | accept | Output is read-only aggregation; source artifacts are immutable; output includes generation_metadata with generated_at and source_artifacts for traceability |
</threat_model>

<verification>
Overall phase checks:
- All 13 files created in skills/render-testset-view/
- bash -n on all 3 scripts passes
- JSON schema validates
- SKILL.md follows Phase 2 structure pattern
- Agents have Role/Instructions and Validation Checklist sections
- run.sh accepts 8 optional input arguments and requires at least one complete chain
</verification>

<success_criteria>
- render-testset-view skill has complete directory with all 13 files
- SKILL.md describes backward compatibility purpose and execution protocol
- ll.contract.yaml, ll.lifecycle.yaml follow Phase 2 patterns
- agents/executor.md describes aggregation workflow for plan/manifest/spec/settlement
- scripts/run.sh parses 8 optional input arguments, requires at least one complete chain
- scripts/validate_input.sh validates against all 4 schema types (plan, manifest, spec, settlement)
- scripts/validate_output.sh checks legacy testset format (assigned_id, test_set_ref, title, functional_areas)
- evidence/testset-view.schema.json is valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/03-settlement-exec/03-03-SUMMARY.md`
</output>
