---
phase: "09-impl-spec-test"
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - "skills/ll-dev-feat-to-tech/scripts/validate_output.sh"
  - "skills/ll-dev-tech-to-impl/scripts/validate_output.sh"
  - "skills/ll-dev-feat-to-ui/scripts/validate_output.sh"
  - "skills/ll-dev-proto-to-ui/scripts/validate_output.sh"
  - "skills/ll-dev-feat-to-proto/scripts/validate_output.sh"
  - "skills/ll-dev-feat-to-surface-map/scripts/validate_output.sh"
autonomous: true
requirements:
  - STAB-03

must_haves:
  truths:
    - "Every dev skill validate_output.sh runs silent_override.py after skill-specific validation"
    - "Layered baselines applied: full for feat-to-tech/tech-to-impl, journey_sm for feat-to-ui/proto-to-ui, product_boundary for feat-to-proto/feat-to-surface-map"
    - "silent_override.py block verdict causes validate_output.sh to exit non-zero"
  artifacts:
    - path: "skills/ll-dev-feat-to-tech/scripts/validate_output.sh"
      provides: "Full FRZ anchor comparison + skill validation"
      contains: "silent_override.py check --mode full"
    - path: "skills/ll-dev-tech-to-impl/scripts/validate_output.sh"
      provides: "Full FRZ anchor comparison + skill validation"
      contains: "silent_override.py check --mode full"
    - path: "skills/ll-dev-feat-to-ui/scripts/validate_output.sh"
      provides: "JRN/SM anchor comparison + skill validation"
      contains: "silent_override.py check --mode journey_sm"
    - path: "skills/ll-dev-proto-to-ui/scripts/validate_output.sh"
      provides: "JRN/SM anchor comparison + skill validation"
      contains: "silent_override.py check --mode journey_sm"
    - path: "skills/ll-dev-feat-to-proto/scripts/validate_output.sh"
      provides: "Product boundary check + skill validation"
      contains: "silent_override.py check --mode product_boundary"
    - path: "skills/ll-dev-feat-to-surface-map/scripts/validate_output.sh"
      provides: "Product boundary check + skill validation"
      contains: "silent_override.py check --mode product_boundary"
  key_links:
    - from: "skills/ll-dev-*/scripts/validate_output.sh"
      to: "cli/lib/silent_override.py"
      via: "python cli/lib/silent_override.py check"
      pattern: "python cli/lib/silent_override.py check"
---

<objective>
Update all 6 `ll-dev-*` skills' validate_output.sh scripts to add silent_override.py checks after the skill-specific validation. Apply layered baselines per D-07. Create validate_output.sh for feat-to-surface-map if missing.

Purpose: Post-output silent override prevention for all dev skills (STAB-03)
Output: 6 modified/created validate_output.sh scripts
</objective>

<execution_context>
@cli/lib/silent_override.py (from Plan 09-01)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-impl-spec-test/09-CONTEXT.md
@.planning/phases/09-impl-spec-test/09-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Current validate_output.sh scripts (all follow this pattern):
```bash
#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -lt 1 ]; then
  echo "Usage: scripts/validate_output.sh <artifacts-dir>"
  exit 1
fi

python scripts/<skill_name>.py validate-output --artifacts-dir "$1"
```

silent_override.py CLI entry point (to be created in Plan 09-01):
```bash
python cli/lib/silent_override.py check \
  --output <artifacts-dir> \
  --frz <frz-id> \
  [--workspace <workspace-root>] \
  [--mode full|journey_sm|product_boundary]
```

Exit behavior: silent_override.py exits 0 if passed, 1 if blocked.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update all 6 dev skill validate_output.sh scripts with silent_override checks</name>
  <files>skills/ll-dev-feat-to-tech/scripts/validate_output.sh, skills/ll-dev-tech-to-impl/scripts/validate_output.sh, skills/ll-dev-feat-to-ui/scripts/validate_output.sh, skills/ll-dev-proto-to-ui/scripts/validate_output.sh, skills/ll-dev-feat-to-proto/scripts/validate_output.sh, skills/ll-dev-feat-to-surface-map/scripts/validate_output.sh</files>
  <behavior>
    - Each validate_output.sh runs skill-specific validation first, then silent_override check
    - feat-to-tech and tech-to-impl use --mode full (all FRZ anchors)
    - feat-to-ui and proto-to-ui use --mode journey_sm (JRN/SM anchors only)
    - feat-to-proto and feat-to-surface-map use --mode product_boundary (lightweight check)
    - silent_override.py block causes validate_output.sh to exit non-zero (set -e ensures this)
    - feat-to-surface-map validate_output.sh created if it doesn't exist (currently exists but may need enhancement)
    - FRZ reference extracted from output artifacts by silent_override.py (Pitfall 2 resolution)
  </behavior>
  <read_first>
    - skills/ll-dev-feat-to-tech/scripts/validate_output.sh — current pattern for feat-to-tech
    - skills/ll-dev-tech-to-impl/scripts/validate_output.sh — current pattern for tech-to-impl
    - skills/ll-dev-feat-to-ui/scripts/validate_output.sh — current pattern for feat-to-ui
    - skills/ll-dev-proto-to-ui/scripts/validate_output.sh — current pattern for proto-to-ui
    - skills/ll-dev-feat-to-proto/scripts/validate_output.sh — current pattern for feat-to-proto
    - skills/ll-dev-feat-to-surface-map/scripts/validate_output.sh — current pattern for feat-to-surface-map
    - cli/lib/silent_override.py — understand CLI arguments (--output, --frz, --mode) (read after Plan 09-01 creates it)
  </read_first>
  <action>
Update each validate_output.sh by appending the silent_override check AFTER the existing skill-specific validation line. The `set -euo pipefail` at the top ensures the script exits non-zero if silent_override fails.

For each script, add these lines at the END of the file (after the existing `python scripts/... validate-output` line):

**feat-to-tech and tech-to-impl (mode=full — all FRZ anchors):**
```bash
python cli/lib/silent_override.py check \
  --output "$1" \
  --mode full
```

**feat-to-ui and proto-to-ui (mode=journey_sm — JRN/SM anchors only):**
```bash
python cli/lib/silent_override.py check \
  --output "$1" \
  --mode journey_sm
```

**feat-to-proto and feat-to-surface-map (mode=product_boundary — lightweight product_boundary check):**
```bash
python cli/lib/silent_override.py check \
  --output "$1" \
  --mode product_boundary
```

For feat-to-surface-map specifically: the file currently exists with:
```bash
#!/usr/bin/env bash
set -euo pipefail
python scripts/feat_to_surface_map.py validate-output --artifacts-dir "$1"
```
Append the product_boundary check after the existing validation line.

Important notes:
1. Use `cli/lib/silent_override.py` path (relative to workspace root), not a relative path from the skill directory. The script should assume it's run from the workspace root (standard for skill execution).
2. Do NOT pass --frz parameter explicitly; silent_override.py will extract frz_ref from the output artifacts (per RESEARCH.md Pitfall 2 resolution).
3. Do NOT add --workspace parameter; silent_override.py defaults to current working directory.
4. Preserve the existing skill-specific validation line; do not remove or modify it.
5. Ensure each file ends with a trailing newline.
  </action>
  <verify>
    <automated>grep -l "silent_override.py check" skills/ll-dev-*/scripts/validate_output.sh | wc -l</automated>
  </verify>
  <done>
    - All 6 validate_output.sh files exist and are executable
    - Each file contains the line `python cli/lib/silent_override.py check`
    - feat-to-tech and tech-to-impl use `--mode full`
    - feat-to-ui and proto-to-ui use `--mode journey_sm`
    - feat-to-proto and feat-to-surface-map use `--mode product_boundary`
    - Each file preserves the original skill-specific validation line
    - Each file starts with `#!/usr/bin/env bash` and `set -euo pipefail`
    - grep confirms: 6 files contain "silent_override.py check"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate_output.sh → silent_override.py | Script must invoke with correct mode for skill scope |
| Artifacts directory → silent_override.py | FRZ reference must be extractable from output artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | validate_output.sh silent_override invocation | mitigate | Each script uses set -euo pipefail so silent_override failure (exit 1) propagates as script failure; verify mode matches skill's layered baseline per D-07 |
| T-09-10 | Tampering | FRZ reference extraction from artifacts | mitigate | silent_override.py raises CommandError if frz_ref cannot be found in output artifacts; this causes validate_output.sh to fail via set -e |
| T-09-11 | Elevation of Privilege | Wrong mode bypasses anchor checks | mitigate | Mode hardcoded per skill (not user-configurable); feat-to-tech/tech-to-impl always use full, feat-to-ui/proto-to-ui always use journey_sm, feat-to-proto/feat-to-surface-map always use product_boundary |
</threat_model>

<verification>
## Overall Phase Checks

1. `grep -l "silent_override.py check" skills/ll-dev-*/scripts/validate_output.sh | wc -l` returns 6
2. `grep -- "--mode full" skills/ll-dev-feat-to-tech/scripts/validate_output.sh skills/ll-dev-tech-to-impl/scripts/validate_output.sh` confirms full mode
3. `grep -- "--mode journey_sm" skills/ll-dev-feat-to-ui/scripts/validate_output.sh skills/ll-dev-proto-to-ui/scripts/validate_output.sh` confirms journey_sm mode
4. `grep -- "--mode product_boundary" skills/ll-dev-feat-to-proto/scripts/validate_output.sh skills/ll-dev-feat-to-surface-map/scripts/validate_output.sh` confirms product_boundary mode
5. Each file still contains the original `python scripts/... validate-output` line
6. All files start with `#!/usr/bin/env bash` and `set -euo pipefail`
</verification>

<success_criteria>
1. All 6 dev skill validate_output.sh scripts invoke silent_override.py check after skill-specific validation
2. Layered baselines correctly applied per D-07:
   - feat-to-tech: --mode full
   - tech-to-impl: --mode full
   - feat-to-ui: --mode journey_sm
   - proto-to-ui: --mode journey_sm
   - feat-to-proto: --mode product_boundary
   - feat-to-surface-map: --mode product_boundary
3. set -euo pipefail ensures silent_override failure propagates as script failure
4. Original skill-specific validation lines preserved in all scripts
5. FRZ reference extracted from output artifacts (no explicit --frz parameter needed)
</success_criteria>

<output>
After completion, create `.planning/phases/09-impl-spec-test/09-03-SUMMARY.md`
</output>
