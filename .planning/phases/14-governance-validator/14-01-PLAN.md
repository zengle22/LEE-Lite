---
phase: "14"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "cli/lib/governance_validator.py"
autonomous: true
requirements:
  - "GOV-01"
  - "GOV-02"
  - "GOV-03"
must_haves:
  truths:
    - "All 11 governance objects pass field validation"
    - "Required fields absent trigger clear missing-field errors"
    - "Forbidden fields present trigger clear forbidden-field errors"
    - "Objects with enum fields call enum_guard.validate_enums() internally"
  artifacts:
    - path: "cli/lib/governance_validator.py"
      provides: "All 11 object validators with required/optional/forbidden field enforcement"
      min_lines: 300
    - path: "tests/cli/lib/test_governance_validator.py"
      provides: "Comprehensive tests for all 11 objects"
      min_lines: 200
  key_links:
    - from: "cli/lib/governance_validator.py"
      to: "cli/lib/enum_guard.py"
      via: "validate_enums() call for enum field objects"
      pattern: "validate_enums.*label"
---

<objective>
Implement governance_validator.py covering all 11 SRC-009 governance objects with required/optional/forbidden field validation, collect-all violation reporting, and enum_guard integration.

Purpose: Validate governance objects against SRC-009 field contracts. Follow enum_guard.py collect-all pattern (D-02, D-07) and frozen dataclass pattern (D-03).
Output: cli/lib/governance_validator.py, tests/cli/lib/test_governance_validator.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@ssot/src/SRC-009__adr-052-ssot-semantic-governance-upgrade.md
@cli/lib/enum_guard.py
@cli/lib/task_pack_schema.py
@cli/lib/gate_schema.py

# SRC-009 11 governance objects with field contracts

## Skill
- required: skill_id, purpose, orchestrates
- optional: modes, interaction_options
- forbidden: internal_module_registration, direct_implementation

## Module
- required: module_id, axis, input, output
- optional: phase, priority, states, code_location
- forbidden: skill_registration

## AssertionLayer
- required: layer_id, name, description, verification_method
- optional: phase_requirement, verification_path
- forbidden: optional_for_golden_paths

## FailureClass
- required: class_id, name, description, common_manifestations
- optional: post_classification_action, confidence_threshold
- forbidden: ad_hoc_classification

## GoldenPath
- required: path_id, priority, description, dependencies
- optional: acceptance_criteria, evidence_template
- forbidden: undefined_environment, undefined_data

## Gate
- required: verdict, case_pass_rate, assertion_coverage, bypass_violations, verifier_verdict, product_bugs, env_consistency
- optional: phase, provisional_notice
- forbidden: hidden_verifier_failure

## StateMachine
- required: states, transitions, on_fail_behavior
- optional: skip_states, phase_variant
- forbidden: free_form_execution

## RunManifest
- required: run_id, app_commit, base_url, browser, generated_at
- optional: frontend_build, backend_version, feature_flags, test_data_snapshot, accounts
- forbidden: mutable_after_creation

## Environment
- required: base_url, browser, timeout, headless
- optional: account_runner, account_verifier, managed, version
- forbidden: embedded_in_testset

## Accident
- required: case_id, manifest, screenshots, traces, network_log, console_log, failure_classification
- optional: videos, storage_state, dom_snapshot, entity_snapshot
- forbidden: ad_hoc_format

## Verifier
- required: verdict, confidence, c_layer_verdict, detail
- optional: query_path, account_isolation
- forbidden: shared_context_with_runner
</context>

<tasks>

<task type="auto">
<name>Task 1: Define violation dataclass + object validator base structure</name>
<files>cli/lib/governance_validator.py</files>
<action>
Create cli/lib/governance_validator.py with the following structure:

1. IMPORTS: dataclasses, yaml, pathlib.Path, typing.Any, sys
2. IMPORTS from enum_guard: ENUM_REGISTRY, validate_enums, validate_field (for enum field checks)
3. IMPORTS from gate_schema: GateVerdict

ERROR CLASSES:
- GovernanceValidatorError(ValueError) — raised after collecting ALL violations
- GovernanceViolation dataclass(frozen=True): field, value, expected, label, violation_type (required_missing | forbidden_field | extra_field)
  - violation_type enum: required_missing, forbidden_field, extra_field
  - __str__() format: "{label}: field '{field}' is required|forbidden|extra"

BASE HELPERS:
- _require(data: dict, key: str, label: str) -> list[GovernanceViolation] — returns violations list (collect-all)
- _forbidden(data: dict, forbidden_fields: list[str], label: str) -> list[GovernanceViolation]
- _extra_field(data: dict, allowed_fields: set[str], label: str) -> list[GovernanceViolation]
  - Note: fields in data but not in required+optional are "extra"

11 VALIDATOR CLASSES (all @dataclass(frozen=True)):
Each takes data: dict, label: str and has:
- required_fields: class variable (frozenset)
- optional_fields: class variable (frozenset) 
- forbidden_fields: class variable (frozenset)
- enum_fields: class variable (frozenset) = set() for objects without enum fields
- validate() -> list[GovernanceViolation] — collects ALL violations

Object definitions (use exact field names from SRC-009):

SkillValidator:
  required_fields = frozenset({"skill_id", "purpose", "orchestrates"})
  optional_fields = frozenset({"modes", "interaction_options"})
  forbidden_fields = frozenset({"internal_module_registration", "direct_implementation"})
  enum_fields = frozenset({"skill_id"})

ModuleValidator:
  required_fields = frozenset({"module_id", "axis", "input", "output"})
  optional_fields = frozenset({"phase", "priority", "states", "code_location"})
  forbidden_fields = frozenset({"skill_registration"})
  enum_fields = frozenset({"module_id"})

AssertionLayerValidator:
  required_fields = frozenset({"layer_id", "name", "description", "verification_method"})
  optional_fields = frozenset({"phase_requirement", "verification_path"})
  forbidden_fields = frozenset({"optional_for_golden_paths"})
  enum_fields = frozenset({"assertion_layer"})

FailureClassValidator:
  required_fields = frozenset({"class_id", "name", "description", "common_manifestations"})
  optional_fields = frozenset({"post_classification_action", "confidence_threshold"})
  forbidden_fields = frozenset({"ad_hoc_classification"})
  enum_fields = frozenset({"failure_class"})

GoldenPathValidator:
  required_fields = frozenset({"path_id", "priority", "description", "dependencies"})
  optional_fields = frozenset({"acceptance_criteria", "evidence_template"})
  forbidden_fields = frozenset({"undefined_environment", "undefined_data"})
  enum_fields = frozenset()

GateValidator:
  required_fields = frozenset({"verdict", "case_pass_rate", "assertion_coverage", "bypass_violations", "verifier_verdict", "product_bugs", "env_consistency"})
  optional_fields = frozenset({"phase", "provisional_notice"})
  forbidden_fields = frozenset({"hidden_verifier_failure"})
  enum_fields = frozenset({"verdict"})

StateMachineValidator:
  required_fields = frozenset({"states", "transitions", "on_fail_behavior"})
  optional_fields = frozenset({"skip_states", "phase_variant"})
  forbidden_fields = frozenset({"free_form_execution"})
  enum_fields = frozenset({"phase"})

RunManifestValidator:
  required_fields = frozenset({"run_id", "app_commit", "base_url", "browser", "generated_at"})
  optional_fields = frozenset({"frontend_build", "backend_version", "feature_flags", "test_data_snapshot", "accounts"})
  forbidden_fields = frozenset({"mutable_after_creation"})
  enum_fields = frozenset()

EnvironmentValidator:
  required_fields = frozenset({"base_url", "browser", "timeout", "headless"})
  optional_fields = frozenset({"account_runner", "account_verifier", "managed", "version"})
  forbidden_fields = frozenset({"embedded_in_testset"})
  enum_fields = frozenset()

AccidentValidator:
  required_fields = frozenset({"case_id", "manifest", "screenshots", "traces", "network_log", "console_log", "failure_classification"})
  optional_fields = frozenset({"videos", "storage_state", "dom_snapshot", "entity_snapshot"})
  forbidden_fields = frozenset({"ad_hoc_format"})
  enum_fields = frozenset({"failure_classification"})

VerifierValidator:
  required_fields = frozenset({"verdict", "confidence", "c_layer_verdict", "detail"})
  optional_fields = frozenset({"query_path", "account_isolation"})
  forbidden_fields = frozenset({"shared_context_with_runner"})
  enum_fields = frozenset({"verdict"})

VALIDATE METHOD PATTERN (each validator):
```python
def validate(data: dict, label: str) -> list[GovernanceViolation]:
    violations: list[GovernanceViolation] = []
    
    # Required fields missing
    for field in cls.required_fields:
        if field not in data or data[field] is None:
            violations.append(GovernanceViolation(
                field=field,
                value=None,
                expected="required field",
                label=label,
                violation_type="required_missing"
            ))
    
    # Forbidden fields present
    for field in cls.forbidden_fields:
        if field in data and data[field] is not None:
            violations.append(GovernanceViolation(
                field=field,
                value=str(data[field]),
                expected="forbidden field",
                label=label,
                violation_type="forbidden_field"
            ))
    
    # Extra fields (in data but not in required+optional)
    allowed = cls.required_fields | cls.optional_fields
    for field in data:
        if field not in allowed and field not in cls.forbidden_fields:
            violations.append(GovernanceViolation(
                field=field,
                value=str(data[field]),
                expected=f"one of {sorted(allowed)}",
                label=label,
                violation_type="extra_field"
            ))
    
    # Enum field validation (if enum_fields is non-empty)
    if cls.enum_fields:
        enum_violations = validate_enums(data, label)
        violations.extend(enum_violations)
    
    return violations
```

VALIDATORS MAP (top of file):
VALIDATORS: dict[str, type] = {
    "skill": SkillValidator,
    "module": ModuleValidator,
    "assertion_layer": AssertionLayerValidator,
    "failure_class": FailureClassValidator,
    "golden_path": GoldenPathValidator,
    "gate": GateValidator,
    "state_machine": StateMachineValidator,
    "run_manifest": RunManifestValidator,
    "environment": EnvironmentValidator,
    "accident": AccidentValidator,
    "verifier": VerifierValidator,
}

OBJECT_TOP_KEYS: dict[str, str] = {
    "skill": "skill",
    "module": "module",
    "assertion_layer": "assertion_layer",
    "failure_class": "failure_class",
    "golden_path": "golden_path",
    "gate": "gate",
    "state_machine": "state_machine",
    "run_manifest": "run_manifest",
    "environment": "environment",
    "accident": "accident",
    "verifier": "verifier",
}
</action>
<verify>
<python>python -c "from cli.lib.governance_validator import VALIDATORS, GovernanceViolation; print(f'VALIDATORS: {len(VALIDATORS)}'); print(f'GovernanceViolation fields: {[f for f in GovernanceViolation.__dataclass_fields__]}')"</python>
</verify>
<done>
All 11 validators registered in VALIDATORS dict; GovernanceViolation is frozen dataclass with 5 fields</done>
</task>

<task type="auto">
<name>Task 2: Implement public API functions + CLI entry point</name>
<files>cli/lib/governance_validator.py</files>
<action>
Add to the governance_validator.py file (appending after the validator classes):

1. VALIDATE FUNCTION — detects object type and calls correct validator:
```python
def validate(data: dict, object_type: str, label: str | None = None) -> list[GovernanceViolation]:
    """Validate governance object data and return all violations.
    
    Args:
        data: Raw YAML dict (with or without top-level key).
        object_type: One of VALIDATORS keys.
        label: Human-readable label for error messages (defaults to object_type).
    
    Returns:
        List of all violations (collect-all, not fail-fast).
    """
    if label is None:
        label = object_type
    
    validator_cls = VALIDATORS.get(object_type)
    if validator_cls is None:
        return [GovernanceViolation(
            field="object_type",
            value=object_type,
            expected=f"one of {sorted(VALIDATORS.keys())}",
            label=label,
            violation_type="required_missing"
        )]
    
    inner = data
    top_key = OBJECT_TOP_KEYS.get(object_type, object_type)
    if top_key in data:
        inner = data[top_key]
    
    return validator_cls.validate(inner, label)
```

2. VALIDATE FILE FUNCTION — loads YAML and validates:
```python
def validate_file(path: str | Path, object_type: str | None = None) -> list[GovernanceViolation]:
    """Load a YAML file and validate it as a governance object.
    
    Args:
        path: Path to the YAML file.
        object_type: Object type or None for auto-detect from top-level key.
    
    Returns:
        List of all violations.
    
    Raises:
        FileNotFoundError: If file does not exist.
    """
    p = Path(path)
    if not p.exists():
        raise FileNotFoundError(f"File not found: {p}")
    
    with open(p, encoding="utf-8") as f:
        data = yaml.safe_load(f) or {}
    
    # Auto-detect if object_type not provided
    if object_type is None:
        for obj_type, top_key in OBJECT_TOP_KEYS.items():
            if top_key in data:
                object_type = obj_type
                break
        if object_type is None:
            return [GovernanceViolation(
                field="top_level_key",
                value="none",
                expected=f"one of {sorted(OBJECT_TOP_KEYS.values())}",
                label="file",
                violation_type="required_missing"
            )]
    
    return validate(data, object_type)
```

3. MAIN CLI ENTRY POINT — unified entry with --object flag (per D-04):
```python
def main() -> None:
    """CLI for governance object validation.

    Usage:
        python -m cli.lib.governance_validator --object <type> <file.yaml>
        python -m cli.lib.governance_validator --object skill --check '{"skill_id": "qa.test-plan", ...}'
        python -m cli.lib.governance_validator --list-types
    """
    import sys
    
    args = sys.argv[1:]
    if not args:
        print("Usage: python -m cli.lib.governance_validator [--object <type>] [--check '<json>'] [--list-types] <file>")
        print(f"  Valid types: {sorted(VALIDATORS.keys())}")
        sys.exit(1)
    
    if "--list-types" in args:
        print("Valid governance object types:")
        for obj_type in sorted(VALIDATORS.keys()):
            print(f"  - {obj_type}")
        sys.exit(0)
    
    object_type = None
    file_path = None
    check_json = None
    
    if "--object" in args:
        idx = args.index("--object")
        object_type = args[idx + 1] if idx + 1 < len(args) else None
        if object_type is None:
            print("Error: --object requires a value", file=sys.stderr)
            sys.exit(1)
    
    if "--check" in args:
        idx = args.index("--check")
        raw = args[idx + 1] if idx + 1 < len(args) else "{}"
        check_json = json.loads(raw)
        if object_type is None:
            print("Error: --object required with --check", file=sys.stderr)
            sys.exit(1)
        violations = validate(check_json, object_type, label="cli")
        if violations:
            for v in violations:
                print(f"FAIL: {v}")
            sys.exit(1)
        else:
            print("OK: all fields valid")
        sys.exit(0)
    
    # File validation
    for arg in args:
        if not arg.startswith("--") and Path(arg).exists():
            file_path = arg
            break
    
    if file_path is None:
        print("Error: no file specified", file=sys.stderr)
        sys.exit(1)
    
    if object_type is None:
        print("Error: --object required for file validation", file=sys.stderr)
        sys.exit(1)
    
    violations = validate_file(file_path, object_type)
    if violations:
        for v in violations:
            print(f"FAIL: {v}")
        sys.exit(1)
    else:
        print(f"OK: {file_path} is valid")
        sys.exit(0)
```

4. Add json import at top of file (already imported in main).
</action>
<verify>
<python>python -m cli.lib.governance_validator --list-types</python>
</verify>
<done>
CLI responds with valid types list; --object flag is the only required parameter for validation</done>
</task>

<task type="auto">
<name>Task 3: Write comprehensive tests for all 11 objects</name>
<files>tests/cli/lib/test_governance_validator.py</files>
<action>
Create tests/cli/lib/test_governance_validator.py with pytest tests covering all 11 objects:

IMPORTS: pytest, all public symbols from governance_validator

TEST CLASSES (one per object + violation structure):

1. TestGovernanceViolation — 5 tests
   - test_violation_str_format_includes_label_field_type
   - test_violation_is_frozen
   - test_violation_types (required_missing, forbidden_field, extra_field)

2. TestSkillValidator — 6 tests
   - test_valid_skill_passes (with required fields)
   - test_missing_required_field (each of skill_id, purpose, orchestrates)
   - test_forbidden_field_present (internal_module_registration, direct_implementation)
   - test_extra_field_triggers_error (field not in required|optional)
   - test_skill_id_enum_validated_via_enum_guard

3. TestModuleValidator — 5 tests
   - test_valid_module_passes
   - test_missing_required_field (module_id, axis, input, output)
   - test_forbidden_field_present (skill_registration)
   - test_module_id_enum_validated

4. TestAssertionLayerValidator — 4 tests
   - test_valid_assertion_layer_passes
   - test_missing_required_field (layer_id, name, description, verification_method)
   - test_forbidden_field_present (optional_for_golden_paths)
   - test_assertion_layer_enum_validated (layer_id is not enum but uses assertion_layer)

5. TestFailureClassValidator — 4 tests
   - test_valid_failure_class_passes
   - test_missing_required_field (class_id, name, description, common_manifestations)
   - test_forbidden_field_present (ad_hoc_classification)
   - test_failure_class_enum_validated (class_id uses failure_class enum)

6. TestGoldenPathValidator — 4 tests
   - test_valid_golden_path_passes
   - test_missing_required_field (path_id, priority, description, dependencies)
   - test_forbidden_field_present (undefined_environment, undefined_data)
   - test_no_enum_fields

7. TestGateValidator — 5 tests
   - test_valid_gate_passes
   - test_missing_required_field (all 7 required fields)
   - test_forbidden_field_present (hidden_verifier_failure)
   - test_gate_verdict_enum_validated (verdict field)
   - test_extra_field_triggers_error

8. TestStateMachineValidator — 4 tests
   - test_valid_state_machine_passes
   - test_missing_required_field (states, transitions, on_fail_behavior)
   - test_forbidden_field_present (free_form_execution)
   - test_phase_enum_validated

9. TestRunManifestValidator — 4 tests
   - test_valid_run_manifest_passes
   - test_missing_required_field (run_id, app_commit, base_url, browser, generated_at)
   - test_forbidden_field_present (mutable_after_creation)
   - test_no_enum_fields

10. TestEnvironmentValidator — 4 tests
    - test_valid_environment_passes
    - test_missing_required_field (base_url, browser, timeout, headless)
    - test_forbidden_field_present (embedded_in_testset)
    - test_no_enum_fields

11. TestAccidentValidator — 4 tests
    - test_valid_accident_passes
    - test_missing_required_field (case_id, manifest, screenshots, traces, network_log, console_log, failure_classification)
    - test_forbidden_field_present (ad_hoc_format)
    - test_failure_classification_enum_validated

12. TestVerifierValidator — 4 tests
    - test_valid_verifier_passes
    - test_missing_required_field (verdict, confidence, c_layer_verdict, detail)
    - test_forbidden_field_present (shared_context_with_runner)
    - test_verdict_enum_validated

13. TestCollectAllViolations — 3 tests
    - test_returns_multiple_violations_for_missing_and_forbidden
    - test_returns_empty_list_when_valid
    - test_validate_file_with_valid_object

14. TestValidatorsMap — 2 tests
    - test_validators_map_has_11_entries
    - test_all_validators_are_frozen_dataclass

15. TestCLI — 2 tests (use subprocess to test CLI)
    - test_list_types_output
    - test_validate_invalid_returns_fail

Each test creates a minimal valid dict for the object, runs validation, and asserts expected violations.
Use fixture or parameterized tests where appropriate to avoid repetition.
</action>
<verify>
<python>python -m pytest tests/cli/lib/test_governance_validator.py -v --tb=short 2>&1 | head -60</python>
</verify>
<done>
All tests pass (expected ~50 tests covering 11 objects x multiple cases)</done>
</task>

</tasks>

<verification>
# Source audit: All SRC-009 objects covered
- Skill (D-01) — SkillValidator
- Module — ModuleValidator
- AssertionLayer — AssertionLayerValidator
- FailureClass — FailureClassValidator
- GoldenPath — GoldenPathValidator
- Gate — GateValidator
- StateMachine — StateMachineValidator
- RunManifest — RunManifestValidator
- Environment — EnvironmentValidator
- Accident — AccidentValidator
- Verifier — VerifierValidator

# Decision traceability
- D-01: Single file governance_validator.py
- D-02: Collect-all violations (list return, not fail-fast)
- D-03: Frozen dataclass per validator
- D-04: --object CLI flag
- D-05: enum_guard.validate_enums() called for objects with enum fields
- D-06: Consistent error format with enum_guard
- D-07: All violations collected before raising
</verification>

<success_criteria>
1. python -m cli.lib.governance_validator --object Skill file.yaml returns violations list
2. Missing required field produces GovernanceViolation with violation_type=required_missing
3. Forbidden field present produces GovernanceViolation with violation_type=forbidden_field
4. Extra field produces GovernanceViolation with violation_type=extra_field
5. Objects with enum fields (Gate, Module, Skill, StateMachine, Accident, Verifier) validate via validate_enums()
6. --list-types shows all 11 object types
7. --check mode validates inline JSON
8. validate_file() loads YAML and validates
9. All 11 validators are frozen dataclasses
10. Tests pass: pytest tests/cli/lib/test_governance_validator.py
</success_criteria>

<output>
After completion, create `.planning/phases/14-governance-validator/14-01-SUMMARY.md`
</output>
