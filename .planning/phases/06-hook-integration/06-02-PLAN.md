---
phase: "06-hook-integration"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "CLAUDE.md"
  - "ssot/experience-patches/.gitkeep"
  - "ssot/experience-patches/TEMPLATE.yaml"
autonomous: true
requirements:
  - REQ-PATCH-06

must_haves:
  truths:
    - "CLAUDE.md 包含 Edit/Write 前自动读取 Patch 的规则"
    - "CLAUDE.md 包含改代码后自动登记 Patch 的规则"
    - "ssot/experience-patches/ 目录存在且包含模板文件"
  artifacts:
    - path: "CLAUDE.md"
      provides: "AI 自动读取 + 登记 Patch 的规则指令"
    - path: "ssot/experience-patches/.gitkeep"
      provides: "目录占位文件"
    - path: "ssot/experience-patches/TEMPLATE.yaml"
      provides: "Patch YAML 模板，AI 登记时参考"

---

<objective>
在 CLAUDE.md 中添加规则，指示 AI 在 Edit/Write 前自动读取相关 Patch context，在改代码后自动调用 patch_auto_register.py 预填 Patch YAML。同时创建 experience-patches 目录和模板文件。

Purpose: 用声明式规则替代 hook 实现 Patch 注入 + 自动登记。
Output: CLAUDE.md 规则更新 + experience-patches 目录 + 模板文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-hook-integration/06-CONTEXT.md
@ssot/adr/ADR-049-引入体验修正层-Experience-Patch-Layer.md
@CLAUDE.md

# ADR-049 关键约束
<adr_rules>
- §3.4: "AI 必须能读取 Patch"，通过 CLAUDE.md 规则（MVP 实现）
- §9.1: "零手动登记步骤"，AI 改代码后自动填充 Patch YAML，用户仅确认分类
- §12.1: Context 预算 3000 tokens max，10 条完整 Patch max
- §10.1: test_impact 强制声明
- §12.2: AI 不得自动提交 Patch，必须由人工确认
</adr_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with patch injection + auto-registration rules</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current content — append, do not replace)
    - ssot/adr/ADR-049-引入体验修正层-Experience-Patch-Layer.md (§3.4, §9.1, §12.1, §12.2)
  </read_first>
  <action>Append the following section to CLAUDE.md (do NOT modify existing content, only append):

```markdown
## ADR-049: Experience Patch Layer — Automatic Patch Context

### Patch Context Injection (Before Code Changes)

When you are about to modify code files, BEFORE making any Edit or Write:

1. Run: `python cli/lib/patch_context_injector.py inject --workspace-root . --target-files {list of files you plan to edit}`
2. Read the output — it contains active Patch summaries relevant to your target files
3. Incorporate Patch constraints into your code changes
4. Proceed with the Edit/Write

If the script returns "No active patches found", proceed normally.

### Automatic Patch Registration (After Code Changes)

After you have made code changes (Edit/Write completed):

1. Run: `python cli/lib/patch_auto_register.py detect --workspace-root .`
2. If changes are detected, run: `python cli/lib/patch_auto_register.py draft --workspace-root .`
3. Review the drafted Patch YAML — confirm or adjust `change_class` and `test_impact`
4. Present the Patch to the user for confirmation
5. Only after user confirmation, register the Patch

**Critical constraints:**
- Do NOT auto-register Patches without user confirmation (ADR-049 §12.2)
- test_impact MUST be specified (ADR-049 §10.1)
- Context budget: max 3000 tokens for Patch injection, max 10 full Patches (ADR-049 §12.1)
- Only show Patches relevant to the files being edited (by changed_files matching)
```

The rules must be clearly separated from existing content with a header.
  </action>
  <acceptance_criteria>
    - CLAUDE.md contains "ADR-049" section
    - CLAUDE.md contains "patch_context_injector.py inject" reference
    - CLAUDE.md contains "patch_auto_register.py detect" reference
    - CLAUDE.md states "Do NOT auto-register Patches without user confirmation"
    - CLAUDE.md mentions test_impact requirement
    - CLAUDE.md mentions 3000 token / 10 patch budget limit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create experience-patches directory with template</name>
  <files>ssot/experience-patches/.gitkeep, ssot/experience-patches/TEMPLATE.yaml</files>
  <read_first>
    - ssot/adr/ADR-049-引入体验修正层-Experience-Patch-Layer.md (Patch YAML schema from §5)
  </read_first>
  <action>Create:

1. `ssot/experience-patches/.gitkeep` — empty file to keep the directory in git

2. `ssot/experience-patches/TEMPLATE.yaml` — Patch YAML template with all required fields:
```yaml
# Experience Patch Template
# ID format: UXPATCH-{feat_ref}-{sequence}
id: UXPATCH-{FEAT}-{NNN}
title: "Brief description of the experience change"
status: draft  # draft → validated → pending_backwrite → settled → archived
change_class: visual  # visual | interaction | semantic
scope:
  page: "Page name or route"
  module: "Component or module name"
description: "What changed and why"
changed_files:
  - "path/to/changed/file.tsx"
test_impact: "Describe test impact or false if none"
backwrite_targets:
  - "Which upstream docs need updating (UI spec, TECH, etc.)"
created_at: "2026-04-17T00:00:00Z"
created_by: "AI + human confirmation"
```

The template should be a valid YAML with comments explaining each field.
  </action>
  <acceptance_criteria>
    - ssot/experience-patches/.gitkeep exists
    - ssot/experience-patches/TEMPLATE.yaml exists
    - TEMPLATE.yaml is valid YAML
    - Contains all required fields: id, title, status, change_class, scope, description, changed_files, test_impact, backwrite_targets
    - Contains comments explaining each field
    - change_class enum matches ADR-049 §4 (visual/interaction/semantic)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- CLAUDE.md has ADR-049 section with both injection and registration rules
- CLAUDE.md references correct Python module paths
- experience-patches directory exists with .gitkeep
- TEMPLATE.yaml is valid YAML with all required fields and enum values matching ADR-049
</verification>

<success_criteria>
1. CLAUDE.md rules clearly separate from existing content (append-only)
2. Rules reference the correct Python modules from Plan 06-01
3. TEMPLATE.yaml matches ADR-049 §5 Patch schema exactly
4. experience-patches directory is git-tracked (via .gitkeep)
</success_criteria>

<output>
After completion, create `.planning/phases/06-hook-integration/06-02-SUMMARY.md`
</output>
