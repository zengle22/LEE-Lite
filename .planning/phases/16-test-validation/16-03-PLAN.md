---
phase: "16"
plan: "03"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
user_setup: []

must_haves:
  truths:
    - "CI pipeline runs v2.1 tests (tests/cli/lib/) on CLI changes"
    - "CI installs pytest-cov for coverage evidence"
    - "Phase 16 state transitions from contracts_traceable to ready_for_test"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI workflow with pytest-cov and cli_lib_tests execution"
      contains: "pytest-cov"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16 marked complete with success criteria checked"
      contains: "Phase 16"
  key_links:
    - from: ".github/workflows/ci.yml cli-governance job"
      to: "tools/ci/run_checks.py cli-governance"
      via: "--run-tests flag triggers test manifest execution"
      pattern: "cli-governance.*run-tests"
    - from: "tools/ci/run_checks.py"
      to: "test_manifests.json cli_lib_tests"
      via: "run_test_manifest lookup"
      pattern: "manifest\\[name\\]"

threat_model:
  trust_boundaries:
    - boundary: "CI workflow -> test execution"
      description: "Malformed CI config could skip tests silently; must verify --run-tests flag actually triggers cli_lib_tests"
    - boundary: "CI pip install -> pytest-cov"
      description: "Missing pytest-cov in CI pip install would break --cov flag at evidence collection time"
  stride_register:
    - threat_id: T-16-06
      category: Integrity
      component: "ci.yml pip install step"
      disposition: mitigate
      mitigation: "Add pytest-cov to existing pip install line (not a separate step) to ensure it is always present when tests run"
    - threat_id: T-16-07
      category: Integrity
      component: "ci.yml cli-governance --run-tests"
      disposition: mitigate
      mitigation: "Verify tools/ci/checks_runtime.py check_cli_governance already uses cli_tests manifest; note that cli_lib_tests runs via run-test-manifest subcommand separately"
---

<objective>
Update CI workflow to install pytest-cov and integrate v2.1 tests into the cli-governance job, then transition Phase 16 to ready_for_test state.

Purpose: Ensure CI pipeline produces the same evidence format as local runs (D-03, D-04) and that v2.1 tests are exercised on every CLI change. Complete phase state transition.
Output: Updated ci.yml, updated ROADMAP.md
</objective>

<execution_context>
@E:\ai\LEE-Lite-skill-first\.claude\get-shit-done\workflows\execute-plan.md
@E:\ai\LEE-Lite-skill-first\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\ai\LEE-Lite-skill-first\.planning\PROJECT.md
@E:\ai\LEE-Lite-skill-first\.planning\ROADMAP.md
@E:\ai\LEE-Lite-skill-first\.planning\phases\16-test-validation\16-CONTEXT.md
@E:\ai\LEE-Lite-skill-first\.planning\phases\16-test-validation\16-02-SUMMARY.md

# CI workflow current state (lines 76-87 of .github/workflows/ci.yml)

```yaml
  cli-governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install --upgrade pip pytest pyyaml coverage
      - run: python tools/ci/write_changed_files.py --event-name "${{ github.event_name }}" --event-path "${{ github.event_path }}" --head-sha "${{ github.sha }}" --output .ci-changed-files.txt
      - run: python -m tools.ci.run_checks cli-governance --changed-files .ci-changed-files.txt --output-dir .artifacts/ci/cli-governance --run-tests
```

The pip install line needs `pytest-cov` added (currently has `coverage` but not `pytest-cov`).

# checks_runtime.py check_cli_governance flow

```python
def check_cli_governance(changed_files, output_dir, snapshot_path, run_tests_flag, manifest_path):
    tests = load_json(manifest_path)["cli_tests"]  # Only tests/unit/test_cli_* files
    if run_tests_flag and changed_cli:
        exit_code, payload = run_pytest(tests, output_dir / "cli-governance-pytest-report.json")
```

Note: check_cli_governance uses "cli_tests" manifest. The cli_lib_tests manifest runs via `run-test-manifest` subcommand. The CI update should add a step or extend the existing step to also run cli_lib_tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI workflow to install pytest-cov</name>
  <read_first>
    - E:\ai\LEE-Lite-skill-first\.github\workflows\ci.yml (lines 72, 85, 98 — all pip install lines)
  </read_first>
  <action>
    Update the pip install line in the cli-governance job (line 85) from:
    ```
    python -m pip install --upgrade pip pytest pyyaml coverage
    ```
    To:
    ```
    python -m pip install --upgrade pip pytest pytest-cov pyyaml coverage
    ```

    This adds pytest-cov to the CI environment, ensuring the --cov flag works when tests run with --run-tests.

    Also update the skill-governance job pip install line (line 72) and cross-domain-compat job pip install line (line 98) to include pytest-cov for consistency across all jobs that may run tests.

    Do NOT change any other CI workflow configuration. Only modify the pip install lines.
  </action>
  <acceptance_criteria>
    - .github/workflows/ci.yml line 72 contains `pytest-cov` in pip install command
    - .github/workflows/ci.yml line 85 contains `pytest-cov` in pip install command
    - .github/workflows/ci.yml line 98 contains `pytest-cov` in pip install command
    - YAML is valid (parseable by yaml.safe_load)
    - No other lines in ci.yml were modified
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=d['jobs']; [print(f'{k}: pip install has pytest-cov' if 'pytest-cov' in str(jobs[k]['steps']) else f'{k}: MISSING pytest-cov') for k in jobs]"</automated>
  </verify>
  <done>All CI jobs that run tests include pytest-cov in their pip install step</done>
</task>

<task type="auto">
  <name>Task 2: Finalize phase state transition and update ROADMAP</name>
  <read_first>
    - E:\ai\LEE-Lite-skill-first\.planning\ROADMAP.md (Phase 16 section)
    - E:\ai\LEE-Lite-skill-first\.planning\STATE.md (current state)
    - E:\ai\LEE-Lite-skill-first\.planning\phases\16-test-validation\16-02-SUMMARY.md (test results)
  </read_first>
  <action>
    1. Update ROADMAP.md Phase 16 section to mark success criteria as complete:

    Change the Phase 16 success criteria from unchecked to checked:
    ```
    **Success Criteria:**
    1. [x] All schema tests passed
    2. [x] All enum guard tests passed
    3. [x] All governance validator tests passed
    4. [x] Integration tests confirm enum_guard in SSOT writes auto-triggers
    5. [x] Frozen Contract traceability tests passed
    ```

    2. Update the Phase 16 state transition comment:
    ```
    **State transition:** contracts_traceable -> ready_for_test [COMPLETE]
    ```

    3. Add evidence summary to Phase 16 section:
    ```
    **Evidence:**
    - test-results.xml: 207 tests, 0 failures (JUnit XML)
    - coverage.xml: >= 80% line coverage on cli.lib (Cobertura XML)
    - htmlcov/index.html: Interactive HTML coverage report
    - test-output.log: Full test run output
    ```

    4. Create .planning/phases/16-test-validation/16-03-SUMMARY.md documenting phase completion with evidence links and requirement satisfaction matrix.
  </action>
  <acceptance_criteria>
    - .planning/ROADMAP.md Phase 16 section has all 5 success criteria marked [x]
    - .planning/ROADMAP.md Phase 16 section has state transition marked [COMPLETE]
    - .planning/ROADMAP.md Phase 16 section has Evidence subsection listing 4 artifacts
    - .planning/phases/16-test-validation/16-03-SUMMARY.md exists
    - 16-03-SUMMARY.md documents TEST-01 through TEST-05 satisfaction with test counts
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\[x\]" .planning/ROADMAP.md | head -1 && test -f .planning/phases/16-test-validation/16-03-SUMMARY.md && echo "SUMMARY exists"</automated>
  </verify>
  <done>ROADMAP.md updated with Phase 16 completion status, evidence summary, and state transition to ready_for_test confirmed</done>
</task>

</tasks>

<verification>
- Plan 03 depends on Plan 02 being complete (all tests passing, evidence collected)
- CI workflow YAML must be valid after modification
- ROADMAP.md must reflect actual test results (not fabricated)
- State transition only occurs after all evidence verified
</verification>

<success_criteria>
- CI workflow installs pytest-cov in all test-running jobs (skill-governance, cli-governance, cross-domain-compat)
- Phase 16 success criteria all marked [x] in ROADMAP.md
- State transition contracts_traceable -> ready_for_test confirmed
- Evidence artifacts committed: test-results.xml, coverage.xml, htmlcov/, test-output.log
- 16-03-SUMMARY.md documents requirement satisfaction
</success_criteria>

<output>
After completion, create `.planning/phases/16-test-validation/16-03-SUMMARY.md`
</output>
