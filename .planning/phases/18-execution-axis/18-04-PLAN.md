---
phase: "18"
plan: "04"
type: execute
wave: 2
depends_on:
  - "18-01"
  - "18-02"
  - "18-03"
files_modified:
  - tests/integration/test_e2e_chain.py
  - tests/integration/test_resume.py
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "E2E chain test-run with --app-url and --api-url executes end-to-end"
    - "--resume re-runs failed cases without repeating completed steps"
  artifacts:
    - path: "tests/integration/test_e2e_chain.py"
      provides: "Integration tests for E2E chain execution"
      min_lines: 120
    - path: "tests/integration/test_resume.py"
      provides: "Integration tests for resume functionality"
      min_lines: 100
  key_links:
    - from: "tests/integration/test_e2e_chain.py"
      to: "cli/lib/test_orchestrator.py"
      via: "import"
      pattern: "test_orchestrator"
    - from: "tests/integration/test_resume.py"
      to: "cli/lib/state_machine_executor.py"
      via: "import"
      pattern: "state_machine_executor"
---

<objective>
Create integration tests for E2E chain execution (TEST-02) and resume functionality (TEST-03). Tests verify end-to-end execution with --app-url and --api-url parameters, and resume from failed state without repeating completed steps.
</objective>

<context>
@cli/lib/test_orchestrator.py
@cli/lib/state_machine_executor.py
@cli/lib/run_manifest_gen.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From cli/lib/test_orchestrator.py:
```python
def run_spec_test(
    workspace_root: Path,
    *,
    feat_ref: str | None = None,
    proto_ref: str | None = None,
    base_url: str = "http://localhost:8000",
    app_url: str = "http://localhost:3000",
    api_url: str | None = None,
    modality: str = "api",
    coverage_mode: str = "smoke",
    resume: bool = False,
    resume_from: str | None = None,
) -> StepResult: ...
```

From cli/lib/contracts.py:
```python
@dataclass
class StepResult:
    run_id: str
    execution_refs: dict[str, str] = field(default_factory=dict)
    candidate_path: str = ""
    case_results: list[dict[str, Any]] = field(default_factory=list)
    manifest_items: list[dict[str, Any]] = field(default_factory=list)
    execution_output_dir: str = ""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for E2E chain</name>
  <files>tests/integration/test_e2e_chain.py</files>
  <action>
Create `tests/integration/test_e2e_chain.py` with pytest integration tests:

1. Import `pytest`, `Path` from standard library
2. Import `yaml`, `shutil` from stdlib
3. Import `run_spec_test` from `cli.lib.test_orchestrator`
4. Import `generate_run_manifest`, `load_run_manifest` from `cli.lib.run_manifest_gen`
5. Import `StateMachineExecutor`, `ExecutionState` from `cli.lib.state_machine_executor`
6. Import `compile_scenario_spec` from `cli.lib.scenario_spec_compile`

7. Create test class `TestE2EChainExecution`:
   - `test_generates_run_manifest_for_e2e`: Verify run_manifest_gen creates manifest for E2E run
   - `test_run_manifest_contains_app_url`: Verify manifest.base_url.app matches --app-url
   - `test_run_manifest_contains_api_url`: Verify manifest.base_url.api matches --api-url
   - `test_run_manifest_contains_browser`: Verify manifest.browser field

8. Create test class `TestE2EChainIntegration`:
   - `test_e2e_chain_with_separated_urls`: Verify E2E chain works with separated app_url and api_url
     - Use mock spec directory with e2e-journey-spec
     - Call run_spec_test with modality="web_e2e"
     - Verify StepResult returned with run_id
   - `test_e2e_chain_creates_evidence_directory`: Verify evidence directory created under run artifacts
   - `test_e2e_chain_returns_case_results`: Verify StepResult contains case_results list

9. Create test class `TestE2EScenarioSpecCompilation`:
   - `test_compiles_e2e_spec_to_scenario_spec`: Verify e2e spec is compiled to scenario spec
   - `test_scenario_spec_has_ab_layers`: Verify scenario spec has A-layer and B-layer assertions
   - `test_scenario_spec_has_c_missing`: Verify scenario spec has C_MISSING for C-layer

10. Create test class `TestE2EStateMachine`:
    - `test_state_machine_execution_flow`: Verify state machine executes through SETUP -> EXECUTE -> VERIFY/DONE
    - `test_state_machine_persists_state`: Verify state persisted to state.yaml
    - `test_state_machine_tracks_steps`: Verify completed_steps list is populated

11. Create helper fixtures:
    - `mock_e2e_spec_dir(tmp_path)`: Creates mock e2e-journey-spec directory with sample spec
    - `mock_proto_manifest(tmp_path, proto_ref)`: Creates mock e2e-coverage-manifest.yaml
    - `workspace_with_e2e_context(tmp_path)`: Sets up full workspace structure for E2E chain

Per TEST-02: Verify `qa test-run --proto-ref XXX --app-url http://localhost:3000 --api-url http://localhost:8000` E2E chain
Per D-09: CLI supports --app-url and --api-url with separated architecture
  </action>
  <verify>
    <automated>cd E:/ai/LEE-Lite-skill-first && python -m pytest tests/integration/test_e2e_chain.py -x -q --ignore=tests/integration/test_resume.py 2>&1 | head -20</automated>
  </verify>
  <done>E2E chain integration tests created. Tests verify --app-url/--api-url support, scenario spec compilation, state machine execution.</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for resume functionality</name>
  <files>tests/integration/test_resume.py</files>
  <action>
Create `tests/integration/test_resume.py` with pytest integration tests:

1. Import `pytest`, `Path` from standard library
2. Import `yaml` from stdlib
3. Import `StateMachineExecutor`, `ExecutionState`, `CompletedStep`, `ExecutionStateData` from `cli.lib.state_machine_executor`
4. Import `ScenarioSpec`, `ScenarioAssertions`, `ScenarioStep`, `ALayerAssertion`, `BLayerAssertion`, `CLayerAssertion` from `cli.lib.scenario_spec_compile`

2. Create test class `TestResumeFromState`:
   - `test_resume_loads_existing_state`: Verify resume=True loads existing state.yaml
   - `test_resume_continues_from_step_3`: Verify resume continues from step index 3
   - `test_resume_skips_completed_steps`: Verify steps 0, 1, 2 are not re-executed
   - `test_resume_returns_correct_checkpoint`: Verify get_next_uncompleted_step returns correct journey/step

3. Create test class `TestResumeWithPartialExecution`:
   - `test_partial_execution_state_persisted`: Verify state persisted after partial execution
   - `test_resume_after_first_journey_failure`: Verify resume continues to next journey after first fails
   - `test_resume_with_multiple_journeys`: Verify resume works with multiple journey scenario

4. Create test class `TestResumeFailureScenarios`:
   - `test_resume_nonexistent_run_fails`: Verify FileNotFoundError when run_id doesn't exist
   - `test_resume_with_invalid_state_file`: Verify error handling for corrupted state.yaml
   - `test_resume_after_complete_run`: Verify resume after complete run returns early

5. Create test class `TestResumeStateFileFormat`:
   - `test_state_file_contains_required_fields`: Verify state.yaml has run_id, current_state, completed_steps
   - `test_state_file_format_yaml`: Verify state.yaml is valid YAML
   - `test_state_file_timestamp_updated`: Verify updated_at changes on state update

6. Create test class `TestResumeEvidenceCollection`:
   - `test_collect_state_on_resume_failure`: Verify COLLECT state entered when resume encounters failure
   - `test_evidence_preserved_on_resume`: Verify evidence from failed run is preserved

Per TEST-03: Verify `--resume` re-runs failed cases without repeating completed steps
Per D-10: `--resume` reads from `{run_id}-state.yaml` to continue from last completed step
  </action>
  <verify>
    <automated>cd E:/ai/LEE-Lite-skill-first && python -m pytest tests/integration/test_resume.py -x -q 2>&1 | head -20</automated>
  </verify>
  <done>Resume integration tests created. Tests verify --resume functionality, state file format, failure scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {mock_app_url}->{integration_test} | Test environment URL crosses into integration test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Information Disclosure | integration_tests | accept | Tests use localhost only; no real credentials |
</threat_model>

<verification>
```bash
# Phase 18-04 verification
cd E:/ai/LEE-Lite-skill-first
python -m pytest tests/integration/test_e2e_chain.py tests/integration/test_resume.py -x -q 2>&1 | head -30
```
</verification>

<success_criteria>
- [ ] E2E chain integration tests verify --app-url and --api-url support
- [ ] Resume integration tests verify state file loading and step skipping
- [ ] All integration tests pass or skip gracefully when external services unavailable
- [ ] State file format verified for correctness
- [ ] Evidence collection on COLLECT state verified
</success_criteria>

<output>
After completion, create `.planning/phases/18-execution-axis/18-04-SUMMARY.md`
</output>
