---
phase: 026-acceptance-layer-integration
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - skills/ll-qa-gate-evaluate/input/contract.yaml
  - skills/ll-qa-gate-evaluate/output/contract.yaml
  - skills/ll-qa-gate-evaluate/evidence/gate-eval.schema.json
  - skills/ll-qa-settlement/input/contract.yaml
  - skills/ll-qa-settlement/output/contract.yaml
  - skills/ll-qa-settlement/evidence/settlement.schema.json
autonomous: false
requirements:
  - GATE-INTEG-01
  - GATE-INTEG-02
---

<objective>
Update gate-evaluate and settlement contracts with bug association info, maintaining backward compatibility.

Purpose: Add bug_ids map (coverage_id → bug_id) in gate output, and optional bug registry path in settlement input.
Output: Updated contract.yaml and schema.json files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/026-acceptance-layer-integration/026-CONTEXT.md
@.planning/REQUIREMENTS.md

# Current contracts
@skills/ll-qa-gate-evaluate/input/contract.yaml
@skills/ll-qa-gate-evaluate/output/contract.yaml
@skills/ll-qa-gate-evaluate/evidence/gate-eval.schema.json
@skills/ll-qa-settlement/input/contract.yaml
@skills/ll-qa-settlement/output/contract.yaml
@skills/ll-qa-settlement/evidence/settlement.schema.json
</context>

<interfaces>
Backward Compatibility Rule: ONLY ADD NEW FIELDS, NEVER DELETE OR RENAME EXISTING ONES
</interfaces>

<tasks>

<task type="auto">
<name>Task 1: Update gate-evaluate contracts</name>
<files>skills/ll-qa-gate-evaluate/output/contract.yaml, skills/ll-qa-gate-evaluate/evidence/gate-eval.schema.json</files>
<action>
Update `skills/ll-qa-gate-evaluate/output/contract.yaml`:
- Add section to Output Schema section: `bug_ids: map[string, string] (coverage_id → bug_id mapping
- Add to semantic checklist: "bug_ids map exists in output when final_decision=fail"

Update `skills/ll-qa-gate-evaluate/evidence/gate-eval.schema.json`:
- Add `bug_ids` to GateEvaluationEvidence properties:
  ```json
  "bug_ids": {
    "type": "object",
    "additionalProperties": { "type": "string" },
    "description": "Mapping from coverage_id to bug_id"
  }
  ```
- Do NOT add to required list (backward compatible)
</action>
<verify>
<automated>python -c "import json; d=json.load(open('skills/ll-qa-gate-evaluate/evidence/gate-eval.schema.json')); assert 'bug_ids' in d['properties'] or print('OK')" </automated>
</verify>
<done>
- gate-evaluate output contract has bug_ids map documented
- JSON schema has bug_ids optional property
- Backward compatible (no breaking changes)
</done>
</task>

<task type="auto">
<name>Task 2: Update settlement contracts</name>
<files>skills/ll-qa-settlement/input/contract.yaml, skills/ll-qa-settlement/evidence/settlement.schema.json</files>
<action>
Update `skills/ll-qa-settlement/input/contract.yaml`:
- Add to Required Input section: `bug_registry_path: string (optional, path to bug registry yaml)
- Add to semantic checklist: "bug_registry_path accepted but not required"

Update `skills/ll-qa-settlement/evidence/settlement.schema.json`:
- Add `bug_registry_path` optional property to SettlementEvidence:
  ```json
  "bug_registry_path": {
    "type": "string",
    "description": "Optional path to bug registry YAML file"
  }
  ```
- Do NOT add to required list
</action>
<verify>
<automated>python -c "import json; d=json.load(open('skills/ll-qa-settlement/evidence/settlement.schema.json')); assert 'bug_registry_path' in d['properties'] or print('OK')" </automated>
</verify>
<done>
- settlement input contract has bug_registry_path documented as optional
- JSON schema has bug_registry_path optional property
- Backward compatible (no breaking changes)
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract files → consumers | Contracts are documentation only, no runtime enforcement in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | Contract changes | mitigate | Backward compatibility strictly enforced (add only) |
</threat_model>

<verification>
- All 4 contract files updated
- No breaking changes (only additions
- JSON schemas have optional new fields
</verification>

<success_criteria>
- GATE-INTEG-01 and GATE-INTEG-02 requirements met
- Backward compatible (no fields deleted/renamed)
</success_criteria>

<output>
After completion, create `.planning/phases/026-acceptance-layer-integration/026-02-SUMMARY.md`
</output>
