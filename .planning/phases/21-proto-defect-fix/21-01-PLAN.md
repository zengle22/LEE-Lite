---
phase: "21"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py"
  - "skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html"
  - "skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html"
  - "skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html"
autonomous: true
requirements: ["FIX-P1-01"]
user_setup: []

must_haves:
  truths:
    - "All overlays (sheet, modal, drawer) are hidden by default in HTML"
    - "Placeholder text is minimized in templates"
    - "Placeholder lint threshold is reduced from 10 to 3"
  artifacts:
    - path: "skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py"
      provides: "Fixed _check_initial_view_integrity() and _placeholder_lint()"
    - path: "skills/ll-dev-feat-to-proto/resources/templates/"
      provides: "Templates with proper hidden overlays and minimal placeholders"
  key_links:
    - from: "skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py"
      to: "_check_initial_view_integrity()"
      via: "Line 568-584 - Initial view integrity check"
    - from: "skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py"
      to: "_placeholder_lint()"
      via: "Line 586-591 - Placeholder lint check"

---

<objective>
Fix FIX-P1-01: ll-dev-feat-to-proto low-fidelity issues - menu overlay blocking, generic placeholders, unrealistic components.
Purpose: Ensure prototypes have proper initial state (overlays hidden) and realistic content quality.
Output: Fixed templates and improved lint checks.
</objective>

<execution_context>
@.planning/phases/21-proto-defect-fix/21-CONTEXT.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
@skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html
@skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html
@skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Verify and fix template overlay hidden state</name>
  <files>skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html</files>
  <read_first>
    - skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html
  </read_first>
  <action>
Check all three templates and ensure:
1. All overlay elements (sheet, modal, drawer) have the `hidden` attribute by default
2. The pattern matches: `<section class="sheet" id="sheet" hidden>`
3. Verify in each template:
   - src002-journey-hifi/prototype/index.html
   - generic-hifi/prototype/index.html
   - src001-onboarding-hifi/prototype/index.html

If any overlay is missing the `hidden` attribute, add it.
</action>
<verify>
<automated>
grep -n 'class="[^"]*sheet[^"]*"' skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html || echo "No sheet found"
grep -n 'class="[^"]*modal[^"]*"' skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html || echo "No modal found"
grep -n 'class="[^"]*drawer[^"]*"' skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html || echo "No drawer found"
</automated>
</verify>
<done>All overlays in all templates have hidden attribute by default</done>
</task>

<task type="auto" tdd="true">
  <name>Check and minimize placeholder text in templates</name>
  <files>
    skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html
    skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/app.js
    skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html
    skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/app.js
    skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html
    skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/app.js
  </files>
  <read_first>
    - skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/app.js
    - skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/app.js
    - skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/app.js
  </read_first>
  <action>
Scan all template files for placeholder text:
- "Lorem ipsum"
- "占位"
- "placeholder"
- "TODO"
- "来自 ui spec"
- "仅供说明"
- "原型说明"

Replace generic placeholders with realistic content where possible.
For example:
- Instead of "Placeholder", use real application text
- Keep only necessary placeholders that indicate where dynamic content will go

Count remaining placeholders to ensure we're under the new threshold.
</action>
<verify>
<automated>
# Count placeholders in all templates
for file in skills/ll-dev-feat-to-proto/resources/templates/*/prototype/*.html skills/ll-dev-feat-to-proto/resources/templates/*/prototype/*.js; do
  if [ -f "$file" ]; then
    count=$(grep -i -E 'lorem|ipsum|占位|placeholder|todo|来自 ui spec|仅供说明|原型说明' "$file" | wc -l)
    if [ "$count" -gt 0 ]; then
      echo "$file: $count placeholders"
    fi
  fi
done
</automated>
</verify>
<done>Placeholder text is minimized in all templates</done>
</task>

<task type="auto" tdd="true">
  <name>Reduce placeholder lint threshold from 10 to 3</name>
  <files>skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py</files>
  <read_first>
    - skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
  </read_first>
  <action>
Modify the `_placeholder_lint()` function (line 586-591):

Change from:
```python
decision = "pass" if sum(hits.values()) <= 10 else "warn"
```

Change to:
```python
decision = "pass" if sum(hits.values()) <= 3 else "warn"
```

This reduces the allowed placeholder count from 10 to 3, enforcing higher fidelity.
</action>
<verify>
<automated>
grep -n -A2 -B2 'sum(hits.values())' skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
</automated>
</verify>
<done>Placeholder lint threshold reduced from 10 to 3</done>
</task>

<task type="auto" tdd="true">
  <name>Enhance initial view integrity check</name>
  <files>skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py</files>
  <read_first>
    - skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
  </read_first>
  <action>
Enhance the `_check_initial_view_integrity()` function (line 568-584):

Current implementation checks:
- `[hidden]` in CSS
- `display:none` in CSS
- Overlays have `hidden` attribute in HTML

Enhance to:
1. Check all three overlay types explicitly: sheet, modal, drawer
2. Provide more specific error messages about which overlay is missing hidden
3. Verify the hidden attribute is directly on the overlay element, not just somewhere in the file

Modified logic:
```python
def _check_initial_view_integrity(prototype_dir: Path) -> dict[str, Any]:
    css = _load_text(prototype_dir / "styles.css")
    html = _load_text(prototype_dir / "index.html")
    css_hidden_ok = "[hidden]" in css and "display:none" in css.replace(" ", "")

    overlays_declared_hidden = True
    missing_hidden = []

    # Check all overlay types explicitly
    for overlay_type, marker_id in [
        ("sheet", 'id="sheet"'),
        ("modal", 'id="modal"'),
        ("drawer", 'id="drawer"'),
    ]:
        if marker_id in html:
            # Find the line with this marker
            lines = html.split("\n")
            found = False
            for line in lines:
                if marker_id in line:
                    if "hidden" not in line:
                        missing_hidden.append(overlay_type)
                        overlays_declared_hidden = False
                    found = True
                    break
            if not found:
                # Marker exists but we couldn't find the line - be conservative
                missing_hidden.append(f"{overlay_type} (marker found but couldn't verify)")
                overlays_declared_hidden = False

    ok = css_hidden_ok and overlays_declared_hidden

    result = {
        "gate_name": "Initial View Integrity",
        "decision": "pass" if ok else "fail",
        "css_hidden_rule": css_hidden_ok,
        "overlays_hidden_attr": overlays_declared_hidden,
        "checked_at": utc_now(),
    }

    if missing_hidden:
        result["missing_hidden_overlays"] = missing_hidden
        result["notes"] = f"Missing hidden attribute on: {', '.join(missing_hidden)}"

    return result
```
</action>
<verify>
<automated>
grep -n -A30 'def _check_initial_view_integrity' skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
</automated>
</verify>
<done>Initial view integrity check enhanced with explicit overlay verification</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Template files → Generated prototype: Templates are read-only sources for prototype generation
- Lint checks → Prototype quality: Lint checks are advisory, not blocking (warn only)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Tampering | Template modification | mitigate | Templates are source-controlled, changes reviewed |
| T-21-01-02 | Denial of Service | Overly strict lint checks | accept | Threshold set to 3 is reasonable for high-fidelity |
</threat_model>

<verification>
- All overlays in all templates have hidden attribute by default
- Placeholder text is minimized in all templates
- Placeholder lint threshold reduced from 10 to 3
- Initial view integrity check enhanced with explicit overlay verification
</verification>

<success_criteria>
1. All overlay elements (sheet, modal, drawer) in all templates have `hidden` attribute by default
2. Placeholder text is minimized (no Lorem ipsum, minimal necessary placeholders)
3. Placeholder lint threshold is reduced from 10 to 3
4. Initial view integrity check explicitly verifies all three overlay types
5. Prototype starts with clean view (no overlays blocking content)
</success_criteria>

<output>
After completion, create `.planning/phases/21-proto-defect-fix/21-01-SUMMARY.md`
</output>
