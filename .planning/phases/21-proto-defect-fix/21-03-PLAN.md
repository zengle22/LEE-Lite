---
phase: "21"
plan: "03"
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - ".planning/STATE.md"
  - ".planning/ROADMAP.md"
autonomous: true
requirements: ["FIX-P1-01", "FIX-P1-02"]
user_setup: []

must_haves:
  truths:
    - "All Phase 21 repairs verified"
    - "Phase 21 requirements marked as complete"
    - "Project state updated"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated project state with Phase 21 marked complete"
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with Phase 21 status"
  key_links: []

---

<objective>
Verify repairs made in Phase 21-01 and 21-02, then update project state to mark Phase 21 as complete.
Purpose: Close the PROTO-related defect repair phase and prepare the project for subsequent phases.
Output: Updated STATE.md and ROADMAP.md, with Phase 21 marked complete.
</objective>

<execution_context>
@.planning/phases/21-proto-defect-fix/21-CONTEXT.md
@.planning/phases/21-proto-defect-fix/21-01-SUMMARY.md
@.planning/phases/21-proto-defect-fix/21-02-SUMMARY.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
</context>

<tasks>

<task type="auto">
  <name>Verify all Phase 21 changes</name>
  <files>
    skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
    skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html
    skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html
    skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html
  </files>
  <read_first>
    - skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
    - skills/ll-dev-feat-to-proto/resources/templates/src002-journey-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/generic-hifi/prototype/index.html
    - skills/ll-dev-feat-to-proto/resources/templates/src001-onboarding-hifi/prototype/index.html
  </read_first>
  <action>
Verify all Phase 21 repairs are in place:

1. **FIX-P1-01 Verification**:
   - [ ] All overlays (sheet, modal, drawer) have `hidden` attribute by default
   - [ ] Placeholder lint threshold is 3 (not 10)
   - [ ] `_check_initial_view_integrity()` has enhanced overlay checking
   - [ ] Templates have minimal placeholder text

2. **FIX-P1-02 Verification**:
   - [ ] Journey structural spec has wizard/hub + sheets pattern notes
   - [ ] Route map has `surface_kind` and `journey_pattern` fields
   - [ ] Reachability check has wizard pattern awareness
   - [ ] `build_package()` prioritizes journey coherence

Run a quick verification by checking the code:
</action>
<verify>
<automated>
# Verify placeholder threshold changed
grep -n 'sum(hits.values()) <= 3' skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
echo "---"
# Verify journey_pattern exists in route map
grep -n 'journey_pattern' skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
echo "---"
# Verify wizard pattern note in journey spec
grep -n 'wizard/hub + sheets' skills/ll-dev-feat-to-proto/scripts/feat_to_proto.py
echo "---"
# Verify templates have hidden overlays
for file in skills/ll-dev-feat-to-proto/resources/templates/*/prototype/index.html; do
  if [ -f "$file" ]; then
    echo "Checking $file..."
    grep -n 'id="sheet" hidden' "$file" || echo "  sheet not found or missing hidden"
    grep -n 'id="modal" hidden' "$file" || echo "  modal not found or missing hidden"
    grep -n 'id="drawer" hidden' "$file" || echo "  drawer not found or missing hidden"
  fi
done
</automated>
</verify>
<done>All Phase 21 changes verified</done>
</task>

<task type="auto">
  <name>Update project state to mark Phase 21 as complete</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/STATE.md
    .planning/ROADMAP.md
  </read_first>
  <action>
Update project state files to mark Phase 21 as complete:

1. **Update .planning/STATE.md**:
   - Set current_phase to 22 (next phase)
   - Add Phase 21 to completed_phases
   - Update progress section

2. **Update .planning/ROADMAP.md**:
   - Update Phase 21 status to "Complete"
   - Update requirement tracking (FIX-P1-01, FIX-P1-02)

3. **Update .planning/REQUIREMENTS.md**:
   - Mark FIX-P1-01 and FIX-P1-02 as completed (add [x])
</action>
<verify>
<automated>
cat .planning/STATE.md | grep -A20 -B5 "Phase 21"
echo "---"
cat .planning/ROADMAP.md | grep -A10 -B5 "Phase 21"
echo "---"
cat .planning/REQUIREMENTS.md | grep -A5 -B5 "FIX-P1"
</automated>
</verify>
<done>STATE.md, ROADMAP.md, and REQUIREMENTS.md updated to mark Phase 21 as complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Phase 21 changes → Project state: Phase changes are reviewed before state update

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | Project state updates | mitigate | Manual verification before state update |
</threat_model>

<verification>
- All Phase 21 repairs verified in code
- Project state updated to mark Phase 21 as complete
- Requirements marked as completed
</verification>

<success_criteria>
1. All Phase 21 repairs are verified present and working
2. STATE.md updated with Phase 21 marked as complete
3. ROADMAP.md updated with Phase 21 status
4. REQUIREMENTS.md updated with FIX-P1-01 and FIX-P1-02 marked complete
5. Project ready to proceed to Phase 22
</success_criteria>

<output>
After completion, create `.planning/phases/21-proto-defect-fix/21-03-SUMMARY.md`
</output>
