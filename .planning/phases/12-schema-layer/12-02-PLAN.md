---
phase: 12-schema-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/lib/environment_schema.py
  - tests/cli/lib/test_environment_schema.py
  - ssot/schemas/qa/environment.yaml
autonomous: true
requirements:
  - SCHEMA-03
  - SCHEMA-04

must_haves:
  truths:
    - "Environment YAML without base_url is rejected"
    - "Environment YAML without browser, timeout, headless is rejected"
    - "Valid Environment YAML parses and returns Environment dataclass"
    - "External YAML schema file exists and is valid"
  artifacts:
    - path: "cli/lib/environment_schema.py"
      provides: "Environment schema dataclass, enums, validation"
      exports: ["Environment", "EnvironmentSchemaError", "validate", "validate_file"]
      min_lines: 100
    - path: "tests/cli/lib/test_environment_schema.py"
      provides: "Environment schema unit tests"
      exports: ["test_"]
    - path: "ssot/schemas/qa/environment.yaml"
      provides: "External YAML schema definition"
  key_links:
    - from: "cli/lib/environment_schema.py"
      to: "ssot/schemas/qa/environment.yaml"
      via: "schema file reference"
      pattern: "_SCHEMA_DIR.*schemas.*qa"
    - from: "cli/lib/environment_schema.py"
      to: "ssot/src/SRC-009__adr-052-ssot-semantic-governance-upgrade.md"
      via: "environment field requirements"
      pattern: "base_url|browser|timeout|headless"

<interfaces>
<!-- Patterns from task_pack_schema.py, qa_schemas.py — same as Plan 01 -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Implement Environment schema with tests</name>
<files>cli/lib/environment_schema.py, tests/cli/lib/test_environment_schema.py</files>
<behavior>
- Test: valid Environment dict with base_url, browser, timeout, headless returns Environment dataclass
- Test: Environment dict missing base_url raises EnvironmentSchemaError
- Test: Environment dict missing browser raises EnvironmentSchemaError
- Test: Environment dict missing timeout raises EnvironmentSchemaError
- Test: Environment dict missing headless raises EnvironmentSchemaError
- Test: Environment dict with forbidden field 'embedded_in_testset' raises EnvironmentSchemaError
- Test: validate_file() with valid YAML returns Environment
- Test: CLI main() exits 0 for valid, 1 for invalid
</behavior>
<action>
Create `cli/lib/environment_schema.py` following the exact pattern of `cli/lib/task_pack_schema.py`:

1. **Module docstring:** Reference ADR-052, Phase 12. Environment YAML schema definition.

2. **Imports:** `from __future__ import annotations`, `dataclass, field`, `Enum`, `Path`, `Any`, `yaml`.

3. **Error class:** `class EnvironmentSchemaError(ValueError)` — own error type per D-06.

4. **Schema directory:** `_SCHEMA_DIR = Path(__file__).parent.parent.parent / "ssot" / "schemas" / "qa"`

5. **Environment dataclass** with `@dataclass(frozen=True)`:
   - `artifact_type: str = "environment"`
   - `env_id: str | None = None`
   - `base_url: str`
   - `browser: str`  # e.g. "chromium", "firefox", "webkit"
   - `timeout: int`
   - `headless: bool`
   - `viewport: dict[str, int] | None = None`
   - `notes: str | None = None`

6. **Helper functions:** `_require()`, `_enum_check()` — raise EnvironmentSchemaError.

7. **`_parse_environment_dict(data: dict) -> Environment`** — convert raw YAML dict.

8. **`validate(data: dict) -> Environment`** — dict-level validation:
   - Required fields: base_url, browser, timeout, headless
   - Forbidden field: embedded_in_testset
   - Type checks: timeout must be int, headless must be bool

9. **`_load_yaml(path: Path) -> dict`** — YAML loader.

10. **`validate_file(path, schema_type=None) -> Environment`** — file entry with `_VALIDATORS`, `_detect_schema_type`.

11. **`main()`** — CLI entry with `--type` flag, multi-file support.

Then create `tests/cli/lib/test_environment_schema.py` with pytest tests covering all behaviors.
</action>
<verify>
<automated>pytest tests/cli/lib/test_environment_schema.py -x -v</automated>
</verify>
<done>
- environment_schema.py exists with Environment dataclass, validate(), validate_file(), main()
- Required fields enforced (base_url, browser, timeout, headless)
- Forbidden field (embedded_in_testset) rejected
- All tests pass with pytest
- CLI entry works
</done>
</task>

<task type="auto">
<name>Task 2: Create external YAML schema file</name>
<files>ssot/schemas/qa/environment.yaml</files>
<action>
Create `ssot/schemas/qa/environment.yaml` following the format of existing schema YAML files:

```yaml
# Environment schema (ADR-052, Phase 12)
# Required fields: base_url, browser, timeout, headless
# Forbidden fields: embedded_in_testset

type: object
required:
  - base_url
  - browser
  - timeout
  - headless
properties:
  artifact_type:
    type: string
    const: environment
  env_id:
    type: string
  base_url:
    type: string
    format: uri
  browser:
    type: string
    enum: [chromium, firefox, webkit]
  timeout:
    type: integer
  headless:
    type: boolean
  viewport:
    type: object
    properties:
      width:
        type: integer
      height:
        type: integer
  notes:
    type: string
forbidden:
  - embedded_in_testset
```

Include a validation test in test_environment_schema.py that confirms this YAML file parses correctly.
</action>
<verify>
<automated>python -c "import yaml; d=yaml.safe_load(open('ssot/schemas/qa/environment.yaml')); assert d['required']==['base_url','browser','timeout','headless']"</automated>
</verify>
<done>
- ssot/schemas/qa/environment.yaml exists with valid YAML schema
- Schema defines required fields (base_url, browser, timeout, headless)
- Schema defines forbidden field (embedded_in_testset)
- File parses as valid YAML
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → validator | User-provided YAML must be validated before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Injection | YAML parser | mitigate | Use yaml.safe_load() only |
| T-12-05 | Tampering | Required field check | mitigate | Strict _require() checks for all required fields |
| T-12-06 | Elevation | Data parsing | accept | Local YAML files only, no privilege escalation vector |
</threat_model>

<verification>
- All 3 files created
- Tests pass with pytest
- Required fields enforced
- Forbidden field rejected
- Valid input parses
- Invalid input raises EnvironmentSchemaError
</verification>

<success_criteria>
- Environment schema module follows task_pack_schema.py pattern exactly
- Required field guard enforced for base_url, browser, timeout, headless
- Unit test coverage >= 80%
- External YAML schema file matches Python dataclass
- CLI entry works with --type flag
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-layer/12-02-SUMMARY.md`
</output>
