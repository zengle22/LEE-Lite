---
phase: 12-schema-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/lib/gate_schema.py
  - tests/cli/lib/test_gate_schema.py
  - ssot/schemas/qa/gate.yaml
autonomous: true
requirements:
  - SCHEMA-05
  - SCHEMA-06

must_haves:
  truths:
    - "Gate YAML with unknown verdict values is rejected"
    - "Gate YAML only accepts pass, conditional_pass, fail, provisional_pass verdicts"
    - "Valid Gate YAML parses and returns Gate dataclass"
    - "External YAML schema file exists and is valid"
  artifacts:
    - path: "cli/lib/gate_schema.py"
      provides: "Gate schema dataclass, enums, validation"
      exports: ["Gate", "GateSchemaError", "validate", "validate_file"]
      min_lines: 100
    - path: "tests/cli/lib/test_gate_schema.py"
      provides: "Gate schema unit tests"
      exports: ["test_"]
    - path: "ssot/schemas/qa/gate.yaml"
      provides: "External YAML schema definition"
  key_links:
    - from: "cli/lib/gate_schema.py"
      to: "ssot/schemas/qa/gate.yaml"
      via: "schema file reference"
      pattern: "_SCHEMA_DIR.*schemas.*qa"
    - from: "cli/lib/gate_schema.py"
      to: "ssot/src/SRC-009__adr-052-ssot-semantic-governance-upgrade.md"
      via: "gate verdict enum values"
      pattern: "pass|conditional_pass|fail|provisional_pass"

<interfaces>
<!-- Patterns from task_pack_schema.py, qa_schemas.py — same as Plan 01 -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Implement Gate schema with tests</name>
<files>cli/lib/gate_schema.py, tests/cli/lib/test_gate_schema.py</files>
<behavior>
- Test: valid Gate dict with verdict 'pass' returns Gate dataclass
- Test: valid Gate dict with verdict 'conditional_pass' returns Gate dataclass
- Test: valid Gate dict with verdict 'fail' returns Gate dataclass
- Test: valid Gate dict with verdict 'provisional_pass' returns Gate dataclass
- Test: Gate dict with verdict 'unknown_value' raises GateSchemaError
- Test: Gate dict with verdict 'skip' raises GateSchemaError
- Test: Gate dict with forbidden field 'hidden_verifier_failure' raises GateSchemaError
- Test: validate_file() with valid YAML returns Gate
- Test: CLI main() exits 0 for valid, 1 for invalid
</behavior>
<action>
Create `cli/lib/gate_schema.py` following the exact pattern of `cli/lib/task_pack_schema.py`:

1. **Module docstring:** Reference ADR-052, Phase 12. Gate verdict YAML schema with 4-verdict enum.

2. **Imports:** `from __future__ import annotations`, `dataclass, field`, `Enum`, `Path`, `Any`, `yaml`.

3. **Error class:** `class GateSchemaError(ValueError)` — own error type per D-06.

4. **Schema directory:** `_SCHEMA_DIR = Path(__file__).parent.parent.parent / "ssot" / "schemas" / "qa"`

5. **GateVerdict enum** (str, Enum):
   - `PASS = "pass"`
   - `CONDITIONAL_PASS = "conditional_pass"`
   - `FAIL = "fail"`
   - `PROVISIONAL_PASS = "provisional_pass"`

6. **Gate dataclass** with `@dataclass(frozen=True)`:
   - `artifact_type: str = "gate"`
   - `gate_id: str | None = None`
   - `verdict: GateVerdict`
   - `feature_id: str | None = None`
   - `evaluated_at: str | None = None`
   - `reason: str | None = None`
   - `notes: str | None = None`

7. **Helper functions:** `_require()`, `_enum_check()` — raise GateSchemaError.

8. **`_parse_gate_dict(data: dict) -> Gate`** — convert raw YAML dict. Include enum check for verdict using the 4 allowed values.

9. **`validate(data: dict) -> Gate`** — dict-level validation:
   - Required field: verdict
   - Verdict must be one of: pass, conditional_pass, fail, provisional_pass
   - Forbidden field: hidden_verifier_failure (FC-004 related)
   - Use _enum_check() for verdict validation

10. **`_load_yaml(path: Path) -> dict`** — YAML loader.

11. **`validate_file(path, schema_type=None) -> Gate`** — file entry with `_VALIDATORS`, `_detect_schema_type`.

12. **`main()`** — CLI entry with `--type` flag, multi-file support.

Then create `tests/cli/lib/test_gate_schema.py` with pytest tests covering all behaviors.
</action>
<verify>
<automated>pytest tests/cli/lib/test_gate_schema.py -x -v</automated>
</verify>
<done>
- gate_schema.py exists with Gate dataclass, GateVerdict enum, validate(), validate_file(), main()
- 4-verdict enum enforced (pass, conditional_pass, fail, provisional_pass)
- Unknown verdict values rejected
- Forbidden field (hidden_verifier_failure) rejected
- All tests pass with pytest
- CLI entry works
</done>
</task>

<task type="auto">
<name>Task 2: Create external YAML schema file</name>
<files>ssot/schemas/qa/gate.yaml</files>
<action>
Create `ssot/schemas/qa/gate.yaml` following the format of existing schema YAML files:

```yaml
# Gate verdict schema (ADR-052, Phase 12)
# 4-verdict enum: pass, conditional_pass, fail, provisional_pass
# Forbidden fields: hidden_verifier_failure

type: object
required:
  - verdict
properties:
  artifact_type:
    type: string
    const: gate
  gate_id:
    type: string
  verdict:
    type: string
    enum: [pass, conditional_pass, fail, provisional_pass]
  feature_id:
    type: string
  evaluated_at:
    type: string
  reason:
    type: string
  notes:
    type: string
forbidden:
  - hidden_verifier_failure
```

Include a validation test in test_gate_schema.py that confirms this YAML file parses correctly.
</action>
<verify>
<automated>python -c "import yaml; d=yaml.safe_load(open('ssot/schemas/qa/gate.yaml')); assert 'pass' in d['properties']['verdict']['enum']; assert len(d['properties']['verdict']['enum'])==4"</automated>
</verify>
<done>
- ssot/schemas/qa/gate.yaml exists with valid YAML schema
- Schema defines verdict enum with exactly 4 values
- Schema defines forbidden field (hidden_verifier_failure)
- File parses as valid YAML
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → validator | User-provided YAML must be validated before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Injection | YAML parser | mitigate | Use yaml.safe_load() only |
| T-12-08 | Tampering | Verdict enum check | mitigate | Strict _enum_check() against 4 allowed values only |
| T-12-09 | Elevation | Data parsing | accept | Local YAML files only, no privilege escalation vector |
</threat_model>

<verification>
- All 3 files created
- Tests pass with pytest
- 4-verdict enum enforced
- Unknown verdicts rejected
- Valid input parses
- Invalid input raises GateSchemaError
</verification>

<success_criteria>
- Gate schema module follows task_pack_schema.py pattern exactly
- 4-verdict enum guard implemented
- Forbidden field (hidden_verifier_failure) rejected
- Unit test coverage >= 80%
- External YAML schema file matches Python dataclass
- CLI entry works with --type flag
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-layer/12-03-SUMMARY.md`
</output>
