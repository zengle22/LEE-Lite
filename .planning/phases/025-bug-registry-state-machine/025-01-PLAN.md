---
phase: 025-bug-registry-state-machine
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - cli/lib/bug_registry.py
  - cli/lib/test_bug_registry.py
autonomous: true
requirements:
  - BUG-REG-01
  - BUG-REG-02
  - BUG-REG-03
user_setup: []

must_haves:
  truths:
    - "bug_registry.py exists as a module with YAML-backed CRUD and state machine"
    - "State machine enforces 10 valid states with correct transition rules"
    - "Terminal states (wont_fix, duplicate, not_reproducible) enforce field requirements"
    - "YAML persistence uses atomic write (tempfile + os.replace) from frz_registry pattern"
    - "Optimistic lock via version UUID prevents concurrent write corruption"
    - "Resurrection from terminal states creates new bug_id with resurrected_from linkage"
    - "Unit tests pass covering state transitions, persistence, and terminal state validation"
  artifacts:
    - path: "cli/lib/bug_registry.py"
      provides: "Bug registry CRUD + state machine + YAML persistence"
      min_lines: 180
      exports:
        - "BUG_STATE_TRANSITIONS"
        - "transition_bug_status"
        - "load_or_create_registry"
        - "sync_bugs_to_registry"
        - "check_not_reproducible"
    - path: "cli/lib/test_bug_registry.py"
      provides: "Unit tests for bug_registry state machine and persistence"
      min_lines: 200
  key_links:
    - from: "cli/lib/bug_registry.py"
      to: "cli/lib/frz_registry.py"
      via: "copies _load_registry/_save_registry atomic write pattern verbatim"
      pattern: "tempfile\\.mkstemp.*os\\.replace"
    - from: "cli/lib/bug_registry.py"
      to: "cli/lib/errors.py"
      via: "raises CommandError on invalid transitions and missing fields"
      pattern: "raise CommandError\\("

---

<objective>
Implement bug_registry.py core module with independent state machine, YAML persistence, and CRUD operations.

Purpose: Foundation for the ADR-055 bug lifecycle. Bugs discovered during test runs need a persistent registry with state tracking. This module is the single source of truth for bug state.
Output: cli/lib/bug_registry.py (~200 lines) + cli/lib/test_bug_registry.py (core TDD tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/025-bug-registry-state-machine/025-CONTEXT.md
@.planning/phases/025-bug-registry-state-machine/025-RESEARCH.md
@.planning/phases/025-bug-registry-state-machine/025-PATTERNS.md

<interfaces>
From cli/lib/frz_registry.py (analog -- copy patterns):
```python
def _load_registry(path: Path) -> list[dict[str, Any]]:  # YAML safe_load pattern
def _save_registry(path: Path, records: list[dict[str, Any]]) -> None:  # atomic write pattern
  # Uses: ensure_parent(path), yaml.dump(), tempfile.mkstemp(), os.fdopen(), os.replace()
```

From cli/lib/errors.py:
```python
class CommandError(Exception):
    def __init__(self, status_code: str, message: str): ...
```

From cli/lib/fs.py:
```python
def ensure_parent(path: Path) -> None: ...
def read_text(path: Path) -> str: ...
```

State machine spec (from ADR-055 §2.2, locked D-02):
```python
BUG_STATE_TRANSITIONS: dict[str, set[str]] = {
    "detected":         {"open", "wont_fix", "duplicate"},
    "open":             {"fixing", "wont_fix", "duplicate"},
    "fixing":           {"fixed", "wont_fix", "duplicate"},
    "fixed":            {"re_verify_passed", "open", "wont_fix", "duplicate"},
    "re_verify_passed": {"closed", "wont_fix", "duplicate"},
    "archived":         {"wont_fix", "duplicate", "not_reproducible"},
    "closed":           set(),
    "wont_fix":         set(),
    "duplicate":        set(),
    "not_reproducible": set(),
}
```

NOT_REPRODUCIBLE_THRESHOLDS (locked D-08):
```python
NOT_REPRODUCIBLE_THRESHOLDS = {"unit": 3, "integration": 4, "e2e": 5}
```

bug_id format (locked D-06): `BUG-{case_id}-{md5_hash_6char_uppercase}`
  hash_input = f"{case_id}{run_id}{timestamp}"
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD — bug_registry.py state machine, CRUD, YAML persistence</name>
  <files>cli/lib/bug_registry.py, cli/lib/test_bug_registry.py</files>
  <read_first>
    cli/lib/frz_registry.py — _load_registry, _save_registry, atomic write pattern (lines 31-66)
    cli/lib/errors.py — CommandError class, status_code taxonomy
    cli/lib/fs.py — ensure_parent, read_text helpers
    cli/lib/test_frz_registry.py — tmp_workspace fixture pattern, @pytest.mark.unit, error assertion style
  </read_first>
  <behavior>
    TDD behavior specifications (write tests FIRST, then implement):

    test_load_or_create: Load nonexistent path returns empty registry with schema_version, version (UUID), bugs=[]; load existing YAML returns parsed dict; subsequent load preserves bugs list.

    test_persist_and_reload: Save registry via _save_registry, reload via _load_registry, verify all fields round-trip. Verify file exists at artifacts/bugs/{feat_ref}/bug-registry.yaml.

    test_bug_id_format: Generate bug_id from case_id="api.job.gen.fail" + run_id="RUN-001" + timestamp. Format: BUG-api.job.gen.fail-{6 uppercase hex chars}. Verify MD5 hash portion is 6 chars.

    test_happy_path_transitions: Transition detected->open->fixing->fixed->re_verify_passed->closed. Each step returns new immutable bug dict (original unchanged). Each step appends trace entry with event, from, to, actor, at.

    test_invalid_transition_raises: Attempt detected->fixed raises CommandError("INVALID_REQUEST", "Cannot transition ... from 'detected' to 'fixed'"). Verify original bug status unchanged.

    test_wont_fix_requires_reason: Attempt open->wont_fix without reason raises CommandError. With reason="not worth fixing" succeeds. Verify resolution_reason in result.

    test_duplicate_requires_duplicate_of: Attempt fixing->duplicate without duplicate_of raises CommandError. With duplicate_of="BUG-other-ABC123" succeeds.

    test_not_reproducible_thresholds: check_not_reproducible returns False when bug status is not "archived". Returns False when consecutive_nonappearances < threshold (e.g., 2 for unit). Returns True when >= threshold (3 for unit, 4 for integration, 5 for e2e).

    test_wont_fix_from_any_nonterminal: Verify wont_fix reachable from detected, open, fixing, fixed, re_verify_passed, archived. Verify NOT reachable from closed, wont_fix, duplicate, not_reproducible.

    test_duplicate_from_any_nonterminal: Same as wont_fix — reachable from all non-terminal, not from terminal.

    test_optimistic_lock: Create registry, verify version is UUID. Simulate version mismatch by passing wrong expected_version to transition function — should raise CommandError("CONFLICT").

    test_sync_persists: Call sync_bugs_to_registry(workspace_root, "FEAT-001", None, "RUN-001", [{"case_id": "c1", "status": "failed", "title": "t", "actual": "a", "expected": "e"}]). Verify YAML file created, contains one bug with status "detected", gap_type inferred. Verify reading back yields same data.

    test_gap_type_inference: _infer_gap_type with "timeout" in diagnostics -> "env_issue". _infer_gap_type with no keywords -> "code_defect". _infer_gap_type with "connection reset" in diagnostics -> "env_issue".

    test_immutable_transitions: transition_bug_status returns new dict. Original dict status unchanged. Trace in new dict has entry, original has none.

    test_resurrection_new_record: When existing bug is in terminal state (e.g., wont_fix) and same case_id fails again, sync creates new record with different bug_id and resurrected_from pointing to old bug_id.
  </behavior>
  <action>
    TDD workflow — write tests first, then implement:

    Step 1 (RED): Create cli/lib/test_bug_registry.py with ALL test functions listed in &lt;behavior&gt; above. Import stub symbols from bug_registry.py. Run `pytest cli/lib/test_bug_registry.py -x -v` — all tests MUST fail. Commit: test(025-01): add failing tests for bug_registry state machine.

    Step 2 (GREEN): Create cli/lib/bug_registry.py implementing:

    Imports (match frz_registry.py pattern):
    ```python
    from __future__ import annotations
    import hashlib
    import os
    import re
    import tempfile
    import uuid
    from datetime import datetime, timezone
    from pathlib import Path
    from typing import Any
    import yaml
    from cli.lib.errors import CommandError
    from cli.lib.fs import ensure_parent, read_text
    ```

    Constants:
    - BUG_STATE_TRANSITIONS dict (exact values from context block above)
    - NOT_REPRODUCIBLE_THRESHOLDS = {"unit": 3, "integration": 4, "e2e": 5}
    - TERMINAL_STATES = {"closed", "wont_fix", "duplicate", "not_reproducible"}

    Functions:
    - `_timestamp() -> str` — copy verbatim from test_orchestrator.py line 27-29
    - `registry_path(workspace_root, feat_ref) -> Path` — returns workspace_root / "artifacts" / "bugs" / feat_ref / "bug-registry.yaml"
    - `_load_registry(path) -> dict` — if not exists, return _empty_registry(). yaml.safe_load, return data.get("bug_registry", _empty_registry())
    - `_save_registry(path, registry) -> None` — verbatim atomic write from frz_registry.py but wrapping {"bug_registry": registry}
    - `_empty_registry() -> dict` — returns {schema_version:"1.0", registry_id:"", feat_ref:"", proto_ref:None, version:uuid4, generated_at:_timestamp(), last_synced_at:_timestamp(), last_sync_run_id:"", bugs:[]}
    - `load_or_create_registry(workspace_root, feat_ref, proto_ref=None) -> dict` — calls _load_registry, sets feat_ref/proto_ref/registry_id if empty
    - `_build_bug_record(case_id, run_id, case_result, feat_ref, proto_ref) -> dict` — builds full bug record per schema (all fields from RESEARCH.md YAML schema). bug_id = f"BUG-{case_id}-{hashlib.md5(f'{case_id}{run_id}{now}'.encode()).hexdigest()[:6].upper()}"
    - `_infer_gap_type(case_result) -> str` — check diagnostics for "timeout","connection reset","flaky","intermittent" -> "env_issue", default "code_defect"
    - `transition_bug_status(bug, new_status, *, reason=None, actor="system", **extra_fields) -> dict` — validates transition, checks terminal field requirements, returns new immutable dict with trace appended
    - `check_not_reproducible(bug, consecutive_nonappearances, test_level="integration") -> bool` — archived status AND count >= threshold
    - `sync_bugs_to_registry(workspace_root, feat_ref, proto_ref, run_id, case_results) -> None` — filters failed cases, builds bug records, upserts to registry, saves YAML

    Run `pytest cli/lib/test_bug_registry.py -x -v` — all tests MUST pass. Commit: feat(025-01): implement bug_registry state machine and CRUD.

    Step 3 (REFACTOR if needed): Clean up while keeping tests green. Commit: refactor(025-01): clean up bug_registry.
  </action>
  <verify>
    <automated>pytest cli/lib/test_bug_registry.py -x -v</automated>
  </verify>
  <done>
    - cli/lib/bug_registry.py exists with all exports: BUG_STATE_TRANSITIONS, transition_bug_status, load_or_create_registry, sync_bugs_to_registry, check_not_reproducible, _infer_gap_type
    - All 14+ test functions pass
    - YAML atomic write uses tempfile.mkstemp + os.replace (verified by grep)
    - bug_id format matches BUG-{case_id}-{6 uppercase hex} (verified by test)
    - State machine has exactly 10 states (verified by len(BUG_STATE_TRANSITIONS))
    - transition_bug_status returns new dict without mutating original (verified by test_immutable_transitions)
    - No imports from state_machine_executor.py (independence per D-01)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→registry | YAML file read from disk — could be corrupted or tampered |
| caller→transition | transition_bug_status receives arbitrary new_status string |
| concurrent-writes | Multiple test runs may write bug-registry.yaml simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | bug-registry.yaml on disk | accept | YAML contains no secrets; corruption handled by atomic write + re-run recovery |
| T-25-02 | Denial of Service | Concurrent writes corrupt YAML | mitigate | Atomic write (tempfile + os.replace) prevents partial writes; optimistic lock (version UUID) prevents silent overwrite — second writer gets CommandError("CONFLICT") |
| T-25-03 | Information Disclosure | bug-registry.yaml contains execution evidence paths | accept | No PII; paths reference local workspace artifacts only |
| T-25-04 | Spoofing | Caller passes fake actor="system" | accept | CLI tool, not multi-user system; actor field is audit metadata only |
</threat_model>

<verification>
- TDD cycle: RED (all fail) -> GREEN (all pass) -> REFACTOR (all pass)
- grep: "tempfile.mkstemp" present in bug_registry.py
- grep: "os.replace" present in bug_registry.py
- grep: "state_machine_executor" NOT present in bug_registry.py
- grep: "CommandError" present in bug_registry.py (invalid transitions)
- file: cli/lib/bug_registry.py has "from cli.lib.errors import CommandError"
- file: cli/lib/test_bug_registry.py has "@pytest.mark.unit" on all test functions
</verification>

<success_criteria>
- bug_registry.py module complete (~200 lines) with 10 exported symbols
- test_bug_registry.py with 14+ test functions, all passing
- State machine enforces all transitions per ADR-055 §2.2
- YAML persistence uses atomic write pattern from frz_registry.py
- Zero new dependencies (stdlib + existing PyYAML only)
</success_criteria>

<output>
After completion, create `.planning/phases/025-bug-registry-state-machine/025-01-SUMMARY.md`
</output>
