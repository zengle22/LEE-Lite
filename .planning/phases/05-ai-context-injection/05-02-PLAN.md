---
phase: "05-ai-context-injection"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/ll-patch-aware-context/SKILL.md"
  - "skills/ll-patch-aware-context/ll.lifecycle.yaml"
  - "skills/ll-patch-aware-context/input/contract.yaml"
  - "skills/ll-patch-aware-context/output/contract.yaml"
  - "skills/ll-patch-aware-context/agents/executor.md"
autonomous: true
requirements:
  - REQ-PATCH-05

must_haves:
  truths:
    - "Skill definition is complete with all required files (SKILL.md + lifecycle + contracts + executor)"
    - "Executor instructs AI to verify patch-awareness.yaml exists before generating SSOT artifacts"
    - "Skill can be invoked as prerequisite step by SSOT chain skills"
  artifacts:
    - path: "skills/ll-patch-aware-context/SKILL.md"
      provides: "Skill definition with execution protocol"
      min_lines: 30
    - path: "skills/ll-patch-aware-context/ll.lifecycle.yaml"
      provides: "Lifecycle state machine definition"
    - path: "skills/ll-patch-aware-context/input/contract.yaml"
      provides: "Input contract (feat_ref)"
    - path: "skills/ll-patch-aware-context/output/contract.yaml"
      provides: "Output contract (patch-awareness.yaml schema)"
    - path: "skills/ll-patch-aware-context/agents/executor.md"
      provides: "AI agent instructions for awareness recording"
  key_links:
    - from: "skills/ll-patch-aware-context/agents/executor.md"
      to: "scripts/patch_aware_context.py"
      via: "CLI invocation instruction"
      pattern: "patch_aware_context\\.py|run\\.sh"

---

<objective>
Define the `ll-patch-aware-context` skill: SKILL.md, lifecycle metadata, input/output contracts, and executor agent instructions. This skill is a prerequisite invoked before SSOT chain generation to ensure AI has change management awareness.

Purpose: Provide the skill definition that makes patch awareness a structured, repeatable step (per D-04, D-09, D-10).
Output: 5 skill definition files under skills/ll-patch-aware-context/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-context-injection/05-CONTEXT.md
@.planning/phases/05-ai-context-injection/05-RESEARCH.md
@skills/ll-patch-capture/SKILL.md
@skills/ll-patch-capture/ll.lifecycle.yaml
@skills/ll-experience-patch-settle/SKILL.md

# Existing skill patterns to replicate
<interfaces>
From skills/ll-patch-capture/SKILL.md:
- SKILL.md frontmatter: name, description
- Required Read Order section
- Execution Protocol with numbered steps
- Workflow Boundary (input/output/out of scope)
- Non-Negotiable Rules section
- Canonical Authority section
- Runtime Boundary Baseline section

From skills/ll-patch-capture/ll.lifecycle.yaml:
```yaml
skill: ll-patch-capture
version: "1.0"
state: draft
lifecycle_states:
  - draft
  - active
  - validated
  - pending_backwrite
  - resolved
  - archived
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md + ll.lifecycle.yaml + contracts</name>
  <files>skills/ll-patch-aware-context/SKILL.md
skills/ll-patch-aware-context/ll.lifecycle.yaml
skills/ll-patch-aware-context/input/contract.yaml
skills/ll-patch-aware-context/output/contract.yaml</files>
  <read_first>
    - skills/ll-patch-capture/SKILL.md (pattern reference for SKILL.md structure)
    - skills/ll-patch-capture/ll.lifecycle.yaml (pattern reference for lifecycle)
    - skills/ll-patch-capture/input/contract.yaml (pattern reference for input contract)
    - skills/ll-patch-capture/output/contract.yaml (pattern reference for output contract)
    - .planning/phases/05-ai-context-injection/05-CONTEXT.md (decisions D-01 through D-11)
  </read_first>
  <action>Create the following files matching the existing skill patterns exactly:

**1. SKILL.md** — Frontmatter with `name: ll-patch-aware-context` and `description: "ADR-049 governed skill for patch context injection before SSOT chain generation (awareness recording, not enforcement)." Include these sections:

- **Canonical Authority**: Reference ADR-049 §12.1 (Patch-Aware Context) and §14.2 (AI Context 注入). Upstream handoff: user triggers SSOT chain generation (feat-to-tech/feat-to-ui/feat-to-proto). Downstream consumer: SSOT chain executor agents.
- **Runtime Boundary Baseline**: "Interpret this workflow using ssot/adr/ADR-038-运行时核心抽象边界与对象分层基线.MD. This capability is a governed `Skill` for `SSOT Generation Prerequisite → Patch Awareness Recording`."
- **Required Read Order**: 1. ll.contract.yaml, 2. input/contract.yaml, 3. output/contract.yaml, 4. agents/executor.md
- **Execution Protocol** (numbered steps):
  1. Receive feat_ref from SSOT chain trigger
  2. Run `python scripts/patch_aware_context.py resolve --workspace-root $WORKSPACE_ROOT --feat-ref {feat_ref} --output-dir {output_dir}`
  3. Read generated patch-awareness.yaml
  4. If has_active_patches is true, review each patch's scope and change_class
  5. Document consideration in ai_consideration field (re-run with --ai-reasoning if needed)
  6. Proceed with SSOT artifact generation
- **Workflow Boundary**: Input = feat_ref (e.g. "FEAT-SRC-001-001"). Output = patch-awareness.yaml in artifacts directory. Out of scope: Patch enforcement (Phase 6), PreToolUse hooks (Phase 6), 24h blocking (Phase 7).
- **Non-Negotiable Rules**:
  - Do NOT modify existing SSOT skill executor.md files (D-10)
  - Do NOT enforce Patch compliance — only record awareness (D-03)
  - Always produce patch-awareness.yaml even when no patches exist (RESEARCH.md Pitfall 4)
  - Use resolve_patch_context() from Phase 4 — do not reimplement scanning (D-07)
  - Only inject validated + pending_backwrite patches (D-08)

**2. ll.lifecycle.yaml** — Copy pattern from ll-patch-capture:
```yaml
skill: ll-patch-aware-context
version: "1.0"
state: draft
lifecycle_states:
  - draft
  - active
  - completed
```

**3. input/contract.yaml** — Define input contract:
```yaml
input_contract:
  skill: ll-patch-aware-context
  parameters:
    - name: feat_ref
      type: string
      required: true
      description: "FEAT reference identifier (e.g. FEAT-SRC-001-001)"
      example: "FEAT-SRC-001-001"
    - name: workspace_root
      type: string
      required: false
      description: "Workspace root directory path (defaults to current directory)"
    - name: output_dir
      type: string
      required: true
      description: "Directory where patch-awareness.yaml will be written"
```

**4. output/contract.yaml** — Define output contract matching the awareness recording schema:
```yaml
output_contract:
  skill: ll-patch-aware-context
  artifacts:
    - name: patch-awareness.yaml
      format: yaml
      description: "Patch awareness recording for SSOT chain executor"
      schema:
        top_level_key: patch_awareness
        fields:
          - name: feat_ref
            type: string
            required: true
          - name: scan_timestamp
            type: string
            required: true
            description: "ISO 8601 timestamp"
          - name: has_active_patches
            type: boolean
            required: true
          - name: patch_scan_status
            type: string
            required: true
            enum: ["patches_found", "none_found"]
          - name: validated_patches_summary
            type: array
            required: false
          - name: pending_patches_summary
            type: array
            required: false
          - name: conflict_resolution
            type: object
            required: false
          - name: directory_hash
            type: string
            required: true
          - name: ai_consideration
            type: string
            required: true
            description: "AI's reasoning for patch consideration or divergence"
```
  </action>
  <acceptance_criteria>
    - skills/ll-patch-aware-context/SKILL.md exists and contains "ll-patch-aware-context" in name
    - SKILL.md contains "ADR-049" reference (grep verifiable)
    - SKILL.md contains "patch_aware_context.py" or "run.sh" in Execution Protocol
    - SKILL.md contains Non-Negotiable Rules section with "Do NOT modify existing SSOT skill executor.md"
    - SKILL.md contains "patch-awareness.yaml" as output artifact
    - skills/ll-patch-aware-context/ll.lifecycle.yaml exists and contains "ll-patch-aware-context"
    - skills/ll-patch-aware-context/input/contract.yaml exists and contains "feat_ref" parameter
    - skills/ll-patch-aware-context/output/contract.yaml exists and contains "patch_awareness" top-level key
    - output/contract.yaml contains "patches_found" and "none_found" enum values
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/ll-patch-aware-context/SKILL.md && test -f skills/ll-patch-aware-context/ll.lifecycle.yaml && test -f skills/ll-patch-aware-context/input/contract.yaml && test -f skills/ll-patch-aware-context/output/contract.yaml && echo "all skill files exist"</automated>
  </verify>
  <done>SKILL.md with execution protocol, lifecycle.yaml, input/contract.yaml with feat_ref parameter, output/contract.yaml with patch-awareness.yaml schema — all 4 files created</done>
</task>

<task type="auto">
  <name>Task 2: Create agents/executor.md — AI awareness recording instructions</name>
  <files>skills/ll-patch-aware-context/agents/executor.md</files>
  <read_first>
    - skills/ll-patch-capture/agents/executor.md (pattern reference for executor.md structure)
    - .planning/phases/05-ai-context-injection/05-CONTEXT.md (decisions D-01 through D-11)
    - .planning/phases/05-ai-context-injection/05-RESEARCH.md (Pattern 3: Executor Agent Reads Awareness)
  </read_first>
  <action>Create `skills/ll-patch-aware-context/agents/executor.md` as the AI agent instruction file. This file tells the AI how to perform patch awareness recording when invoked as part of an SSOT chain. Structure it as follows:

```markdown
# Executor: Patch-Aware Context

## Role
You are the patch-awareness executor. Before any SSOT artifact generation (TECH/UI/PROTO), you ensure the AI agent has awareness of active experience patches for the target FEAT.

## Prerequisites
- `scripts/patch_aware_context.py` exists and is executable
- Workspace root contains `ssot/experience-patches/` directory
- FEAT reference is provided (e.g., FEAT-SRC-001-001)

## Execution Steps

### Step 1: Resolve Patch Context
Run:
```bash
python scripts/patch_aware_context.py resolve \
  --workspace-root $WORKSPACE_ROOT \
  --feat-ref {FEAT_REF} \
  --output-dir {OUTPUT_DIR}
```

### Step 2: Read Awareness File
Read the generated `{OUTPUT_DIR}/patch-awareness.yaml`.

### Step 3: Evaluate Patches
- If `patch_scan_status` is `"none_found"`: Note "No active patches found for this FEAT. Proceeding without patch constraints." and continue to SSOT generation.
- If `has_active_patches` is `true`:
  - Review each entry in `validated_patches_summary` and `pending_patches_summary`
  - For each patch, note its `change_class` (visual/interaction/semantic) and `scope`
  - Determine whether the patch's scope overlaps with the SSOT artifact you are about to generate

### Step 4: Record Consideration
If patches were found and you need to document your reasoning, re-run the resolver with your consideration:
```bash
python scripts/patch_aware_context.py resolve \
  --workspace-root $WORKSPACE_ROOT \
  --feat-ref {FEAT_REF} \
  --output-dir {OUTPUT_DIR} \
  --ai-reasoning "{your consideration text}"
```

Your consideration text should be specific:
- If following patches: "Patch UXPATCH-NNNN change_class=semantic for scope.page=X was considered. The generated TECH document incorporates the documented behavior."
- If diverging: "Patch UXPATCH-NNNN change_class=interaction for scope.page=X was reviewed but not followed because [specific rationale]."

### Step 5: Proceed
SSOT chain generation proceeds normally. The patch-awareness.yaml file serves as an audit record that patch context was acknowledged.

## Constraints
- Do NOT enforce patch compliance — this is awareness only (Phase 6 handles enforcement)
- Do NOT modify existing SSOT skill executor.md files
- Always produce patch-awareness.yaml, even with zero patches found
```
  </action>
  <acceptance_criteria>
    - File exists at skills/ll-patch-aware-context/agents/executor.md
    - Contains "resolve" subcommand invocation (grep for "patch_aware_context.py resolve")
    - Contains "patch-awareness.yaml" reference
    - Contains "none_found" handling instruction
    - Contains "has_active_patches" evaluation step
    - Contains "--ai-reasoning" re-run instruction
    - Contains constraint: "Do NOT enforce patch compliance" or equivalent
    - Contains 5 numbered execution steps (Step 1 through Step 5)
  </acceptance_criteria>
  <verify>
    <automated>test -f skills/ll-patch-aware-context/agents/executor.md && echo "executor.md exists"</automated>
  </verify>
  <done>agents/executor.md created with 5-step execution protocol, awareness file reading, patch evaluation, consideration recording with --ai-reasoning, and explicit awareness-only constraints (no enforcement)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor.md → script | AI agent invokes CLI — must be well-defined interface |
| awareness file → downstream agents | AI reads patch-awareness.yaml — must be valid, parseable YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | executor.md CLI invocation | mitigate | executor.md specifies exact command syntax with named arguments (--workspace-root, --feat-ref, --output-dir) — no shell interpolation of user input |
| T-05-05 | Repudiation | patch-awareness.yaml audit trail | mitigate | File includes scan_timestamp, directory_hash, and ai_consideration — provides non-repudiation that AI reviewed patches at a specific point in time |
| T-05-06 | Elevation of Privilege | awareness-only vs enforcement confusion | mitigate | executor.md explicitly states "Do NOT enforce patch compliance" — prevents AI from overstepping awareness-only scope (D-03) |
</threat_model>

<verification>
- All 5 skill files exist under skills/ll-patch-aware-context/
- SKILL.md references resolve_patch_context from Phase 4, awareness recording format, and D-10 constraint
- executor.md has 5-step protocol matching RESEARCH.md Pattern 3
- lifecycle.yaml defines draft → active → completed states
- input contract requires feat_ref parameter
- output contract matches awareness recording schema from Plan 01
- No executor.md files in existing SSOT skills (ll-dev-feat-to-tech, ll-dev-feat-to-ui, ll-dev-feat-to-proto) are modified
</verification>

<success_criteria>
1. skills/ll-patch-aware-context/ directory has all required files: SKILL.md, ll.lifecycle.yaml, input/contract.yaml, output/contract.yaml, agents/executor.md
2. SKILL.md execution protocol includes 6 numbered steps from feat_ref receipt to SSOT generation
3. SKILL.md Non-Negotiable Rules include D-03 (no enforcement) and D-10 (no executor.md modification)
4. executor.md implements 5-step awareness recording protocol matching RESEARCH.md Pattern 3
5. output/contract.yaml schema matches the patch-awareness.yaml structure produced by Plan 01's script
6. No existing SSOT skill files are modified (per D-02, D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-context-injection/05-02-SUMMARY.md`
</output>
