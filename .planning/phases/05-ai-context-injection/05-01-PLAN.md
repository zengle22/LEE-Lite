---
phase: "05-ai-context-injection"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/ll-patch-aware-context/scripts/patch_aware_context.py"
  - "skills/ll-patch-aware-context/scripts/run.sh"
autonomous: true
requirements:
  - REQ-PATCH-05

must_haves:
  truths:
    - "Running the script with a feat_ref produces a patch-awareness.yaml file"
    - "The awareness file contains scan status even when no patches exist"
    - "Validated and pending_backwrite patches are summarized in the output"
  artifacts:
    - path: "skills/ll-patch-aware-context/scripts/patch_aware_context.py"
      provides: "Patch context resolution + awareness recording generation"
      exports: ["resolve_and_record", "write_awareness_recording", "summarize_patch"]
    - path: "skills/ll-patch-aware-context/scripts/run.sh"
      provides: "CLI shell wrapper for Claude Code skill invocation"
  key_links:
    - from: "skills/ll-patch-aware-context/scripts/patch_aware_context.py"
      to: "cli/lib/test_exec_artifacts.py"
      via: "import resolve_patch_context"
      pattern: "from cli\\.lib\\.test_exec_artifacts import resolve_patch_context"

---

<objective>
Implement the Python patch-aware context resolver script and its CLI shell wrapper. This script reuses `resolve_patch_context()` from Phase 4 to scan for active patches, then produces a lightweight `patch-awareness.yaml` recording file.

Purpose: Provide the core runtime that SSOT chain executor agents invoke before generating artifacts, ensuring AI has change management awareness (per D-01, D-07, D-08).
Output: `patch_aware_context.py` (Python runtime) + `run.sh` (CLI wrapper)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-context-injection/05-CONTEXT.md
@.planning/phases/05-ai-context-injection/05-RESEARCH.md
@.planning/phases/04-test-integration/04-02-PLAN.md
@cli/lib/test_exec_artifacts.py
@cli/lib/patch_schema.py

# Key interfaces from Phase 4 — use directly, do not reimplement
<interfaces>
From cli/lib/test_exec_artifacts.py:
```python
@dataclass(frozen=True)
class PatchContext:
    has_active_patches: bool
    validated_patches: list[dict[str, Any]]
    pending_patches: list[dict[str, Any]]
    conflict_resolution: dict[str, str]
    directory_hash: str
    reviewed_at_latest: str | None
    feat_ref: str | None

def resolve_patch_context(workspace_root: Path, feat_ref: str | None = None) -> PatchContext:
    """Scan experience-patches for validated + pending_backwrite patches."""
```

From cli/lib/patch_schema.py:
```python
class ChangeClass(str, Enum):
    VISUAL = "visual"
    INTERACTION = "interaction"
    SEMANTIC = "semantic"

class PatchStatus(str, Enum):
    VALIDATED = "validated"
    PENDING_BACKWRITE = "pending_backwrite"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create patch_aware_context.py — resolve + awareness recording</name>
  <files>skills/ll-patch-aware-context/scripts/patch_aware_context.py</files>
  <read_first>
    - cli/lib/test_exec_artifacts.py (resolve_patch_context function signature and PatchContext fields)
    - cli/lib/patch_schema.py (ChangeClass enum, PatchStatus enum)
    - .planning/phases/05-ai-context-injection/05-RESEARCH.md (Pattern 2: Write Awareness Recording code example)
  </read_first>
  <action>Create `skills/ll-patch-aware-context/scripts/patch_aware_context.py` with these concrete components:

1. **Imports**: `from cli.lib.test_exec_artifacts import resolve_patch_context`, `from pathlib import Path`, `import yaml`, `import sys`, `import argparse`, `import json`, `from datetime import datetime, timezone`.

2. **`summarize_patch(patch: dict) -> dict` function**: Extracts minimal summary from a full Patch YAML:
   - `id`, `change_class`, `title`, `status`
   - `scope.page`, `scope.module` (nested as `scope: {page, module}`)
   - `test_impact: bool` (True if test_impact field is present and non-empty)
   - For patches beyond the first 5, replace with `{"id": patch["id"], "summary": patch.get("title", ""), "summarized": true}`

3. **`write_awareness_recording(patch_context, output_dir: Path, ai_reasoning: str = "") -> Path` function**:
   - Builds recording dict with top-level key `"patch_awareness"` containing:
     - `feat_ref`: from patch_context.feat_ref
     - `scan_timestamp`: datetime.now(timezone.utc).isoformat()
     - `has_active_patches`: bool(patch_context.has_active_patches)
     - `patch_scan_status`: `"patches_found"` if has_active_patches else `"none_found"` (per RESEARCH.md Pitfall 4)
     - `validated_patches_summary`: list of summarize_patch() results for validated patches
     - `pending_patches_summary`: list of summarize_patch() results for pending patches
     - `conflict_resolution`: dict from patch_context.conflict_resolution (copy as-is)
     - `directory_hash`: from patch_context.directory_hash
     - `ai_consideration`: ai_reasoning or default `"Patches reviewed; no divergence required."` (per D-09 awareness recording)
   - Writes to `output_dir / "patch-awareness.yaml"` via yaml.safe_dump(sort_keys=False, allow_unicode=True)
   - Returns the output path

4. **`resolve_and_record(workspace_root: Path, feat_ref: str, output_dir: Path, ai_reasoning: str = "") -> Path` function**:
   - Calls `resolve_patch_context(workspace_root, feat_ref=feat_ref)`
   - Calls `write_awareness_recording(context, output_dir, ai_reasoning)`
   - Prints confirmation: `"Patch awareness recorded: {output_path}"`
   - Returns the output path

5. **CLI main() with argparse**:
   - Subcommand `resolve`: requires `--workspace-root PATH`, `--feat-ref STRING`, `--output-dir PATH`, optional `--ai-reasoning STRING`
   - Invokes resolve_and_record() and exits 0 on success
   - Exit 1 on any error with stderr message

6. **`if __name__ == "__main__"` block calls main()**

Context budget protection: when `len(validated_patches + pending_patches) > 5`, the summarize_patch function must use the summarized format (one-line) for patches beyond index 5. This prevents context explosion per ADR-049 §12.1 (3000 token budget).

Do NOT reimplement patch scanning logic — use resolve_patch_context() from test_exec_artifacts.py directly.
  </action>
  <acceptance_criteria>
    - File exists at skills/ll-patch-aware-context/scripts/patch_aware_context.py
    - Contains `from cli.lib.test_exec_artifacts import resolve_patch_context` (grep verifiable)
    - Contains `def summarize_patch(` function definition
    - Contains `def write_awareness_recording(` function definition
    - Contains `def resolve_and_record(` function definition
    - Contains `def main(` function definition
    - Contains argparse with "resolve" subcommand
    - Contains `"patches_found"` and `"none_found"` string literals for patch_scan_status
    - Contains `"patch_awareness"` as top-level YAML key
    - No duplicate implementation of patch directory scanning (no glob("UXPATCH-") outside of the imported resolve_patch_context call)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); from skills.ll_patch_aware_context.scripts.patch_aware_context import summarize_patch, write_awareness_recording, resolve_and_record; print('imports OK')"</automated>
  </verify>
  <done>Python module created with 4 exported functions (summarize_patch, write_awareness_recording, resolve_and_record, main), imports resolve_patch_context from Phase 4, argparse CLI with resolve subcommand, context budget protection for >5 patches, produces patch-awareness.yaml with correct top-level structure</done>
</task>

<task type="auto">
  <name>Task 2: Create run.sh CLI wrapper</name>
  <files>skills/ll-patch-aware-context/scripts/run.sh</files>
  <read_first>
    - skills/ll-patch-capture/scripts/run.sh (pattern for skill run.sh wrapper)
    - skills/ll-experience-patch-settle/scripts/run.sh (pattern for skill run.sh wrapper)
  </read_first>
  <action>Create `skills/ll-patch-aware-context/scripts/run.sh` as a POSIX shell wrapper that:

1. Determines the workspace root (default: current directory, or from `$WORKSPACE_ROOT` env var, or `--workspace-root` first arg)
2. Delegates to `python scripts/patch_aware_context.py resolve` with all passed-through arguments
3. Propagates the Python script exit code

Concrete structure (matching existing skill run.sh patterns):
```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
WORKSPACE_ROOT="${WORKSPACE_ROOT:-$(cd "$SCRIPT_DIR/../../../.." 2>/dev/null && pwd || pwd)}"

exec python "$SCRIPT_DIR/patch_aware_context.py" resolve \
  --workspace-root "$WORKSPACE_ROOT" \
  "$@"
```

The wrapper must:
- Use `set -euo pipefail` for strict error handling
- Use `exec` to replace the shell process with the Python process
- Pass through all arguments (`"$@"`) after the `--workspace-root` flag
- Compute WORKSPACE_ROOT as 4 levels up from the script directory (skills/ll-patch-aware-context/scripts/ → repo root)

Make the file executable (chmod +x).
  </action>
  <acceptance_criteria>
    - File exists at skills/ll-patch-aware-context/scripts/run.sh
    - Contains `set -euo pipefail` (grep verifiable)
    - Contains `exec python` (delegates to Python script)
    - Contains `patch_aware_context.py` in the delegation line
    - Contains `"--workspace-root"` flag
    - File has execute permission
    - First line is `#!/usr/bin/env bash`
  </acceptance_criteria>
  <verify>
    <automated>test -x skills/ll-patch-aware-context/scripts/run.sh && echo "executable OK" || echo "NOT executable"</automated>
  </verify>
  <done>run.sh created, executable, delegates to patch_aware_context.py resolve with workspace-root auto-detection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → script | Reads Patch YAML files from ssot/experience-patches/ (untrusted input) |
| script → output | Writes patch-awareness.yaml to output directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | patch_aware_context.py reading patch YAML files | mitigate | Use yaml.safe_load (not yaml.load) to prevent arbitrary code execution from malicious YAML; already enforced by Phase 4's validate_patch() |
| T-05-02 | Information Disclosure | patch-awareness.yaml output | accept | Contains only patch metadata summaries (id, title, change_class, scope), not full patch content; no secrets or sensitive data |
| T-05-03 | TOCTOU | resolve_patch_context() directory scan | mitigate | Reuses Phase 4's _compute_patch_dir_hash() TOCTOU protection — directory_hash captured in awareness output for downstream verification |
</threat_model>

<verification>
- Python script imports work (resolve_patch_context accessible)
- run.sh is executable and delegates correctly
- Script produces valid YAML output when given a valid feat_ref
- Script handles no-patches case gracefully (produces awareness file with patch_scan_status="none_found")
- Context budget protection triggers when >5 patches exist
</verification>

<success_criteria>
1. patch_aware_context.py exists with 4 exportable functions and CLI entry point
2. run.sh exists, is executable, and delegates to the Python script
3. No reimplemented patch scanning — all scanning via resolve_patch_context() import
4. Awareness recording schema matches RESEARCH.md Pattern 2 structure
5. Context budget protection implemented (summarize beyond 5 patches)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-context-injection/05-01-SUMMARY.md`
</output>
