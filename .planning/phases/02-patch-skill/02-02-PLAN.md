---
phase: "02-patch-skill"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/ll-patch-capture/agents/executor.md"
  - "skills/ll-patch-capture/agents/supervisor.md"
autonomous: true
requirements: [REQ-PATCH-02]

must_haves:
  truths:
    - "Executor Agent prompt instructs AI to analyze change and generate Patch YAML"
    - "Executor pre-fills all fields per ADR-049 §2.4 decision tree (D-10, D-11)"
    - "Supervisor Agent prompt validates generated Patch with schema + conflict checks"
    - "Supervisor decides auto-pass vs escalate per D-08/D-09 escalation rules"
    - "Both agents follow the ll-qa-settlement executor/supervisor split pattern"
  artifacts:
    - path: "skills/ll-patch-capture/agents/executor.md"
      provides: "Executor Agent prompt for Patch YAML generation"
      min_lines: 30
    - path: "skills/ll-patch-capture/agents/supervisor.md"
      provides: "Supervisor Agent prompt for validation and escalation"
      min_lines: 30
  key_links:
    - from: "skills/ll-patch-capture/agents/executor.md"
      to: "cli/lib/patch_schema.py"
      via: "Executor references enum values from schema for correct generation"
      pattern: "change_class|PatchStatus"
    - from: "skills/ll-patch-capture/agents/supervisor.md"
      to: "cli/lib/patch_schema.py"
      via: "Supervisor calls validate_file() for mechanical schema validation"
      pattern: "validate_file|patch_schema"
---

<objective>
Create the Executor and Supervisor Agent prompt files for ll-patch-capture. The Executor generates Patch YAML from user input; the Supervisor validates it and decides whether to auto-pass or escalate to human confirmation.

Purpose: Implement the two-agent model (Executor + Supervisor) that all 29 governed skills use, adapted for patch capture with dual-path routing and schema validation.
Output: Two agent prompt files in skills/ll-patch-capture/agents/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patch-skill/02-CONTEXT.md
@.planning/phases/02-patch-skill/02-RESEARCH.md

@skills/ll-qa-settlement/agents/executor.md
@skills/ll-qa-settlement/agents/supervisor.md
@cli/lib/patch_schema.py
@ssot/schemas/qa/patch.yaml
@ssot/adr/ADR-049-引入体验修正层-Experience-Patch-Layer.md

<interfaces>
Key enums from cli/lib/patch_schema.py that Executor MUST use for correct values:

PatchStatus enum values: draft, active, validated, pending_backwrite, backwritten, retain_in_code, upgraded_to_src, superseded, discarded, archived

ChangeClass enum values: visual, interaction, semantic

Severity enum values: low, medium, high

SourceActor enum values: human, ai_suggested

BackwriteStatus enum values: pending, backwritten, discarded, upgraded_to_src, superseded

ADR-049 §2.4 path selection decision tree (for change_class pre-fill per D-11):
```
体验变更
    ├─ 门控 1：是否影响业务规则/状态机/数据含义？
    │   ├─ 是 → SRC (not a Patch)
    │   └─ 否 → 继续
    ├─ 门控 2：是否需要多方利益相关者对齐？
    │   ├─ 是 → SRC (not a Patch)
    │   └─ 否 → 继续
    └─ 门控 3（最终判断）：是否仅影响视觉/文案/布局？
        ├─ 是 → Patch (visual)
        └─ 否 → 是否影响页面跳转/入口位置/操作顺序？
                   ├─ 是 → Patch (interaction)
                   └─ 否 → Patch (visual)
```

test_impact pre-fill rules (D-11):
- interaction/semantic → test_impact defaults to true (impacts_user_path, impacts_acceptance)
- visual → test_impact defaults to false

ll-qa-settlement executor pattern (from skills/ll-qa-settlement/agents/executor.md):
```markdown
# Executor Agent: ll-qa-settlement
## Role
Generate settlement reports from post-execution manifests
## Instructions
1. Read updated API and E2E manifests
2. Compute statistics for each chain
3. Generate gap list (failed/blocked/uncovered items)
4. Generate waiver list (non-none waiver_status items)
5. Write settlement reports to ssot/tests/.artifacts/settlement/
```

ll-qa-settlement supervisor pattern (from skills/ll-qa-settlement/agents/supervisor.md):
```markdown
# Supervisor Agent: ll-qa-settlement
## Validation Checklist
1. Statistics are self-consistent (executed = passed + failed + blocked)
2. pass_rate = passed / max(executed, 1)
3. Gap list includes all failed/blocked/uncovered
...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Executor Agent prompt (agents/executor.md)</name>
  <read_first>
    - skills/ll-qa-settlement/agents/executor.md
    - ssot/schemas/qa/patch.yaml
    - cli/lib/patch_schema.py
  </read_first>
  <files>
    skills/ll-patch-capture/agents/executor.md
  </files>
  <action>
    Create skills/ll-patch-capture/agents/executor.md with this exact structure and content:

    ```markdown
    # Executor Agent: ll-patch-capture

    ## Role
    Analyze experience change descriptions and generate Patch YAML files conforming to the ADR-049 schema. Implements dual-path routing: Prompt-to-Patch (user describes change → AI generates Patch) and Document-to-SRC (route to ll-product-raw-to-src).

    ## Input
    - feat_id: The FEAT this patch belongs to (e.g., "feat.training-plan")
    - input_type: "prompt" or "document"
    - input_value: Free-form text (prompt) or file path (document)

    ## Instructions

    ### Step 1: Classify Input
    Determine the input type:
    - If input_type is "prompt": The input_value is a free-form description of a UX change. Proceed to Step 2a.
    - If input_type is "document": The input_value is a file path to a structured document (BMAD/Superpowers/OMC output). Route to ll-product-raw-to-src skill. If the resulting SRC involves experience-layer changes, generate a semantic Patch with resolution.src_created set to the SRC ID. Proceed to Step 2b.

    ### Step 2a: Analyze Change (Prompt-to-Patch)
    Read the user's description. Apply the ADR-049 §2.4 decision tree to classify:
    1. Does it affect business rules, state machines, or data meaning? → If YES, this is NOT a Patch (it's an SRC). Report this to the caller.
    2. Does it require multi-stakeholder alignment? → If YES, this is NOT a Patch. Report this.
    3. Does it only affect visuals/copy/layout? → change_class = "visual"
    4. Does it affect page navigation, entry points, or action order? → change_class = "interaction"
    5. Otherwise → change_class = "visual"

    ### Step 2b: Analyze Change (Document-to-SRC)
    For Document-to-SRC path, check if the SRC candidate's scope overlaps with known experience-layer FEATs (scan ssot/experience-patches/ for matching feat_ref). If yes, generate a semantic Patch with:
    - change_class: "semantic"
    - resolution.src_created: the SRC ID
    - status: "active"

    ### Step 3: Generate Patch YAML
    Write the Patch YAML file to: ssot/experience-patches/{FEAT-ID}/UXPATCH-NNNN__{slug}.yaml
    The file must conform to ssot/schemas/qa/patch.yaml with these fields:

    #### Required fields (ALL must be present):
    - id: "UXPATCH-NNNN" format — the Python runtime will assign this; write your output as "UXPATCH-NNNN" placeholder or omit; use title slug for filename suggestion.
    - type: "experience_patch"
    - status: "active"
    - created_at: ISO 8601 timestamp (current time, e.g., "2026-04-16T10:00:00Z")
    - updated_at: Same as created_at for new patches
    - title: Short human-readable title describing the change
    - summary: One-sentence summary of the issue or change
    - source:
        from: "product_experience"
        actor: "ai_suggested"
        session: The request_id from the CLI request
        prompt_ref: Reference to the prompt or input that produced this patch
        ai_suggested_class: The change_class you determined in Step 2
        human_confirmed_class: Must equal ai_suggested_class (auto-confirmed by supervisor in auto-pass mode)
    - scope:
        feat_ref: The input feat_id
        page: The page or route name (derive from description; use "unknown" if unclear)
        module: The module or subsystem name (derive from description; use "unknown" if unclear)
    - change_class: One of "visual", "interaction", "semantic" (from Step 2)

    #### Optional fields (include when applicable):
    - severity: "low", "medium", or "high" (default: omit)
    - implementation:
        code_changed: true (if code was modified)
        changed_files: List of file paths that were changed
    - test_impact:
        impacts_user_path: true if change_class is "interaction" or "semantic", false if "visual"
        impacts_acceptance: true if change_class is "interaction" or "semantic", false if "visual"
        impacts_existing_testcases: true if test targets exist, false otherwise
        affected_routes: List of affected API/UI routes (derive from description)
        test_targets: List of specific test targets to update (derive from description)
    - decision:
        code_hotfix_allowed: true for visual, false for interaction/semantic
        must_backwrite_ssot: true for interaction/semantic, false for visual
        backwrite_targets: Derived from ADR-049 §4.4 mapping:
          - visual: ["ui-spec", "component-docs"] (if applicable)
          - interaction: ["ui-spec", "page-flow-diagram", "testset"]
          - semantic: ["feat-doc", "api-spec", "testset", "user-guide"]
    - problem:
        user_issue: Description of the user-facing issue
        evidence: Evidence supporting the claim

    ### Step 4: Output Summary
    Emit a one-line summary:
    "已登记 UXPATCH-NNNN ({change_class}) → ssot/experience-patches/{FEAT-ID}/"

    ## Important Notes
    - ALL enum values must match exactly: change_class must be "visual", "interaction", or "semantic" (lowercase).
    - source.human_confirmed_class must NEVER be null — set it to match ai_suggested_class for auto-pass mode.
    - The Python runtime handles patch_registry.json updates — do NOT write to registry directly.
    - If the input describes a change that affects business rules or state machines (ADR-049 §2.4 gate 1), do NOT generate a Patch. Report that this requires an SRC via Document-to-SRC path instead.
    ```
  </action>
  <verify>
    <automated>grep -q "Executor Agent: ll-patch-capture" skills/ll-patch-capture/agents/executor.md && grep -q "human_confirmed_class" skills/ll-patch-capture/agents/executor.md && grep -q "patch_registry.json" skills/ll-patch-capture/agents/executor.md && grep -q "UXPATCH-NNNN" skills/ll-patch-capture/agents/executor.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-patch-capture/agents/executor.md exists
    - Contains "# Executor Agent: ll-patch-capture" heading
    - Contains "## Role" section
    - Contains "## Instructions" section with at least 4 numbered steps
    - Contains change_class enum values: "visual", "interaction", "semantic"
    - Contains "human_confirmed_class" instruction (must never be null)
    - Contains "patch_registry.json" update instructions
    - Contains "UXPATCH-NNNN" format specification
    - Contains ADR-049 §2.4 decision tree reference
    - Contains test_impact pre-fill rules (interaction/semantic = true, visual = false)
    - Contains backwrite_targets mapping per ADR-049 §4.4
    - Minimum 30 lines of content
  </acceptance_criteria>
  <done>Executor Agent prompt created with complete instructions for dual-path Patch YAML generation</done>
</task>

<task type="auto">
  <name>Task 2: Create Supervisor Agent prompt (agents/supervisor.md)</name>
  <read_first>
    - skills/ll-qa-settlement/agents/supervisor.md
    - cli/lib/patch_schema.py
    - ssot/schemas/qa/patch.yaml
  </read_first>
  <files>
    skills/ll-patch-capture/agents/supervisor.md
  </files>
  <action>
    Create skills/ll-patch-capture/agents/supervisor.md with this exact structure and content:

    ```markdown
    # Supervisor Agent: ll-patch-capture

    ## Role
    Validate generated Patch YAML files and decide whether to auto-pass (register immediately) or escalate to human confirmation. Implements triple validation: mechanical schema checks, semantic conflict detection, and escalation decision logic.

    ## Input
    - Path to generated Patch YAML file
    - FEAT-ID for conflict scanning
    - Input type (prompt or document)

    ## Validation Protocol

    The Supervisor runs validation in THREE layers. Layer 1 is mechanical (Python), Layers 2-3 are semantic (LLM judgment).

    ### Layer 1: Mechanical Schema Validation
    Run: python -m cli.lib.patch_schema --type patch <patch_file_path>
    This validates against cli/lib/patch_schema.py PatchExperience dataclass.
    - Check exit code: 0 = pass, non-zero = fail
    - If fail: Record the error, set status to "draft", ESCALATE to human (D-09: schema validation failure)

    ### Layer 2: Semantic Validation Checklist
    After Layer 1 passes, verify each item:

    1. [SCHEMA-PASS] Schema validation completed successfully (confirmed from Layer 1 result)
    2. [REQUIRED-FIELDS] All required fields present: id, type, status, created_at, updated_at, title, summary, source, scope, change_class
    3. [SOURCE-VALID] source.from = "product_experience", source.actor = "ai_suggested", source.human_confirmed_class is non-null and matches source.ai_suggested_class
    4. [SCOPE-VALID] scope.feat_ref is non-empty, scope.page is non-empty, scope.module is non-empty
    5. [CHANGE-CLASS] change_class is one of: "visual", "interaction", "semantic" (exact lowercase match)
    6. [STATUS] status is "active" for new registrations
    7. [ENUM-VALUES] All enum fields use valid values from ssot/schemas/qa/patch.yaml
    8. [TIMESTAMPS] created_at and updated_at are valid ISO 8601 timestamps
    9. [TEST-IMPACT] test_impact pre-fill rules followed: interaction/semantic → impacts_user_path=true; visual → impacts_user_path=false

    ### Layer 3: Conflict Detection
    Scan ssot/experience-patches/{FEAT-ID}/ for existing active patches:
    - For each UXPATCH-*.yaml in the directory:
      - Skip if status is not in (active, validated, pending_backwrite)
      - Skip if id matches the current patch
      - Compare changed_files arrays for overlap
    - If any overlapping files found: set conflict=true, add conflict_details, ESCALATE to human (D-09: conflict detected)

    ### Layer 4: Escalation Decision
    After all validation layers, decide:

    **Auto-pass conditions (ALL must be true, per D-08):**
    - Layer 1 schema validation passed
    - Layer 2 all checklist items pass
    - Layer 3 no conflicts detected
    - change_class is NOT "semantic"
    - change_class confidence is high (clear decision tree match from ADR-049 §2.4)

    **Escalation triggers (ANY triggers escalation, per D-09):**
    - Schema validation failed (Layer 1 failure)
    - change_class confidence is low (visual vs interaction ambiguous)
    - Conflict detected (Layer 3 found overlapping files)
    - change_class is "semantic" (requires SRC decision)
    - This is the first Patch for this FEAT (patch_registry.json has 0 entries before this one)
    - test_impact is disputed (impacts test path but AI confidence is low)

    ### Auto-pass Action
    If auto-pass decision:
    1. Confirm patch_registry.json has been updated with the new entry
    2. Confirm the Patch YAML file exists at the expected path
    3. Emit success: "已登记 UXPATCH-NNNN ({change_class}) → ssot/experience-patches/{FEAT-ID}/"

    ### Escalation Action
    If escalate decision:
    1. Set patch status to "draft"
    2. Present structured review checklist to user:
       - List each failed validation item with explanation
       - For conflicts: show which existing Patch conflicts and which files overlap
       - For low confidence: explain why change_class is ambiguous
       - For semantic: explain that this may require SRC creation instead
    3. Wait for user confirmation before proceeding

    ## Output
    - If auto-pass: Patch registered, registry updated, one-line notification emitted
    - If escalate: Structured review checklist presented to user, patch held in "draft" status
    ```
  </action>
  <verify>
    <automated>grep -q "Supervisor Agent: ll-patch-capture" skills/ll-patch-capture/agents/supervisor.md && grep -q "cli.lib.patch_schema" skills/ll-patch-capture/agents/supervisor.md && grep -q "Auto-pass" skills/ll-patch-capture/agents/supervisor.md && grep -q "Escalation" skills/ll-patch-capture/agents/supervisor.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-patch-capture/agents/supervisor.md exists
    - Contains "# Supervisor Agent: ll-patch-capture" heading
    - Contains "## Role" section
    - Contains "## Validation Protocol" section
    - Contains "Layer 1: Mechanical Schema Validation" with python -m cli.lib.patch_schema command
    - Contains "Layer 2: Semantic Validation Checklist" with at least 5 checklist items
    - Contains "Layer 3: Conflict Detection" with file overlap scanning logic
    - Contains "Layer 4: Escalation Decision" section
    - Contains Auto-pass conditions (schema valid + no conflict + change_class confidence high + non-semantic)
    - Contains Escalation triggers list (schema fail, low confidence, conflict, semantic, first patch, disputed test_impact)
    - Contains Auto-pass Action and Escalation Action sections
    - Minimum 30 lines of content
  </acceptance_criteria>
  <done>Supervisor Agent prompt created with 4-layer validation protocol, auto-pass/escalate decision logic, and structured review checklist</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Both agent prompt files exist under skills/ll-patch-capture/agents/
2. Executor prompt covers dual-path routing (Prompt-to-Patch + Document-to-SRC)
3. Executor prompt includes all enum values inline (no hallucination risk)
4. Supervisor prompt calls Python validator (Layer 1), not just LLM judgment
5. Supervisor prompt has both auto-pass and escalation paths with clear criteria
</verification>

<success_criteria>
- [ ] skills/ll-patch-capture/agents/executor.md exists and >=30 lines
- [ ] skills/ll-patch-capture/agents/supervisor.md exists and >=30 lines
- [ ] Executor prompt contains all change_class enum values: visual, interaction, semantic
- [ ] Executor prompt contains human_confirmed_class = ai_suggested_class rule
- [ ] Executor prompt contains UXPATCH-NNNN sequential ID generation instructions
- [ ] Supervisor prompt calls python -m cli.lib.patch_schema for mechanical validation
- [ ] Supervisor prompt contains both auto-pass and escalation decision criteria
- [ ] Supervisor prompt contains conflict detection layer (scans existing active patches)
- [ ] Supervisor prompt references all 6 escalation triggers from D-09
</success_criteria>

<output>
After completion, create `.planning/phases/02-patch-skill/02-02-SUMMARY.md`
</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor LLM output → File System | Executor generates YAML — must be validated before disk write |
| Supervisor judgment → Registration decision | Supervisor decides auto-pass vs escalate — incorrect decision leads to invalid patches or unnecessary friction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Executor-generated YAML content | mitigate | Supervisor Layer 1 calls Python `validate_file()` — mechanical check, not LLM judgment; invalid YAML cannot pass |
| T-02-06 | Elevation of Privilege | Supervisor auto-pass decision | mitigate | Auto-pass requires ALL conditions (schema valid + no conflict + confidence high + non-semantic); any failure triggers escalation |
| T-02-07 | Repudiation | Patch registered without audit trail | mitigate | CLI protocol `request_id` logged; Supervisor validation result recorded in response JSON with evidence_refs |
</threat_model>
