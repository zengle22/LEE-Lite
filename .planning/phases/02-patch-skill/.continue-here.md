---
context: phase
phase: 02-patch-skill
task: 0
total_tasks: 10
status: planned_ready_for_execution
last_updated: 2026-04-16T06:34:39.516Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: feat_id path traversal — unvalidated feat_id allows arbitrary directory writes — mitigate: runtime validates feat_id regex + path containment before any filesystem operation
- [ ] CONSTRAINT: Shell injection in validate_input.sh — string interpolation into Python -c allows code execution — mitigate: use sys.argv, never interpolate user paths
- [ ] CONSTRAINT: Double registry write — Executor and runtime both writing patch_registry.json causes corruption — mitigate: runtime is sole registry writer, Executor only writes YAML
- [ ] CONSTRAINT: JSON injection in run.sh — heredoc interpolation of user values breaks JSON structure — mitigate: use Python json.dumps to construct safely

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| pytest.raises(Exception) | Catches any exception, masking wrong error types | advisory | Use pytest.raises(CommandError, match="...") for specific validation |
| AI-generated source fields trusted | AI can set source.actor="human" to bypass audit | blocking | Runtime programmatically sets source fields, overwrites any AI value |
| Escalation triggers ignored | Runtime auto-registers without checking D-09 escalation conditions | blocking | Runtime checks first-patch, semantic, disputed test_impact before registration |

## Current State

Phase 2 (Patch Skill) planning is COMPLETE with all reviews done and fixes applied. 4 plans in 3 waves, 10 total tasks. Ready for execution.

## Completed Work

- Phase 2 PLAN.md files: 02-01 (skeleton), 02-02 (agents), 02-03 (runtime+CLI), 02-04 (tests)
- Multi-role review: security (2 CRITICAL, 4 HIGH), architecture (2 CRITICAL, 3 HIGH), code quality (2 HIGH)
- All 7 P0/P1 fixes applied to plans
- Test suite expanded from 11 to 23 cases
- RESEARCH.md open questions marked as RESOLVED

## Remaining Work

- Execute Phase 2: `/gsd-execute-phase 2`
- Remaining MEDIUM review issues can be addressed during/after execution

## Decisions Made

- Skill named `ll-patch-capture` (not `ll-experience-patch-register`)
- Three-step flow: AI draft → Supervisor → auto-register (human only if escalated)
- Document-to-SRC delegates to existing `ll-product-raw-to-src`
- Dual-path routing: prompt vs document input classification

## Required Reading

1. `.planning/phases/02-patch-skill/02-CONTEXT.md` — all locked decisions (D-01 to D-16)
2. `.planning/phases/02-patch-skill/02-RESEARCH.md` — technical research, resolved questions
3. `.planning/phases/02-patch-skill/02-01-PLAN.md` through `02-04-PLAN.md` — execution plans

## Infrastructure State

- Python 3.13.3 available
- Git Bash for Windows (bash shell scripts)
- PyYAML, pytest available

## Next Action

```
/gsd-execute-phase 2
```

This will execute all 4 plans in 3 waves to build the `ll-patch-capture` skill.
