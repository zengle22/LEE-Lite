---
phase: "02-patch-skill"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "skills/ll-patch-capture/SKILL.md"
  - "skills/ll-patch-capture/ll.contract.yaml"
  - "skills/ll-patch-capture/ll.lifecycle.yaml"
  - "skills/ll-patch-capture/input/contract.yaml"
  - "skills/ll-patch-capture/input/semantic-checklist.md"
  - "skills/ll-patch-capture/output/contract.yaml"
  - "skills/ll-patch-capture/output/semantic-checklist.md"
autonomous: true
requirements: [REQ-PATCH-02]

must_haves:
  truths:
    - "Skill directory exists at skills/ll-patch-capture/"
    - "SKILL.md describes dual-path registration (Prompt-to-Patch + Document-to-SRC)"
    - "ll.contract.yaml contains skill metadata with adr: ADR-049"
    - "ll.lifecycle.yaml defines patch-relevant lifecycle states"
    - "Input/output contracts define the skill boundary"
    - "Semantic checklists are human-verifiable"
  artifacts:
    - path: "skills/ll-patch-capture/SKILL.md"
      provides: "Skill entry point with execution protocol"
      min_lines: 40
    - path: "skills/ll-patch-capture/ll.contract.yaml"
      provides: "Skill metadata (name, version, adr, category)"
      contains: "skill: ll-patch-capture"
    - path: "skills/ll-patch-capture/ll.lifecycle.yaml"
      provides: "Lifecycle state machine definition"
      contains: "lifecycle_states:"
    - path: "skills/ll-patch-capture/input/contract.yaml"
      provides: "Input schema: prompt text or document path"
      contains: "input"
    - path: "skills/ll-patch-capture/input/semantic-checklist.md"
      provides: "Human-readable input validation checklist"
      contains: "- [ ]"
    - path: "skills/ll-patch-capture/output/contract.yaml"
      provides: "Output schema: generated patch YAML + registry update"
      contains: "output"
    - path: "skills/ll-patch-capture/output/semantic-checklist.md"
      provides: "Human-readable output validation checklist"
      contains: "- [ ]"
  key_links:
    - from: "skills/ll-patch-capture/SKILL.md"
      to: "skills/ll-patch-capture/ll.contract.yaml"
      via: "Required Read Order section"
      pattern: "ll.contract.yaml"
    - from: "skills/ll-patch-capture/SKILL.md"
      to: "skills/ll-patch-capture/agents/executor.md"
      via: "Execution Protocol references executor agent"
      pattern: "agents/executor.md"
    - from: "skills/ll-patch-capture/SKILL.md"
      to: "skills/ll-patch-capture/agents/supervisor.md"
      via: "Execution Protocol references supervisor agent"
      pattern: "agents/supervisor.md"
---

<objective>
Create the ll-patch-capture skill directory structure with all contract, metadata, and lifecycle files. This plan establishes the skill skeleton following the exact pattern of ll-qa-settlement (the project template for governed skills).

Purpose: Provide the structural foundation that downstream plans (agent prompts, runtime, CLI registration) will plug into. All 29 existing ll-* skills follow this exact structure, ensuring consistency.
Output: Complete skill skeleton at skills/ll-patch-capture/ with 7 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patch-skill/02-CONTEXT.md
@.planning/phases/02-patch-skill/02-RESEARCH.md

@skills/ll-qa-settlement/SKILL.md
@skills/ll-qa-settlement/ll.contract.yaml
@skills/ll-qa-settlement/ll.lifecycle.yaml
@skills/ll-qa-settlement/input/contract.yaml
@skills/ll-qa-settlement/input/semantic-checklist.md
@skills/ll-qa-settlement/output/contract.yaml
@skills/ll-qa-settlement/output/semantic-checklist.md

<interfaces>
Key patterns from ll-qa-settlement that this plan must replicate:

ll.contract.yaml structure:
```yaml
skill: ll-qa-settlement
version: "1.0"
adr: ADR-047
category: qa
chain: settlement
phase: settlement-generation
```

ll.lifecycle.yaml structure:
```yaml
skill: ll-qa-settlement
version: "1.0"
state: draft
lifecycle_states:
  - draft
  - validated
  - executed
  - frozen
```

Input/Output contract pattern:
- input/contract.yaml: Required input + Input Validation checklist
- output/contract.yaml: Required output + Output Schema section
- input/semantic-checklist.md: Human-readable input checks with "- [ ]" items
- output/semantic-checklist.md: Human-readable output checks with "- [ ]" items
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill metadata and lifecycle files (ll.contract.yaml, ll.lifecycle.yaml)</name>
  <read_first>
    - skills/ll-qa-settlement/ll.contract.yaml
    - skills/ll-qa-settlement/ll.lifecycle.yaml
  </read_first>
  <files>
    skills/ll-patch-capture/ll.contract.yaml
    skills/ll-patch-capture/ll.lifecycle.yaml
  </files>
  <action>
    Create two files following the ll-qa-settlement pattern exactly:

    1. skills/ll-patch-capture/ll.contract.yaml — Use this exact content:
    ```yaml
    skill: ll-patch-capture
    version: "1.0"
    adr: ADR-049
    category: experience
    chain: patch-capture
    phase: registration
    ```
    Key differences from ll-qa-settlement: adr is ADR-049 (not ADR-047), category is "experience" (not "qa"), chain is "patch-capture", phase is "registration".

    2. skills/ll-patch-capture/ll.lifecycle.yaml — Use this exact content:
    ```yaml
    skill: ll-patch-capture
    version: "1.0"
    state: draft
    lifecycle_states:
      - draft
      - active
      - validated
      - pending_backwrite
      - resolved
      - archived
    ```
    The lifecycle states follow the PatchExperience status enum from cli/lib/patch_schema.py (PatchStatus): draft, active, validated, pending_backwrite, then resolved (covers backwritten/retain_in_code/upgraded_to_src/superseded/discarded), archived. This is patch-appropriate (different from qa-settlement's draft/validated/executed/frozen).

    Create the parent directory skills/ll-patch-capture/ first with mkdir -p.
  </action>
  <verify>
    <automated>grep -q "skill: ll-patch-capture" skills/ll-patch-capture/ll.contract.yaml && grep -q "lifecycle_states:" skills/ll-patch-capture/ll.lifecycle.yaml</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-patch-capture/ll.contract.yaml exists and contains "skill: ll-patch-capture"
    - skills/ll-patch-capture/ll.contract.yaml contains "adr: ADR-049"
    - skills/ll-patch-capture/ll.contract.yaml contains "category: experience"
    - skills/ll-patch-capture/ll.lifecycle.yaml exists and contains "lifecycle_states:"
    - skills/ll-patch-capture/ll.lifecycle.yaml contains all 6 states: draft, active, validated, pending_backwrite, resolved, archived
  </acceptance_criteria>
  <done>Both metadata files created with correct content, directory structure established</done>
</task>

<task type="auto">
  <name>Task 2: Create SKILL.md with dual-path execution protocol</name>
  <read_first>
    - skills/ll-qa-settlement/SKILL.md
    - ssot/adr/ADR-049-引入体验修正层-Experience-Patch-Layer.md
  </read_first>
  <files>
    skills/ll-patch-capture/SKILL.md
  </files>
  <action>
    Create skills/ll-patch-capture/SKILL.md following the ll-qa-settlement SKILL.md structure but adapted for patch capture. The file must contain these sections in order:

    1. YAML frontmatter:
    ```yaml
    ---
    name: ll-patch-capture
    description: ADR-049 governed skill for dual-path experience patch registration (Prompt-to-Patch + Document-to-SRC routing).
    ---
    ```

    2. H1 heading: "# LL Patch Capture"

    3. Intro paragraph describing the skill purpose — dual-path registration for experience patches per ADR-049.

    4. "## Canonical Authority" section with:
       - ADR path: ssot/adr/ADR-049-引入体验修正层-Experience-Patch-Layer.md
       - Upstream handoff: user prompt or ll-product-raw-to-src output
       - Downstream consumer: ll-experience-patch-settle (Phase 3)

    5. "## Runtime Boundary Baseline" section referencing ADR-038 and identifying this as a governed Skill.

    6. "## Required Read Order" section listing:
       1. ll.contract.yaml
       2. input/contract.yaml
       3. output/contract.yaml
       4. agents/executor.md
       5. agents/supervisor.md
       6. input/semantic-checklist.md
       7. output/semantic-checklist.md

    7. "## Execution Protocol" section with numbered steps describing:
       - Step 1: Accept input — either free-form prompt text (user describes UX change) or document path (BMAD/Superpowers/OMC output)
       - Step 2: Route detection — classify input as "prompt" (free-form text) or "document" (file path to structured document)
       - Step 3a (Prompt-to-Patch path): Executor Agent analyzes change description → generates Patch YAML draft with all fields pre-filled per ADR-049 §2.4 decision tree → writes to ssot/experience-patches/{FEAT-ID}/UXPATCH-NNNN__{slug}.yaml
       - Step 3b (Document-to-SRC path): Route to ll-product-raw-to-src skill; if experience-layer change detected, generate semantic Patch with resolution.src_created = SRC ID
       - Step 4: Supervisor Agent validates generated Patch — runs schema validation via cli/lib/patch_schema.py, checks for conflicts, decides auto-pass vs escalate to human
       - Step 5: Auto-pass → register Patch, update patch_registry.json, emit "已登记 UXPATCH-XXXX" notification
       - Step 6: Escalate → present structured review checklist to user for confirmation
       - Step 7: Validate output — confirm generated YAML passes schema validation, registry updated

    8. "## Workflow Boundary" section:
       - Input: user prompt text describing UX change OR document path to raw product requirements
       - Output: Patch YAML file in ssot/experience-patches/{FEAT-ID}/ + updated patch_registry.json
       - Out of scope: settlement (Phase 3), test linkage (Phase 4), hook auto-trigger (Phase 6), AI context injection (Phase 5), 24h blocking (Phase 7)

    9. "## Non-Negotiable Rules" section with rules:
       - Do not generate a Patch YAML without running schema validation (cli/lib/patch_schema.py validate_file)
       - Do not auto-submit Patch without Supervisor validation pass
       - Do not write source.human_confirmed_class as null — must match source.ai_suggested_class in auto-pass mode
       - Do not bypass dual-path routing — always classify input as prompt or document first
       - Document-to-SRC path delegates to ll-product-raw-to-src; this skill only routes and associates
       - All AI pre-filled fields must be marked as human-reviewed per ADR-049 §12.2
       - Patch IDs must be sequential UXPATCH-NNNN format derived from patch_registry.json max sequence

    Use the same markdown style as ll-qa-settlement/SKILL.md (headings, list formatting, code blocks).
  </action>
  <verify>
    <automated>grep -q "name: ll-patch-capture" skills/ll-patch-capture/SKILL.md && grep -q "Prompt-to-Patch" skills/ll-patch-capture/SKILL.md && grep -q "Document-to-SRC" skills/ll-patch-capture/SKILL.md && grep -q "patch_registry.json" skills/ll-patch-capture/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-patch-capture/SKILL.md exists
    - Contains YAML frontmatter with "name: ll-patch-capture"
    - Contains "Prompt-to-Patch" text
    - Contains "Document-to-SRC" text
    - Contains "## Canonical Authority" section
    - Contains "## Execution Protocol" section with at least 5 numbered steps
    - Contains "## Workflow Boundary" section
    - Contains "## Non-Negotiable Rules" section with at least 5 rules
    - References "cli/lib/patch_schema.py" in execution protocol or rules
    - References "patch_registry.json" in execution protocol or rules
    - Minimum 40 lines of content
  </acceptance_criteria>
  <done>SKILL.md created with complete execution protocol covering both paths, workflow boundaries, and non-negotiable rules</done>
</task>

<task type="auto">
  <name>Task 3: Create input/output contracts and semantic checklists</name>
  <read_first>
    - skills/ll-qa-settlement/input/contract.yaml
    - skills/ll-qa-settlement/output/contract.yaml
    - skills/ll-qa-settlement/input/semantic-checklist.md
    - skills/ll-qa-settlement/output/semantic-checklist.md
    - ssot/schemas/qa/patch.yaml
  </read_first>
  <files>
    skills/ll-patch-capture/input/contract.yaml
    skills/ll-patch-capture/input/semantic-checklist.md
    skills/ll-patch-capture/output/contract.yaml
    skills/ll-patch-capture/output/semantic-checklist.md
  </files>
  <action>
    Create four files following the ll-qa-settlement contract pattern, adapted for patch capture:

    1. skills/ll-patch-capture/input/contract.yaml — Content:
    ```yaml
    # Input Contract: ll-patch-capture

    ## Required Input
    - feat_id: string (e.g., "feat.training-plan") — the FEAT this patch belongs to
    - input_type: enum[prompt, document] — classification of input
    - input_value: string — either free-form prompt text or absolute file path to document

    ## Input Validation
    - [ ] feat_id is non-empty string
    - [ ] input_type is "prompt" or "document"
    - [ ] input_value is non-empty
    - [ ] If input_type is "document", input_value points to an existing file
    - [ ] If input_type is "document", file path is within workspace root
    ```

    2. skills/ll-patch-capture/input/semantic-checklist.md — Content:
    ```markdown
    # Input Semantic Checklist: ll-patch-capture

    - [ ] feat_id is provided and non-empty
    - [ ] input_type is either "prompt" or "document"
    - [ ] input_value contains meaningful content (not whitespace-only)
    - [ ] If input_type is "document", the file exists and is readable
    - [ ] If input_type is "document", the file is within the workspace root (no path traversal)
    - [ ] Dual-path routing decision is explicit: Prompt-to-Patch or Document-to-SRC
    ```

    3. skills/ll-patch-capture/output/contract.yaml — Content:
    ```yaml
    # Output Contract: ll-patch-capture

    ## Required Output
    - Patch YAML file at ssot/experience-patches/{FEAT-ID}/UXPATCH-NNNN__{slug}.yaml
    - Updated patch_registry.json at ssot/experience-patches/{FEAT-ID}/patch_registry.json

    ## Output Schema
    Each Patch YAML file conforms to ssot/schemas/qa/patch.yaml with:
    - Root key: experience_patch
    - id: UXPATCH-NNNN format (4-digit zero-padded)
    - type: "experience_patch"
    - status: "active" (default for new registrations)
    - change_class: one of visual, interaction, semantic
    - source.actor: "ai_suggested" with source.human_confirmed_class set
    - scope.feat_ref: matches the input feat_id
    - All required source sub-fields populated (from, actor, session, prompt_ref, human_confirmed_class)
    - All required scope sub-fields populated (feat_ref, page, module)

    patch_registry.json contains:
    - patch_registry_version: "1.0.0"
    - feat_id: matches the FEAT directory name
    - patches: array with new entry appended (id, status, change_class, created_at, title, patch_file)
    - last_updated: ISO 8601 timestamp
    ```

    4. skills/ll-patch-capture/output/semantic-checklist.md — Content:
    ```markdown
    # Output Semantic Checklist: ll-patch-capture

    - [ ] Patch YAML file exists at ssot/experience-patches/{FEAT-ID}/UXPATCH-NNNN__{slug}.yaml
    - [ ] Patch YAML passes schema validation (python -m cli.lib.patch_schema --type patch <file>)
    - [ ] Patch ID follows UXPATCH-NNNN format with sequential numbering
    - [ ] Patch status is "active" for new registrations
    - [ ] change_class is one of: visual, interaction, semantic
    - [ ] source.human_confirmed_class is non-null (matches ai_suggested_class in auto-pass mode)
    - [ ] scope.feat_ref matches the input feat_id
    - [ ] patch_registry.json has been updated with the new entry
    - [ ] patch_registry.json last_updated timestamp is current
    - [ ] For Document-to-SRC path: resolution.src_created is set if SRC was generated
    - [ ] For Document-to-SRC path with experience-layer changes: semantic Patch exists as associated record
    ```

    Create parent directories with mkdir -p: skills/ll-patch-capture/input/ and skills/ll-patch-capture/output/
  </action>
  <verify>
    <automated>grep -q "Input Contract" skills/ll-patch-capture/input/contract.yaml && grep -q "Output Contract" skills/ll-patch-capture/output/contract.yaml && grep -q "UXPATCH-NNNN" skills/ll-patch-capture/output/semantic-checklist.md && grep -q "human_confirmed_class" skills/ll-patch-capture/output/semantic-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/ll-patch-capture/input/contract.yaml exists and contains "Input Contract: ll-patch-capture"
    - skills/ll-patch-capture/input/contract.yaml contains input_type enum[prompt, document]
    - skills/ll-patch-capture/input/semantic-checklist.md exists and contains at least 4 "- [ ]" items
    - skills/ll-patch-capture/output/contract.yaml exists and contains "Output Contract: ll-patch-capture"
    - skills/ll-patch-capture/output/contract.yaml references "ssot/schemas/qa/patch.yaml"
    - skills/ll-patch-capture/output/semantic-checklist.md exists and contains at least 8 "- [ ]" items
    - skills/ll-patch-capture/output/semantic-checklist.md contains "human_confirmed_class" check
    - skills/ll-patch-capture/output/semantic-checklist.md contains "patch_registry.json" check
  </acceptance_criteria>
  <done>All four contract and checklist files created with patch-specific content</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. All 7 files exist under skills/ll-patch-capture/
2. SKILL.md contains both Prompt-to-Patch and Document-to-SRC paths
3. ll.contract.yaml references ADR-049
4. Input/output contracts define clear boundaries
5. Semantic checklists contain actionable "- [ ]" items
</verification>

<success_criteria>
- [ ] skills/ll-patch-capture/ directory exists with all 7 files
- [ ] SKILL.md is >=40 lines and contains execution protocol with both paths
- [ ] ll.contract.yaml contains skill: ll-patch-capture, adr: ADR-049
- [ ] ll.lifecycle.yaml contains 6 patch-appropriate lifecycle states
- [ ] input/contract.yaml defines feat_id, input_type, input_value
- [ ] output/contract.yaml references patch.yaml schema and registry update
- [ ] Both semantic checklists contain human-verifiable "- [ ]" items
</success_criteria>

<output>
After completion, create `.planning/phases/02-patch-skill/02-01-SUMMARY.md`
</output>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User prompt → Executor Agent | Unstructured text input, must be classified and validated |
| AI-generated Patch YAML → Schema Validator | AI may produce invalid enum values or missing fields |
| Document path → File System | Path traversal risk if user-provided paths are not sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Executor Agent (AI-generated YAML) | mitigate | Supervisor agent calls `cli/lib/patch_schema.py` `validate_file()` before accepting generated Patch YAML; reject if schema validation fails |
| T-02-02 | Spoofing | Input document path | mitigate | Runtime validates document path exists within workspace root using `Path.resolve().relative_to(workspace_root)` before processing |
| T-02-03 | Information Disclosure | Patch registry reads | accept | Registry contains no secrets — only patch IDs, statuses, timestamps; low-value target |
| T-02-04 | Repudiation | Patch registration without trace | mitigate | CLI protocol requires `request_id` in every request; runtime logs `request_id` with generated patch ID for audit trail |
</threat_model>
