
---
phase: "20"
plan: "03"
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified: [
  ".planning/STATE.md",
  ".planning/ROADMAP.md"
]
autonomous: true
requirements: ["FIX-P0-01", "FIX-P0-02"]
user_setup: []

must_haves:
  truths: [
    "Semantic drift scanner can be run on real SSOT and produces actionable output",
    "FEAT decomposition fix is verified to work with existing tests and EPIC structures",
    "Phase 20 requirements are marked as complete in STATE.md and ROADMAP.md"
  ]
  artifacts: [
    {
      "path": ".planning/STATE.md",
      "provides": "Updated project state with Phase 20 marked complete"
    },
    {
      "path": ".planning/ROADMAP.md",
      "provides": "Updated roadmap with Phase 20 status"
    }
  ]
  key_links: []
---

&lt;objective&gt;
Verify repairs made in Phase 20-01 and 20-02, then update project state to mark Phase 20 as complete.
Purpose: Close the P0 defect repair phase and prepare the project for subsequent phases.
Output: Updated STATE.md and ROADMAP.md, with Phase 20 marked complete.
&lt;/objective&gt;

&lt;execution_context&gt;
@.planning/phases/20-p0-defect-fix/20-CONTEXT.md
@.planning/phases/20-p0-defect-fix/20-01-SUMMARY.md
@.planning/phases/20-p0-defect-fix/20-02-SUMMARY.md
&lt;/execution_context&gt;

&lt;context&gt;
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@cli/lib/semantic_drift_scanner.py
@skills/ll-product-epic-to-feat/scripts/epic_to_feat_derivation.py
&lt;/context&gt;

&lt;tasks&gt;

&lt;task type="auto"&gt;
  &lt;name&gt;Verify semantic drift scanner on real SSOT&lt;/name&gt;
  &lt;files&gt;cli/lib/semantic_drift_scanner.py&lt;/files&gt;
  &lt;read_first&gt;
    - cli/lib/semantic_drift_scanner.py
  &lt;/read_first&gt;
  &lt;action&gt;
Run the semantic drift scanner on the real SSOT directory:

1. Execute: `python -m cli.lib.semantic_drift_scanner --ssot-dir ./ssot --output human`
2. Capture the output
3. Also run with JSON output to verify machine-readable format: `python -m cli.lib.semantic_drift_scanner --ssot-dir ./ssot --output json`
4. Verify both commands complete successfully
  &lt;/action&gt;
  &lt;verify&gt;
    &lt;automated&gt;
python -m cli.lib.semantic_drift_scanner --ssot-dir ./ssot --output human 2>&1 | head -50
    &lt;/automated&gt;
  &lt;/verify&gt;
  &lt;done&gt;
Scanner runs successfully on real SSOT, produces both human and JSON output formats
  &lt;/done&gt;
&lt;/task&gt;

&lt;task type="auto"&gt;
  &lt;name&gt;Verify all tests pass for both repairs&lt;/name&gt;
  &lt;files&gt;
    cli/lib/test_semantic_drift_scanner.py
    skills/ll-product-epic-to-feat/tests/test_epic_to_feat_derivation.py
  &lt;/files&gt;
  &lt;read_first&gt;
    - cli/lib/test_semantic_drift_scanner.py
    - skills/ll-product-epic-to-feat/tests/test_epic_to_feat_derivation.py
  &lt;/read_first&gt;
  &lt;action&gt;
Run all relevant tests to verify both repairs:

1. Run semantic drift scanner tests: `python -m pytest cli/lib/test_semantic_drift_scanner.py -xvs`
2. Run ll-product-epic-to-feat tests: `python -m pytest skills/ll-product-epic-to-feat/tests/ -xvs`
3. Verify all tests pass
  &lt;/action&gt;
  &lt;verify&gt;
    &lt;automated&gt;
python -m pytest cli/lib/test_semantic_drift_scanner.py -xvs 2>&1 | tail -20 &amp;&amp; \
python -m pytest skills/ll-product-epic-to-feat/tests/ -xvs 2>&1 | tail -20
    &lt;/automated&gt;
  &lt;/verify&gt;
  &lt;done&gt;
All tests pass for both semantic drift scanner and FEAT decomposition fix
  &lt;/done&gt;
&lt;/task&gt;

&lt;task type="auto"&gt;
  &lt;name&gt;Update project state to mark Phase 20 as complete&lt;/name&gt;
  &lt;files&gt;
    .planning/STATE.md
    .planning/ROADMAP.md
  &lt;/files&gt;
  &lt;read_first&gt;
    - .planning/STATE.md
    - .planning/ROADMAP.md
  &lt;/read_first&gt;
  &lt;action&gt;
Update project state files to mark Phase 20 as complete:

1. **Update .planning/STATE.md**:
   - Set Phase 20 status to "Complete"
   - Update progress section
   - Add Phase 20 results summary

2. **Update .planning/ROADMAP.md**:
   - Update Phase 20 status to "Complete"
   - Update requirement tracking (FIX-P0-01, FIX-P0-02)

3. Make sure both files are properly updated
  &lt;/action&gt;
  &lt;verify&gt;
    &lt;automated&gt;
cat .planning/STATE.md | grep -A 20 "Phase 20" &amp;&amp; cat .planning/ROADMAP.md | grep -A 10 "Phase 20"
    &lt;/automated&gt;
  &lt;/verify&gt;
  &lt;done&gt;
STATE.md and ROADMAP.md are updated to mark Phase 20 as complete
  &lt;/done&gt;
&lt;/task&gt;

&lt;/tasks&gt;

&lt;threat_model&gt;
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanner → SSOT | Read-only access - no modifications to SSOT files
| Tests → codebase | Read-only test execution - no modifications to production code

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Tampering | Project state updates | mitigate | Manual verification after updates
&lt;/threat_model&gt;

&lt;verification&gt;
- Scanner runs successfully on real SSOT
- All tests pass
- Project state updated to mark Phase 20 as complete
&lt;/verification&gt;

&lt;success_criteria&gt;
1. Semantic drift scanner runs successfully on real SSOT
2. All tests pass (scanner tests and FEAT decomposition tests)
3. STATE.md and ROADMAP.md are updated to mark Phase 20 as complete
4. FIX-P0-01 and FIX-P0-02 requirements are marked as addressed
&lt;/success_criteria&gt;

&lt;output&gt;
After completion, create `.planning/phases/20-p0-defect-fix/20-03-SUMMARY.md`
&lt;/output&gt;

